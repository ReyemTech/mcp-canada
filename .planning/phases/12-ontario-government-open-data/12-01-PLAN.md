---
phase: 12-ontario-government-open-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_canada/modules/ontario/__init__.py
  - src/mcp_canada/modules/ontario/constants.py
  - src/mcp_canada/modules/ontario/schemas.py
  - src/mcp_canada/modules/ontario/client.py
  - src/mcp_canada/modules/ontario/__tests__/__init__.py
  - src/mcp_canada/modules/ontario/__tests__/conftest.py
  - src/mcp_canada/modules/ontario/__tests__/test_client.py
autonomous: true
requirements:
  - ONT-01
  - ONT-02
  - ONT-03
  - ONT-04
  - ONT-05
  - ONT-06

must_haves:
  truths:
    - "Ontario CKAN search returns shaped dataset dicts with bilingual title/description"
    - "Ontario dataset detail returns full metadata with capped resources"
    - "Ontario resource detail returns shaped resource dict"
    - "Ontario organizations list returns ministry names and dataset counts"
    - "Ontario portal stats returns total dataset count"
    - "Ontario population projections returns parsed XLSX rows as list[dict]"
    - "All client functions return (data, was_cached) tuples"
    - "All client functions use cached_fetch() and get_limiter()"
  artifacts:
    - path: "src/mcp_canada/modules/ontario/__init__.py"
      provides: "MODULE_NAME and MODULE_DESCRIPTION"
      contains: "MODULE_NAME"
    - path: "src/mcp_canada/modules/ontario/constants.py"
      provides: "BASE_URL, RATE_GROUP, RATE_LIMIT, CACHE_TTLs, population projections URL"
      contains: "BASE_URL"
    - path: "src/mcp_canada/modules/ontario/schemas.py"
      provides: "Flat Pydantic v2 models for Ontario responses"
      contains: "BaseModel"
    - path: "src/mcp_canada/modules/ontario/client.py"
      provides: "All async client functions with caching and rate limiting"
      exports: ["fetch_search_datasets", "fetch_dataset_details", "fetch_organizations", "fetch_resource", "fetch_dataset_count", "fetch_population_projections"]
    - path: "src/mcp_canada/modules/ontario/__tests__/conftest.py"
      provides: "Sample CKAN API response fixtures"
      contains: "fixture"
    - path: "src/mcp_canada/modules/ontario/__tests__/test_client.py"
      provides: "Unit tests for all client functions with mocked HTTP"
      min_lines: 80
  key_links:
    - from: "src/mcp_canada/modules/ontario/client.py"
      to: "src/mcp_canada/shared/cache.py"
      via: "cached_fetch import"
      pattern: "from mcp_canada\\.shared\\.cache import cached_fetch"
    - from: "src/mcp_canada/modules/ontario/client.py"
      to: "src/mcp_canada/shared/rate_limiter.py"
      via: "get_limiter import"
      pattern: "from mcp_canada\\.shared\\.rate_limiter import get_limiter"
    - from: "src/mcp_canada/modules/ontario/client.py"
      to: "src/mcp_canada/shared/parsers.py"
      via: "fetch_and_parse import for population projections"
      pattern: "from mcp_canada\\.shared\\.parsers import fetch_and_parse"
---

<objective>
Create the Ontario open data module skeleton with CKAN API client layer, population projections parser, and unit tests.

Purpose: Establish the data layer for Ontario's Open Data Catalogue (data.ontario.ca), reusing the proven federal CKAN module pattern with Ontario-specific constants and a curated population projections dataset tool.

Output: Complete client.py with 6 public functions, all unit tested with mocked HTTP. Module auto-registers via FileSystemProvider.
</objective>

<execution_context>
@/Users/mariomeyer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomeyer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ontario-government-open-data/12-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs from existing codebase -->

From src/mcp_canada/modules/ckan/client.py (pattern to replicate):
```python
async def _api_get(path: str, params: dict[str, Any], cache_ttl: int) -> tuple[Any, bool]:
    """Fetch from CKAN API with caching, rate limiting, envelope unwrapping."""
    url = BASE_URL + path
    cache_key = _build_cache_key(path, params)
    limiter = get_limiter(RATE_GROUP, rate=RATE_LIMIT)
    async def fetcher() -> Any:
        await limiter.acquire()
        async with httpx.AsyncClient(timeout=30.0) as http:
            response = await http.get(url, params=params)
            response.raise_for_status()
            envelope = response.json()
            return envelope["result"]
    return await cached_fetch(cache_key, cache_ttl, fetcher)

def _shape_dataset(raw: dict[str, Any], lang: str = "en") -> dict[str, Any]:
    """Shape raw CKAN dataset dict for token-efficient agent consumption."""

def _shape_resource(raw: dict[str, Any]) -> dict[str, Any]:
    """Extract key fields from raw CKAN resource dict."""
```

From src/mcp_canada/shared/parsers.py:
```python
async def fetch_and_parse(url, sheet=0, skip_rows=0, ttl=3600) -> tuple[list[dict], bool]
```

From src/mcp_canada/shared/cache.py:
```python
async def cached_fetch(key: str, ttl: int, fetcher: Callable) -> tuple[Any, bool]
```

From src/mcp_canada/shared/rate_limiter.py:
```python
def get_limiter(source: str, rate: float) -> TokenBucket
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Ontario module skeleton and CKAN client layer</name>
  <files>
    src/mcp_canada/modules/ontario/__init__.py,
    src/mcp_canada/modules/ontario/constants.py,
    src/mcp_canada/modules/ontario/schemas.py,
    src/mcp_canada/modules/ontario/client.py,
    src/mcp_canada/modules/ontario/__tests__/__init__.py,
    src/mcp_canada/modules/ontario/__tests__/conftest.py,
    src/mcp_canada/modules/ontario/__tests__/test_client.py
  </files>
  <behavior>
    - fetch_search_datasets("population", rows=5) returns list of shaped dataset dicts with id, name, title, description, organization, num_resources, tags, resources, metadata_created, metadata_modified
    - fetch_search_datasets with lang="fr" extracts French title/description from title_translated/notes_translated
    - fetch_dataset_details("population-projections") returns single shaped dataset dict
    - fetch_dataset_details with 404 raises httpx.HTTPStatusError
    - fetch_organizations() returns list of org dicts with id, name, title, description, package_count
    - fetch_resource("some-uuid") returns shaped resource dict with id, name, format, size, url
    - fetch_resource with 404 raises httpx.HTTPStatusError
    - fetch_dataset_count() returns (int, bool) tuple
    - fetch_population_projections() returns (list[dict], bool) from parsed XLSX via fetch_and_parse
    - fetch_population_projections(lang="fr") uses French URL variant (or same URL if no FR variant)
    - All functions use cached_fetch and get_limiter (verify via mock assertions)
    - _truncate(None) returns None; _truncate("short") returns "short"; _truncate(long_text) truncates with "..."
    - _shape_dataset falls back to raw title when title_translated absent
    - _build_cache_key produces deterministic "ontario:" prefixed keys
  </behavior>
  <action>
Create the Ontario module directory and all 7 files:

**__init__.py:** Set MODULE_NAME = "ontario" and MODULE_DESCRIPTION describing Ontario's 2,946 datasets across 20+ ministries (Health, Education, Finance, Agriculture, Transportation, Environment). Mention curated datasets including population projections. Reference Open Government Licence - Ontario.

**constants.py:**
- BASE_URL = "https://data.ontario.ca/api/3/"
- RATE_GROUP = "ontario"
- RATE_LIMIT = 10.0 (conservative; no published limit)
- CACHE_TTL_SEARCH = 3600 (1 hour for search/dataset details)
- CACHE_TTL_META = 86400 (24 hours for org lists)
- CACHE_TTL_DATA = 86400 (24 hours for parsed XLSX data)
- MAX_DESCRIPTION_CHARS = 500
- MAX_RESOURCES = 10
- POPULATION_PROJECTIONS_DATASET_ID = "f52a6457-fb37-4267-acde-11a1e57c4dc8"
- POPULATION_PROJECTIONS_RESOURCE_URL = the direct XLSX download URL from research (https://data.ontario.ca/dataset/f52a6457-fb37-4267-acde-11a1e57c4dc8/resource/31376797-1e4c-4426-ba75-0d93f4bb9f45/download/ontario_mof_population_projections_for_2024-2051.xlsx)
- Note: if no separate FR URL exists for population projections, use same URL for both languages

**schemas.py:** Replicate the ckan module's Pydantic models: Resource, DatasetSummary, DatasetDetail, Organization. Keep models flat. Add PopulationProjectionRow if the XLSX has a known schema, otherwise skip (tool can return raw list[dict]).

**client.py:** Mirror the federal ckan/client.py exactly:
- _truncate(), _limit_resources(), _shape_resource(), _shape_dataset(), _build_cache_key() — same logic, "ontario:" prefix for cache keys
- _api_get() — same pattern but with Ontario BASE_URL, RATE_GROUP, RATE_LIMIT
- fetch_search_datasets(query, fq, rows, start, sort, lang) -> (list[dict], bool)
- fetch_dataset_details(dataset_id, lang) -> (dict, bool)
- fetch_organizations(all_fields, sort) -> (list[dict], bool)
- fetch_resource(resource_id) -> (dict, bool)
- fetch_dataset_count() -> (int, bool)
- fetch_population_projections(lang) -> (list[dict], bool) — uses fetch_and_parse() from shared/parsers.py with the XLSX URL from constants.py, sheet=0, ttl=CACHE_TTL_DATA

**conftest.py:** Create fixtures with sample Ontario CKAN API response data:
- sample_search_result: {"count": 96, "results": [{"id": "f52a...", "name": "population-projections", "title": "Population projections", "title_translated": {"en": "Population projections", "fr": "Projections demographiques"}, "notes_translated": {"en": "Ontario population...", "fr": "Population de l'Ontario..."}, "organization": {"name": "finance", "title": "Finance"}, "num_resources": 12, "resources": [...], "metadata_created": "2020-01-01", "metadata_modified": "2025-08-01"}]}
- sample_package: single dataset dict (same shape as results[0])
- sample_org_list: [{"id": "org1", "name": "finance", "title": "Finance", "description": "...", "package_count": 42}]
- sample_resource: {"id": "res1", "name": "data.xlsx", "format": "XLSX", "size": 244000, "url": "https://..."}
- sample_population_rows: [{"geography": "Ontario", "year": 2024, "population": 15000000}]

**test_client.py:** Test all client functions by mocking httpx.AsyncClient to return fixture data wrapped in CKAN envelope {"success": true, "result": ...}. For fetch_population_projections, mock shared.parsers.fetch_and_parse. Test:
- Each function returns (data, was_cached) tuple
- _shape_dataset extracts bilingual fields (en and fr)
- _shape_dataset truncates long descriptions
- _shape_dataset falls back when title_translated absent
- _build_cache_key is deterministic with "ontario:" prefix
- _truncate edge cases (None, short, long)
- fetch_population_projections delegates to fetch_and_parse with correct URL and TTL

Use `patch("httpx.AsyncClient")` pattern from existing ckan/test_client.py.
  </action>
  <verify>
    <automated>cd /Users/mariomeyer/code/ReyemTech/mcp-canada && uv run pytest src/mcp_canada/modules/ontario/__tests__/test_client.py -x -v</automated>
  </verify>
  <done>
    - Ontario module auto-registers via FileSystemProvider (has MODULE_NAME and MODULE_DESCRIPTION in __init__.py)
    - 6 public client functions exist, all returning (data, was_cached) tuples
    - All client functions use cached_fetch() and get_limiter()
    - Population projections function uses fetch_and_parse() from shared/parsers.py
    - All unit tests pass with mocked HTTP
  </done>
</task>

</tasks>

<verification>
- `uv run pytest src/mcp_canada/modules/ontario/__tests__/test_client.py -x -v` passes
- `uv run pyright src/mcp_canada/modules/ontario/` has no errors
- `uv run ruff check src/mcp_canada/modules/ontario/` has no errors
</verification>

<success_criteria>
- Ontario module directory exists with 5 production files + test directory
- All 6 client functions return (data, was_cached) tuples
- Unit tests cover happy path and error paths for all client functions
- Module auto-registers when server starts (MODULE_NAME = "ontario" in __init__.py)
</success_criteria>

<output>
After completion, create `.planning/phases/12-ontario-government-open-data/12-01-SUMMARY.md`
</output>
