---
phase: 20-nova-scotia-government-open-data
plan: 06
type: execute
wave: 5
depends_on: [20-05]
files_modified:
  - src/mcp_canada/modules/nova_scotia/prompts.py
  - src/mcp_canada/modules/nova_scotia/resources.py
  - src/mcp_canada/modules/nova_scotia/__tests__/test_prompts_resources.py
autonomous: true
requirements:
  - NS-18
must_haves:
  truths:
    - "6 bilingual @prompt functions exist (3 guided list[Message] + 3 quick-lookup str) with ns_ prefix and a lang param, chaining the 17 implemented tools"
    - "7 zero-parameter @resource functions exist using data://, docs://, template:// URI schemes with bilingual content inline"
    - "All prompts/resources use standalone fastmcp decorators (NEVER @mcp.*) and are auto-discovered by FileSystemProvider (no server.py changes)"
    - "Resources have ZERO parameters (no lang — would promote to ResourceTemplate and drop from resources/list)"
    - "docs://ns/socrata-guide explains SODA/SoQL ($where/$select/$order/$group/$limit/$offset, the categories= broken-param workaround, geometry via $select) with NS examples; docs://ns/portal-guide documents Socrata-first + deferred transport/511 + deferred NS ArcGIS Hub + the Open Government Licence – Nova Scotia v1.1"
  artifacts:
    - path: src/mcp_canada/modules/nova_scotia/prompts.py
      provides: "6 @prompt functions (3 guided + 3 quick lookups)"
      contains: "def ns_explore_aquaculture_data"
    - path: src/mcp_canada/modules/nova_scotia/resources.py
      provides: "7 @resource functions (data://, docs://, template://)"
      contains: "data://ns/health-zones"
  key_links:
    - from: "src/mcp_canada/modules/nova_scotia/prompts.py"
      to: "FileSystemProvider prompts/list"
      via: "standalone @prompt from fastmcp.prompts"
      pattern: "@prompt"
    - from: "src/mcp_canada/modules/nova_scotia/resources.py"
      to: "FileSystemProvider resources/list"
      via: "standalone @resource, zero-parameter functions"
      pattern: "@resource"
---

<objective>
Add Nova Scotia's prompts and resources (Phase 40 pattern) — 6 bilingual prompts and 7 zero-parameter resources, scaled to the shipped 17-tool surface. Guided prompts chain the new tools for aquaculture, health-zone, and water-quality workflows; quick lookups guide single-tool calls. Resources embed NS reference data (categories, health zones, fishing areas, departments) and guidance docs/templates — including the Socrata/SoQL guide (the FIRST Socrata portal's how-to) and a portal guide documenting the deferred domains and licence.

Purpose: Native MCP discoverability — prompts as slash-commands, resources as reference data agents read repeatedly. Also the place the Socrata-specific SODA/SoQL knowledge lives for agents.
Output: prompts.py (6) + resources.py (7) + tests.
</objective>

<execution_context>
@/Users/mariomeyer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomeyer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-nova-scotia-government-open-data/20-RESEARCH.md
@.planning/phases/20-nova-scotia-government-open-data/20-CONTEXT.md
@.planning/phases/20-nova-scotia-government-open-data/20-SPIKE.md
@CLAUDE.md

# Reference: prompts/resources pattern (zero-parameter resources, bilingual inline, guided vs quick)
@src/mcp_canada/modules/saskatchewan/prompts.py
@src/mcp_canada/modules/saskatchewan/resources.py
@src/mcp_canada/modules/alberta/resources.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 6 bilingual prompts</name>
  <files>
    src/mcp_canada/modules/nova_scotia/prompts.py,
    src/mcp_canada/modules/nova_scotia/__tests__/test_prompts_resources.py
  </files>
  <action>
    Add 6 @prompt functions (standalone @prompt from fastmcp.prompts — NEVER @mcp.prompt). Each takes
    lang: Annotated[Literal["en","fr"], "Language: 'en' or 'fr'"] = "en" and has a docstring describing when to use
    it. Use ns_ prefix.

    Guided workflows (return list[Message], user + assistant roles, >=2 messages, name specific tools):
    - ns_explore_aquaculture_data — chain ns_get_marine_aquaculture_leases → ns_get_landbased_aquaculture_licenses → ns_get_fish_hatchery_stocking → ns_get_aquaculture_production (the signature domain)
    - ns_health_zone_analysis — chain ns_get_health_facilities(facility_type="hospital") → ns_get_health_facilities(facility_type="long_term_care") → ns_get_chronic_disease_prevalence → ns_get_vital_statistics
    - ns_water_quality_analysis — chain ns_get_air_quality_stations / ns_get_water_quality_monitoring → ns_get_boil_water_advisories (filter by date range; check active advisories)

    Quick lookups (return str with tool name + param instructions):
    - ns_quick_find_dataset — guide ns_search_datasets (mention q= keyword search + that categories filtering is client-side)
    - ns_quick_protected_areas — guide ns_get_protected_areas (mention status filter; geometry via ns_query_dataset)
    - ns_quick_vital_stats — guide ns_get_vital_statistics (mention county UPPERCASE + year-as-string)

    Fill TestNsPrompts: assert 6 prompts discoverable, bilingual (en/fr produce different content), guided ones
    return list[Message], quick ones return str. (Message.content is TextContent — access via .text where needed.)
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/nova_scotia/__tests__/test_prompts_resources.py::TestNsPrompts -x -v</automated>
  </verify>
  <done>6 prompts defined (3 guided + 3 quick), bilingual, standalone @prompt, ns_ prefix. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: 7 zero-parameter resources</name>
  <files>
    src/mcp_canada/modules/nova_scotia/resources.py,
    src/mcp_canada/modules/nova_scotia/__tests__/test_prompts_resources.py
  </files>
  <action>
    Add 7 @resource functions (standalone @resource from fastmcp.resources — NEVER @mcp.resource). ZERO parameters
    each (no lang — would promote to ResourceTemplate). Type-prefixed + module-prefixed URIs. Use the reference data
    from 20-RESEARCH.md "Prompts and Resources" + "Categories" list:
    - data://ns/categories — the 20+ NS domain categories (from the research category enumeration), bilingual labels inline (json.dumps)
    - data://ns/health-zones — the 4 NS health zones (Western/Northern/Eastern/Central) with member counties per zone (hard-coded from public NS Health docs per research Q4), bilingual
    - data://ns/fishing-areas — NS aquaculture lease counties + speciestyp values (Shellfish/Finfish/Marine Plant), bilingual
    - data://ns/departments — NS government departments publishing on data.novascotia.ca (from domain_metadata Department values), bilingual
    - docs://ns/socrata-guide — markdown: how SODA/SoQL works ($where/$select/$order/$group/$limit/$offset with NS examples), the categories= broken-param workaround (use q= / ns_list_categories), geometry control via $select, and the optional X-App-Token for higher throttle. This is the canonical Socrata how-to for agents (FIRST Socrata portal).
    - docs://ns/portal-guide — markdown: data.novascotia.ca is Socrata (4th portal technology); Socrata-first geospatial; transport/511 deferred (HTML-only, no stubs); NS ArcGIS Hub (novagis) deferred (no public no-auth FeatureServers); rockweed exhe-htib discovery-only (per spike); air-quality individual pollutant series via ns_query_dataset; Open Government Licence – Nova Scotia v1.1 (commercial use OK, attribution required)
    - template://ns/aquaculture-report — markdown template with {placeholder} fields (lease counts by county, production by species, employment summary)

    data:// resources must return valid JSON (json.dumps), bilingual content inline. Fill TestNsResources: assert
    7 resources discoverable via resources/list, data:// return valid JSON, str(r.uri) containment checks
    (AnyUrl→str), zero-parameter (appear in resources/list not ResourceTemplate). Use async def test bodies
    (Python 3.14 get_event_loop() lesson).
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/nova_scotia/__tests__/test_prompts_resources.py::TestNsResources -x -v</automated>
    <automated>uv run pytest src/mcp_canada/modules/nova_scotia/__tests__/ -q 2>&1 | tail -10</automated>
  </verify>
  <done>7 resources defined (4 data + 2 docs + 1 template), zero-parameter, valid JSON for data://, bilingual inline. socrata-guide documents SODA/SoQL + the categories= workaround + geometry control; portal-guide documents the deferred domains + the NS Open Government Licence v1.1. Tests pass.</done>
</task>

</tasks>

<verification>
1. `uv run pytest src/mcp_canada/modules/nova_scotia/__tests__/test_prompts_resources.py -x -v`
2. `uv run python -c "from mcp_canada.modules.nova_scotia import prompts, resources; print('OK')"`
3. `uv run pyright src/mcp_canada/modules/nova_scotia/` — clean
</verification>

<success_criteria>
- NS-18 (prompts/resources portion) advanced: 6 prompts (3 guided + 3 quick) bilingual, standalone @prompt, ns_ prefix
- 7 resources zero-parameter, type-prefixed URIs, bilingual data:// JSON
- socrata-guide documents SODA/SoQL + categories= workaround + geometry-via-$select (the canonical Socrata how-to)
- portal-guide documents deferred transport/511 + deferred NS ArcGIS Hub + Open Government Licence – Nova Scotia v1.1
- All auto-discoverable by FileSystemProvider (no server.py changes)
</success_criteria>

<output>
After completion, create `.planning/phases/20-nova-scotia-government-open-data/20-06-SUMMARY.md`: list the 6 prompts + 7 resources with URIs, confirm zero-parameter resource compliance, and note where the Socrata/SoQL knowledge + deferred-domain documentation live.
</output>
