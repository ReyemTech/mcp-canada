---
phase: 15-british-columbia-government-open-data
plan: 04
type: execute
wave: 4
depends_on: ["15-01", "15-02", "15-03"]
files_modified:
  - src/mcp_canada/modules/british_columbia/prompts.py
  - src/mcp_canada/modules/british_columbia/resources.py
  - src/mcp_canada/modules/british_columbia/__tests__/test_prompts_resources.py
  - tests/integration/test_tool_scenarios.py
  - tests/integration/test_prompts_resources_scenarios.py
  - README.md
  - CLAUDE.md
autonomous: true
requirements: []
# Phase 15 has no explicit REQ IDs in REQUIREMENTS.md yet — this plan finalizes
# the BC module (prompts, resources, integration tests, docs) and updates
# README.md + CLAUDE.md to reflect WFS as a third portal tech alongside CKAN
# and ArcGIS Hub.

must_haves:
  truths:
    - "prompts.py contains 6 bilingual standalone @prompt functions (bc_explore_wildfires, bc_explore_forestry, bc_explore_environment, bc_quick_dataset_search, bc_check_water_quality, bc_wildfire_status_now) auto-discovered by FileSystemProvider"
    - "3 of the 6 prompts are guided workflows returning list[Message] with user + assistant roles; 3 are quick lookups returning str"
    - "resources.py contains 7 zero-parameter standalone @resource functions using data://, docs://, template:// URIs with bilingual content inline"
    - "data://bc/ministries and data://bc/wildfire-status-codes and data://bc/object-name-prefixes return json.dumps(...) — not raw dicts"
    - "docs://bc/wfs-query-guide explains the CKAN -> WFS two-step workflow with a concrete example agents can copy"
    - "Integration tests in tests/integration/test_tool_scenarios.py::TestBcToolScenarios hit the live BCDC + WFS endpoints through the MCP Client layer and assert on _meta envelope shape (not specific values)"
    - "Integration tests in tests/integration/test_prompts_resources_scenarios.py::TestBcPromptsResources assert BC prompts are discoverable via client.list_prompts() and BC resources via client.read_resource()"
    - "README.md Tool Catalog has a new British Columbia section with 20 bc_ tools; README tool count updated from 155 to 175"
    - "CLAUDE.md notes WFS/OGC as the third portal technology (alongside CKAN and ArcGIS Hub)"
    - "uv run pytest --cov=src/mcp_canada --cov-fail-under=95 passes with BC module included"
  artifacts:
    - path: "src/mcp_canada/modules/british_columbia/prompts.py"
      provides: "6 bilingual @prompt functions covering BC workflows"
      contains: "bc_explore_wildfires"
      min_lines: 150
    - path: "src/mcp_canada/modules/british_columbia/resources.py"
      provides: "7 zero-parameter @resource functions with inline bilingual content"
      contains: "data://bc"
      min_lines: 200
    - path: "src/mcp_canada/modules/british_columbia/__tests__/test_prompts_resources.py"
      provides: "Populated TestBcPrompts and TestBcResources classes — asserts types, registrations, and bilingual content"
      contains: "list_prompts"
    - path: "tests/integration/test_tool_scenarios.py"
      provides: "Populated TestBcToolScenarios class — at least 8 live scenarios through MCP Client"
      contains: "TestBcToolScenarios"
    - path: "tests/integration/test_prompts_resources_scenarios.py"
      provides: "Populated TestBcPromptsResources class — discovery via list_prompts/read_resource"
      contains: "TestBcPromptsResources"
    - path: "README.md"
      provides: "British Columbia section in Tool Catalog with all 20 tools; updated tool count header"
      contains: "British Columbia"
    - path: "CLAUDE.md"
      provides: "Architecture notes referencing WFS/OGC as third portal tech"
      contains: "WFS"
  key_links:
    - from: "src/mcp_canada/modules/british_columbia/prompts.py"
      to: "FileSystemProvider auto-discovery"
      via: "Standalone @prompt decorator"
      pattern: "from fastmcp.prompts import prompt"
    - from: "src/mcp_canada/modules/british_columbia/resources.py"
      to: "FileSystemProvider auto-discovery"
      via: "Standalone @resource decorator + zero-parameter functions"
      pattern: "from fastmcp.resources import resource"
    - from: "tests/integration/test_tool_scenarios.py::TestBcToolScenarios"
      to: "BCDC + BCGW WFS live endpoints"
      via: "MCP Client call_tool path for bc_ tools"
      pattern: "client.call_tool"
    - from: "tests/integration/test_prompts_resources_scenarios.py::TestBcPromptsResources"
      to: "FastMCP prompts/list and resources/list"
      via: "client.list_prompts() and client.read_resource()"
      pattern: "list_prompts|read_resource"
---

<objective>
Complete the BC module: add prompts.py (6 bilingual prompts), resources.py (7 zero-parameter resources with inline bilingual content), populate integration test scenarios through the MCP Client layer, and update README.md + CLAUDE.md. This plan closes the phase and ensures the BC module is end-to-end functional with Phase 40 parity (7-file pattern, prompts discoverable, resources readable).

Purpose: The 20 bc_ tools from Plans 02/03 are the raw capability. Prompts turn that capability into guided agent workflows (wildfire exploration, forestry chain, two-step WFS workflow demo). Resources ship the reference data (ministry slugs, wildfire status codes, WFS query guide) that agents would otherwise fail to discover. Integration tests verify the entire stack against live BCDC + BCGW endpoints. README/CLAUDE.md updates are load-bearing documentation rules (per .claude/rules/modules.md).

Output: Populated prompts.py, resources.py, integration test classes, updated README.md and CLAUDE.md.
</objective>

<execution_context>
@/Users/mariomeyer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomeyer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-british-columbia-government-open-data/15-CONTEXT.md
@.planning/phases/15-british-columbia-government-open-data/15-RESEARCH.md
@.planning/phases/15-british-columbia-government-open-data/15-VALIDATION.md
@.planning/phases/15-british-columbia-government-open-data/15-01-SUMMARY.md
@.planning/phases/15-british-columbia-government-open-data/15-02-SUMMARY.md
@.planning/phases/15-british-columbia-government-open-data/15-03-SUMMARY.md

@src/mcp_canada/modules/british_columbia/constants.py
@src/mcp_canada/modules/british_columbia/tools.py
@src/mcp_canada/modules/british_columbia/prompts.py
@src/mcp_canada/modules/british_columbia/resources.py

# Closest references — york_region 7-file module with prompts+resources (Phase 14):
@src/mcp_canada/modules/york_region/prompts.py
@src/mcp_canada/modules/york_region/resources.py
@src/mcp_canada/modules/york_region/__tests__/test_prompts_resources.py

# Pattern references from Phase 40:
@src/mcp_canada/modules/bank_of_canada/prompts.py
@src/mcp_canada/modules/bank_of_canada/resources.py

# Integration test patterns:
@tests/integration/test_tool_scenarios.py
@tests/integration/test_prompts_resources_scenarios.py

@README.md
@CLAUDE.md

<interfaces>
From fastmcp.prompts (standalone, NEVER @mcp.prompt):
```
from fastmcp.prompts import prompt
from fastmcp import Message

@prompt
async def my_prompt(lang: Annotated[Literal["en", "fr"], "Language"] = "en") -> list[Message]:
    ...

@prompt
async def my_lookup(lang: Annotated[Literal["en", "fr"], "Language"] = "en") -> str:
    ...
```

From fastmcp.resources (standalone, NEVER @mcp.resource, ZERO parameters):
```
from fastmcp.resources import resource
import json

@resource(uri="data://bc/ministries")
async def bc_ministries() -> str:
    return json.dumps({"en": [...], "fr": [...]})

@resource(uri="docs://bc/wfs-query-guide")
async def bc_wfs_query_guide() -> str:
    return "# WFS Query Guide (EN)\n...\n# Guide de requete WFS (FR)\n..."

@resource(uri="template://bc/wildfire-report")
async def bc_wildfire_report_template() -> str:
    return "# Wildfire Report\nSeason: {fire_season}\nTotal: {total_fires}\n..."
```

Integration test pattern (from TestYorkRegionToolScenarios):
```
import json, pytest
from fastmcp import Client
from mcp_canada.server import mcp, _build_providers, _MODULES_DIR, _META_DIR
from fastmcp.server.providers import FileSystemProvider
from fastmcp.server.transforms.search import BM25SearchTransform

@pytest.fixture(scope="class")
def mcp_server():
    providers = _build_providers("")
    for p in providers:
        mcp.add_provider(p)
    if _META_DIR.is_dir():
        mcp.add_provider(FileSystemProvider(root=_META_DIR))
    mcp.add_transform(BM25SearchTransform(
        max_results=5,
        always_visible=["discover_tools", "list_modules"],
        search_tool_name="discover_tools",
        call_tool_name="call_tool",
    ))
    return mcp

@pytest.mark.integration
class TestBcToolScenarios:
    async def test_active_fires_returns_meta(self, mcp_server):
        async with Client(mcp_server) as client:
            result = await client.call_tool("call_tool", {
                "name": "bc_get_active_fires",
                "arguments": {"max_records": 5},
            })
            data = json.loads(result.content[0].text)
            assert "_meta" in data
            assert data["_meta"]["source"]["api"] == "bc-wfs"
```

Required 6 prompts (RESEARCH.md §Prompts and Resources Specification):
- bc_explore_wildfires (guided workflow, list[Message]): discover active fires -> check perimeters -> summarize fire season
- bc_explore_forestry (guided workflow, list[Message]): tenure -> cut blocks -> protected areas
- bc_explore_environment (guided workflow, list[Message]): water wells -> parks -> mining overview
- bc_quick_dataset_search (quick lookup, str): bc_search_datasets + bc_get_dataset_details flow
- bc_check_water_quality (quick lookup, str): bc_get_water_wells with city + well_class
- bc_wildfire_status_now (quick lookup, str): bc_get_active_fires with status filter

Required 7 resources (RESEARCH.md):
- data://bc/ministries — JSON catalog of BC ministry org slugs (inline en/fr labels)
- data://bc/wildfire-status-codes — JSON: FIRE_STATUS codes (Active, Being Held, Under Control, Out, Out of Control) + FIRE_CAUSE codes + bilingual labels
- data://bc/object-name-prefixes — JSON: WHSE_CATEGORY -> domain mapping (all 15 curated layers)
- docs://bc/wfs-query-guide — Markdown: CKAN -> WFS two-step workflow, CQL syntax examples, pagination (bilingual — both EN and FR sections in one document)
- docs://bc/bcdc-api-quirks — Markdown: bcgov custom fields, queryable_via_wfs detection, no groups (tags only), org slugs
- template://bc/wildfire-report — Markdown template with {fire_season}, {total_fires}, {largest_fire}, {cause_breakdown} placeholders
- template://bc/dataset-report — Markdown template for dataset exploration reports
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement prompts.py + resources.py + unit tests</name>
  <files>
    src/mcp_canada/modules/british_columbia/prompts.py,
    src/mcp_canada/modules/british_columbia/resources.py,
    src/mcp_canada/modules/british_columbia/__tests__/test_prompts_resources.py
  </files>
  <action>
    Read `src/mcp_canada/modules/york_region/prompts.py` and `src/mcp_canada/modules/york_region/resources.py` FIRST — they are the closest 7-file pattern reference and have 4-6 prompts + 6-10 resources with inline bilingual content.

    Also read `src/mcp_canada/modules/york_region/__tests__/test_prompts_resources.py` to mirror the unit test pattern (assertions on prompt return types, resource return types, URI prefixes, bilingual content presence).

    Step A — implement prompts.py:

    Import:
    ```
    from typing import Annotated, Literal
    from fastmcp.prompts import prompt
    from fastmcp import Message
    ```

    Define 6 @prompt functions. Each MUST:
    - Have bilingual lang: Annotated[Literal["en", "fr"], "Language: 'en' or 'fr'"] = "en"
    - Use bc_ prefix in function name
    - Have a docstring describing when agents should use it

    Guided workflow prompts (return list[Message], at least 2 messages each — first user, second assistant):

    1. `bc_explore_wildfires` — multi-step analysis:
       - First message (user role): "I want to understand the current BC wildfire situation. What's burning right now?"
       - Second message (assistant role): "I'll help you explore BC wildfires. Step 1: Call bc_get_active_fires to get current incidents. Step 2: For the most significant fires, use bc_get_fire_perimeters with year=current_year to get historical context. Step 3: Summarize by fire centre and cause. Use bc_get_wildfire_weather_stations for related weather data."

    2. `bc_explore_forestry` — tenure -> cut blocks -> protected areas chain:
       - User: "I want to understand forestry activity and protected land in a specific BC region."
       - Assistant: "Step 1: Call bc_get_forest_tenure with district=<region> to see active tenures. Step 2: Call bc_get_cut_blocks with the same district to see harvesting. Step 3: Call bc_get_protected_areas to see what's protected. Compare areas and tenure holders."

    3. `bc_explore_environment` — water wells -> parks -> mining:
       - User: "I want to understand environmental pressures in a BC city or aquifer."
       - Assistant: "Step 1: Call bc_get_water_wells with city=<name> or aquifer_id=<id>. Step 2: Call bc_get_local_parks with municipality=<name> to see greenspace. Step 3: Call bc_get_mining_tenure with owner_name or tenure_type for nearby mineral activity. Overlay locations."

    Quick lookup prompts (return str with tool name + parameter instructions):

    4. `bc_quick_dataset_search` — returns a string instruction telling the agent to call bc_search_datasets with {q} then optionally bc_get_dataset_details with the returned id. Includes the two-step workflow note (discover -> details -> query_features).

    5. `bc_check_water_quality` — returns a string instruction telling the agent to call bc_get_water_wells with city parameter. Notes that WELL_CLASS=DRINKING WATER gives drinking water wells.

    6. `bc_wildfire_status_now` — returns a string instruction telling the agent to call bc_get_active_fires (optionally with status="Out of Control" for the most urgent incidents).

    All 6 prompts must return EN content when lang=="en" and FR content when lang=="fr". Implement bilingual branching inside each function with a simple if lang == "fr": ... else: ... block.

    Step B — implement resources.py:

    Import:
    ```
    import json
    from fastmcp.resources import resource
    ```

    Define 7 @resource functions. Each MUST:
    - Use standalone @resource from fastmcp.resources
    - Have ZERO parameters (no lang!) — adding a parameter would promote the function to ResourceTemplate and hide it from resources/list
    - Embed bilingual content inline

    1. `@resource(uri="data://bc/ministries")` async def bc_ministries() -> str:
       Returns json.dumps() of a dict with bilingual ministry entries. Use a handful of known real BC ministry slugs (env-air-quality, bc-wildfire-service, min-forests, min-health, etc.). Each entry has {slug, name_en, name_fr, description_en, description_fr}. Minimum 8 entries.

    2. `@resource(uri="data://bc/wildfire-status-codes")` async def bc_wildfire_status_codes() -> str:
       Returns json.dumps() with FIRE_STATUS codes (Active, Being Held, Under Control, Out, Out of Control) each with label_en + label_fr + description_en + description_fr. Also FIRE_CAUSE codes (Lightning, Human, Person, Unknown) with bilingual labels.

    3. `@resource(uri="data://bc/object-name-prefixes")` async def bc_object_name_prefixes() -> str:
       Returns json.dumps() mapping WHSE_CATEGORY prefix -> domain description (bilingual). Must include all 10 categories from RESEARCH.md §BCGW Object Name Schema (WHSE_LAND_AND_NATURAL_RESOURCE, WHSE_FOREST_TENURE, WHSE_TANTALIS, WHSE_MINERAL_TENURE, WHSE_WATER_MANAGEMENT, WHSE_WILDLIFE_MANAGEMENT, WHSE_ENVIRONMENTAL_MONITORING, WHSE_IMAGERY_AND_BASE_MAPS, WHSE_BASEMAPPING, WHSE_PARKS_ECOLOGY — note the WHSE_PARKS_ECOLOGY caveat). Also include a "curated_layers" sub-dict listing the 15 layer constants from constants.py.

    4. `@resource(uri="docs://bc/wfs-query-guide")` async def bc_wfs_query_guide() -> str:
       Returns a markdown string explaining the CKAN -> WFS two-step workflow. Must include:
       - Title + overview
       - The two-step workflow: discover (bc_search_datasets), inspect (bc_get_dataset_details to get object_name + queryable_via_wfs), query (bc_query_features or a curated bc_get_* tool)
       - A concrete copyable example with FIRE_YEAR=2023
       - A CQL syntax primer (equality, numeric, LIKE, AND/OR)
       - A note about the 5000-record cap and truncated flag
       - Both EN and FR sections in the same document

    5. `@resource(uri="docs://bc/bcdc-api-quirks")` async def bc_bcdc_api_quirks() -> str:
       Returns a markdown string documenting bcgov custom fields (bcdc_type, object_name, resource_storage_location, projection_name), the queryable_via_wfs derivation logic, the no-groups quirk (CKAN group_list returns 403; use tag_list), and a list of known ministry organization slugs with descriptions. Bilingual.

    6. `@resource(uri="template://bc/wildfire-report")` async def bc_wildfire_report_template() -> str:
       Returns a markdown template with {placeholder} syntax:
       ```
       # BC Wildfire Season Report
       **Season:** {fire_season}
       **Total fires:** {total_fires}
       **Largest fire:** {largest_fire}
       **Fires by centre:** {centre_breakdown}
       **Causes:** {cause_breakdown}
       ...
       ```

    7. `@resource(uri="template://bc/dataset-report")` async def bc_dataset_report_template() -> str:
       Returns a markdown template for dataset exploration reports with placeholders for dataset name, organization, object_name, queryable_via_wfs, resources_count, sample_rows.

    Step C — populate test_prompts_resources.py:

    Replace the xfail placeholders in TestBcPrompts and TestBcResources with real tests. Use the york_region test_prompts_resources.py as a template. Required tests:

    TestBcPrompts:
    - test_prompts_module_imports
    - test_bc_explore_wildfires_returns_list_of_messages_en
    - test_bc_explore_wildfires_returns_list_of_messages_fr — asserts FR content present
    - test_bc_explore_forestry_guided_workflow_shape
    - test_bc_explore_environment_guided_workflow_shape
    - test_bc_quick_dataset_search_returns_str
    - test_bc_check_water_quality_returns_str
    - test_bc_wildfire_status_now_returns_str
    - test_all_prompts_accept_lang_parameter
    - test_all_prompts_use_bc_prefix

    TestBcResources:
    - test_resources_module_imports
    - test_bc_ministries_is_valid_json_with_bilingual_entries
    - test_bc_wildfire_status_codes_is_valid_json_with_fire_status_and_cause
    - test_bc_object_name_prefixes_lists_all_10_whse_categories
    - test_bc_wfs_query_guide_is_markdown_with_cql_example
    - test_bc_bcdc_api_quirks_is_markdown_with_bilingual_content
    - test_bc_wildfire_report_template_has_placeholders
    - test_bc_dataset_report_template_has_placeholders
    - test_all_resources_use_type_prefixed_uri  (data://, docs://, or template://)
    - test_all_resources_use_bc_module_path  (URI path contains /bc/)
    - test_no_resource_has_lang_parameter  (zero-parameter invariant)

    Use unittest.mock or direct import calls (resources are async functions — await them in tests). Mirror york_region test patterns exactly.
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/british_columbia/__tests__/test_prompts_resources.py -x -q</automated>
  </verify>
  <done>
    - prompts.py has 6 @prompt functions (3 guided + 3 quick lookup) all bilingual
    - resources.py has 7 @resource functions, all zero-parameter, with bilingual content inline
    - test_prompts_resources.py tests all pass green
    - Every prompt returns bilingual content branched on lang
    - Every resource URI uses data://, docs://, or template:// prefix and contains /bc/ in the path
    - uv run ruff check src/mcp_canada/modules/british_columbia/ is clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate integration tests + update README.md + CLAUDE.md</name>
  <files>
    tests/integration/test_tool_scenarios.py,
    tests/integration/test_prompts_resources_scenarios.py,
    README.md,
    CLAUDE.md
  </files>
  <action>
    Step A — populate TestBcToolScenarios in `tests/integration/test_tool_scenarios.py` (the class skeleton was added in Plan 01 Task 3). Read `TestYorkRegionToolScenarios` in the same file to mirror style.

    Replace the xfail placeholder methods with real integration tests calling the MCP Client layer. Required scenarios (all marked @pytest.mark.integration and using the mcp_server fixture pattern from york_region):

    1. `test_search_finds_wildfire_data` — call discover_tools with query "wildfire british columbia", assert a bc_ tool is returned OR call call_tool -> bc_search_datasets with q="wildfire" and assert results contain a dataset with wildfire-related title
    2. `test_active_fires_returns_meta` — call_tool -> bc_get_active_fires with max_records=5; assert _meta.source.api == "bc-wfs" and data has features + truncated
    3. `test_fire_perimeters_by_year` — call bc_get_fire_perimeters with year=2023, max_records=10; assert features returned (676 in 2023 per RESEARCH); timeout=60
    4. `test_protected_areas_returns_parks` — call bc_get_protected_areas with designation="PROVINCIAL PARK", max_records=10; assert features returned with PROTECTED_LANDS_NAME and PROTECTED_LANDS_DESIGNATION keys
    5. `test_mining_tenure_mineral_claims` — call bc_get_mining_tenure with tenure_type="mineral", max_records=5; assert features returned
    6. `test_discover_bc_wildfire_tools` — call call_tool -> discover_tools with q="british columbia wildfire"; assert at least one bc_ tool appears in results
    7. `test_query_features_routes_to_wfs` — call bc_search_datasets to find a WFS-queryable dataset id, then bc_get_dataset_details to confirm queryable_via_wfs=True, then bc_query_features and assert _meta.source.api == "bc-wfs"
    8. `test_query_features_routes_to_file_parser` — call bc_search_datasets with fq or keyword that likely returns a CSV-only dataset; if the test finds one with queryable_via_wfs=False, call bc_query_features and assert _meta.source.api != "bc-wfs". Use pytest.skip if no suitable dataset is found at runtime.

    Assert on shape only, never specific counts (data changes daily). Handle flaky responses: check r.content before .json(), check content-type.

    Step B — populate TestBcPromptsResources in `tests/integration/test_prompts_resources_scenarios.py`:

    1. `test_bc_prompts_discoverable_via_list_prompts` — async with Client(mcp_server) as client: prompts = await client.list_prompts(); assert any(p.name.startswith("bc_") for p in prompts); assert at least 6 bc_ prompts
    2. `test_bc_resources_readable_via_read_resource` — resources = await client.list_resources(); filter those with str(r.uri) containing "/bc/"; assert at least 7; for each, call read_resource and assert content length > 0
    3. `test_bc_wfs_query_guide_resource_returns_markdown` — read_resource("docs://bc/wfs-query-guide"); assert "CKAN" in content and "WFS" in content and "bc_query_features" in content

    Note: use `str(r.uri)` not `r.uri` directly (FastMCP ResourceInfo.uri is AnyUrl — see STATE.md Phase 40 decision).

    Step C — update README.md:

    1. Update the header tool count: the README currently says something like "155 tools across N APIs" — increment by 20 (total 175) and add British Columbia as a new API. Locate the exact count via a grep for the tool count line.
    2. Add a new British Columbia section to the Tool Catalog listing all 20 bc_ tools with the first line of their docstring. Group as:
       - **Discovery (5):** bc_search_datasets, bc_get_dataset_details, bc_query_features, bc_list_organizations, bc_list_categories
       - **Wildfire (2):** bc_get_active_fires, bc_get_fire_perimeters
       - **Forestry (3):** bc_get_forest_tenure, bc_get_cut_blocks, bc_get_protected_areas
       - **Environment (3):** bc_get_water_wells, bc_get_wildfire_weather_stations, bc_get_local_parks
       - **Natural resources (2):** bc_get_mining_tenure, bc_get_fish_habitat
       - **Health (2):** bc_get_emergency_rooms, bc_get_walk_in_clinics
       - **Transportation (2):** bc_get_highway_profiles, bc_get_road_structures
       - **Climate (1):** bc_get_climate_stations
    3. Add a short intro paragraph noting BC is the first OGC WFS 2.0 integration in the project (alongside CKAN + ArcGIS Hub), and linking to the docs://bc/wfs-query-guide resource for the two-step workflow.
    4. If README has a "Portal Technologies" or similar section, add WFS/OGC as the third portal tech.
    5. Add bc_ prompts (6) to the README prompts catalog and bc_ resources (7) to the resources catalog.

    Step D — update CLAUDE.md:

    1. In the Architecture section (or wherever portal technologies are listed), add a note that BC introduces WFS 2.0 (OGC) support via `shared/ogc.py`, making WFS the third portal technology alongside CKAN and ArcGIS Hub.
    2. Briefly describe the two-step CKAN -> WFS workflow: discover datasets via CKAN, then query features via WFS using object_name.
    3. Note the shared/ogc.py location alongside shared/arcgis_hub.py so future modules (Quebec, other provinces) know to reuse it.
    4. Do NOT change existing rules or the Module Pattern table — additive edits only.

    Final checks:
    - uv run pytest --cov=src/mcp_canada --cov-fail-under=95 passes with BC module included (coverage >= 95)
    - uv run pytest tests/integration/test_tool_scenarios.py::TestBcToolScenarios -m integration --timeout=120 passes against live endpoints (run separately from unit suite)
    - uv run pytest tests/integration/test_prompts_resources_scenarios.py::TestBcPromptsResources -m integration --timeout=30 passes
    - README.md bc_ tool count matches the actual count in tools.py (grep -c "^async def bc_" should equal 20)
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/british_columbia/ -x -q && uv run pytest --cov=src/mcp_canada --cov-fail-under=95 -q</automated>
  </verify>
  <done>
    - TestBcToolScenarios has 8 populated integration tests (no xfail placeholders)
    - TestBcPromptsResources has 3 populated integration tests
    - README.md contains a British Columbia section with all 20 bc_ tools
    - README.md tool count header updated (155 -> 175 or whatever the prior count was +20)
    - README.md prompt catalog and resource catalog include bc_ entries
    - CLAUDE.md references WFS/OGC as the third portal tech and shared/ogc.py
    - uv run pytest src/mcp_canada/modules/british_columbia/ -x -q is green
    - uv run pytest --cov=src/mcp_canada --cov-fail-under=95 passes (>= 95% coverage)
    - Integration suite (run manually with -m integration) passes against live endpoints
  </done>
</task>

</tasks>

<verification>
- uv run pytest src/mcp_canada/modules/british_columbia/ -x -q passes green
- uv run pytest --cov=src/mcp_canada --cov-fail-under=95 passes with BC module included
- uv run pytest tests/integration/test_tool_scenarios.py::TestBcToolScenarios -v -m integration --timeout=120 passes against live endpoints (manual run)
- uv run pytest tests/integration/test_prompts_resources_scenarios.py::TestBcPromptsResources -v -m integration --timeout=30 passes
- README.md includes British Columbia tool catalog section with all 20 tools and updated count
- CLAUDE.md references WFS/OGC as the third portal technology
- 6 bc_ prompts discoverable via client.list_prompts()
- 7 bc_ resources discoverable via client.list_resources()
</verification>

<success_criteria>
- BC module is production-ready: 20 tools + 6 prompts + 7 resources
- 7-file pattern compliance verified
- Integration tests verify live BCDC + BCGW WFS through MCP Client layer
- README + CLAUDE.md reflect the new capability and portal technology
- Coverage >= 95% across the project including BC module
- Phase 15 is complete — ready for /gsd:verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/15-british-columbia-government-open-data/15-04-SUMMARY.md`
</output>
</content>
</invoke>