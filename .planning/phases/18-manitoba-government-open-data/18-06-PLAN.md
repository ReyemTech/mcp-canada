---
phase: 18-manitoba-government-open-data
plan: 06
type: execute
wave: 5
depends_on: [18-05]
files_modified:
  - src/mcp_canada/modules/manitoba/client.py
  - src/mcp_canada/modules/manitoba/tools.py
  - src/mcp_canada/modules/manitoba/__tests__/test_client.py
  - src/mcp_canada/modules/manitoba/__tests__/test_tools.py
autonomous: true
requirements:
  - MB-17
must_haves:
  truths:
    - "manitoba_get_road_events, manitoba_get_winter_road_conditions, manitoba_get_traffic_cameras call Manitoba 511 API v3 via _511_get (key-gated, raw JSON list — Pitfall 3)"
    - "When MANITOBA_511_KEY env var is absent, every 511 tool returns make_error('NOT_CONFIGURED', ...) with instructions to obtain a key — NOT an exception"
    - "511 tools never call arcgis_hub.query_feature_service (511 is a custom REST API, not a FeatureServer)"
    - "Unit tests cover both the NOT_CONFIGURED (no key) path and the mocked-key happy path"
  artifacts:
    - path: src/mcp_canada/modules/manitoba/tools.py
      provides: "manitoba_get_road_events, manitoba_get_winter_road_conditions, manitoba_get_traffic_cameras"
      contains: "NOT_CONFIGURED"
    - path: src/mcp_canada/modules/manitoba/client.py
      provides: "fetch_road_events, fetch_winter_road_conditions, fetch_traffic_cameras bodies (via _511_get)"
  key_links:
    - from: "src/mcp_canada/modules/manitoba/client.py"
      to: "Manitoba 511 API v3"
      via: "_511_get(endpoint) — key from MANITOBA_511_KEY env var; raises Five11NotConfigured if absent"
      pattern: "_511_get\\(|Five11NotConfigured"
    - from: "src/mcp_canada/modules/manitoba/tools.py"
      to: "make_error NOT_CONFIGURED"
      via: "catch Five11NotConfigured → make_error"
      pattern: "Five11NotConfigured|NOT_CONFIGURED"
---

<objective>
Implement Manitoba's transport / 511 domain (MB-17) — CONDITIONAL on the Wave 0 spike's 511-key verdict. Unlike Alberta's keyless v2 feed, Manitoba 511 v3 requires a developer key. Tools ship regardless: if MANITOBA_511_KEY is set they fetch live data; if absent they return NOT_CONFIGURED with key-acquisition instructions. Live integration testing is deferred unless the spike confirmed the key is freely obtainable.

Purpose: Deliver the high-seasonal-value transport tools while honoring the no-auth-scraping discipline.
Output: 3 transport tools (road events, winter roads, cameras) with NOT_CONFIGURED fallback + unit tests for both paths.
</objective>

<execution_context>
@/Users/mariomeyer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomeyer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-manitoba-government-open-data/18-RESEARCH.md
@.planning/phases/18-manitoba-government-open-data/18-SPIKE.md
@.planning/phases/18-manitoba-government-open-data/18-01-SUMMARY.md
@CLAUDE.md
@.claude/rules/modules.md

# Reference: Alberta 511 (keyless) — adapt to key-gated v3
@src/mcp_canada/modules/alberta/client.py
@src/mcp_canada/modules/alberta/tools.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 511 client bodies (key-gated) + tests</name>
  <files>
    src/mcp_canada/modules/manitoba/client.py,
    src/mcp_canada/modules/manitoba/__tests__/test_client.py
  </files>
  <behavior>
    - fetch_road_events(max_records=5000): _511_get("events") → flatten to {rows, count} at CACHE_TTL_LIVE. Raises Five11NotConfigured (propagated) if no key.
    - fetch_winter_road_conditions(area_name=None, max_records): _511_get("winterroads"), optional client-side area filter. CACHE_TTL_LIVE.
    - fetch_traffic_cameras(max_records): _511_get("cameras") → flatten incl. Views array. CACHE_TTL_META (stable locations, 24h).
    - All three use get_limiter(RATE_GROUP_511, RATE_LIMIT_511) (10 calls/60s documented → conservative 2 r/s).
    - Tests: (a) with MANITOBA_511_KEY set (monkeypatch.setenv + AsyncMock api_get returning a JSON list) → returns rows; (b) without key (monkeypatch.delenv) → _511_get raises Five11NotConfigured.
  </behavior>
  <action>
    Fill fetch_road_events, fetch_winter_road_conditions, fetch_traffic_cameras using _511_get (NEVER arcgis_hub). _511_get already reads MANITOBA_511_KEY and raises Five11NotConfigured when absent (Plan 01). Flatten 511 rows to the Manitoba511Event/WinterRoad/Camera schemas. Fill TestManitoba511 (both key-present and key-absent paths). Use monkeypatch for env var; AsyncMock(return_value=[...]) for api_get — never call the live API in unit tests.
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/manitoba/__tests__/test_client.py::TestManitoba511 -x -v</automated>
  </verify>
  <done>3 511 client functions implemented via _511_get. Key-present path returns rows; key-absent path raises Five11NotConfigured. No arcgis_hub usage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 3 transport @tool functions (NOT_CONFIGURED fallback) + tool tests</name>
  <files>
    src/mcp_canada/modules/manitoba/tools.py,
    src/mcp_canada/modules/manitoba/__tests__/test_tools.py
  </files>
  <behavior>
    - manitoba_get_road_events(lang="en"): catches Five11NotConfigured → make_error("NOT_CONFIGURED", "Manitoba 511 API key not set. Obtain a developer key from https://www.manitoba511.ca/developers/doc and set MANITOBA_511_KEY."). Otherwise make_response.
    - manitoba_get_winter_road_conditions(area_name=None, lang="en"): same NOT_CONFIGURED fallback.
    - manitoba_get_traffic_cameras(lang="en"): same NOT_CONFIGURED fallback.
  </behavior>
  <action>
    Add the 3 @tool functions following conventions (standalone @tool, lang, make_response/make_error, manitoba_ prefix, single-line Use for: + 8+ Keywords:). Wrap client calls in try/except Five11NotConfigured → make_error("NOT_CONFIGURED", ...). Docstrings must state the key requirement and the env var name. Fill TestManitoba511 tool tests: (a) no-key → NOT_CONFIGURED error code; (b) with-key (mock client) → make_response envelope. Patch the client at the tools namespace.
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/manitoba/__tests__/test_tools.py -x -k "511 or RoadEvents or WinterRoad or Cameras" -v</automated>
  </verify>
  <done>3 transport tools defined. No-key path returns NOT_CONFIGURED (not exception). With-key path returns envelope. Docstrings document key requirement.</done>
</task>

</tasks>

<verification>
1. `uv run pytest src/mcp_canada/modules/manitoba/__tests__/ -x`
2. `uv run python -c "from mcp_canada.modules.manitoba import tools; assert hasattr(tools,'manitoba_get_road_events') and hasattr(tools,'manitoba_get_traffic_cameras'); print('OK')"`
</verification>

<success_criteria>
- MB-17 implemented: 3 transport tools ship
- NOT_CONFIGURED returned when key absent (verified via unit test)
- With-key happy path verified via mocked unit test
- 511 never uses arcgis_hub
- If spike verdict was "gated/paid": live integration deferred (documented in summary); tools still ship with NOT_CONFIGURED
</success_criteria>

<output>
Create `.planning/phases/18-manitoba-government-open-data/18-06-SUMMARY.md`: 3 tools, restate the spike's 511-key verdict, confirm NOT_CONFIGURED fallback, note whether live integration is enabled or deferred.
</output>
