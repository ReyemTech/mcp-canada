---
phase: 11-ircc-immigration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/mcp_canada/shared/parsers.py
  - src/mcp_canada/shared/__tests__/test_parsers.py
autonomous: true
requirements: [IRCC-01, IRCC-02, IRCC-03, IRCC-09]

must_haves:
  truths:
    - "fetch_and_parse() downloads XLSX bytes and returns list[dict] with snake_case keys"
    - "fetch_and_parse() downloads CSV bytes and returns list[dict] with snake_case keys"
    - "Privacy masking converts '--' string values to None during parsing"
    - "Column headers are normalized to snake_case matching IDENTIFIER_RE pattern"
    - "Parsed results are cached via cached_fetch() with configurable TTL"
    - "XLSX parsing uses pandas when available, falls back to openpyxl"
  artifacts:
    - path: "src/mcp_canada/shared/parsers.py"
      provides: "fetch_and_parse(), _parse_xlsx(), _parse_xlsx_pandas(), _parse_xlsx_openpyxl(), _parse_csv(), _normalize_key(), _mask_privacy()"
      exports: ["fetch_and_parse"]
    - path: "src/mcp_canada/shared/__tests__/test_parsers.py"
      provides: "Unit tests for all parser functions including pandas/openpyxl fallback"
      min_lines: 100
  key_links:
    - from: "src/mcp_canada/shared/parsers.py"
      to: "shared/cache.py"
      via: "cached_fetch(key, ttl, fetcher)"
      pattern: "cached_fetch"
    - from: "src/mcp_canada/shared/parsers.py"
      to: "httpx"
      via: "AsyncClient.get(url) for binary download"
      pattern: "httpx\\.AsyncClient"
---

<objective>
Create the shared file parser library that any module can use to fetch and parse XLSX, CSV, and XLS files from government URLs.

Purpose: Foundation utility for IRCC module and future modules needing tabular file parsing. Equivalent to shared/cache.py or shared/envelope.py in reusability.
Output: `shared/parsers.py` with `fetch_and_parse()` function, openpyxl added to pyproject.toml, full unit test coverage.
</objective>

<execution_context>
@/Users/mariomeyer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomeyer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ircc-immigration/11-RESEARCH.md

@src/mcp_canada/shared/cache.py
@src/mcp_canada/shared/envelope.py

<interfaces>
<!-- Executor needs these existing contracts -->

From src/mcp_canada/shared/cache.py:
```python
async def cached_fetch(key: str, ttl: int, fetcher: Callable) -> tuple[Any, bool]:
    """Returns (data, was_cached). fetcher is async callable returning data on miss."""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add openpyxl dependency and create shared parser with TDD</name>
  <files>pyproject.toml, src/mcp_canada/shared/parsers.py, src/mcp_canada/shared/__tests__/test_parsers.py</files>
  <behavior>
    - Test: _normalize_key("Country of Citizenship") returns "country_of_citizenship"
    - Test: _normalize_key("  Year ") returns "year" (strips whitespace)
    - Test: _normalize_key("123col") returns "col_123col" (leading digit prefixed)
    - Test: _normalize_key("") returns "col" (empty fallback)
    - Test: _normalize_key("a/b-c d") returns "a_b_c_d" (special chars to underscore)
    - Test: _mask_privacy("--") returns None
    - Test: _mask_privacy(" -- ") returns None (whitespace around dashes)
    - Test: _mask_privacy("123") returns "123" (non-masked value unchanged)
    - Test: _mask_privacy(42) returns 42 (non-string unchanged)
    - Test: _mask_privacy(None) returns None
    - Test: _parse_xlsx() uses pandas path when pandas is importable — mock `pandas.read_excel` to return a DataFrame, verify it was called with BytesIO(content) and sheet_name=sheet and skiprows=skip_rows
    - Test: _parse_xlsx() falls back to openpyxl path when pandas import fails — mock `builtins.__import__` to raise ImportError for pandas, verify openpyxl load_workbook is called instead
    - Test: _parse_xlsx_pandas() with synthetic DataFrame returns list[dict] with normalized keys and masked values
    - Test: _parse_xlsx_openpyxl() with synthetic openpyxl workbook bytes returns list[dict] with normalized keys and masked values
    - Test: _parse_xlsx_openpyxl() with skip_rows=1 skips first row before header
    - Test: _parse_xlsx_openpyxl() with sheet index selects correct sheet
    - Test: _parse_xlsx_openpyxl() with empty workbook returns []
    - Test: _parse_csv() with BOM-prefixed CSV returns list[dict] with normalized keys
    - Test: _parse_csv() with '--' values returns None in output
    - Test: fetch_and_parse() calls cached_fetch with correct cache key, returns (list[dict], was_cached)
    - Test: fetch_and_parse() detects .csv suffix and routes to _parse_csv
    - Test: fetch_and_parse() detects .xlsx suffix and routes to _parse_xlsx
  </behavior>
  <action>
    1. Run `uv add openpyxl` to add openpyxl as a base dependency. Also run `uv add --optional ircc pandas xlrd` to add optional extras per user decision.

    2. Write failing tests in `src/mcp_canada/shared/__tests__/test_parsers.py`. Use synthetic XLSX bytes created via openpyxl in fixtures (create a minimal workbook in memory, save to BytesIO, read .getvalue()). For CSV tests, use plain string bytes. Mock `httpx.AsyncClient` and `cached_fetch` for fetch_and_parse tests. For pandas path tests, mock `pandas.read_excel` returning a DataFrame with sample data; for fallback tests, mock the pandas import to raise ImportError.

    3. Create `src/mcp_canada/shared/parsers.py` implementing:
       - `_normalize_key(header: str) -> str`: regex replace non-alphanumeric with underscore, collapse multiple underscores, strip, lowercase, prefix "col_" if starts with digit, fallback "col" if empty
       - `_mask_privacy(value: Any) -> Any`: if isinstance str and strip() == '--' return None, else return value. Also convert pandas NaN/NaT to None if pandas is available.
       - `_parse_xlsx_pandas(content: bytes, sheet: str | int = 0, skip_rows: int = 0) -> list[dict]`: use `pandas.read_excel(BytesIO(content), sheet_name=sheet, skiprows=skip_rows)`, convert DataFrame to list[dict] via `df.to_dict(orient="records")`, apply _normalize_key to column names and _mask_privacy to all values. Replace pandas NaN with None.
       - `_parse_xlsx_openpyxl(content: bytes, sheet: str | int = 0, skip_rows: int = 0) -> list[dict]`: use openpyxl load_workbook(BytesIO(content), read_only=True, data_only=True), try/finally wb.close(), extract headers from first row after skip_rows, build list[dict] with _normalize_key and _mask_privacy
       - `_parse_xlsx(content: bytes, sheet: str | int = 0, skip_rows: int = 0) -> list[dict]`: try pandas first (`try: import pandas`), call `_parse_xlsx_pandas()`. On ImportError, fall back to `_parse_xlsx_openpyxl()`. Per user decision: "Parser uses pandas when available (better handling of multi-sheet, encoding, types), falls back to openpyxl + stdlib csv."
       - `_parse_csv(content: bytes, skip_rows: int = 0) -> list[dict]`: decode with utf-8-sig, csv.DictReader, apply _normalize_key and _mask_privacy. Note: skip_rows for CSV skips lines before passing to DictReader (skip metadata lines before header)
       - `_parse_xls(content: bytes, sheet: str | int = 0, skip_rows: int = 0) -> list[dict]`: try importing xlrd, if unavailable raise ImportError with message "Install xlrd for .xls support: pip install mcp-canada[ircc]". If xlrd available, use xlrd.open_workbook(file_contents=content) to parse
       - `async def fetch_and_parse(url: str, sheet: str | int = 0, skip_rows: int = 0, ttl: int = 86400) -> tuple[list[dict], bool]`: build cache_key from url+sheet+skip_rows, define inner async _fetch() that uses httpx.AsyncClient(timeout=60.0) to GET url and route to correct parser by URL suffix (.csv -> _parse_csv, .xls -> _parse_xls, else -> _parse_xlsx). Return via cached_fetch(cache_key, ttl, _fetch).

    IMPORTANT: Do NOT use the shared api_get() — it calls .json() on the response. Use httpx.AsyncClient directly and read .content (bytes).
    IMPORTANT: openpyxl workbook must be closed in try/finally, NOT context manager.
    IMPORTANT: Let exceptions propagate from _fetch() — never return [] on error. cached_fetch should NOT cache errors.
    IMPORTANT: The pandas-first pattern is a locked user decision. _parse_xlsx() MUST attempt `import pandas` first and only fall back to openpyxl on ImportError.
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/shared/__tests__/test_parsers.py -x -v</automated>
  </verify>
  <done>All parser unit tests pass. _normalize_key produces IDENTIFIER_RE-safe keys. _mask_privacy converts '--' to None. _parse_xlsx() uses pandas when available and falls back to openpyxl when not. Both pandas and openpyxl paths are tested (mock import failure for fallback test). fetch_and_parse routes XLSX/CSV/XLS correctly and caches via cached_fetch. openpyxl is in pyproject.toml dependencies, pandas in optional [ircc] extra.</done>
</task>

</tasks>

<verification>
- `uv run pytest src/mcp_canada/shared/__tests__/test_parsers.py -x -v` — all tests green
- `uv run pyright src/mcp_canada/shared/parsers.py` — no type errors
- `python -c "from mcp_canada.shared.parsers import fetch_and_parse"` — importable
</verification>

<success_criteria>
- shared/parsers.py exists with fetch_and_parse() as public API
- openpyxl in pyproject.toml base dependencies, pandas+xlrd in optional [ircc] extra
- _parse_xlsx() tries pandas first, falls back to openpyxl (per user decision)
- All unit tests pass covering XLSX (both pandas and openpyxl paths), CSV, privacy masking, key normalization
- No type errors from pyright
</success_criteria>

<output>
After completion, create `.planning/phases/11-ircc-immigration/11-01-SUMMARY.md`
</output>
