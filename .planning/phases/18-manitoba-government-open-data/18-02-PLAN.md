---
phase: 18-manitoba-government-open-data
plan: 02
type: execute
wave: 1
depends_on: [18-01]
files_modified:
  - src/mcp_canada/modules/manitoba/client.py
  - src/mcp_canada/modules/manitoba/tools.py
  - src/mcp_canada/modules/manitoba/__tests__/test_client.py
  - src/mcp_canada/modules/manitoba/__tests__/test_tools.py
autonomous: true
requirements:
  - MB-01
  - MB-02
  - MB-03
  - MB-04
  - MB-05
must_haves:
  truths:
    - "manitoba_search_datasets queries the ArcGIS Hub Search API (/api/search/v1/collections/all/items) by keyword with optional category and pagination, returning flat dataset summaries"
    - "manitoba_get_dataset_details returns a dataset's FeatureServer URL, download URLs, and flattened metadata"
    - "manitoba_query_dataset auto-routes: ESRI FeatureServer → arcgis_hub.query_feature_service; CSV/JSON/GeoJSON/XLSX → fetch_and_parse; else metadata-only"
    - "manitoba_list_organizations returns Manitoba government publishing orgs from the Hub"
    - "manitoba_list_categories returns Hub tags/categories"
    - "TestSharedApiGetContract is filled: patches mcp_canada.modules.manitoba.client.api_get and asserts _hub_get processes Hub JSON directly (never .get('success'))"
    - "Every discovery tool: standalone @tool, lang Literal en/fr, make_response/make_error, manitoba_ prefix, single-line Use for: + 8+ Keywords:"
  artifacts:
    - path: src/mcp_canada/modules/manitoba/tools.py
      provides: "5 discovery @tool functions"
      contains: "def manitoba_search_datasets"
    - path: src/mcp_canada/modules/manitoba/client.py
      provides: "5 discovery client function bodies (fetch_search_datasets, fetch_dataset_details, fetch_query_dataset, fetch_organizations, fetch_categories)"
  key_links:
    - from: "src/mcp_canada/modules/manitoba/client.py"
      to: "ArcGIS Hub Search API"
      via: "_hub_get(params) — Hub JSON, NOT CKAN envelope"
      pattern: "_hub_get\\("
    - from: "src/mcp_canada/modules/manitoba/client.py"
      to: "mcp_canada.shared.arcgis_hub.query_feature_service / fetch_and_parse"
      via: "fetch_query_dataset auto-router"
      pattern: "query_feature_service|fetch_and_parse"
---

<objective>
Implement the 5 ArcGIS Hub discovery tools (MB-01…MB-05) — the same shape as York Region Phase 14 and Alberta Phase 17. These are the entry point for everything not curated: agents search the Manitoba geoportal, get dataset details (incl. FeatureServer URLs), and query any dataset via the auto-router.

Purpose: Establish the Hub discovery surface and fill TestSharedApiGetContract so the parsed-dict contract is enforced from the first wave.
Output: 5 working discovery tools + client bodies + unit tests.
</objective>

<execution_context>
@/Users/mariomeyer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomeyer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-manitoba-government-open-data/18-RESEARCH.md
@.planning/phases/18-manitoba-government-open-data/18-01-SUMMARY.md
@CLAUDE.md
@.claude/rules/modules.md
@.claude/rules/tests.md

# Reference: ArcGIS Hub discovery pattern + auto-router
@src/mcp_canada/modules/york_region/client.py
@src/mcp_canada/modules/york_region/tools.py
@src/mcp_canada/modules/alberta/client.py
@src/mcp_canada/modules/alberta/tools.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Discovery client bodies + TestSharedApiGetContract</name>
  <files>
    src/mcp_canada/modules/manitoba/client.py,
    src/mcp_canada/modules/manitoba/__tests__/test_client.py
  </files>
  <behavior>
    - fetch_search_datasets(query, category=None, limit=10, offset=0): calls _hub_get with q/num/start/category params; returns ({"results":[flat summaries], "total":N}, was_cached). Wrapped in cached_fetch (CACHE_TTL_SEARCH).
    - fetch_dataset_details(dataset_id): resolves Hub item; returns ({"details": {...feature_server_url, download_urls, metadata}}, was_cached) at CACHE_TTL_META.
    - fetch_query_dataset(dataset_id, where, max_records, include_geometry): resolves the dataset's resource; if FeatureServer → query_feature_service; if CSV/JSON/GeoJSON/XLSX → fetch_and_parse; else metadata-only payload. Returns (payload, was_cached).
    - fetch_organizations(): Hub orgs list → ({"organizations":[...]}, was_cached) at CACHE_TTL_META.
    - fetch_categories(): Hub tags/categories → ({"categories":[...]}, was_cached) at CACHE_TTL_META.
    - TestSharedApiGetContract: patch mcp_canada.modules.manitoba.client.api_get → AsyncMock returning a Hub-shaped dict (results/total). Assert fetch_search_datasets calls api_get once and never inspects .get("success"). Assert non-dict response raises httpx.HTTPStatusError.
  </behavior>
  <action>
    Fill the 5 discovery client stubs from Plan 01. Use _hub_get for all Hub Search calls (Hub returns JSON directly — Pitfall 2: never call .get("success")/.get("result")). Use CACHE_KEY_PREFIX for cache keys and get_limiter(RATE_GROUP_HUB, RATE_LIMIT_HUB) before each network call. Flatten Hub `results`/`features` entries into ManitobaDatasetSummary shape (id, title, snippet, type, owner, url, modified). For the auto-router in fetch_query_dataset, follow alberta.client.fetch_query_dataset exactly (prefer FeatureServer over MapServer at resource_index 0; route by format; metadata-only fallback for PDF/ZIP/KML).

    Fill TestSharedApiGetContract + TestManitobaSearchDatasets/GetDatasetDetails/QueryDataset/ListOrgs/ListCategories client test classes. Patch api_get at mcp_canada.modules.manitoba.client.api_get (module-local, BC/Alberta pattern). Use AsyncMock(return_value=...) — NEVER MagicMock(json=lambda...) (Pitfall). Cover happy path + the non-dict-response HTTPStatusError path. Patch arcgis_hub.query_feature_service / fetch_and_parse for the router tests.
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/manitoba/__tests__/test_client.py::TestSharedApiGetContract src/mcp_canada/modules/manitoba/__tests__/test_client.py::TestManitobaSearchDatasets src/mcp_canada/modules/manitoba/__tests__/test_client.py::TestManitobaQueryDataset -x -v</automated>
  </verify>
  <done>5 discovery client functions implemented; TestSharedApiGetContract + 5 discovery client test classes pass. _hub_get never inspects CKAN envelope keys. Auto-router routes FeatureServer/file/metadata correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 5 discovery @tool functions + tool tests</name>
  <files>
    src/mcp_canada/modules/manitoba/tools.py,
    src/mcp_canada/modules/manitoba/__tests__/test_tools.py
  </files>
  <behavior>
    - manitoba_search_datasets(query, category=None, limit=10, offset=0, lang="en"): returns make_response with _meta source api="manitoba-geoportal-hub"; data has results+total.
    - manitoba_get_dataset_details(dataset_id, lang="en"): returns details incl. feature_server_url.
    - manitoba_query_dataset(dataset_id, where="1=1", max_records=5000, include_geometry=False, lang="en"): routed payload.
    - manitoba_list_organizations(lang="en") / manitoba_list_categories(lang="en"): list payloads.
    - Each maps client exceptions to make_error (UPSTREAM_ERROR / NOT_FOUND).
  </behavior>
  <action>
    Add the 5 @tool functions. Standalone `@tool` from fastmcp.tools (NEVER @mcp.tool). Every tool: lang: Literal["en","fr"] = "en" (or Annotated), returns make_response(...)/make_error(...), manitoba_ prefix, single-line docstring with "Use for:" + "Keywords:" (8+ terms). Document in manitoba_search_datasets docstring that this is the Government of Manitoba's provincial ArcGIS Hub (geoportal.gov.mb.ca); municipal data (Winnipeg/Brandon) is on separate portals. Patch the client at the tools namespace (mcp_canada.modules.manitoba.tools._client.*) in tests. Fill the 5 discovery tool test classes (happy path + error path + lang passthrough to envelope).
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/manitoba/__tests__/test_tools.py -x -k "SearchDatasets or GetDatasetDetails or QueryDataset or ListOrgs or ListCategories" -v</automated>
  </verify>
  <done>5 discovery tools defined and tested. Envelope has _meta source; lang passes through; errors return make_error not exceptions. test_quality docstring checks pass.</done>
</task>

</tasks>

<verification>
1. `uv run pytest src/mcp_canada/modules/manitoba/__tests__/ -x` — green
2. `uv run python -c "from mcp_canada.modules.manitoba import tools; assert hasattr(tools,'manitoba_search_datasets') and hasattr(tools,'manitoba_query_dataset'); print('OK')"`
</verification>

<success_criteria>
- MB-01…MB-05 implemented: 5 discovery tools callable
- TestSharedApiGetContract green (parsed-dict contract enforced)
- Auto-router (fetch_query_dataset) handles FeatureServer / file / metadata-only
- All discovery tools follow conventions (standalone @tool, lang, envelope, prefix, docstrings)
</success_criteria>

<output>
After completion, create `.planning/phases/18-manitoba-government-open-data/18-02-SUMMARY.md`: list the 5 tools, confirm _hub_get Hub-JSON contract, note the auto-router routing table, any Hub Search param quirks discovered (num vs limit, start vs offset).
</output>
