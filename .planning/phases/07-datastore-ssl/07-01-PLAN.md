---
phase: 07-datastore-ssl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/mcp_canada/server.py
  - src/mcp_canada/modules/datastore/__init__.py
  - src/mcp_canada/modules/datastore/constants.py
  - src/mcp_canada/modules/datastore/schemas.py
  - src/mcp_canada/modules/datastore/client.py
  - src/mcp_canada/modules/datastore/__tests__/conftest.py
  - src/mcp_canada/modules/datastore/__tests__/test_client.py
autonomous: true
requirements:
  - DS-07
  - DS-08

must_haves:
  truths:
    - "Identifier validation rejects SQL metacharacters before any SQL executes"
    - "Concurrent tool calls remain responsive (async I/O via aiosqlite)"
    - "DB file auto-created at ~/.mcp-canada/datastore.db on first use"
    - "Server accepts --ephemeral flag and stores it in _config"
  artifacts:
    - path: "src/mcp_canada/modules/datastore/__init__.py"
      provides: "MODULE_NAME and MODULE_DESCRIPTION"
      contains: "MODULE_NAME"
    - path: "src/mcp_canada/modules/datastore/constants.py"
      provides: "DB_PATH, IDENTIFIER_RE, MAX_QUERY_ROWS, ALLOWED_PREFIXES"
      contains: "IDENTIFIER_RE"
    - path: "src/mcp_canada/modules/datastore/schemas.py"
      provides: "Pydantic models for datastore responses"
      contains: "class"
    - path: "src/mcp_canada/modules/datastore/client.py"
      provides: "Async DB singleton, identifier validation, all CRUD operations"
      exports: ["get_db", "close_db", "create_table", "insert_rows", "run_query", "list_tables", "get_schema", "drop_table"]
    - path: "src/mcp_canada/modules/datastore/__tests__/test_client.py"
      provides: "Unit tests for client functions and identifier validation"
      min_lines: 80
  key_links:
    - from: "src/mcp_canada/modules/datastore/client.py"
      to: "aiosqlite"
      via: "aiosqlite.connect() singleton"
      pattern: "aiosqlite\\.connect"
    - from: "src/mcp_canada/modules/datastore/client.py"
      to: "src/mcp_canada/modules/datastore/constants.py"
      via: "imports IDENTIFIER_RE, DB_PATH, MAX_QUERY_ROWS"
      pattern: "from.*constants import"
---

<objective>
Create the datastore module infrastructure: constants, schemas, async SQLite client with identifier validation, and the --ephemeral server flag.

Purpose: Establishes the async SQLite persistence layer that all 6 datastore tools (Plan 02) will call. Identifier validation (DS-07) and async guarantees (DS-08) must be proven with tests before any tools are built on top.

Output: Working client.py with all DB operations, fully tested. Tools.py is NOT created yet (Plan 02).
</objective>

<execution_context>
@/Users/mariomeyer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomeyer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-datastore-ssl/07-CONTEXT.md
@.planning/phases/07-datastore-ssl/07-RESEARCH.md

@src/mcp_canada/server.py
@src/mcp_canada/modules/bank_of_canada/__init__.py
@src/mcp_canada/modules/bank_of_canada/constants.py
@src/mcp_canada/modules/bank_of_canada/schemas.py
@pyproject.toml

<interfaces>
<!-- Existing project interfaces the executor needs -->

From src/mcp_canada/server.py:
```python
_config: dict[str, Any] = {}  # Module-level config, populated by main() via _config.update(vars(args))

def _build_parser() -> argparse.ArgumentParser:
    # Add --ephemeral here, before subparsers
```

From src/mcp_canada/shared/envelope.py:
```python
def make_response(data, *, api_name: str, api_url: str = "", cached: bool = False, lang: str = "en") -> dict
def make_error(code: str, message: str, *, lang: str = "en", **extras) -> dict
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Datastore module skeleton + client with TDD</name>
  <files>
    pyproject.toml,
    src/mcp_canada/server.py,
    src/mcp_canada/modules/datastore/__init__.py,
    src/mcp_canada/modules/datastore/constants.py,
    src/mcp_canada/modules/datastore/schemas.py,
    src/mcp_canada/modules/datastore/client.py,
    src/mcp_canada/modules/datastore/__tests__/__init__.py,
    src/mcp_canada/modules/datastore/__tests__/conftest.py,
    src/mcp_canada/modules/datastore/__tests__/test_client.py
  </files>
  <behavior>
    - _validate_identifier("valid_name") passes silently
    - _validate_identifier("has;semicolon") raises ValueError
    - _validate_identifier("") raises ValueError
    - _validate_identifier("a" * 65) raises ValueError (over 63 chars)
    - _validate_identifier("123starts_with_digit") raises ValueError
    - get_db(":memory:") returns aiosqlite.Connection with WAL mode
    - get_db() called twice returns same connection (singleton)
    - create_table("test", [("id", "INTEGER"), ("name", "TEXT")]) returns (None, False)
    - create_table("bad;name", ...) raises ValueError
    - insert_rows("test", [{"id": 1, "name": "a"}]) returns (1, False) — count and was_cached
    - run_query("SELECT * FROM test") returns ((columns, rows, truncated), False)
    - run_query("DELETE FROM test") raises ValueError (not in allowed prefixes)
    - run_query("select * from test") works (case insensitive)
    - list_tables() returns (["test"], False) — list and was_cached
    - get_schema("test") returns ([column_info_dicts], False)
    - drop_table("test") returns (None, False)
    - _infer_sqlite_type(True) returns "INTEGER" (bool before int check)
    - _infer_sqlite_type(1) returns "INTEGER"
    - _infer_sqlite_type(1.5) returns "REAL"
    - _infer_sqlite_type("hello") returns "TEXT"
  </behavior>
  <action>
    RED phase first — write all tests in test_client.py against the behavior list above. All client functions return `(data, was_cached)` tuples per CLAUDE.md convention. For datastore, `was_cached` is always `False` (SQLite is local I/O, not a cached API). Tests must unpack tuples: `result, was_cached = await create_table(...)`.

    conftest.py: Create an async fixture `db` that calls `aiosqlite.connect(":memory:")`, sets row_factory and WAL mode, yields the connection, then closes it. Also create a fixture that patches `client._db` with this in-memory connection so `get_db()` returns it.

    GREEN phase — implement all files:

    1. **pyproject.toml**: Add `"aiosqlite>=0.22.0"` to `[project] dependencies` list. Run `uv sync` after.

    2. **server.py**: Add `--ephemeral` argument to `_build_parser()`, right before the `subparsers` line (line ~119). Use `action="store_true"`, `default=False`, help="Use in-memory SQLite datastore (no persistence)". This gets stored in `_config` automatically via `_config.update(vars(args))`.

    3. **__init__.py**: Follow bank_of_canada pattern exactly:
       ```python
       MODULE_NAME = "datastore"
       MODULE_DESCRIPTION = (
           "Local SQLite datastore tools: create tables, insert data, "
           "run SQL queries, list tables, inspect schemas, and drop tables. "
           "Persists data across server restarts at ~/.mcp-canada/datastore.db."
       )
       ```

    4. **constants.py**:
       - `DB_PATH = Path.home() / ".mcp-canada" / "datastore.db"`
       - `IDENTIFIER_RE = re.compile(r'^[a-zA-Z_][a-zA-Z0-9_]{0,63}$')`
       - `MAX_QUERY_ROWS = 1000`
       - `ALLOWED_QUERY_PREFIXES = ("SELECT", "PRAGMA", "EXPLAIN", "CREATE INDEX")`

    5. **schemas.py**: Pydantic v2 models (flat):
       - `ColumnDef(name: str, type: str, notnull: bool = False, pk: bool = False)`
       - `QueryResult(columns: list[str], rows: list[dict], row_count: int, truncated: bool)`
       - `TableInfo(name: str, column_count: int)`

    6. **client.py**: Module-level singleton pattern per RESEARCH.md. ALL public functions return `(data, False)` tuples to comply with CLAUDE.md convention (`(data, was_cached)`):
       - `_db: aiosqlite.Connection | None = None`
       - `_validate_identifier(name: str) -> None` — raises ValueError if not matching IDENTIFIER_RE
       - `_infer_sqlite_type(value: object) -> str` — bool before int check
       - `_db_path() -> str` — reads `server._config.get("ephemeral")` to decide `:memory:` vs DB_PATH. Creates parent dirs for file path.
       - `get_db() -> aiosqlite.Connection` — lazy singleton, sets WAL + foreign_keys + row_factory on first call
       - `close_db() -> None` — closes and sets _db = None
       - `create_table(table: str, columns: list[tuple[str, str]]) -> tuple[None, bool]` — validates all identifiers, CREATE TABLE IF NOT EXISTS, returns `(None, False)`
       - `insert_rows(table: str, rows: list[dict]) -> tuple[int, bool]` — validates table + column names, executemany with parameterized values, returns `(row_count, False)`
       - `run_query(sql: str) -> tuple[tuple[list[str], list[dict], bool], bool]` — validates prefix (case-insensitive), fetchmany(MAX_QUERY_ROWS), returns `((columns, rows_as_dicts, truncated), False)`
       - `list_tables() -> tuple[list[str], bool]` — SELECT name FROM sqlite_master WHERE type='table' ORDER BY name, returns `(table_names, False)`
       - `get_schema(table: str) -> tuple[list[dict], bool]` — validates identifier, PRAGMA table_info, returns `(column_info, False)`
       - `drop_table(table: str) -> tuple[None, bool]` — validates identifier, DROP TABLE IF EXISTS, returns `(None, False)`

    IMPORTANT: Do NOT use `cached_fetch()` or `get_limiter()` — datastore is local I/O, not an external API. Per RESEARCH.md: "no cached_fetch (SQLite is not cached via aiocache), no rate limiter (local I/O, no external API)". The `was_cached=False` convention is maintained for interface consistency per CLAUDE.md.

    After implementing, run `uv sync` to install aiosqlite, then run all tests.
  </action>
  <verify>
    <automated>cd /Users/mariomeyer/code/ReyemTech/mcp-canada && uv run pytest src/mcp_canada/modules/datastore/__tests__/test_client.py -x -v</automated>
  </verify>
  <done>
    All client unit tests pass. Every public client function returns (data, False) tuples. _validate_identifier rejects SQL metacharacters. get_db returns async singleton with WAL mode. create_table, insert_rows, run_query, list_tables, get_schema, drop_table all work with in-memory DB and return (data, was_cached) tuples. --ephemeral flag accepted by server parser. aiosqlite in pyproject.toml dependencies.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest src/mcp_canada/modules/datastore/__tests__/ -x -v` — all client tests pass
- `uv run ruff check src/mcp_canada/modules/datastore/` — no lint errors
- `uv run pyright src/mcp_canada/modules/datastore/` — no type errors
- `grep "aiosqlite" pyproject.toml` — dependency listed
- `uv run python -c "from mcp_canada.modules.datastore import MODULE_NAME; print(MODULE_NAME)"` — prints "datastore"
</verification>

<success_criteria>
- Datastore client functions are fully tested with in-memory SQLite
- All public client functions return (data, was_cached) tuples
- Identifier validation rejects all SQL metacharacters
- All operations are async (aiosqlite, no asyncio.to_thread)
- Server accepts --ephemeral flag
- aiosqlite added to pyproject.toml
- Module auto-discovers via FileSystemProvider
</success_criteria>

<output>
After completion, create `.planning/phases/07-datastore-ssl/07-01-SUMMARY.md`
</output>
