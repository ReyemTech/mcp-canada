---
phase: 14-york-region-municipal-government-open-data
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - src/mcp_canada/modules/york_region/prompts.py
  - src/mcp_canada/modules/york_region/resources.py
  - src/mcp_canada/modules/york_region/__tests__/test_prompts_resources.py
  - tests/integration/test_tool_scenarios.py
  - tests/integration/test_prompts_resources_scenarios.py
  - README.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [YR-12, YR-13, YR-14]

must_haves:
  truths:
    - "York Region module exposes 4-6 bilingual @prompt functions via prompts.py"
    - "York Region module exposes 6-10 @resource functions via resources.py with data://, docs://, template:// URIs"
    - "All prompts appear via prompts/list and resources via resources/list through MCP Client"
    - "Integration test suite TestYorkRegionToolScenarios calls tools through call_tool against live ArcGIS Hub endpoints"
    - "Integration tests for prompts/resources verify discovery and content retrieval"
    - "README has a York Region section listing all ~27 tools + portal catalog"
    - "README tool count is updated to reflect new York Region tools"
    - "REQUIREMENTS.md has YR-01 through YR-14 defined and mapped to Phase 14 plans"
    - "Module structure matches the 7-file pattern (init, constants, schemas, client, tools, prompts, resources)"
  artifacts:
    - path: "src/mcp_canada/modules/york_region/prompts.py"
      provides: "4-6 bilingual @prompt functions with york_region_ prefix"
      contains: "@prompt"
    - path: "src/mcp_canada/modules/york_region/resources.py"
      provides: "6-10 zero-parameter @resource functions with data://, docs://, template:// URIs"
      contains: "@resource"
    - path: "src/mcp_canada/modules/york_region/__tests__/test_prompts_resources.py"
      provides: "Unit tests for prompts and resources (content + URI + bilingual)"
      contains: "class Test"
    - path: "tests/integration/test_tool_scenarios.py"
      provides: "TestYorkRegionToolScenarios integration tests through MCP Client"
      contains: "TestYorkRegionToolScenarios"
    - path: "tests/integration/test_prompts_resources_scenarios.py"
      provides: "TestYorkRegionPromptsResources integration tests"
      contains: "york_region"
    - path: "README.md"
      provides: "York Region section with tool list + updated total count"
      contains: "York Region"
    - path: ".planning/REQUIREMENTS.md"
      provides: "YR-01 through YR-14 requirement definitions + traceability rows"
      contains: "YR-01"
  key_links:
    - from: "src/mcp_canada/modules/york_region/prompts.py"
      to: "fastmcp.prompts @prompt decorator"
      via: "Standalone @prompt imports"
      pattern: "from fastmcp.prompts import"
    - from: "src/mcp_canada/modules/york_region/resources.py"
      to: "fastmcp.resources @resource decorator"
      via: "Standalone @resource imports"
      pattern: "from fastmcp.resources import"
    - from: "tests/integration/test_tool_scenarios.py"
      to: "MCP Client layer via call_tool('call_tool', ...)"
      via: "Integration fixture using mcp instance"
      pattern: "call_tool.*york_region_|call_tool.*markham_"
---

<objective>
Complete Phase 14 by adding the 7-file pattern's missing pieces (prompts.py, resources.py), wiring integration tests through MCP Client, updating README, and finalizing REQUIREMENTS.md with YR-01 through YR-14.

Purpose: Make the York Region module fully discoverable and documented. Agents should see york_region prompts in prompts/list and resources in resources/list. README should accurately reflect the new tool catalog.
Output: 2 new module files + integration tests + README update + REQUIREMENTS.md finalization.
</objective>

<execution_context>
@/Users/mariomeyer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomeyer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-york-region-municipal-government-open-data/14-CONTEXT.md
@.planning/phases/14-york-region-municipal-government-open-data/14-RESEARCH.md
@.planning/phases/14-york-region-municipal-government-open-data/14-01-SUMMARY.md
@.planning/phases/14-york-region-municipal-government-open-data/14-02-SUMMARY.md

@src/mcp_canada/modules/toronto/prompts.py
@src/mcp_canada/modules/toronto/resources.py
@src/mcp_canada/modules/toronto/__tests__/test_prompts_resources.py
@tests/integration/test_tool_scenarios.py
@tests/integration/test_prompts_resources_scenarios.py
@README.md

<interfaces>
<!-- FastMCP prompt and resource contracts (from CLAUDE.md Prompt and Resource Rules) -->

```python
# Prompts — bilingual, prefixed
from typing import Annotated, Literal
from fastmcp.prompts import Message, prompt

@prompt
async def york_region_explore_transit(
    lang: Annotated[Literal["en", "fr"], "Language: 'en' or 'fr'"] = "en",
) -> list[Message]:
    """Guided workflow for exploring YRT/Viva transit data.

    Use for: starting a transit exploration session.
    """
    ...

# Resources — zero-parameter, URI-prefixed
from fastmcp.resources import resource

@resource(uri="data://york_region/portals")
def york_region_portals() -> str:
    """JSON catalog of the 4 verified ArcGIS Hub portals + 6 municipalities without public portals."""
    return json.dumps({...})
```

From tests/integration/test_tool_scenarios.py (existing fixture pattern):
```python
# Build mcp instance with providers + transforms, then:
async with Client(mcp) as client:
    result = await client.call_tool('call_tool', {
        'name': 'york_region_search_datasets',
        'arguments': {'query': 'transit', 'limit': 5},
    })
    data = json.loads(result.content[0].text)
    assert "_meta" in data
    assert data["_meta"]["source"]["api"] == "arcgis-hub"
```
</interfaces>

<requirements_to_add>
<!-- Insert into .planning/REQUIREMENTS.md after "### Toronto Open Data" block. Also add traceability rows. -->

### York Region Municipal Open Data

- **YR-01**: Shared `shared/arcgis_hub.py` provides a reusable ArcGIS Hub Search API + FeatureServer async client usable by any Canadian municipal module that publishes via ArcGIS Hub
- **YR-02**: Client supports `/api/search/v1/collections/all/items` Hub Search endpoint with pagination via offset/limit (NOT the deprecated `/api/v2/datasets` which returns 404)
- **YR-03**: Client supports FeatureServer query with `&f=geojson` and auto-paginates while `exceededTransferLimit=true` up to a MAX_RECORDS cap of 5000 per call, returning a `truncated` flag
- **YR-04**: York Region module covers 4 verified ArcGIS Hub portals (york_region, markham, newmarket, aurora) and 6 municipalities without public portals return structured NOT_FOUND (vaughan, richmond_hill, king, east_gwillimbury, whitchurch_stouffville general data, georgina)
- **YR-05**: Each verified portal gets 5 discovery tools: search_datasets, get_dataset_details, query_features, list_organizations, list_categories (total 20 discovery tools)
- **YR-06**: Agent can search YRT/Viva transit stops and routes from York Region Transportation FeatureServer (no licensed GTFS feed)
- **YR-07**: Agent can fetch York Region regional road network from the Transportation FeatureServer
- **YR-08**: Agent can query York Region public health & safety datasets (beach water testing, hospitals, drinking water adverse incidents)
- **YR-09**: Agent can query York Region 2021 Census demographics (age/sex or income) by Dissemination Area with optional CSDNAME filter, returning a focused field set
- **YR-10**: Agent can query York Region waste management data (annual diversion tonnages or solid waste site locations)
- **YR-11**: Agent can search Markham civic addresses and road network (SLRN) via curated tools
- **YR-12**: York Region module has prompts.py with 4-6 bilingual @prompt functions covering discovery workflows and curated data
- **YR-13**: York Region module has resources.py with 6-10 zero-parameter @resource functions using data://, docs://, template:// URI schemes, including a portal catalog and municipality list
- **YR-14**: All York Region tools follow mcp-canada conventions (standalone @tool, make_response/make_error envelope, Use-for + Keywords single-line docstrings, municipality prefix), are discoverable via discover_tools, and README reflects the new tool catalog
</requirements_to_add>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create prompts.py + resources.py + unit tests</name>
  <files>
    src/mcp_canada/modules/york_region/prompts.py,
    src/mcp_canada/modules/york_region/resources.py,
    src/mcp_canada/modules/york_region/__tests__/test_prompts_resources.py
  </files>
  <behavior>
    - prompts.py has 4-6 @prompt functions with york_region_, markham_, or generic yr_ prefix. All accept `lang: Annotated[Literal["en", "fr"], "Language: 'en' or 'fr'"] = "en"`.
    - Guided workflow prompts return list[Message] with ≥2 messages (user + assistant)
    - Quick lookup prompts return str with specific tool name + parameters
    - resources.py has 6-10 @resource functions, all zero-parameter (no lang arg — bilingual content inline)
    - Resources cover: portals catalog (data://), municipality list with population (data://), dataset type glossary (data://), ESRI field naming guide (docs://), census variable reference (docs://), waste diversion interpretation guide (docs://), transit query template (template://), feature query response template (template://)
    - All @prompt functions are discoverable via FastMCP — test verifies with `mcp.list_prompts()` pattern
    - All @resource URIs follow the scheme: `data://york_region/<name>`, `docs://york_region/<name>`, `template://york_region/<name>`
    - data:// resources return valid JSON (json.dumps parses successfully)
    - docs:// resources return non-empty markdown
    - template:// resources contain `{placeholder}` syntax
  </behavior>
  <action>
    **Create `src/mcp_canada/modules/york_region/prompts.py`** following the Toronto pattern (see `src/mcp_canada/modules/toronto/prompts.py`). Include exactly 5 prompts:

    1. `york_region_explore_transit(lang)` — **guided workflow** (list[Message]):
       - Use for: starting a YRT/Viva transit analysis session
       - User message: "I want to explore York Region transit data. Where should I start?"
       - Assistant message (EN): "Start with `york_region_get_transit_stops` to search stops by name, or `york_region_get_transit_routes` to list all YRT/Viva routes. For spatial analysis add `include_geometry=true`. Combine with `york_region_get_road_network` to see routes in context."
       - Provide FR variant for lang="fr"

    2. `york_region_explore_census(lang)` — **guided workflow** (list[Message]):
       - Use for: analyzing 2021 census demographics across York Region municipalities
       - User message: "I want to compare demographics across York Region municipalities"
       - Assistant message: step-by-step — call `york_region_get_census_demographics(dataset="age_sex", csdname="Markham")` for a single municipality, then repeat for others (Vaughan, Newmarket, Aurora, etc.). Note the focused field set (10 columns) — use `york_region_query_features` directly with YR_AGE_SEX_FS and custom out_fields if more columns needed.

    3. `york_region_explore_health(lang)` — **guided workflow** (list[Message]):
       - Use for: exploring public health & safety data
       - User message: "What health data is available for York Region?"
       - Assistant message: "Use `york_region_get_public_health(location_type='beach_water')` for beach testing, `location_type='hospital'` for hospital locations, `location_type='drinking_water'` for adverse incidents. Add `include_geometry=true` for mapping."

    4. `york_region_quick_dataset_search(query, lang)` — **quick lookup** (str):
       - Takes `query: str` parameter
       - Use for: one-shot dataset search across a specific portal
       - Returns: "Call `york_region_search_datasets` with query='{query}' and limit=10. For Markham use `markham_search_datasets`, Newmarket `newmarket_search_datasets`, Aurora `aurora_search_datasets`."

    5. `markham_explore_infrastructure(lang)` — **guided workflow** (list[Message]):
       - Use for: exploring Markham civic infrastructure (addresses + roads)
       - User message: "I want to explore Markham's street and address data"
       - Assistant message: call `markham_get_addresses(street='Main St')` for address lookups; `markham_get_road_network(name='Highway 7')` for road queries; combine with `markham_search_datasets` for related datasets.

    All 5 prompts must have bilingual EN/FR content (if lang="fr", return French strings — keep translations reasonable, don't invent jargon; standard municipal vocabulary).

    **Create `src/mcp_canada/modules/york_region/resources.py`** following the Toronto pattern. Include exactly 8 resources:

    1. `@resource(uri="data://york_region/portals")` → `york_region_portals() -> str`
       - JSON catalog of all 10 municipalities: `[{"key": "york_region", "name_en": "York Region (regional)", "name_fr": "Région de York (régional)", "portal_url": "...", "item_count_approx": 442, "status": "verified"}, ...]`
       - Include the 5 None-portal entries with `"portal_url": null, "status": "no_public_portal"` and 1 census-only entry for whitchurch-stouffville

    2. `@resource(uri="data://york_region/municipalities")` → `york_region_municipalities() -> str`
       - JSON with population/area stats for all 9 local municipalities + regional (from 2021 census approximations). Include name_en, name_fr, population_2021, area_km2, has_portal boolean.

    3. `@resource(uri="data://york_region/feature_services")` → `york_region_feature_services() -> str`
       - JSON catalog of York Region's 34 OpenData feature services on ww8.yorkmaps.ca with name, category, has_feature_server bool, primary_layers list

    4. `@resource(uri="docs://york_region/esri-field-naming")` → `york_region_esri_field_naming() -> str`
       - Markdown guide explaining ESRI field naming conventions: OBJECTID, GLOBALID, ALL_CAPS fields, Shape__Length, Shape__Area. Note these are NOT renamed by mcp-canada — agents see them verbatim.

    5. `@resource(uri="docs://york_region/portal-landscape")` → `york_region_portal_landscape() -> str`
       - Markdown documenting which 4 of 10 York Region municipalities have public ArcGIS Hub portals and which don't, with rationale. Explain the NOT_FOUND response agents receive for missing portals.

    6. `@resource(uri="docs://york_region/census-variables")` → `york_region_census_variables() -> str`
       - Markdown guide to the focused census field set returned by `york_region_get_census_demographics`: CSDNAME, DAUID, TOT_POP, M_TOTAL, F_TOTAL, TOT_AVG_AGE_POP, TOT_MED_AGE_POP, TOT_0_TO_14_YRS, TOT_15_TO_64_YRS, TOT_65_YRS_OVER. Explain the 2021 Census 140-DA model. Link to full 364-field list.

    7. `@resource(uri="docs://york_region/arcgis-query-patterns")` → `york_region_arcgis_query_patterns() -> str`
       - Markdown guide: WHERE clause syntax (ESRI SQL-92 subset), LIKE for string contains, case sensitivity, single-quote escaping, `1=1` for all records. Include examples for transit stops, census, hospitals.

    8. `@resource(uri="template://york_region/transit-query-response")` → `york_region_transit_query_response_template() -> str`
       - Markdown template: "## YRT/Viva Transit Query Results\n\n**Stop:** {stop_name}\n**Route:** {route_short_name}\n**Accessibility:** {wheelchair_boarding}\n..."

    **Critical rules for resources** (from CLAUDE.md):
    - ALL resource functions must be zero-parameter (no `lang` arg). Bilingual content must be inline (both EN and FR in the same JSON/markdown).
    - data:// resources must return `json.dumps(...)` output, not Python dict
    - URIs must use the exact scheme + module path: `data://york_region/<name>` etc.

    **Create `src/mcp_canada/modules/york_region/__tests__/test_prompts_resources.py`** mirroring `src/mcp_canada/modules/toronto/__tests__/test_prompts_resources.py`. Tests:

    - `TestYorkRegionPrompts::test_all_prompts_have_lang_param` — inspect signature via `inspect.signature`
    - `TestYorkRegionPrompts::test_guided_workflow_prompts_return_list_message` — call each guided prompt with lang="en", assert `isinstance(result, list)` and all items have `.role` attribute
    - `TestYorkRegionPrompts::test_quick_lookup_returns_str` — call `york_region_quick_dataset_search(query="transit", lang="en")`, assert `isinstance(result, str)`
    - `TestYorkRegionPrompts::test_french_variants` — call each prompt with lang="fr", assert output differs from EN variant (or contains French keyword like "Région")
    - `TestYorkRegionResources::test_all_resources_zero_params` — inspect each resource function signature, assert 0 parameters
    - `TestYorkRegionResources::test_data_resources_valid_json` — call each `data://` resource, `json.loads` must succeed
    - `TestYorkRegionResources::test_portals_catalog_has_all_10` — parse `york_region_portals()`, assert length == 10, at least 4 entries have `portal_url` set, at least 5 have `portal_url: null`
    - `TestYorkRegionResources::test_docs_resources_non_empty_markdown` — call each `docs://` resource, assert `len(result) > 200` and contains `#` heading
    - `TestYorkRegionResources::test_template_resources_have_placeholders` — call `york_region_transit_query_response_template()`, assert `{stop_name}` and `{route_short_name}` present

    Run: `uv run pytest src/mcp_canada/modules/york_region/__tests__/test_prompts_resources.py -x -v`
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/york_region/__tests__/test_prompts_resources.py -x -v</automated>
  </verify>
  <done>prompts.py has 5 @prompt functions, resources.py has 8 @resource functions, all unit tests pass, all resources are zero-parameter with inline bilingual content, all data:// resources return valid JSON</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests + README + REQUIREMENTS.md finalization</name>
  <files>
    tests/integration/test_tool_scenarios.py,
    tests/integration/test_prompts_resources_scenarios.py,
    README.md,
    .planning/REQUIREMENTS.md
  </files>
  <behavior>
    - TestYorkRegionToolScenarios class exists in tests/integration/test_tool_scenarios.py with at least 6 scenarios
    - Each scenario calls a York Region tool through `client.call_tool('call_tool', {...})` — NOT directly through client functions
    - All integration tests are marked `@pytest.mark.integration` and use timeout=60-90s
    - TestYorkRegionPromptsResources class exists in tests/integration/test_prompts_resources_scenarios.py with ≥2 scenarios (list + read)
    - README has a "York Region Municipal Open Data" section after Toronto listing all ~27 tools grouped by portal + curated category
    - README tool count header is updated (add ~27 new tools, add York Region to the module count)
    - README mentions ArcGIS Hub as a second portal technology alongside CKAN
    - REQUIREMENTS.md has YR-01 through YR-14 defined in a new "### York Region Municipal Open Data" section after Toronto's block
    - REQUIREMENTS.md traceability table has 14 new rows mapping YR-01..YR-14 to Phase 14
    - REQUIREMENTS.md "Coverage" section is updated with York Region count
  </behavior>
  <action>
    **1. Integration tests — `tests/integration/test_tool_scenarios.py`:**

    Append a `TestYorkRegionToolScenarios` class at the end of the file (after the existing Toronto class). Use the same fixture/setup pattern as existing classes (check the file to see how `mcp` instance is built — likely via a session-scoped fixture that constructs providers + transforms). Mark every test method `@pytest.mark.integration`.

    Scenarios (think in sample prompts):
    1. `test_york_region_search_transit` — "What York Region datasets are about transit?" → `call_tool('call_tool', {'name': 'york_region_search_datasets', 'arguments': {'query': 'transit', 'limit': 5}})`. Assert `_meta.source.api == "arcgis-hub"`, assert `len(data["data"]) >= 1`.
    2. `test_york_region_transit_stops_live` — "Where are the YRT bus stops near Finch?" → `call_tool('call_tool', {'name': 'york_region_get_transit_stops', 'arguments': {'query': 'Finch'}})`. Assert response has `features` list. Timeout 60s.
    3. `test_york_region_hospitals` — "Where are York Region hospitals?" → `york_region_get_public_health` with `location_type="hospital"`. Assert features list returned.
    4. `test_markham_search_addresses` — "Find Main Street addresses in Markham" → `markham_get_addresses` with `street="Main"`. Assert features list returned.
    5. `test_aurora_list_categories_live` — "What dataset categories exist in Aurora's open data?" → `aurora_list_categories`. Assert `_meta` present, data is list.
    6. `test_newmarket_search_datasets_live` — "Search Newmarket open data" → `newmarket_search_datasets` with `query=""`, `limit=5`. Assert features present.
    7. `test_no_portal_returns_not_found` — The 4 real portals only expose tools; there are no `vaughan_*` tools. But we can test NoPortalError indirectly by mocking: call a Python-level direct function, or add a parametrized unit-level integration test that asserts the tool list contains no vaughan_/richmond_hill_/king_/east_gwillimbury_/georgina_ tools via `client.list_tools()` (prefix check). Add this as the final scenario.
    8. `test_discovery_finds_york_region_tool` — use `discover_tools` (BM25 search transform) with `query="york region transit bus stops"`. Assert at least one result matches `york_region_get_transit_stops`.

    Use `try: data = json.loads(result.content[0].text) except: ...` — the response may be flaky. Handle empty features as acceptable (assert response shape only, not specific values).

    **2. Integration tests — `tests/integration/test_prompts_resources_scenarios.py`:**

    Append a `TestYorkRegionPromptsResources` class. Scenarios:
    1. `test_york_region_prompts_discoverable` — use `async with Client(mcp) as client: prompts = await client.list_prompts()`. Assert at least one prompt name starts with `york_region_` or `markham_`. Assert total list includes ≥5 York Region-related prompts.
    2. `test_york_region_portals_resource` — `resource = await client.read_resource(AnyUrl("data://york_region/portals"))`. Assert it returns JSON with ≥10 entries (all 10 municipalities including None entries).
    3. `test_york_region_esri_field_naming_docs` — `await client.read_resource(AnyUrl("docs://york_region/esri-field-naming"))`. Assert content contains "OBJECTID" and is markdown.

    Remember: `r.uri` on FastMCP ResourceInfo is AnyUrl not str — must `str(r.uri)` before string containment tests (Phase 40 decision).

    **3. README.md update:**

    Read current `README.md` first (it's in the working directory root). Find:
    - Header tool count (e.g., "~195 tools" — current number TBD from prior plans)
    - Tool Catalog section
    - Toronto section (use as positional reference)

    Changes:
    - Increment tool count in the header. York Region adds ~27 tools. Add "~28" new tools (5 prompts don't count as tools but mention separately).
    - After the Toronto section in the Tool Catalog, add a new `### York Region Municipal Open Data` section with subsections:
      - Brief intro paragraph mentioning ArcGIS Hub as a second portal technology (alongside CKAN), covering 4 verified portals (York Region regional, Markham, Newmarket, Aurora) and noting 6 other municipalities have no public portals
      - **Discovery Tools (20)** subsection listing all 5 × 4 prefix tools with 1-line descriptions
      - **York Region Curated (5)** subsection listing the 5 york_region_get_* tools
      - **Markham Curated (2)** subsection listing markham_get_addresses and markham_get_road_network
    - If README has a "Supported Data Sources" or modules list, add "York Region Open Data (ArcGIS Hub)"
    - Add a note: "First ArcGIS Hub module — infrastructure in `shared/arcgis_hub.py` is reusable for future municipal modules."

    **4. REQUIREMENTS.md update:**

    Read `.planning/REQUIREMENTS.md`. Make two edits:

    a) After the `### Toronto Open Data` block (after TOR-12 bullet) and before `### MCP Prompts and Resources`, insert a new block:

    ```markdown
    ### York Region Municipal Open Data

    - **YR-01**: Shared `shared/arcgis_hub.py` provides a reusable ArcGIS Hub Search API + FeatureServer async client usable by any Canadian municipal module that publishes via ArcGIS Hub
    - **YR-02**: Client supports `/api/search/v1/collections/all/items` Hub Search endpoint with pagination via offset/limit (NOT the deprecated `/api/v2/datasets` which returns 404)
    - **YR-03**: Client supports FeatureServer query with `&f=geojson` and auto-paginates while `exceededTransferLimit=true` up to a MAX_RECORDS cap of 5000 per call, returning a `truncated` flag
    - **YR-04**: York Region module covers 4 verified ArcGIS Hub portals (york_region, markham, newmarket, aurora) and 6 municipalities without public portals return structured NOT_FOUND
    - **YR-05**: Each verified portal gets 5 discovery tools: search_datasets, get_dataset_details, query_features, list_organizations, list_categories (total 20 discovery tools)
    - **YR-06**: Agent can search YRT/Viva transit stops and routes from York Region Transportation FeatureServer
    - **YR-07**: Agent can fetch York Region regional road network from the Transportation FeatureServer
    - **YR-08**: Agent can query York Region public health & safety datasets (beach water testing, hospitals, drinking water adverse incidents)
    - **YR-09**: Agent can query York Region 2021 Census demographics (age/sex or income) by Dissemination Area with optional CSDNAME filter, returning a focused field set
    - **YR-10**: Agent can query York Region waste management data (annual diversion tonnages or solid waste site locations)
    - **YR-11**: Agent can search Markham civic addresses and road network (SLRN) via curated tools
    - **YR-12**: York Region module has prompts.py with 4-6 bilingual @prompt functions covering discovery workflows and curated data
    - **YR-13**: York Region module has resources.py with 6-10 zero-parameter @resource functions using data://, docs://, template:// URI schemes, including a portal catalog and municipality list
    - **YR-14**: All York Region tools follow mcp-canada conventions (standalone @tool, make_response/make_error envelope, Use-for + Keywords single-line docstrings, municipality prefix), are discoverable via discover_tools, and README reflects the new tool catalog
    ```

    b) In the Traceability table, append 14 rows:
    ```markdown
    | YR-01 | Phase 14 | Planned |
    | YR-02 | Phase 14 | Planned |
    | YR-03 | Phase 14 | Planned |
    | YR-04 | Phase 14 | Planned |
    | YR-05 | Phase 14 | Planned |
    | YR-06 | Phase 14 | Planned |
    | YR-07 | Phase 14 | Planned |
    | YR-08 | Phase 14 | Planned |
    | YR-09 | Phase 14 | Planned |
    | YR-10 | Phase 14 | Planned |
    | YR-11 | Phase 14 | Planned |
    | YR-12 | Phase 14 | Planned |
    | YR-13 | Phase 14 | Planned |
    | YR-14 | Phase 14 | Planned |
    ```

    c) Update the Coverage section to add:
    ```markdown
    - York Region requirements: 14 total (Phase 14)
    ```

    **5. Run verification commands:**

    1. `uv run pytest src/mcp_canada/modules/york_region/ -x -v` — all unit tests pass
    2. `uv run pytest --cov=src/mcp_canada --cov-fail-under=95` — full coverage at 95%+
    3. `uv run pytest tests/integration/test_tool_scenarios.py::TestYorkRegionToolScenarios -v -m integration --timeout=120` — integration tests pass against live APIs
    4. `uv run pytest tests/integration/test_prompts_resources_scenarios.py::TestYorkRegionPromptsResources -v -m integration --timeout=60` — prompts/resources integration pass
    5. `uv run pyright src/mcp_canada/modules/york_region/ src/mcp_canada/shared/arcgis_hub.py` — no type errors
    6. `uv run ruff check src/mcp_canada/modules/york_region/ src/mcp_canada/shared/arcgis_hub.py` — no lint errors
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/york_region/ -x -v && uv run pytest --cov=src/mcp_canada --cov-fail-under=95</automated>
  </verify>
  <done>prompts.py + resources.py exist and are auto-discovered via FileSystemProvider, integration tests for tools and prompts/resources exist and pass against live ArcGIS Hub endpoints, README has York Region section with ~27 tools listed and tool count updated, REQUIREMENTS.md has YR-01..YR-14 defined and mapped to Phase 14, coverage at 95%+</done>
</task>

</tasks>

<verification>
- `uv run pytest src/mcp_canada/modules/york_region/ -x -v` — all York Region unit tests pass
- `uv run pytest --cov=src/mcp_canada --cov-fail-under=95` — coverage at 95%+
- `uv run pytest tests/integration/test_tool_scenarios.py::TestYorkRegionToolScenarios -v -m integration --timeout=120` — integration tests pass
- `uv run pytest tests/integration/test_prompts_resources_scenarios.py -v -m integration --timeout=60` — prompts/resources integration pass
- `uv run pyright src/mcp_canada/modules/york_region/ src/mcp_canada/shared/arcgis_hub.py` — no type errors
- `uv run ruff check src/mcp_canada/modules/york_region/ src/mcp_canada/shared/arcgis_hub.py` — no lint errors
- README.md has "York Region Municipal Open Data" section with all ~27 tools listed
- REQUIREMENTS.md has YR-01..YR-14 and their traceability rows
</verification>

<success_criteria>
- prompts.py exists with 5 @prompt functions (guided workflows + quick lookup) with bilingual support
- resources.py exists with 8 zero-parameter @resource functions using data://, docs://, template:// schemes
- All prompts and resources are discoverable via MCP Client (list_prompts, read_resource)
- TestYorkRegionToolScenarios class exists with ≥6 integration scenarios passing against live ArcGIS Hub endpoints
- TestYorkRegionPromptsResources class exists with ≥2 scenarios passing
- README updated with York Region section and accurate tool count
- REQUIREMENTS.md has YR-01..YR-14 definitions + traceability rows + coverage count
- Coverage at 95%+ across src/mcp_canada
- York Region module fully matches the 7-file pattern
</success_criteria>

<output>
After completion, create `.planning/phases/14-york-region-municipal-government-open-data/14-03-SUMMARY.md`
</output>
