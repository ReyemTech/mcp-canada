---
phase: 18-manitoba-government-open-data
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/mcp_canada/modules/manitoba/__init__.py
  - src/mcp_canada/modules/manitoba/constants.py
  - src/mcp_canada/modules/manitoba/schemas.py
  - src/mcp_canada/modules/manitoba/client.py
  - src/mcp_canada/modules/manitoba/tools.py
  - src/mcp_canada/modules/manitoba/prompts.py
  - src/mcp_canada/modules/manitoba/resources.py
  - src/mcp_canada/modules/manitoba/__tests__/__init__.py
  - src/mcp_canada/modules/manitoba/__tests__/conftest.py
  - src/mcp_canada/modules/manitoba/__tests__/test_client.py
  - src/mcp_canada/modules/manitoba/__tests__/test_tools.py
  - src/mcp_canada/modules/manitoba/__tests__/test_prompts_resources.py
  - .planning/phases/18-manitoba-government-open-data/18-SPIKE.md
autonomous: true
requirements:
  - MB-18
must_haves:
  truths:
    - "manitoba module auto-registers via FileSystemProvider (MODULE_NAME='manitoba', MODULE_DESCRIPTION + MODULE_DESCRIPTION_FR exist)"
    - "constants.py hardcodes every live-verified FeatureServer URL, ArcGIS org id mMUesHYPkXjaFGfS, Hub search URL, 2 rate groups (hub + 511), all cache TTLs, MAX_RECORDS=5000, USER_AGENT, CACHE_KEY_PREFIX='manitoba:' verbatim from 18-RESEARCH.md 'Pattern 1: ArcGIS Hub Discovery Constants'"
    - "schemas.py defines flat Pydantic v2 models for every response type Plans 02-06 return (dataset summary/details, organization, category, park, flood alert, river station, waterway, drought polygon, ag weather station, livestock price, crop region, surgical wait time, fisheries waterbody, provincial forest, health facility, 511 road event, 511 winter road, 511 camera)"
    - "client.py implements _hub_get helper FULLY (ArcGIS Hub Search JSON contract, NOT CKAN envelope) and _511_get helper FULLY (key-gated JSON-list contract); declares every downstream client function as NotImplementedError stub with full signature returning (data, was_cached)"
    - "tools.py, prompts.py, resources.py import standalone fastmcp decorators (NEVER @mcp.*) but define zero @tool/@prompt/@resource yet"
    - "Wave 0 test scaffolds exist (conftest fixtures for every ArcGIS Hub response shape incl. empty-flood-alert edge case + 511 JSON list, placeholder test classes for client/tools/prompts/resources) so downstream plans reference specific pytest node IDs"
    - "Wave 0 spike resolves: (a) Manitoba 511 developer-key obtainability, (b) Rural Health Care Facilities FeatureServer URL, (c) hog prices FeatureServer service name, (d) Manitoba River Conditions FeatureServer URL — findings in 18-SPIKE.md, reflected in constants.py"
    - "pytest collection succeeds on manitoba __tests__ directory (no ImportError, no syntax errors)"
  artifacts:
    - path: src/mcp_canada/modules/manitoba/__init__.py
      provides: "MODULE_NAME='manitoba' and MODULE_DESCRIPTION (en+fr)"
      contains: "MODULE_NAME = \"manitoba\""
    - path: src/mcp_canada/modules/manitoba/constants.py
      provides: "All FeatureServer URLs, Hub search URL, org id, 2 rate groups, cache TTLs, MAX_RECORDS=5000"
      contains: "mMUesHYPkXjaFGfS"
    - path: src/mcp_canada/modules/manitoba/schemas.py
      provides: "Flat Pydantic v2 models for all ~18 response types"
    - path: src/mcp_canada/modules/manitoba/client.py
      provides: "_hub_get + _511_get helpers (implemented) + ~18 client function stubs with signatures"
    - path: src/mcp_canada/modules/manitoba/__tests__/conftest.py
      provides: "ArcGIS Hub JSON fixtures (search results, item detail, FeatureServer query features, empty flood-alert response, 511 JSON list), autouse cache+limiter patch"
    - path: .planning/phases/18-manitoba-government-open-data/18-SPIKE.md
      provides: "Wave 0 spike findings: 511 key status + resolved FeatureServer URLs"
  key_links:
    - from: "src/mcp_canada/modules/manitoba/__init__.py"
      to: "FileSystemProvider"
      via: "MODULE_NAME and MODULE_DESCRIPTION exports"
      pattern: "MODULE_NAME\\s*=\\s*\"manitoba\""
    - from: "src/mcp_canada/modules/manitoba/client.py"
      to: "mcp_canada.shared.http.api_get"
      via: "import — _hub_get and _511_get wrap it (parsed-dict/list contract, no .raise_for_status/.json)"
      pattern: "from mcp_canada\\.shared\\.http import api_get"
    - from: "src/mcp_canada/modules/manitoba/client.py"
      to: "mcp_canada.shared.arcgis_hub"
      via: "import — curated FeatureServer client functions call query_feature_service"
      pattern: "from mcp_canada\\.shared import arcgis_hub"
---

<objective>
Scaffold the `manitoba` module (7-file pattern + test scaffolds with all stubs) and run the Wave 0 spike that resolves the four research open questions. Establishes the contracts (constants, schemas, client signatures, fixtures) that Plans 02-06 implement without colliding on file edits. No tools/prompts/resources are populated here.

This is the ArcGIS Hub pattern (Alberta Phase 17 / York Region Phase 14), NOT CKAN — Manitoba's machine-readable portal is `geoportal.gov.mb.ca` (ArcGIS Online org `mMUesHYPkXjaFGfS`). `data.manitoba.ca` is unreachable and `mli.gov.mb.ca` is retired — never reference either.

Purpose: Lock the file list and contract surface before any business logic, so 5 downstream plans run in waves 1-2 in parallel. Resolve the unconfirmed FeatureServer URLs and the 511 key question up front so later plans have concrete targets.

Output: A pytest-collectable `manitoba` module with stubs + fixtures, and 18-SPIKE.md with the resolved URLs / key status.
</objective>

<execution_context>
@/Users/mariomeyer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomeyer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-manitoba-government-open-data/18-CONTEXT.md
@.planning/phases/18-manitoba-government-open-data/18-RESEARCH.md
@.planning/phases/18-manitoba-government-open-data/18-VALIDATION.md
@CLAUDE.md
@.claude/rules/modules.md
@.claude/rules/tests.md

# Reference modules — copy structure from these
@src/mcp_canada/modules/alberta/__init__.py
@src/mcp_canada/modules/alberta/client.py
@src/mcp_canada/modules/york_region/client.py
@src/mcp_canada/modules/york_region/__tests__/conftest.py

<interfaces>
<!-- Shared utilities the manitoba module consumes. Do NOT redefine; import from shared. -->

From src/mcp_canada/shared/http.py:
  async def api_get(url, params=None, headers=None) -> Any
  Returns parsed JSON (dict OR list — endpoint-dependent).
  Phase 15-05 contract: do NOT call .raise_for_status() / .json() on the return value.

From src/mcp_canada/shared/cache.py:
  async def cached_fetch(key, ttl, fetcher) -> tuple[Any, bool]  # (data, was_cached)

From src/mcp_canada/shared/rate_limiter.py:
  def get_limiter(source, rate) -> TokenBucket  # .acquire() is awaitable

From src/mcp_canada/shared/envelope.py:
  def make_response(data, *, api_name, api_url, cached, lang) -> dict
  def make_error(code, message, *, lang, **extras) -> dict

From src/mcp_canada/shared/arcgis_hub.py:
  async def query_feature_service(service_url, layer_id, where="1=1", out_fields="*",
      include_geometry=False, max_records=5000, *, httpx_client=None) -> tuple[list[dict], bool]
  NOTE: second tuple element is `truncated`, NOT was_cached. Auto-paginates to cap.
  Curated client fns wrap this in cached_fetch() and build payload
  {"features":[...], "count":N, "truncated":bool}; cached_fetch returns (payload, was_cached).
  Mirror alberta.client.fetch_active_fires exactly.
  async def get_layer_metadata(service_url, layer_id, *, httpx_client=None) -> dict
  async def get_count(service_url, layer_id, where="1=1", *, httpx_client=None) -> int

From src/mcp_canada/shared/parsers.py:
  async def fetch_and_parse(url, ttl=3600, ssl_context=None) -> tuple[list[dict], bool]
  Routes by URL suffix/content-type → CSV / XLSX / JSON / GeoJSON / SHP.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 spike — resolve 511 key + 4 FeatureServer URLs</name>
  <files>.planning/phases/18-manitoba-government-open-data/18-SPIKE.md</files>
  <action>
    Resolve the four 18-RESEARCH.md "Open Questions" before writing constants, so Task 2 hardcodes concrete URLs. Use curl/httpx against the live ArcGIS org (no auth) and the Hub Search API. Write findings to 18-SPIKE.md.

    1. Manitoba 511 developer key obtainability — Visit https://www.manitoba511.ca/developers/doc and the signup flow. Determine: free + instantly provisioned, or gated/approval/paid? Do NOT register or fabricate a key. The verdict drives Plan 06: free → live integration possible; gated → tools ship returning NOT_CONFIGURED only.

    2. Rural Health Care Facilities FeatureServer URL — probe the Hub Search API:
       curl -s "https://geoportal.gov.mb.ca/api/search/v1/collections/all/items?q=rural+health+care+facilities&limit=5" -H "User-Agent: mcp-canada/1.0"
       Resolve the underlying FeatureServer service URL (likely https://services.arcgis.com/mMUesHYPkXjaFGfS/arcgis/rest/services/<name>/FeatureServer). Confirm with ?f=json. Record URL + layer id + fields.

    3. Hog prices FeatureServer service name — curl -s "https://services.arcgis.com/mMUesHYPkXjaFGfS/arcgis/rest/services?f=json" and grep the service list for hog/Hog. Or Hub search q=hog prices. Record exact service name + URL. (Cattle already confirmed: MB_Cattle_Prices_Current_year.)

    4. Manitoba River Conditions FeatureServer URL — inspect ArcGIS item 5c57801d0efc4676a2d2c95174ef44d5 operational layers via https://www.arcgis.com/sharing/rest/content/items/5c57801d0efc4676a2d2c95174ef44d5/data?f=json . Extract the station-points FeatureServer URL + layer id + flood-status field name.

    For any URL not resolvable from the sandbox, record the BEST-EFFORT candidate URL + the exact probe command to re-run, and mark it "UNRESOLVED — fall back to discovery". Downstream plans must degrade gracefully (UPSTREAM_ERROR / empty, or route through manitoba_query_dataset) rather than hardcode a guessed broken URL.

    18-SPIKE.md sections:
    - "## 1. Manitoba 511 key — VERDICT: {free-instant | gated | paid | unconfirmed}"
    - "## 2. Rural Health Care Facilities FS — URL / layer / fields / status"
    - "## 3. Hog prices FS — service name / URL / status"
    - "## 4. River Conditions FS — URL / layer / flood-status field / status"
  </action>
  <verify>
    <automated>test -f .planning/phases/18-manitoba-government-open-data/18-SPIKE.md && grep -qi "511" .planning/phases/18-manitoba-government-open-data/18-SPIKE.md && grep -qi "rural health" .planning/phases/18-manitoba-government-open-data/18-SPIKE.md && grep -qi "hog" .planning/phases/18-manitoba-government-open-data/18-SPIKE.md && grep -qi "river" .planning/phases/18-manitoba-government-open-data/18-SPIKE.md && echo OK</automated>
  </verify>
  <done>18-SPIKE.md exists with a 511-key verdict and a resolved-or-best-effort URL for each of the 3 unconfirmed FeatureServers. Each unresolved item carries a re-runnable probe command.</done>
</task>

<task type="auto">
  <name>Task 2: Module scaffolding (init, constants, schemas)</name>
  <files>
    src/mcp_canada/modules/manitoba/__init__.py,
    src/mcp_canada/modules/manitoba/constants.py,
    src/mcp_canada/modules/manitoba/schemas.py
  </files>
  <action>
    Create the manitoba module foundation. Model file structure on alberta.

    __init__.py:
      MODULE_NAME = "manitoba"
      MODULE_DESCRIPTION = "Manitoba provincial government open data via geoportal.gov.mb.ca (Data MB) — an ArcGIS Hub powered by ArcGIS Online org mMUesHYPkXjaFGfS. 5 Hub discovery tools plus curated FeatureServer tools across flood/hydrology, agriculture & drought, environment/parks, regional health, and (conditional) Manitoba 511 transport."
      MODULE_DESCRIPTION_FR = "<French equivalent — mirror alberta>"

    constants.py: Hardcode EVERY constant from 18-RESEARCH.md "Pattern 1: ArcGIS Hub Discovery Constants" verbatim, plus URLs resolved in Task 1's 18-SPIKE.md:
    - HUB_BASE_URL = "https://geoportal.gov.mb.ca"; ARCGIS_ORG_ID = "mMUesHYPkXjaFGfS";
      HUB_ORG_BASE = "https://services.arcgis.com/mMUesHYPkXjaFGfS/arcgis/rest/services";
      HUB_SEARCH_URL = f"{HUB_BASE_URL}/api/search/v1/collections/all/items"
    - Flood/hydrology: FLOOD_ALERTS_FS_URL, PROVINCIAL_WATERWAYS_FS_URL, RIVER_CONDITIONS_FS_URL (from spike; sentinel + comment if unresolved)
    - Agriculture/drought: CATTLE_PRICES_FS_URL, HOG_PRICES_FS_URL (from spike), AG_WEATHER_STATIONS_FS_URL, CROP_REGIONS_FS_URL, DROUGHT_MONITOR_FS_URL
    - Environment/parks: PROVINCIAL_PARKS_FS_URL, WATERBODY_DATA_FS_URL, PROVINCIAL_FORESTS_FS_URL
    - Health: SURGICAL_WAIT_TIMES_FS_URL, RURAL_HEALTH_FACILITIES_FS_URL (from spike)
    - 511 (conditional): FIVE11_BASE_URL = "https://www.manitoba511.ca/api/v3/get"; FIVE11_KEY_ENV = "MANITOBA_511_KEY"
    - Rate groups: RATE_GROUP_HUB = "manitoba_hub" (rate 10.0); RATE_GROUP_511 = "manitoba_511" (rate 2.0)
    - Cache TTLs: CACHE_TTL_LIVE=300, CACHE_TTL_SEARCH=3600, CACHE_TTL_META=86400, CACHE_TTL_STATIC=86400, CACHE_TTL_ANNUAL=604800
    - MAX_RECORDS=5000, DEFAULT_PAGE_SIZE=1000
    - USER_AGENT = "mcp-canada/1.0 (+https://github.com/reyemtech/mcp-canada)"
    - CACHE_KEY_PREFIX = "manitoba:"
    - MANITOBA_BBOX = "-101.36,48.99,-95.15,60.0"  (drought spatial filter, Pitfall 8)
    - Dispatch tuples: PARK_TYPES; WATERWAY_TYPES = ("dike","floodway","dam","diversion","reservoir","waterway")
    Document at top: "Manitoba uses ArcGIS Hub (geoportal.gov.mb.ca / org mMUesHYPkXjaFGfS), NOT CKAN. Never reference data.manitoba.ca or mli.gov.mb.ca (retired 2022-02-09)."

    schemas.py: Flat Pydantic v2 models (from __future__ import annotations; Field(default=None) for optionals), snake_case, aggressive flattening. Define:
    - ManitobaDatasetSummary (id, title, snippet, type, owner, url, num_views, modified, source)
    - ManitobaDatasetDetails (summary fields + resources: list[dict], feature_server_url: str|None, download_urls: list[str])
    - ManitobaOrganization (id, name, title, item_count: int|None)
    - ManitobaCategory (name, count: int|None)
    - ManitobaPark (name_en, nom_fr, biome, area_ha, type_en, type_fr, status_en, prot_date, park_class, url)
    - ManitobaFloodAlert (type_en, type_fr, start_date, end_date, area_sqkm, geometry: dict|None)
    - ManitobaRiverStation (station_name, status, latitude, longitude, watercourse)
    - ManitobaWaterway (f_type, name, watershed, wcw, length_km, geometry: dict|None)
    - ManitobaDroughtPolygon (dm, obs_date, source, geometry: dict|None)
    - ManitobaAgWeatherStation (stn_name, lat_dd, long_dd, elevation, ag_region, url)
    - ManitobaLivestockPrice (week, auction, parameter, measure, value, livestock: str)
    - ManitobaCropRegion (region_en, region_fr, geometry: dict|None)
    - ManitobaWaitTime (year, indicator_data_area, average_wait)
    - ManitobaWaterbody (id, name, surface_area, avg_depth, secchi_depth, fishing_division, species, regulations, boat_launch)
    - ManitobaForest (name, area_ha, geometry: dict|None)
    - ManitobaHealthFacility (community, facility, emergency_dept, acute_care, transitional_care, diagnostic_services, pch, rha: str|None)
    - Manitoba511Event (id, roadway_name, event_type, is_full_closure: bool, latitude, longitude, description, last_updated)
    - Manitoba511WinterRoad (id, area_name, roadway_name, primary_condition, secondary_conditions, visibility, encoded_polyline, last_updated)
    - Manitoba511Camera (id, location, latitude, longitude, views: list[dict])
  </action>
  <verify>
    <automated>uv run python -c "from mcp_canada.modules.manitoba import MODULE_NAME, MODULE_DESCRIPTION; from mcp_canada.modules.manitoba import constants, schemas; assert MODULE_NAME=='manitoba'; assert constants.ARCGIS_ORG_ID=='mMUesHYPkXjaFGfS'; assert constants.HUB_SEARCH_URL.startswith('https://geoportal.gov.mb.ca'); assert constants.MAX_RECORDS==5000; assert schemas.ManitobaPark and schemas.ManitobaFloodAlert and schemas.Manitoba511Event; print('OK')"</automated>
  </verify>
  <done>Module imports cleanly. All constants accessible (org id, hub search url, all FeatureServer URLs, 2 rate groups, TTLs). All ~18 schema models defined and importable. Never references data.manitoba.ca or mli.gov.mb.ca.</done>
</task>

<task type="auto">
  <name>Task 3: Client helpers + stubs + tools/prompts/resources skeletons + test scaffolds</name>
  <files>
    src/mcp_canada/modules/manitoba/client.py,
    src/mcp_canada/modules/manitoba/tools.py,
    src/mcp_canada/modules/manitoba/prompts.py,
    src/mcp_canada/modules/manitoba/resources.py,
    src/mcp_canada/modules/manitoba/__tests__/__init__.py,
    src/mcp_canada/modules/manitoba/__tests__/conftest.py,
    src/mcp_canada/modules/manitoba/__tests__/test_client.py,
    src/mcp_canada/modules/manitoba/__tests__/test_tools.py,
    src/mcp_canada/modules/manitoba/__tests__/test_prompts_resources.py
  </files>
  <action>
    client.py:
    1. Imports: typing Any/Literal, os, httpx, api_get from shared.http, cached_fetch from shared.cache, get_limiter from shared.rate_limiter, arcgis_hub from shared, fetch_and_parse from shared.parsers, from .constants import * (or named).

    2. Implement _hub_get FULLY — Hub Search returns JSON directly (NOT a CKAN success/result envelope; never call .get("success")):
         async def _hub_get(params):
             result = await api_get(HUB_SEARCH_URL, params, headers={"User-Agent": USER_AGENT})
             if not isinstance(result, dict): raise httpx.HTTPStatusError("Hub returned non-dict", request=httpx.Request("GET", HUB_SEARCH_URL), response=httpx.Response(500))
             return result

    3. Define module-level `class Five11NotConfigured(Exception): pass`. Implement _511_get FULLY (key-gated, returns raw JSON list — Pitfall 3). Read key from os.environ.get(FIVE11_KEY_ENV, ""); if absent, raise Five11NotConfigured (tool layer maps to NOT_CONFIGURED):
         async def _511_get(endpoint, params=None):
             key = os.environ.get(FIVE11_KEY_ENV, "")
             if not key: raise Five11NotConfigured("MANITOBA_511_KEY not set")
             rows = await api_get(f"{FIVE11_BASE_URL}/{endpoint}", {**(params or {}), "key": key, "format": "json"}, headers={"User-Agent": USER_AGENT})
             return rows if isinstance(rows, list) else []

    4. Declare every client function as `raise NotImplementedError("filled by Plan NN")` stub with FULL signature returning tuple[..., bool]. Each curated fetcher returns tuple[dict, bool] carrying {"features"/"rows": [...], "count": N, "truncated": bool} per the alberta pattern. Group by plan:
       - Discovery (Plan 02): fetch_search_datasets, fetch_dataset_details, fetch_query_dataset, fetch_organizations, fetch_categories
       - Flood/hydrology (Plan 03): fetch_flood_alerts, fetch_river_stations, fetch_provincial_waterways
       - Agriculture/drought (Plan 04): fetch_drought_status, fetch_ag_weather_stations, fetch_livestock_prices, fetch_crop_regions
       - Environment/health/parks (Plan 05): fetch_provincial_parks, fetch_fisheries_data, fetch_provincial_forests, fetch_surgical_wait_times, fetch_health_facilities
       - Transport 511 (Plan 06): fetch_road_events, fetch_winter_road_conditions, fetch_traffic_cameras
       SIGNATURES ARE LOCKED HERE — downstream plans fill bodies only, never change signatures. Example:
         async def fetch_provincial_parks(park_type: str | None = None, max_records: int = MAX_RECORDS, include_geometry: bool = False) -> tuple[dict[str, Any], bool]:
             """Manitoba provincial parks from Manitoba_Parks FeatureServer. Filled by Plan 05."""
             raise NotImplementedError("Plan 05 implements")

    tools.py: imports + zero @tool:
      from typing import Annotated, Literal
      from fastmcp.tools import tool
      from mcp_canada.shared.envelope import make_response, make_error
      from . import client as _client
      # Tool definitions added by Plans 02-06.

    prompts.py: imports + zero @prompt:
      from typing import Annotated, Literal
      from fastmcp.prompts import prompt
      from fastmcp.prompts.prompt import Message
      # Prompt definitions added by Plan 07.

    resources.py: imports + zero @resource:
      import json
      from fastmcp.resources import resource
      # Resource definitions added by Plan 07.

    __tests__/__init__.py: empty.

    __tests__/conftest.py (model on york_region's): fixtures for every response shape Plans 02-06 reference:
      - sample_hub_search_response — dict with total + results/features list of 2 Manitoba items (with FeatureServer URL in resources)
      - sample_hub_item_detail — dict for one dataset with operational layers / download URLs
      - sample_arcgis_parks_features — (list[dict], False): 2 park feature dicts (NAME_E/NOM_F/TYPE_E/etc.)
      - sample_arcgis_flood_alerts_empty — ([], False) — the no-active-alerts edge case (CRITICAL: valid, NOT an error)
      - sample_arcgis_flood_alerts_active — ([{Type_EN: "Warning", ...}], False)
      - sample_arcgis_drought — ([{"DM":"D2","OBS_DATE":...}], False)
      - sample_arcgis_ag_weather — ([{"StnName":...,"AgRegion":...,"URL":...}], False)
      - sample_arcgis_livestock — ([{"week":...,"Auction":...,"Value":...}], False)
      - sample_arcgis_wait_times — ([{"Year":2021,"IndicatorDataArea":"Cardiac surgery","Average_Wait":144}], False)
      - sample_arcgis_fisheries — ([{...26-field waterbody...}], False)
      - sample_arcgis_waterways — ([{"F_TYPE":"Floodway","Name":...}], False)
      - sample_arcgis_crop_regions — ([{"REGION":"Central","RÉGION":"Centre"}], False)
      - sample_arcgis_forests — ([{"Name":...,"area_ha":...}], False)
      - sample_arcgis_river_stations — ([{"station_name":...,"status":"Flood Watch"}], False)
      - sample_arcgis_health_facilities — ([{"Community":...,"Facility":...,"Emergency Dept":...}], False)
      - sample_511_event_list — list of 2 events (RoadwayName/EventType/IsFullClosure/Latitude)
      - sample_511_winter_roads — list of 2 winter-road dicts
      - sample_511_cameras — list of 2 camera dicts with Views array
      - autouse _clear_cache_and_limiter fixture patching cached_fetch passthrough + get_limiter no-op (copy york_region pattern)

    __tests__/test_client.py: module docstring + placeholder classes (empty pass body — collects + skips). MUST include TestSharedApiGetContract (Plan 02 fills — patches mcp_canada.modules.manitoba.client.api_get per the BC/Alberta module-local pattern). Classes: TestSharedApiGetContract, TestManitobaSearchDatasets, TestManitobaGetDatasetDetails, TestManitobaQueryDataset, TestManitobaListOrgs, TestManitobaListCategories, TestManitobaGetFloodAlerts, TestManitobaGetRiverStations, TestManitobaGetWaterways, TestManitobaGetDroughtStatus, TestManitobaGetAgWeatherStations, TestManitobaGetLivestockPrices, TestManitobaGetCropRegions, TestManitobaGetParks, TestManitobaGetFisheriesData, TestManitobaGetForests, TestManitobaGetWaitTimes, TestManitobaGetHealthFacilities, TestManitoba511.

    __tests__/test_tools.py: module docstring + per-tool placeholder classes + TestManitobaEnvelopes (Plan 08 — parametrized) + TestManitobaLangParam (Plan 08 — parametrized).

    __tests__/test_prompts_resources.py: TestManitobaPrompts (Plan 07) + TestManitobaResources (Plan 07).

    Every file must be valid Python importing cleanly.
  </action>
  <verify>
    <automated>uv run python -c "from mcp_canada.modules.manitoba import client, tools, prompts, resources; assert hasattr(client,'_hub_get') and hasattr(client,'_511_get') and hasattr(client,'Five11NotConfigured'); assert hasattr(client,'fetch_flood_alerts') and hasattr(client,'fetch_provincial_parks') and hasattr(client,'fetch_road_events'); print('OK stubs')"</automated>
    <automated>uv run pytest src/mcp_canada/modules/manitoba/__tests__/ --collect-only -q</automated>
  </verify>
  <done>All ~18 client stubs declared with full signatures. _hub_get, _511_get, Five11NotConfigured fully implemented. tools/prompts/resources import cleanly with zero definitions. pytest collects all placeholder classes without error. Downstream plans only fill bodies — never change signatures.</done>
</task>

</tasks>

<verification>
1. `uv run python -c "from mcp_canada.modules.manitoba import MODULE_NAME, constants, schemas, client, tools, prompts, resources; print('imports OK')"` — clean imports
2. `uv run pytest src/mcp_canada/modules/manitoba/__tests__/ --collect-only -q` — collection passes, all stub classes present
3. `uv run pyright src/mcp_canada/modules/manitoba/` — no type errors on stubs
4. 18-SPIKE.md present with 511-key verdict + 3 resolved/best-effort FeatureServer URLs
</verification>

<success_criteria>
- 12 module/test files exist + 18-SPIKE.md
- MODULE_NAME == "manitoba"; module appears in FileSystemProvider scan
- All ~18 client function stubs have correct signatures returning tuple[..., bool]
- _hub_get + _511_get + Five11NotConfigured fully implemented (not stubs)
- All ~18 schema models defined
- conftest.py fixtures cover every response shape Plans 02-06 will need (incl. empty flood-alert + 511 lists)
- pytest collection passes without errors
- Zero tools/prompts/resources defined yet
- Never references data.manitoba.ca or mli.gov.mb.ca
</success_criteria>

<output>
After completion, create `.planning/phases/18-manitoba-government-open-data/18-01-SUMMARY.md` documenting: final file list (12 + spike), _hub_get/_511_get patterns confirmed, all ~18 client stubs declared, the 4 spike resolutions (511 key verdict + 3 FeatureServer URLs), any deviation from alberta/york_region patterns.
</output>
