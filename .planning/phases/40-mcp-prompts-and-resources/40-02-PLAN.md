---
phase: 40-mcp-prompts-and-resources
plan: 02
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - src/mcp_canada/modules/statcan/prompts.py
  - src/mcp_canada/modules/statcan/resources.py
  - src/mcp_canada/modules/statcan/__tests__/test_prompts_resources.py
  - src/mcp_canada/modules/datastore/prompts.py
  - src/mcp_canada/modules/datastore/resources.py
  - src/mcp_canada/modules/datastore/__tests__/test_prompts_resources.py
  - src/mcp_canada/modules/ckan/prompts.py
  - src/mcp_canada/modules/ckan/resources.py
  - src/mcp_canada/modules/ckan/__tests__/test_prompts_resources.py
autonomous: true
requirements:
  - PR-07
  - PR-08
  - PR-09

must_haves:
  truths:
    - "StatCan guided workflow prompt chains sc_search_cubes -> sc_get_cube_metadata -> sc_get_data_by_vector for data exploration"
    - "Datastore guided workflow prompt chains ds_create_table -> ds_insert_data -> ds_query for SQL workflow"
    - "CKAN guided workflow prompt chains ckan_search_datasets -> ckan_get_dataset_details for federal open data discovery"
    - "StatCan resources include code set catalog (frequency, units, scalar, status codes) and SDMX guide"
    - "Datastore resources include SQL syntax guide and supported column types catalog"
    - "CKAN resources include federal organization catalog and dataset tag list"
    - "All prompts support bilingual en/fr, all resources use zero-parameter functions"
  artifacts:
    - path: "src/mcp_canada/modules/statcan/prompts.py"
      provides: "4-6 @prompt functions for StatCan workflows"
      contains: "@prompt"
    - path: "src/mcp_canada/modules/statcan/resources.py"
      provides: "6-10 @resource functions for StatCan reference data"
      contains: "@resource"
    - path: "src/mcp_canada/modules/datastore/prompts.py"
      provides: "4-5 @prompt functions for Datastore workflows"
      contains: "@prompt"
    - path: "src/mcp_canada/modules/datastore/resources.py"
      provides: "6-8 @resource functions for Datastore reference data"
      contains: "@resource"
    - path: "src/mcp_canada/modules/ckan/prompts.py"
      provides: "4-5 @prompt functions for CKAN workflows"
      contains: "@prompt"
    - path: "src/mcp_canada/modules/ckan/resources.py"
      provides: "6-8 @resource functions for CKAN reference data"
      contains: "@resource"
  key_links:
    - from: "src/mcp_canada/modules/statcan/prompts.py"
      to: "fastmcp.prompts"
      via: "standalone @prompt decorator"
      pattern: "from fastmcp\\.prompts import.*prompt"
    - from: "src/mcp_canada/modules/statcan/resources.py"
      to: "fastmcp.resources"
      via: "standalone @resource decorator"
      pattern: "from fastmcp\\.resources import.*resource"
---

<objective>
Add MCP prompts and resources to the StatCan, Datastore, and CKAN modules — the data-centric core of mcp-canada. These three modules form the data pipeline (discover via CKAN/StatCan, store in Datastore, query with SQL).

Purpose: Agents get guided workflows for the most complex tools (StatCan has 15 tools) and instant reference to code sets, SQL syntax, and federal org lists.
Output: 3 prompts.py files, 3 resources.py files, 3 test files.
</objective>

<execution_context>
@/Users/mariomeyer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomeyer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-mcp-prompts-and-resources/40-RESEARCH.md
@.planning/phases/40-mcp-prompts-and-resources/40-01-SUMMARY.md

@src/mcp_canada/modules/statcan/tools.py
@src/mcp_canada/modules/statcan/constants.py
@src/mcp_canada/modules/datastore/tools.py
@src/mcp_canada/modules/datastore/constants.py
@src/mcp_canada/modules/ckan/tools.py
@src/mcp_canada/modules/ckan/constants.py

<interfaces>
<!-- Follow exact patterns established in Plan 01 (BoC reference implementation) -->
<!-- Read 40-01-SUMMARY.md for the proven prompt/resource patterns -->

From fastmcp.prompts:
```python
from fastmcp.prompts import prompt, Message
from typing import Annotated, Literal
# Guided workflow: async def name(lang: Annotated[Literal["en","fr"], "Language: 'en' or 'fr'"] = "en") -> list[Message]
# Quick lookup: async def name(lang: ...) -> str
```

From fastmcp.resources:
```python
from fastmcp.resources import resource
import json
# @resource("scheme://module/name", mime_type=..., name=..., title=...)
# ZERO function parameters. Return str.
```

StatCan tools (15 total):
```
sc_search_cubes, sc_get_cube_metadata, sc_get_code_sets,
sc_get_series_info_by_vector, sc_get_series_info_by_coord,
sc_get_data_by_vector, sc_get_data_by_coord, sc_get_data_by_date_range,
sc_get_bulk_vector_data, sc_get_changed_series, sc_get_changed_cubes,
sc_get_sdmx_structure, sc_get_sdmx_data, sc_get_sdmx_vector_data,
sc_fetch_vectors_to_store
```

Datastore tools (6 total):
```
ds_create_table, ds_insert_data, ds_query, ds_list_tables, ds_get_schema, ds_drop_table
```

CKAN tools (7 total):
```
ckan_search_datasets, ckan_get_dataset_details, ckan_list_organizations,
ckan_search_by_tag, ckan_get_resource, ckan_list_groups, ckan_get_dataset_stats
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StatCan prompts + resources + unit tests</name>
  <files>
    src/mcp_canada/modules/statcan/prompts.py
    src/mcp_canada/modules/statcan/resources.py
    src/mcp_canada/modules/statcan/__tests__/test_prompts_resources.py
  </files>
  <behavior>
    - statcan_find_data(lang="en") returns list[Message] guiding search -> metadata -> data retrieval
    - statcan_find_data(lang="fr") returns French messages
    - statcan_quick_vector(lang="en") returns str referencing sc_get_data_by_vector
    - statcan_explore_sdmx(lang="en") returns list[Message] for SDMX workflow
    - statcan_store_and_query(lang="en") returns list[Message] chaining sc_fetch_vectors_to_store -> ds_query
    - statcan_monitor_changes(lang="en") returns str referencing sc_get_changed_series
    - data://statcan/frequency-codes resource returns valid JSON mapping frequency IDs to labels
    - data://statcan/scalar-factor-codes resource returns valid JSON
    - data://statcan/status-codes resource returns valid JSON
    - docs://statcan/wds-guide resource returns markdown
    - docs://statcan/sdmx-guide resource returns markdown explaining key syntax
    - template://statcan/data-report resource returns markdown with placeholders
  </behavior>
  <action>
    Follow the exact pattern from Plan 01's BoC implementation (read 40-01-SUMMARY.md for reference).

    **statcan/prompts.py** — 6 prompts:
    1. `statcan_find_data` — Guided workflow: sc_search_cubes -> sc_get_cube_metadata -> sc_get_code_sets -> sc_get_data_by_vector
    2. `statcan_quick_vector` — Quick lookup: instruction to call sc_get_data_by_vector with vectorId and recent params
    3. `statcan_explore_sdmx` — Guided workflow: sc_get_sdmx_structure -> sc_get_sdmx_data with dimension filtering
    4. `statcan_store_and_query` — Guided workflow: sc_fetch_vectors_to_store -> ds_query for cross-module SQL
    5. `statcan_monitor_changes` — Quick lookup: instruction to call sc_get_changed_series/sc_get_changed_cubes
    6. `statcan_compare_series` — Guided workflow: sc_get_bulk_vector_data for comparing multiple vectors

    **statcan/resources.py** — 8 resources:
    1. `data://statcan/frequency-codes` — JSON: {1: "Daily", 2: "Weekly", ...} with en/fr labels (from WDS getCodeSets)
    2. `data://statcan/scalar-factor-codes` — JSON: {0: "Units", 3: "Thousands", 6: "Millions", ...}
    3. `data://statcan/status-codes` — JSON: observation status symbols with descriptions
    4. `data://statcan/uom-codes` — JSON: common units of measure codes with descriptions
    5. `docs://statcan/wds-guide` — Markdown: WDS API overview, rate limits (25 req/s documented, 20 req/s our limit), tiered caching strategy, available endpoints
    6. `docs://statcan/sdmx-key-syntax` — Markdown: how to construct SDMX dimension keys (dot-separated, period for wildcard), examples
    7. `docs://statcan/coordinate-system` — Markdown: how StatCan coordinates work (productId + coordinate resolves to vectorId)
    8. `template://statcan/time-series-report` — Markdown template with {product_id}, {vector_id}, {period}, {observations} placeholders

    **Unit tests** following BoC test pattern from Plan 01.
  </action>
  <verify>
    <automated>cd /Users/mariomeyer/code/ReyemTech/mcp-canada && uv run pytest src/mcp_canada/modules/statcan/__tests__/test_prompts_resources.py -x -v</automated>
  </verify>
  <done>
    - statcan/prompts.py has 6 bilingual prompts (3 guided + 3 quick lookup)
    - statcan/resources.py has 8 resources (4 catalogs + 3 docs + 1 template)
    - All unit tests pass for both languages and all resource formats
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Datastore + CKAN prompts + resources + unit tests</name>
  <files>
    src/mcp_canada/modules/datastore/prompts.py
    src/mcp_canada/modules/datastore/resources.py
    src/mcp_canada/modules/datastore/__tests__/test_prompts_resources.py
    src/mcp_canada/modules/ckan/prompts.py
    src/mcp_canada/modules/ckan/resources.py
    src/mcp_canada/modules/ckan/__tests__/test_prompts_resources.py
  </files>
  <behavior>
    - ds_create_and_query(lang="en") returns list[Message] guiding table creation -> insert -> query
    - ds_quick_query(lang="en") returns str referencing ds_query
    - data://datastore/column-types resource returns valid JSON with supported SQLite column types
    - docs://datastore/sql-guide resource returns markdown with SELECT/PRAGMA syntax
    - ckan_explore_federal_data(lang="en") returns list[Message] guiding dataset search -> details -> resource
    - ckan_quick_search(lang="en") returns str referencing ckan_search_datasets
    - data://ckan/federal-organizations resource returns valid JSON with org names
    - docs://ckan/search-tips resource returns markdown with search techniques
  </behavior>
  <action>
    **datastore/prompts.py** — 4 prompts:
    1. `ds_create_and_query` — Guided workflow: ds_create_table -> ds_insert_data -> ds_query
    2. `ds_quick_query` — Quick lookup: instruction for ds_query with example SQL
    3. `ds_explore_tables` — Quick lookup: instruction for ds_list_tables -> ds_get_schema
    4. `ds_cross_module_join` — Guided workflow: guides storing data from multiple modules then JOINing

    **datastore/resources.py** — 6 resources:
    1. `data://datastore/column-types` — JSON: supported SQLite column types (TEXT, INTEGER, REAL, BLOB, NUMERIC)
    2. `data://datastore/identifier-rules` — JSON: regex pattern, max length, allowed characters for table/column names
    3. `docs://datastore/sql-guide` — Markdown: supported SQL (SELECT, PRAGMA table_info, PRAGMA table_list), what's blocked (INSERT/UPDATE/DELETE via ds_query)
    4. `docs://datastore/cross-module-patterns` — Markdown: examples of fetching from BoC + StatCan, storing, then JOINing
    5. `template://datastore/query-report` — Markdown template for SQL query results
    6. `template://datastore/schema-report` — Markdown template for table schema display

    **ckan/prompts.py** — 5 prompts:
    1. `ckan_explore_federal_data` — Guided workflow: ckan_search_datasets -> ckan_get_dataset_details -> ckan_get_resource
    2. `ckan_quick_search` — Quick lookup: instruction for ckan_search_datasets with query parameter
    3. `ckan_browse_organizations` — Quick lookup: instruction for ckan_list_organizations -> ckan_search_datasets with org filter
    4. `ckan_browse_by_tag` — Quick lookup: instruction for ckan_search_by_tag
    5. `ckan_portal_overview` — Guided workflow: ckan_get_dataset_stats -> ckan_list_groups for portal overview

    **ckan/resources.py** — 7 resources:
    1. `data://ckan/federal-organizations` — JSON: top federal organizations publishing data on open.canada.ca with en/fr names
    2. `data://ckan/popular-tags` — JSON: common dataset tags with en/fr descriptions
    3. `data://ckan/resource-formats` — JSON: common resource formats (CSV, JSON, XML, XLSX) with descriptions
    4. `docs://ckan/search-tips` — Markdown: CKAN search syntax tips, filtering by org/tag/format
    5. `docs://ckan/api-quirks` — Markdown: CKAN API pagination, bilingual metadata handling, resource access patterns
    6. `template://ckan/dataset-summary` — Markdown template for dataset overview
    7. `template://ckan/resource-report` — Markdown template for resource details

    **Unit tests** for both modules following BoC test pattern.
  </action>
  <verify>
    <automated>cd /Users/mariomeyer/code/ReyemTech/mcp-canada && uv run pytest src/mcp_canada/modules/datastore/__tests__/test_prompts_resources.py src/mcp_canada/modules/ckan/__tests__/test_prompts_resources.py -x -v</automated>
  </verify>
  <done>
    - datastore has 4 prompts + 6 resources with passing tests
    - ckan has 5 prompts + 7 resources with passing tests
    - All bilingual, all resources zero-parameter
  </done>
</task>

</tasks>

<verification>
- `uv run pytest src/mcp_canada/modules/statcan/__tests__/test_prompts_resources.py src/mcp_canada/modules/datastore/__tests__/test_prompts_resources.py src/mcp_canada/modules/ckan/__tests__/test_prompts_resources.py -x -v` — all tests pass
- `uv run pytest --cov=src/mcp_canada --cov-fail-under=95` — coverage maintained
</verification>

<success_criteria>
- StatCan: 6 prompts + 8 resources with unit tests
- Datastore: 4 prompts + 6 resources with unit tests
- CKAN: 5 prompts + 7 resources with unit tests
- All prompts bilingual, all resources zero-parameter, correct URI schemes
- Total: 15 prompts + 21 resources added across 3 modules
</success_criteria>

<output>
After completion, create `.planning/phases/40-mcp-prompts-and-resources/40-02-SUMMARY.md`
</output>
