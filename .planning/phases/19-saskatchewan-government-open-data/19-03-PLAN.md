---
phase: 19-saskatchewan-government-open-data
plan: 03
type: execute
wave: 2
depends_on: [19-02]
files_modified:
  - src/mcp_canada/modules/saskatchewan/client.py
  - src/mcp_canada/modules/saskatchewan/tools.py
  - src/mcp_canada/modules/saskatchewan/__tests__/test_client.py
  - src/mcp_canada/modules/saskatchewan/__tests__/test_tools.py
autonomous: true
requirements:
  - SK-06
  - SK-07
  - SK-08
  - SK-09
must_haves:
  truths:
    - "saskatchewan_get_crop_yields returns 16 crop-type estimates dispatched by region: 'provincial' → Province_Summary FeatureServer, one of 5 regions (southeast/southwest/central/northeast/northwest) → Regions_Only FeatureServer; Canola/HRSW/Lentil/Chickpea fields present and non-null"
    - "saskatchewan_get_grain_elevators returns SK grain elevators (default where=PR='SK') with station, railway, licensee, capacity_tonne; optional railway filter (CN/CP/SHORTLINE)"
    - "saskatchewan_get_mineral_mines dispatches by mineral (potash/uranium/helium/coal) to the correct dated FeatureServer, returning Name, Company, Status, Mine_Type, DateOpened; invalid mineral → make_error INVALID_INPUT with valid=[...]"
    - "All 3 curated tools use the PRIMARY org (services3 / zcv98lgAl8xQ04cW), standalone @tool, lang en/fr, make_response/make_error, saskatchewan_ prefix, 8+ Keywords single-line"
  artifacts:
    - path: src/mcp_canada/modules/saskatchewan/tools.py
      provides: "3 curated @tool functions (crop yields, grain elevators, mineral mines)"
      contains: "def saskatchewan_get_crop_yields"
    - path: src/mcp_canada/modules/saskatchewan/client.py
      provides: "fetch_crop_yields, fetch_grain_elevators, fetch_mineral_mines bodies"
      contains: "MINERAL_MINES_FS_URLS"
  key_links:
    - from: "src/mcp_canada/modules/saskatchewan/client.py"
      to: "Provincial_Estimated_Crop_Yields FeatureServers (primary org)"
      via: "query_feature_service with region dispatch (Province_Summary vs Regions_Only)"
      pattern: "query_feature_service"
    - from: "src/mcp_canada/modules/saskatchewan/client.py"
      to: "Potash/Uranium/Helium/Coal dated FeatureServers"
      via: "MINERAL_MINES_FS_URLS[mineral] dispatch"
      pattern: "MINERAL_MINES_FS_URLS"
---

<objective>
Implement Saskatchewan's signature agriculture domain plus its energy/mining backbone — 3 curated tools (SK-06, SK-07, SK-08+SK-09): estimated crop yields, grain elevators, and mineral mines (potash/uranium/helium/coal dispatch). All three call FeatureServers on the PRIMARY org (services3 / zcv98lgAl8xQ04cW). Mineral mines is a single parametrized tool covering all four of Saskatchewan's signature minerals (SK-08 potash and SK-09 uranium both satisfied by the dispatch).

Purpose: Deliver the highest-priority machine-readable domains first; agriculture is Saskatchewan's signature and the most agent-valuable.
Output: 3 curated tools + client bodies + unit tests.
</objective>

<execution_context>
@/Users/mariomeyer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomeyer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-saskatchewan-government-open-data/19-RESEARCH.md
@.planning/phases/19-saskatchewan-government-open-data/19-01-SUMMARY.md
@.planning/phases/19-saskatchewan-government-open-data/19-02-SUMMARY.md
@CLAUDE.md
@.claude/rules/modules.md
@.claude/rules/tests.md

# Reference: curated FeatureServer client + tool pattern (mineral dispatch mirrors alberta ST3 INVALID_INPUT)
@src/mcp_canada/modules/manitoba/client.py
@src/mcp_canada/modules/alberta/client.py
@src/mcp_canada/modules/alberta/tools.py

<interfaces>
<!-- Live-verified field lists (2026-06-15). out_fields should be EXPLICIT (not *) to keep payloads lean. -->
<!-- Crop yields (region dispatch):
       Province_Summary FS .../Provincial_Estimated_Crop_Yields_Province_Summary/FeatureServer/0
       Regions_Only FS    .../Provincial_Estimated_Crop_Yields_Regions_Only/FeatureServer/0
       out_fields: Region,HRSW,Durum,Oat,Barley,Canola,Mustard,Soybean,Pea,Lentil,Chickpea,Canary_seed,Flax,Winter_wheat,Fall_rye,Other_wheat_
       region='provincial' -> Province_Summary, where="1=1"
       region in 5 regions -> Regions_Only, where="Region='<Title-cased region>'" (e.g. 'Southeast')
     Grain elevators: .../Western_Canada_Grain_Elevator_2024/FeatureServer/0
       out_fields: Station,PR,Railway,Licensee,Elevator_type,Capacity_tonne ; default where="PR='SK'"
       optional railway filter -> where += " AND Railway='CN'" etc.
     Mineral mines (dispatch via MINERAL_MINES_FS_URLS[mineral], layer 0):
       out_fields: Commodity,Name,Status,Mine_Type,Company,Mine_Site,Regulation,DateOpened,Website -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Agriculture + mining client bodies + client tests</name>
  <files>
    src/mcp_canada/modules/saskatchewan/client.py,
    src/mcp_canada/modules/saskatchewan/__tests__/test_client.py
  </files>
  <behavior>
    - fetch_crop_yields(region="provincial", max_records=MAX_RECORDS): region "provincial" → CROP_YIELDS_PROVINCE_FS_URL layer 0, where="1=1"; region in CROP_REGIONS (non-provincial) → CROP_YIELDS_REGIONS_FS_URL layer 0, where="Region='<Title>'". Explicit out_fields (16 crops + Region). Returns ({"features":[...], "count":N, "truncated":bool, "region":region}, was_cached) at CACHE_TTL_ANNUAL. Unknown region → ValueError (tool maps to INVALID_INPUT with valid=CROP_REGIONS).
    - fetch_grain_elevators(railway=None, max_records=MAX_RECORDS): GRAIN_ELEVATORS_FS_URL layer 0; where="PR='SK'" (+ AND Railway='<railway>' when set). Explicit out_fields. CACHE_TTL_META.
    - fetch_mineral_mines(mineral, max_records=MAX_RECORDS): fs_url=MINERAL_MINES_FS_URLS.get(mineral.lower()); None → ValueError (tool maps to INVALID_INPUT, valid=list(MINERAL_MINES_FS_URLS)). layer 0; explicit out_fields. Returns ({"features":[...], "count":N, "truncated":bool, "mineral":mineral}, was_cached) at CACHE_TTL_META.
    All wrap query_feature_service in cached_fetch with get_limiter(RATE_GROUP_HUB, RATE_LIMIT_HUB); mirror manitoba.client.fetch_provincial_parks exactly.
  </behavior>
  <action>
    Fill the 3 client stubs. Use CACHE_KEY_PREFIX cache keys. Patch arcgis_hub.query_feature_service
    in tests (AsyncMock returning the conftest sample tuples). Fill TestSaskGetCropYields (provincial
    vs region dispatch — assert correct FS URL + where; assert Canola key present in a flattened
    row), TestSaskGetGrainElevators (PR='SK' default + railway filter where-clause), and
    TestSaskGetMineralMines (dispatch routes to correct FS per mineral; unknown mineral raises
    ValueError). Assert on the where clause and service URL passed to query_feature_service
    (call_args) — this is the layer/dispatch contract mocks must pin.
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/saskatchewan/__tests__/test_client.py::TestSaskGetCropYields src/mcp_canada/modules/saskatchewan/__tests__/test_client.py::TestSaskGetGrainElevators src/mcp_canada/modules/saskatchewan/__tests__/test_client.py::TestSaskGetMineralMines -x -v</automated>
  </verify>
  <done>3 client functions implemented. Crop-yield region dispatch routes to the correct FeatureServer + where clause; mineral dispatch routes to the correct dated FS; grain elevators default to PR='SK'. Client tests pass and assert on the dispatch (where/URL) via call_args.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 3 curated @tool functions + tool tests</name>
  <files>
    src/mcp_canada/modules/saskatchewan/tools.py,
    src/mcp_canada/modules/saskatchewan/__tests__/test_tools.py
  </files>
  <behavior>
    - saskatchewan_get_crop_yields(region="provincial", lang="en"): make_response, api="saskatchewan-geohub"; invalid region → make_error INVALID_INPUT valid=[provincial+5 regions].
    - saskatchewan_get_grain_elevators(railway=None, lang="en"): make_response; railway optional Literal["CN","CP","SHORTLINE"].
    - saskatchewan_get_mineral_mines(mineral, lang="en"): mineral Literal["potash","uranium","helium","coal"]; invalid → make_error INVALID_INPUT valid=[4 minerals].
  </behavior>
  <action>
    Add the 3 @tool functions. Standalone @tool; lang Literal en/fr; make_response/make_error;
    saskatchewan_ prefix; single-line "Use for:" + 8+ "Keywords:". Double-guard the enum inputs
    (tool INVALID_INPUT + client ValueError) using the alberta ST3 pattern; French error message via
    inline `lang == "fr"` ternary (no t() import). Document in the crop-yields docstring that this is
    estimated yield in bu/acre (the weekly PDF crop reports are NOT machine-readable; this FS is the
    substitute). Patch client at tools namespace in tests. Fill the 3 tool test classes
    (happy + invalid-enum error + lang passthrough).
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/saskatchewan/__tests__/test_tools.py -x -k "CropYields or GrainElevators or MineralMines" -v</automated>
  </verify>
  <done>3 curated tools defined and tested. Invalid region/mineral return make_error INVALID_INPUT with valid=[...]; lang passes through; envelope api="saskatchewan-geohub". test_quality docstring checks pass.</done>
</task>

</tasks>

<verification>
1. `uv run pytest src/mcp_canada/modules/saskatchewan/__tests__/ -x` — green
2. `uv run python -c "from mcp_canada.modules.saskatchewan import tools; assert hasattr(tools,'saskatchewan_get_crop_yields') and hasattr(tools,'saskatchewan_get_mineral_mines'); print('OK')"`
</verification>

<success_criteria>
- SK-06, SK-07, SK-08, SK-09 implemented: 3 curated tools callable
- Crop-yield region dispatch + mineral dispatch verified via call_args; grain elevators default PR='SK'
- Invalid enum inputs return structured INVALID_INPUT (not exceptions)
- All tools follow conventions
</success_criteria>

<output>
After completion, create `.planning/phases/19-saskatchewan-government-open-data/19-03-SUMMARY.md`: list the 3 tools, the crop-region + mineral dispatch tables, the out_fields used, any field-name surprises.
</output>
