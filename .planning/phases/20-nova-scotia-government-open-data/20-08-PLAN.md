---
phase: 20-nova-scotia-government-open-data
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_canada/modules/nova_scotia/constants.py
  - src/mcp_canada/modules/nova_scotia/client.py
  - src/mcp_canada/modules/nova_scotia/__tests__/conftest.py
  - src/mcp_canada/modules/nova_scotia/__tests__/test_client.py
  - tests/integration/test_tool_scenarios.py
autonomous: true
gap_closure: true
requirements: [NS-13]

must_haves:
  truths:
    - "ns_get_health_facilities(facility_type='hospital') returns HTTP 200 live with non-null facility_name, county, and type for each row"
    - "ns_get_health_facilities(facility_type='long_term_care') returns HTTP 200 live with non-null facility_name, zone, and beds for at least one row"
    - "Both facility types normalize to the common NovaScotiaHealthFacility shape (facility_name, address, town, county, type, zone, beds, x_coordinate, y_coordinate, facility_category)"
    - "Hospital x_coordinate/y_coordinate are derived from the_geom.coordinates and the_geom is stripped from output"
    - "The UPSTREAM_ERROR early-out block (assert error.code == 'UPSTREAM_ERROR'; return) is ABSENT from both test_health_facilities_hospital_field_presence and test_health_facilities_ltc_beds_and_zone in tests/integration/test_tool_scenarios.py"
    - "Public ns_get_health_facilities tool signature is unchanged (facility_type, county, limit, lang)"
  artifacts:
    - path: "src/mcp_canada/modules/nova_scotia/constants.py"
      provides: "Per-dataset HOSPITAL_SELECT/HOSPITAL_ORDER + LTC_SELECT/LTC_ORDER live-confirmed SoQL"
      contains: "HOSPITAL_SELECT"
    - path: "src/mcp_canada/modules/nova_scotia/client.py"
      provides: "fetch_health_facilities with per-dataset SoQL branch + post-fetch normalization"
      contains: "fetch_health_facilities"
    - path: "src/mcp_canada/modules/nova_scotia/__tests__/conftest.py"
      provides: "Real RAW Socrata schema fixtures for both datasets"
      contains: "SAMPLE_HOSPITALS_ROWS"
    - path: "src/mcp_canada/modules/nova_scotia/__tests__/test_client.py"
      provides: "TestNsGetHealthFacilities asserting per-dataset $select/$order/filter + raw->normalized mapping"
      contains: "TestNsGetHealthFacilities"
    - path: "tests/integration/test_tool_scenarios.py"
      provides: "NS health scenarios with UPSTREAM_ERROR early-out removed; live 200 + non-null assertions"
      contains: "ns_get_health_facilities"
  key_links:
    - from: "client.fetch_health_facilities (hospital path)"
      to: "Socrata tmfr-3h8a"
      via: "HOSPITAL_SELECT (facility,address,town,county,type,the_geom) + HOSPITAL_ORDER (county ASC)"
      pattern: "HOSPITAL_SELECT"
    - from: "client.fetch_health_facilities (LTC path)"
      to: "Socrata x76a-axw2"
      via: "LTC_SELECT (no county/type column) + LTC_ORDER (town ASC) + NO county filter"
      pattern: "LTC_SELECT"
    - from: "integration test_health_facilities_*"
      to: "live data.novascotia.ca"
      via: "no UPSTREAM_ERROR early-out — a live 400 must fail the assertion"
      pattern: "facility_name"
---

<objective>
Close the single major Phase 20 UAT gap: `ns_get_health_facilities` returns HTTP 400 live for BOTH `hospital` and `long_term_care`. The two backing Socrata datasets have INCOMPATIBLE raw schemas, but `fetch_health_facilities` builds ONE shared `$select`/`$order`/`county=` filter from normalized field names that exist on neither raw dataset — so the request dies upstream (400) before normalization ever runs. The bug shipped because (1) conftest fixtures used the post-normalization shape (never validating the real `$select`), and (2) the integration scenarios treat `error.code == "UPSTREAM_ERROR"` as an acceptable pass and `return` early, swallowing the 400.

The fix is FULLY diagnosed and live-confirmed (2026-06-15) in `.planning/debug/ns-health-facilities-400.md`. Restore NS-13 by building per-dataset SoQL from live-confirmed raw column names, normalizing to the common shape AFTER fetch, replacing the masking fixtures + test escape-hatch with real-schema tests that fail RED today, and adding a LIVE verification step.

Purpose: Restore NS-13 (health facility coverage) and remove the mock-vs-real test masking that let it ship broken.
Output: A working `ns_get_health_facilities` (both types live-200, normalized shape), real-schema unit tests, de-masked integration scenarios, and a LIVE verification run.

SCOPE GUARDRAIL: Touch ONLY `fetch_health_facilities` + its constants/fixtures/tests + the two NS health integration scenarios. The other 9 curated Nova Scotia tools were live-validated 2026-06-15 (all `$select` return 200) and MUST NOT change. Do NOT touch any other module.
</objective>

<execution_context>
@/Users/mariomeyer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomeyer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/20-nova-scotia-government-open-data/20-UAT.md
@.planning/debug/ns-health-facilities-400.md

@./CLAUDE.md
@.claude/rules/modules.md
@.claude/rules/tests.md

@src/mcp_canada/modules/nova_scotia/client.py
@src/mcp_canada/modules/nova_scotia/constants.py
@src/mcp_canada/modules/nova_scotia/schemas.py
@src/mcp_canada/modules/nova_scotia/tools.py
@src/mcp_canada/modules/nova_scotia/__tests__/conftest.py
@src/mcp_canada/modules/nova_scotia/__tests__/test_client.py

<live_confirmed_facts>
<!-- All live-confirmed 2026-06-15 against data.novascotia.ca — see debug session for the curl evidence. -->

HOSPITAL dataset tmfr-3h8a — raw columns: facility, address, town, county, type, the_geom
  - NO facility_name, NO x_coordinate, NO y_coordinate
  - the_geom = {"type":"Point","coordinates":[lon, lat]}  e.g. [-60.1741, 46.1130]  (GeoJSON [longitude, latitude])
  - Live-200 SoQL:  $select=facility,address,town,county,type,the_geom   $order=county ASC

LTC dataset x76a-axw2 — raw columns: facility_id, facility_name, address, town, postal_code,
  facility_type, zone, nursing_homes_nh_no_of_beds, nursing_homes_nh_no_of_respite_beds,
  residential_care_facilities_rcf_no_of_beds, rcf_respite_beds,
  single_entry_access_sea_participating, x_coordinate, y_coordinate, location, the_geom
  - HAS zone, HAS facility_type, HAS x_coordinate/y_coordinate
  - NO county, NO type column
  - Live-200 SoQL:  $select=facility_name,address,town,postal_code,facility_type,zone,
      nursing_homes_nh_no_of_beds,residential_care_facilities_rcf_no_of_beds,
      single_entry_access_sea_participating,x_coordinate,y_coordinate
      $order=town ASC   (CANNOT order by county — column does not exist)

CURRENT (broken) code — both 400:
  - hospital $select 'facility_name,...,x_coordinate,y_coordinate' -> 400 "No such column: facility_name"
  - LTC      $select '...,county,...'                              -> 400 "No such column: county"
  - LTC also 400s on $order='county ASC' AND on $where="county='...'"

beds: nursing_homes_nh_no_of_beds arrives as a number string (e.g. "190.0"); schema field beds is int|None.
</live_confirmed_facts>

<common_output_shape>
<!-- NovaScotiaHealthFacility (schemas.py) — already correct, no schema change required. -->
facility_name, address, town, county, type, zone, beds, x_coordinate, y_coordinate, facility_category

HOSPITAL raw -> common:
  facility_name = row["facility"]                       (rename)
  address, town, county, type                            (pass through)
  zone = None                                            (hospital has no zone)
  beds = None                                            (hospital has no beds)
  x_coordinate = the_geom["coordinates"][0]  (longitude) — derive, else None
  y_coordinate = the_geom["coordinates"][1]  (latitude)  — derive, else None
  facility_category = "hospital"
  (strip the_geom from output — Pitfall 5)

LTC raw -> common:
  facility_name, address, town                           (pass through)
  county = None                                          (LTC has no county; zone carries the region)
  type  = row["facility_type"]                           (rename)
  zone  = row["zone"]                                    (pass through)
  beds  = int(float(row["nursing_homes_nh_no_of_beds"])) if present else None
  x_coordinate = float(row["x_coordinate"]) if present else None
  y_coordinate = float(row["y_coordinate"]) if present else None
  facility_category = "long_term_care"
  (strip the_geom from output if present)
</common_output_shape>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — real raw-schema fixtures + per-dataset client tests that fail against current code</name>
  <files>
    src/mcp_canada/modules/nova_scotia/__tests__/conftest.py
    src/mcp_canada/modules/nova_scotia/__tests__/test_client.py
  </files>
  <behavior>
    Replace the masking fixtures with REAL RAW Socrata schemas (this is the test-gap fix), then replace
    TestNsGetHealthFacilities to assert the per-dataset SoQL and the raw->normalized mapping. These MUST fail
    RED against the current (broken) shared-$select code.

    conftest.py fixtures (replace SAMPLE_HOSPITALS_ROWS and SAMPLE_LTC_ROWS in place — keep the SAME names so
    existing imports in test_client.py / test_tools.py keep resolving):
      - SAMPLE_HOSPITALS_ROWS: raw hospital rows with keys EXACTLY {facility, address, town, county, type, the_geom}.
        the_geom = {"type":"Point","coordinates":[-60.1741, 46.1130]} (and a second row with another point).
        Do NOT include facility_name / x_coordinate / y_coordinate (they do not exist on the raw dataset).
      - SAMPLE_LTC_ROWS: raw LTC rows with keys EXACTLY {facility_name, address, town, postal_code,
        facility_type, zone, nursing_homes_nh_no_of_beds, residential_care_facilities_rcf_no_of_beds,
        single_entry_access_sea_participating, x_coordinate, y_coordinate}. Use string values
        (beds like "190.0", coords like "-63.2702"). Do NOT include county / type columns.

    test_client.py — replace the existing TestNsGetHealthFacilities class. Patch
    `mcp_canada.modules.nova_scotia.client.socrata` (AsyncMock query_dataset). The following test methods are
    REQUIRED and must be explicitly named (none are optional / deferrable):
      Per-dataset SoQL (read from mock_socrata.query_dataset.call_args.kwargs — the client's _soql forwards
      select/order/where as kwargs to socrata.query_dataset):
        - HOSPITAL: select == HOSPITAL_SELECT (or asserts it contains "facility," and "the_geom" and does NOT
          contain "facility_name" / "x_coordinate" / "y_coordinate"); order == "county ASC".
        - LTC: select == LTC_SELECT (or asserts it contains "facility_name" + "nursing_homes_nh_no_of_beds" and
          does NOT contain "county"); order == "town ASC" (NOT "county ASC").
        - county filter (hospital): hospital + county="Halifax" -> where contains "county='Halifax'".
        - REQUIRED, explicitly named `test_ltc_county_filter_is_skipped_no_county_column`: call
          fetch_health_facilities(facility_type="long_term_care", county="Halifax") and assert the outgoing
          `where` kwarg is None / contains no "county=". This is the PRIMARY regression guard for the LTC 400 —
          it must NOT be omittable.
      Raw -> normalized mapping (feed the RAW fixtures through, assert on data["facilities"][0]):
        - HOSPITAL: facility_name == raw "facility"; x_coordinate == -60.1741 and y_coordinate == 46.1130
          (derived from the_geom.coordinates[0]/[1]); "the_geom" NOT in the normalized row; zone is None;
          beds is None; facility_category == "hospital".
        - LTC: type == raw "facility_type"; zone == raw "zone"; county is None; beds == 190 (int) when raw was
          "190.0"; x_coordinate/y_coordinate are floats; facility_category == "long_term_care".
      Edge-case helpers (REQUIRED here so coverage stays >=95% — do NOT defer to Task 3; explicitly named):
        - `test_hospital_missing_the_geom_yields_none_coordinates`: a hospital row with no the_geom key ->
          x_coordinate is None and y_coordinate is None (no exception).
        - `test_hospital_short_the_geom_coordinates_yields_none`: a hospital row whose the_geom.coordinates is
          a short/malformed list (len < 2) -> x_coordinate is None and y_coordinate is None (no exception).
        - `test_ltc_empty_beds_string_yields_none`: an LTC row with nursing_homes_nh_no_of_beds == "" ->
          beds is None (coercion never raises).
      Keep: invalid facility_type raises ValueError; truncated=True when rows >= limit (rebuild that test's inline
      rows to the RAW hospital shape so it stays valid).
  </behavior>
  <action>
    Edit conftest.py: redefine SAMPLE_HOSPITALS_ROWS and SAMPLE_LTC_ROWS to the live-confirmed RAW schemas above
    (see <live_confirmed_facts>). Keep all OTHER fixtures untouched. Update the conftest module docstring's
    health-fixtures bullet to note these are now RAW (pre-normalization) shapes.

    Edit test_client.py: replace the whole TestNsGetHealthFacilities class with the REQUIRED, explicitly-named
    test methods listed in <behavior> (including `test_ltc_county_filter_is_skipped_no_county_column`,
    `test_hospital_missing_the_geom_yields_none_coordinates`, `test_hospital_short_the_geom_coordinates_yields_none`,
    and `test_ltc_empty_beds_string_yields_none`). Read select/order/where from
    `mock_socrata.query_dataset.call_args.kwargs` (the client's _soql passes them as keyword args to
    socrata.query_dataset).

    IMPORTANT — constants import placement (RED-state isolation): place
    `from mcp_canada.modules.nova_scotia.constants import HOSPITAL_SELECT, LTC_SELECT, HOSPITAL_ORDER, LTC_ORDER`
    INSIDE the individual test method bodies (or inside the TestNsGetHealthFacilities class body) — NOT at module
    level. These constants are added in Task 2, so the import currently raises ImportError; keeping it scoped to
    TestNsGetHealthFacilities ensures the RED failure affects ONLY this class and does NOT break unrelated test
    classes in the same file (TestNsSearch, TestNsMarineAquaculture, etc.).

    Do NOT modify client.py or constants.py in this task. Do NOT touch test_tools.py (it patches
    fetch_health_facilities entirely with canned HOSPITALS_DATA/LTC_DATA and is schema-agnostic — leave it).
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/nova_scotia/__tests__/test_client.py::TestNsGetHealthFacilities -x 2>&1 | tail -25</automated>
  </verify>
  <done>
    New fixtures use the RAW Socrata schemas. TestNsGetHealthFacilities FAILS RED (ImportError on the new
    constants, scoped to this class only, and/or assertion failures on the wrong shared $select) — proving the
    test now exercises the real contract that current code violates. Unrelated test classes in test_client.py
    still collect and pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — per-dataset SoQL constants + branched fetch_health_facilities with post-fetch normalization</name>
  <files>
    src/mcp_canada/modules/nova_scotia/constants.py
    src/mcp_canada/modules/nova_scotia/client.py
  </files>
  <behavior>
    After this task, Task 1's tests go GREEN: hospital and LTC each build their own live-confirmed $select/$order,
    the county filter is hospital-only, and raw rows normalize to the common shape.
  </behavior>
  <action>
    constants.py — add per-dataset SoQL constants near DS_HOSPITALS / DS_LTC_RCF_FACILITIES (all Final[str]),
    exactly matching the live-200 strings in <live_confirmed_facts>:
      HOSPITAL_SELECT = "facility,address,town,county,type,the_geom"
      HOSPITAL_ORDER  = "county ASC"
      LTC_SELECT = (
          "facility_name,address,town,postal_code,facility_type,zone,"
          "nursing_homes_nh_no_of_beds,residential_care_facilities_rcf_no_of_beds,"
          "single_entry_access_sea_participating,x_coordinate,y_coordinate"
      )
      LTC_ORDER = "town ASC"   # LTC has NO county column — cannot order by county
    Add a short comment block citing the debug session (live-confirmed 2026-06-15).

    client.py — rewrite fetch_health_facilities (keep the public signature
    `(facility_type, county=None, limit=MAX_RECORDS)` unchanged; keep the ValueError guard):
      1. Import HOSPITAL_SELECT, HOSPITAL_ORDER, LTC_SELECT, LTC_ORDER from .constants.
      2. Branch per facility_type:
         - hospital: dataset_id=DS_HOSPITALS; select=HOSPITAL_SELECT; order=HOSPITAL_ORDER;
           where = f"county='{county}'" if county else None.
         - long_term_care: dataset_id=DS_LTC_RCF_FACILITIES; select=LTC_SELECT; order=LTC_ORDER;
           where = None  (NEVER send a county= filter to LTC — no county column; it 400s).
      3. Keep the cache_key including facility_type/county/limit.
      4. In the fetcher, call _soql(dataset_id, where=where, select=select, order=order, limit=limit).
      5. Normalize each raw row to the common shape per <common_output_shape>:
         - Add small private module-level helpers `_normalize_hospital_row(row)` and `_normalize_ltc_row(row)`
           (pure functions) OR inline branches — keep it readable (engineering-standards: no deep nesting).
           Hospital: facility_name=row.get("facility"); derive x/y from
           row.get("the_geom",{}).get("coordinates") (guard: list of len>=2, else None,None); strip the_geom;
           zone=None; beds=None; facility_category="hospital". LTC: type=row.get("facility_type");
           county=None; zone=row.get("zone"); beds=_coerce_int(row.get("nursing_homes_nh_no_of_beds"));
           x/y=_coerce_float(...); facility_category="long_term_care"; strip the_geom if present.
         - Add `_coerce_int` / `_coerce_float` private helpers (handle None, "" , and float-strings like "190.0"
           -> int 190; return None on failure — never raise).
      6. Return {"facilities": normalized, "count": len(normalized), "truncated": len(normalized) >= limit}.
      7. Update the docstring: note county filtering is HOSPITAL-ONLY (LTC has zone, not county); hospital x/y are
         derived from the_geom; both datasets normalize to the common shape. Keep cached_fetch + TTL as-is
         (CACHE_TTL_ANNUAL).

    Do NOT change any other client function, any other $select, or schemas.py (NovaScotiaHealthFacility already
    covers the shape; beds stays int|None and the coercion produces int|None).
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/nova_scotia/__tests__/test_client.py::TestNsGetHealthFacilities src/mcp_canada/modules/nova_scotia/__tests__/test_tools.py -x 2>&1 | tail -20</automated>
  </verify>
  <done>
    TestNsGetHealthFacilities passes GREEN; test_tools.py (canned-data tool tests) still pass. Hospital builds
    HOSPITAL_SELECT + county ASC + optional county filter; LTC builds LTC_SELECT + town ASC + NO county filter;
    raw rows normalize correctly (hospital facility->facility_name + derived x/y + the_geom stripped; LTC
    facility_type->type, county=None, beds coerced to int). Public signature unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: De-mask integration scenarios, run full unit suite at 95%, then LIVE-verify both facility types</name>
  <files>
    tests/integration/test_tool_scenarios.py
    (NOTE: Parts B and C are verification-only runs — they modify NO files beyond the Part A integration-test
    de-mask in tests/integration/test_tool_scenarios.py.)
  </files>
  <action>
    PART A — remove the escape-hatch (the masking fix). In tests/integration/test_tool_scenarios.py edit BOTH NS
    health scenarios:
      - test_health_facilities_hospital_field_presence (~lines 2686-2707)
      - test_health_facilities_ltc_beds_and_zone (~lines 2709-2731)
    In EACH, DELETE the early-out:
        assert "_meta" in data or "error" in data
        if "error" in data:
            assert data["error"]["code"] == "UPSTREAM_ERROR"
            return
    Replace with a hard success assertion so a live 400 FAILS the suite:
        assert "_meta" in data, f"Expected live success, got: {data}"
        assert data["_meta"]["source"]["api"] == "nova-scotia-socrata"
    Keep (and strengthen) the field-presence assertions on the NORMALIZED output:
      - hospital: facilities non-empty; first.facility_name non-null; first.county non-null; first.type non-null.
      - LTC: facilities non-empty; >=1 row with beds non-null; >=1 row with zone non-null; first.facility_name non-null.
    Do NOT touch any other province's scenarios (the same UPSTREAM_ERROR early-out elsewhere is a KNOWN deferred
    systemic follow-up — out of scope here; record it in the SUMMARY only).

    PART B — full unit suite + coverage gate (verification-only run; must stay >=95%):
        uv run pytest --cov=src/mcp_canada --cov-fail-under=95
    The new client branches/helpers and their edge cases (missing/short the_geom -> x/y None; empty beds string
    -> None) are ALREADY covered by the required Task-1 tests
    (test_hospital_missing_the_geom_yields_none_coordinates, test_hospital_short_the_geom_coordinates_yields_none,
    test_ltc_empty_beds_string_yields_none). This part CONFIRMS coverage rather than discovering gaps; if
    coverage is nonetheless <95% on a new line, add a narrowly-scoped test back in Task 1's class.

    PART C — LIVE verification (real API, NOT mocked; verification-only run). Run the two de-masked NS health
    scenarios against data.novascotia.ca:
        uv run pytest tests/integration/test_tool_scenarios.py -v -m integration --timeout=120 \
          -k "health_facilities_hospital_field_presence or health_facilities_ltc_beds_and_zone"
    Both MUST pass with a real _meta envelope and non-null facility_name (hospital: +county +type; LTC: +beds +zone).
    If either returns a 400/UPSTREAM_ERROR, the fix is incomplete — return to Task 2; do NOT weaken the assertions.
  </action>
  <verify>
    <automated>uv run pytest --cov=src/mcp_canada --cov-fail-under=95 2>&1 | tail -30 ; uv run pytest tests/integration/test_tool_scenarios.py -v -m integration --timeout=120 -k "health_facilities" 2>&1 | tail -25</automated>
  </verify>
  <done>
    Both NS health integration scenarios no longer have the UPSTREAM_ERROR early-out. Full unit suite passes with
    coverage >=95%. LIVE run: both ns_get_health_facilities calls return a real _meta envelope with non-null
    facility_name (hospital: county+type; LTC: beds+zone) against data.novascotia.ca. NS-13 restored.
  </done>
</task>

</tasks>

<verification>
Phase-level gap-closure checks (all must hold):
1. `uv run pytest src/mcp_canada/modules/nova_scotia/__tests__/ -x` — all NS unit tests pass (incl. the new real-schema TestNsGetHealthFacilities).
2. `uv run pytest --cov=src/mcp_canada --cov-fail-under=95` — full suite green, coverage >=95%.
3. LIVE: `uv run pytest tests/integration/test_tool_scenarios.py -m integration -k "health_facilities" --timeout=120` — both scenarios pass against data.novascotia.ca with real _meta + non-null normalized fields.
4. `uv run ruff check src/ tests/` and `uv run pyright` clean on touched files.
5. Manual diff review: only `fetch_health_facilities`, its 4 new constants, the two health fixtures, TestNsGetHealthFacilities, and the two NS health integration scenarios changed. NO other NS tool, NO other module, NO schema change.
6. Public `ns_get_health_facilities` tool signature unchanged: `(facility_type, county=None, limit=5000, lang="en")`.
</verification>

<success_criteria>
- ns_get_health_facilities returns live 200 + non-null facility_name for BOTH hospital and long_term_care.
- Hospital rows: county + type non-null; x/y derived from the_geom; the_geom absent from output.
- LTC rows: >=1 row with beds (int) and zone non-null; county is None; type from facility_type.
- The integration suite would now FAIL on a live 400 (escape-hatch removed) — the test that would have caught the ship is in place.
- Unit coverage >=95%; no other NS tool or module touched; public tool signature preserved.
- NS-13 restored.
</success_criteria>

<output>
After completion, create `.planning/phases/20-nova-scotia-government-open-data/20-08-SUMMARY.md`.

In the SUMMARY, record this DEFERRED systemic follow-up (do NOT fix it here):
> Other provinces' integration scenarios (e.g. Manitoba, Quebec, Saskatchewan, Alberta) likely contain the same
> `if error.code == "UPSTREAM_ERROR": return` early-out that masked this NS bug. This is a known mock-vs-real
> masking pattern (same class as Manitoba Phase 18). Flag for a future cross-module integration-test audit —
> OUT OF SCOPE for this gap closure.
</output>
</content>
</invoke>
