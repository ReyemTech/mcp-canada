---
phase: 20-nova-scotia-government-open-data
plan: 07
type: execute
wave: 7
depends_on: [20-06]
files_modified:
  - src/mcp_canada/modules/nova_scotia/__tests__/test_tools.py
  - tests/integration/test_tool_scenarios.py
  - tests/integration/test_prompts_resources_scenarios.py
  - README.md
  - docs/MODULES.md
  - CLAUDE.md
  - EXAMPLES.md
autonomous: true
requirements:
  - NS-18
must_haves:
  truths:
    - "Parametrized TestNsEnvelopes asserts every ns_ tool returns a valid _meta envelope; TestNsLangParam asserts lang='fr' passes through for every ns_ tool"
    - "TestNovaScotiaToolScenarios (integration, via MCP Client) hits the REAL data.novascotia.ca SODA API and asserts FIELD PRESENCE + non-null on documented columns (the Manitoba lesson): license_le (marine leases), stock/number_released (hatchery), kgs/total_value (production), temperature_c (water quality), site_name (boil water), pro_name (protected areas, the_geom ABSENT), station_name/latitude (air quality), facility_name (health facilities), live_births (vital stats), crude_prevalence_rate + normalized zone (chronic disease); discover_tools finds ns_ tools; ns_list_categories returns 20+ categories incl. 'Fishing and Aquaculture'; ns_search_datasets returns h57h-p9mm; invalid disease/facility_type → INVALID_INPUT"
    - "TestNovaScotiaPromptsResources integration verifies the 6 prompts via list_prompts() and the 7 resources via read_resource()"
    - "README.md has a Nova Scotia section in the Tool Catalog with all 17 ns_ tools, the header tool/module/API count updated, and the Open Government Licence – Nova Scotia v1.1 attribution; docs/MODULES.md has a Nova Scotia entry; CLAUDE.md adds Socrata as the 4th Portal Technologies row (data.novascotia.ca, shared/socrata.py, SODA/SoQL) and notes transport/511 + NS ArcGIS Hub deferred; EXAMPLES.md has an NS cross-module SQL example"
    - "Coverage >=95% across src/mcp_canada (incl. shared/socrata.py); full unit suite green; Nova Scotia live integration tests green"
  artifacts:
    - path: tests/integration/test_tool_scenarios.py
      provides: "TestNovaScotiaToolScenarios class with live field-presence assertions"
      contains: "class TestNovaScotiaToolScenarios"
    - path: CLAUDE.md
      provides: "Socrata as the 4th Portal Technologies row"
      contains: "Socrata"
    - path: README.md
      provides: "Nova Scotia tool catalog section + updated tool count"
      contains: "Nova Scotia"
  key_links:
    - from: "tests/integration/test_tool_scenarios.py"
      to: "MCP Client (discover_tools / call_tool) → real data.novascotia.ca SODA API"
      via: "client.call_tool('call_tool', {...}) for ns_ tools, asserting FIELD PRESENCE + non-null"
      pattern: "ns_"
---

<objective>
Close Phase 20 (NS-18): parametrized envelope/lang tests over every Nova Scotia tool, LIVE integration tests through the MCP Client layer that assert FIELD PRESENCE + non-null values against the REAL data.novascotia.ca SODA API (not just response shape — the Manitoba lesson, where mocks masked a live 400), documentation sync (README/MODULES/CLAUDE/EXAMPLES — including adding Socrata as the 4th Portal Technology), and the 95% coverage gate.

Purpose: Prove all 17 NS tools follow conventions, are discoverable through MCP, behave correctly against the real Socrata endpoints (geometry exclusion, the categories= workaround, zone normalization, dispatch validation — all things only live runs confirm), and the catalog docs stay in sync.
Output: Green parametrized + live-integration suites, updated docs (Socrata documented as portal tech #4), coverage >=95%.
</objective>

<execution_context>
@/Users/mariomeyer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomeyer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-nova-scotia-government-open-data/20-RESEARCH.md
@.planning/phases/20-nova-scotia-government-open-data/20-VALIDATION.md
@.planning/phases/20-nova-scotia-government-open-data/20-01-SUMMARY.md
@.planning/phases/20-nova-scotia-government-open-data/20-02-SUMMARY.md
@.planning/phases/20-nova-scotia-government-open-data/20-03-SUMMARY.md
@.planning/phases/20-nova-scotia-government-open-data/20-04-SUMMARY.md
@.planning/phases/20-nova-scotia-government-open-data/20-05-SUMMARY.md
@.planning/phases/20-nova-scotia-government-open-data/20-06-SUMMARY.md
@CLAUDE.md
@.claude/rules/tests.md
@.claude/rules/modules.md

# Reference: Saskatchewan closing plan (parametrized + LIVE field-presence integration + docs sync)
@src/mcp_canada/modules/saskatchewan/__tests__/test_tools.py
@tests/integration/test_tool_scenarios.py
@tests/integration/test_prompts_resources_scenarios.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parametrized envelope/lang tests + LIVE field-presence integration scenarios</name>
  <files>
    src/mcp_canada/modules/nova_scotia/__tests__/test_tools.py,
    tests/integration/test_tool_scenarios.py,
    tests/integration/test_prompts_resources_scenarios.py
  </files>
  <behavior>
    - TestNsEnvelopes: parametrize over the full list of 17 ns_ tools; each (with mocked client) returns make_response with _meta.source.api set. Use AsyncMock for client functions.
    - TestNsLangParam: parametrize over all 17 tools; lang="fr" passes through to envelope _meta.lang.
    - TestNovaScotiaToolScenarios (integration, @pytest.mark.integration, @pytest.mark.timeout(30)): wire providers + BM25 transform per .claude/rules/tests.md; call tools through client.call_tool('call_tool', {...}). MANDATORY field-presence + non-null assertions against REAL data.novascotia.ca (the Manitoba lesson — do NOT settle for "_meta exists"):
        * ns_get_marine_aquaculture_leases(species_type="Shellfish"): assert license_le, county, ownership, species non-null in >=1 lease AND "the_geom" NOT in the row (geometry-exclusion proof).
        * ns_get_fish_hatchery_stocking(stock="Brook Trout", limit=5): assert stock, county, number_released, stocking_date non-null.
        * ns_get_aquaculture_production(): assert year, county, kgs/total_value present (non-null) in >=1 row.
        * ns_get_water_quality_monitoring(): assert station_number, date, temperature_c present in >=1 reading.
        * ns_get_boil_water_advisories(): assert no error envelope; result is a list (empty is VALID — assert "error" not in data, count>=0); when non-empty assert site_name/county/date_advisory_issued non-null.
        * ns_get_protected_areas(status="Designated"): assert pro_name, protect1, owner, status non-null AND "the_geom" NOT in the row.
        * ns_get_air_quality_stations(): assert station_name, latitude, longitude non-null in >=1 station.
        * ns_get_health_facilities(facility_type="hospital"): assert facility_name, county, type non-null; then facility_type="long_term_care" returns >=1 facility with beds/zone present.
        * ns_get_vital_statistics(): assert counties, year, population, live_births non-null in >=1 row.
        * ns_get_chronic_disease_prevalence(disease="ami", sex="F"): assert year, zone (normalized from health_zone), crude_prevalence_rate non-null.
        * ns_list_categories(): assert >=20 category strings incl. "Fishing and Aquaculture" (PROVES the categories=-broken workaround works live).
        * discover_tools "Nova Scotia aquaculture" finds ns_search_datasets; ns_search_datasets(query="aquaculture") returns total>=10 AND "h57h-p9mm" among result ids (PROVES catalog search + pagination).
        * Error: ns_get_chronic_disease_prevalence(disease="tuberculosis") → structured INVALID_INPUT with valid= (not an exception); ns_get_health_facilities(facility_type="clinic") → INVALID_INPUT.
    - TestNovaScotiaPromptsResources (integration): list_prompts() includes the 6 ns_ prompts; read_resource() returns valid content for the 7 resources.
  </behavior>
  <action>
    Fill the parametrized TestNsEnvelopes + TestNsLangParam in test_tools.py (mirror saskatchewan's Plan 07). Append
    TestNovaScotiaToolScenarios to tests/integration/test_tool_scenarios.py and TestNovaScotiaPromptsResources to
    tests/integration/test_prompts_resources_scenarios.py — call through the MCP Client (NOT client functions
    directly). For the curated live tests, assert FIELD PRESENCE + non-null per the behavior list — do NOT settle for
    asserting only "_meta" exists (that is exactly what masked the Manitoba live 400). Assert on field presence, not
    specific daily values (data changes). Use long timeouts (30s). Handle flaky APIs (check r.content before .json();
    re-run once on transient failure). Mark integration tests @pytest.mark.integration (skipped by default). For boil
    water, accept an empty list as a valid success. For the two geometry-bearing datasets (marine leases, protected
    areas), explicitly assert the_geom is ABSENT from the flattened rows.

    Then RUN the live integration suite (-m integration -k NovaScotia) to confirm the field-presence assertions pass
    against the real SODA endpoints — this is the phase's acceptance evidence, especially for the categories= workaround,
    geometry exclusion, and zone normalization.
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/nova_scotia/__tests__/test_tools.py::TestNsEnvelopes src/mcp_canada/modules/nova_scotia/__tests__/test_tools.py::TestNsLangParam -x -v</automated>
    <automated>uv run pytest tests/integration/test_tool_scenarios.py tests/integration/test_prompts_resources_scenarios.py -v -m integration -k "NovaScotia" --timeout=120 2>&1 | tail -40</automated>
  </verify>
  <done>Parametrized envelope/lang tests green over all 17 ns_ tools. Live integration scenarios pass against real data.novascotia.ca asserting field presence + non-null (license_le, number_released, kgs, temperature_c, pro_name with the_geom ABSENT, crude_prevalence_rate with normalized zone, 20+ categories incl. Fishing and Aquaculture, h57h-p9mm discoverable). Empty-boil-water + invalid-disease/facility scenarios present. Prompts/resources discoverable via MCP.</done>
</task>

<task type="auto">
  <name>Task 2: Docs sync (README/MODULES/CLAUDE/EXAMPLES — Socrata as 4th portal tech) + coverage gate</name>
  <files>
    README.md,
    docs/MODULES.md,
    CLAUDE.md,
    EXAMPLES.md
  </files>
  <action>
    - README.md: add a Nova Scotia section to the Tool Catalog listing all 17 ns_ tools with the first line of each
      docstring; update the header tool count (add 17), the API/module count, and add the Open Government Licence –
      Nova Scotia v1.1 attribution note. Recount ns_ tools from tools.py before editing the header.
    - docs/MODULES.md: add a Nova Scotia entry (portal = data.novascotia.ca Socrata SODA; the 17-tool list;
      6 prompts + 7 resources; note transport/511 + NS ArcGIS Hub deferred).
    - CLAUDE.md: ADD SOCRATA as the 4th row in the Portal Technologies table — "Socrata | shared/socrata.py |
      First used Phase 20: Nova Scotia (data.novascotia.ca) | SODA API: /api/catalog/v1 discovery + /resource/{id}.json
      SoQL ($where/$select/$order/$limit); keyless, optional X-App-Token". Add a short note under the table mirroring the
      BC/Alberta notes: Socrata categories= param is broken (use q= + client-side domain_category); geometry excluded via
      $select; transport/511 + NS ArcGIS Hub deferred. Also add nova_scotia to the shared-utilities note for shared/socrata.py.
    - EXAMPLES.md: add one Nova Scotia cross-module SQL example (e.g., fetch aquaculture production via
      ns_get_aquaculture_production or ns_query_dataset, store to datastore, JOIN with StatCan fisheries/economic data).
    Keep counts exact. Then run the full coverage gate.
  </action>
  <verify>
    <automated>uv run pytest --cov=src/mcp_canada --cov-fail-under=95 -q 2>&1 | tail -15</automated>
    <automated>grep -c "ns_" README.md && grep -qi "Socrata" CLAUDE.md && echo "Socrata documented"</automated>
  </verify>
  <done>README/MODULES/CLAUDE/EXAMPLES updated and in sync with the implemented 17-tool set. Tool count accurate. CLAUDE.md has Socrata as the 4th Portal Technologies row + the categories=/geometry notes. Coverage >=95% (incl. shared/socrata.py). Full unit suite green.</done>
</task>

</tasks>

<verification>
1. `uv run pytest --cov=src/mcp_canada --cov-fail-under=95 -q` — coverage gate passes
2. `uv run pytest tests/integration/ -v -m integration --timeout=120 -k "NovaScotia"` — live integration green (field-presence assertions)
3. `grep -c "ns_" README.md` — Nova Scotia tools listed in catalog
4. `grep -i "Socrata" CLAUDE.md` — Socrata documented as 4th portal technology
5. `uv run ruff check src/mcp_canada/modules/nova_scotia/ src/mcp_canada/shared/socrata.py` and `uv run pyright src/mcp_canada/modules/nova_scotia/ src/mcp_canada/shared/socrata.py` — clean
</verification>

<success_criteria>
- NS-18 complete: every ns_ tool envelope/lang verified (parametrized) + discoverable via discover_tools through MCP Client
- LIVE integration scenarios pass asserting field presence + non-null (license_le, number_released, kgs, temperature_c, pro_name + the_geom ABSENT, crude_prevalence_rate + normalized zone, 20+ categories, h57h-p9mm) — the Manitoba lesson honored
- Empty-boil-water + invalid-disease/facility-type scenarios pass
- 6 prompts + 7 resources verified via MCP integration
- README/MODULES/CLAUDE/EXAMPLES in sync; Socrata documented as the 4th portal technology; tool count accurate
- Coverage >=95% (incl. shared/socrata.py), full suite green
</success_criteria>

<output>
Create `.planning/phases/20-nova-scotia-government-open-data/20-07-SUMMARY.md`: final tool count (17), coverage %, the live integration scenario list with the field-presence assertions, docs updated (Socrata as portal tech #4), NS-01…NS-18 matrix status, and any deferred items (transport/511, NS ArcGIS Hub, rockweed, individual air-quality pollutant series).
</output>
