---
phase: 18-manitoba-government-open-data
plan: 05
type: execute
wave: 4
depends_on: [18-04]
files_modified:
  - src/mcp_canada/modules/manitoba/client.py
  - src/mcp_canada/modules/manitoba/tools.py
  - src/mcp_canada/modules/manitoba/__tests__/test_client.py
  - src/mcp_canada/modules/manitoba/__tests__/test_tools.py
autonomous: true
requirements:
  - MB-06
  - MB-14
  - MB-15
  - MB-16
must_haves:
  truths:
    - "manitoba_get_provincial_parks returns 93 parks with bilingual NAME_E/NOM_F and park_type filter from Manitoba_Parks FeatureServer"
    - "manitoba_get_surgical_wait_times returns diagnostic/surgical wait averages by Year + procedure (IndicatorDataArea) from Manitoba_Diagnostic_and_Surgical_Wait_Time_Averages FeatureServer"
    - "manitoba_get_fisheries_data returns 350+ water bodies with species, fishing regulations, stocking records, Secchi depth from Manitoba_Waterbody_Data FeatureServer"
    - "manitoba_get_provincial_forests returns provincial forest management boundaries from Manitoba_Provincial_Forests___Version_6 FeatureServer"
    - "manitoba_get_health_facilities returns rural health care facilities (ED/acute care/PCH flags) with optional RHA filter from the Rural Health Care Facilities FeatureServer (resolved in Wave 0 spike)"
  artifacts:
    - path: src/mcp_canada/modules/manitoba/tools.py
      provides: "manitoba_get_provincial_parks, manitoba_get_surgical_wait_times, manitoba_get_fisheries_data, manitoba_get_provincial_forests, manitoba_get_health_facilities"
      contains: "def manitoba_get_provincial_parks"
    - path: src/mcp_canada/modules/manitoba/client.py
      provides: "fetch_provincial_parks, fetch_surgical_wait_times, fetch_fisheries_data, fetch_provincial_forests, fetch_health_facilities bodies"
  key_links:
    - from: "src/mcp_canada/modules/manitoba/client.py"
      to: "Manitoba_Parks / Waterbody / Forests / Wait-Times / Rural-Health FeatureServers"
      via: "arcgis_hub.query_feature_service"
      pattern: "PROVINCIAL_PARKS_FS_URL|WATERBODY_DATA_FS_URL|SURGICAL_WAIT_TIMES_FS_URL"
---

<objective>
Implement Manitoba's environment/parks and regional health domains (MB-06, MB-14, MB-15, MB-16). Parks (93, bilingual), surgical wait times (by procedure/year), fisheries waterbodies (350+), provincial forests, and rural health facilities (by RHA). MB-16 bundles forests + health facilities into two related tools.

Purpose: Round out the balanced curation across environment and health — parity with Alberta AHS / Quebec MSSS.
Output: 5 tools + client bodies + unit tests.
</objective>

<execution_context>
@/Users/mariomeyer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomeyer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-manitoba-government-open-data/18-RESEARCH.md
@.planning/phases/18-manitoba-government-open-data/18-SPIKE.md
@.planning/phases/18-manitoba-government-open-data/18-01-SUMMARY.md
@CLAUDE.md
@.claude/rules/modules.md

@src/mcp_canada/modules/alberta/client.py
@src/mcp_canada/modules/alberta/tools.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Environment/health/parks client bodies + tests</name>
  <files>
    src/mcp_canada/modules/manitoba/client.py,
    src/mcp_canada/modules/manitoba/__tests__/test_client.py
  </files>
  <behavior>
    - fetch_provincial_parks(park_type=None, max_records, include_geometry=False): query PROVINCIAL_PARKS_FS_URL; where=f"TYPE_E='{park_type}'" if given; out_fields NAME_E,NOM_F,BIOME,O_AREA,TYPE_E,TYPE_F,STATUS_E,PROTDATE,PRK_CLSS,URL. CACHE_TTL_META.
    - fetch_surgical_wait_times(procedure=None, year=None, max_records): query SURGICAL_WAIT_TIMES_FS_URL; where built from IndicatorDataArea LIKE procedure and/or Year=year (32k max records — require at least documents pagination/truncated). out_fields Year,IndicatorDataArea,Average_Wait. CACHE_TTL_ANNUAL.
    - fetch_fisheries_data(region=None, name=None, max_records): query WATERBODY_DATA_FS_URL; where by fishing division/region or Name LIKE. out_fields focused subset of the 26 fields (id,name,species,regulations,secchi,boat_launch,...). CACHE_TTL_META.
    - fetch_provincial_forests(max_records, include_geometry=False): query PROVINCIAL_FORESTS_FS_URL. CACHE_TTL_STATIC.
    - fetch_health_facilities(rha=None, max_records): query RURAL_HEALTH_FACILITIES_FS_URL (from spike); facility fields (Community, Facility, Emergency Dept, Acute Care, PCH); optional RHA filter. CACHE_TTL_META. If FS URL UNRESOLVED in spike, degrade gracefully (return UPSTREAM_ERROR/empty with note + direct agents to manitoba_search_datasets).
  </behavior>
  <action>
    Fill the 5 client stubs. Mirror alberta payload shape {features,count,truncated}. For wait-times and fisheries, build WHERE clauses defensively (escape quotes; uppercase field names only where the layer requires). park_type/rha invalid values → ValueError (tool maps to INVALID_INPUT). Fill TestManitobaGetParks, TestManitobaGetWaitTimes, TestManitobaGetFisheriesData, TestManitobaGetForests, TestManitobaGetHealthFacilities. AsyncMock query_feature_service.
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/manitoba/__tests__/test_client.py -x -k "Parks or WaitTimes or Fisheries or Forests or HealthFacilities" -v</automated>
  </verify>
  <done>5 client functions implemented. Parks bilingual + type filter. Wait-times by procedure/year. Fisheries focused field subset. Health-facilities degrades gracefully if FS URL unresolved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 5 environment/health/parks @tool functions + tool tests</name>
  <files>
    src/mcp_canada/modules/manitoba/tools.py,
    src/mcp_canada/modules/manitoba/__tests__/test_tools.py
  </files>
  <behavior>
    - manitoba_get_provincial_parks(park_type=None, lang="en"): 93 bilingual parks.
    - manitoba_get_surgical_wait_times(procedure=None, year=None, lang="en"): averages by procedure/year.
    - manitoba_get_fisheries_data(region=None, name=None, lang="en"): waterbodies + species/regs.
    - manitoba_get_provincial_forests(lang="en"): forest boundaries.
    - manitoba_get_health_facilities(rha=None, lang="en"): rural facilities by RHA.
  </behavior>
  <action>
    Add the 5 @tool functions following conventions (standalone @tool, lang, make_response/make_error, manitoba_ prefix, single-line Use for: + 8+ Keywords:). health-facilities docstring should mention Manitoba's 5 RHAs (WRHA, PMH, IERHA, SHSS, NHR). wait-times docstring: annual averages, not live ER waits (not published machine-readable). Fill the 5 tool test classes (happy + lang + any INVALID_INPUT).
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/manitoba/__tests__/test_tools.py -x -k "Parks or WaitTimes or Fisheries or Forests or HealthFacilities" -v</automated>
  </verify>
  <done>5 tools defined + tested. Conventions honored. RHA + procedure/year filters work.</done>
</task>

</tasks>

<verification>
1. `uv run pytest src/mcp_canada/modules/manitoba/__tests__/ -x`
2. `uv run python -c "from mcp_canada.modules.manitoba import tools; assert hasattr(tools,'manitoba_get_provincial_parks') and hasattr(tools,'manitoba_get_health_facilities'); print('OK')"`
</verification>

<success_criteria>
- MB-06, MB-14, MB-15, MB-16 implemented
- Parks bilingual + park_type filter
- Wait-times by procedure/year
- Fisheries focused subset of 26 fields
- Forests + health facilities (RHA filter) implemented; health-facilities degrades if FS URL unresolved
</success_criteria>

<output>
Create `.planning/phases/18-manitoba-government-open-data/18-05-SUMMARY.md`: 5 tools, rural-health FS URL resolution status, fisheries field subset chosen, park TYPE_E value list discovered.
</output>
