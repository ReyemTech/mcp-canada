---
phase: 21-new-brunswick-government-open-data
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/mcp_canada/shared/arcgis_hub.py
  - src/mcp_canada/shared/__tests__/test_arcgis_hub.py
  - src/mcp_canada/modules/new_brunswick/__init__.py
  - src/mcp_canada/modules/new_brunswick/constants.py
  - src/mcp_canada/modules/new_brunswick/schemas.py
  - src/mcp_canada/modules/new_brunswick/client.py
  - src/mcp_canada/modules/new_brunswick/tools.py
  - src/mcp_canada/modules/new_brunswick/prompts.py
  - src/mcp_canada/modules/new_brunswick/resources.py
  - src/mcp_canada/modules/new_brunswick/__tests__/__init__.py
  - src/mcp_canada/modules/new_brunswick/__tests__/conftest.py
  - src/mcp_canada/modules/new_brunswick/__tests__/test_client.py
  - src/mcp_canada/modules/new_brunswick/__tests__/test_tools.py
  - src/mcp_canada/modules/new_brunswick/__tests__/test_prompts_resources.py
  - .planning/phases/21-new-brunswick-government-open-data/21-SPIKE.md
autonomous: false
requirements:
  - NB-06
  - NB-14
  - NB-24
  - NB-25
  - ERR-01
  - ERR-05
  - ERR-06
  - ERR-07
must_haves:
  truths:
    - "TRACER: an agent calling nb_get_crown_land gets real GeoNB Crown Land rows in a _meta envelope — proving D-05 (query_feature_service works unchanged against a bare ArcGIS Server MapServer) end to end through constants -> client -> @tool -> FileSystemProvider registration, before any other tool is written"
    - "The GeoNB Crown Land layer id used is 3, never 0 — layer 0 does not exist on that service (RESEARCH Pitfall 1)"
    - "shared/arcgis_hub.py gains exactly two new functions — list_arcgis_server_services(base_url) and get_arcgis_server_layers(service_url) — that walk a bare ArcGIS Server REST directory; both decode through decode_json (ERR-05) and accept an injected httpx_client like every existing function in that file"
    - "TestListArcgisServerServices / TestGetArcgisServerLayers assert the OUTGOING params dict, not just the URL — the Manitoba/Saskatchewan lesson"
    - "query_feature_service, get_layer_metadata, get_count and search_hub_datasets are unchanged — the extension is additive on the discovery side only (D-04, D-05, D-06)"
    - "The user has decided, at a blocking checkpoint, whether the live-verified gnb.socrata.com provincial portal (312 datasets) joins this phase's discovery surface or is documented and deferred — D-01's premise that NB has no provincial catalogue is contradicted by evidence gathered at plan time (NB-25)"
    - "21-SPIKE.md records the live GeoNB service directory (62 services) and re-verifies every curated layer id from the RESEARCH table against {service}/MapServer?f=json — no curated tool is planned on an unresolved layer id"
    - "new_brunswick auto-registers via FileSystemProvider with MODULE_NAME='new_brunswick' — server.py is NOT modified"
    - "constants.py hardcodes every GeoNB service URL and re-verified layer id, the federal CKAN base, the 511 env var name, all cache TTLs, three rate groups, and the locked ALL_NB_TOOL_NAMES manifest, so Plans 02-06 never edit constants.py"
    - "client.py declares every downstream client function as a NotImplementedError stub with its full LOCKED signature returning (data, was_cached); signatures are locked here so Plans 02-06 fill bodies only and never collide"
    - "pytest collects the shared and new_brunswick test directories with no ImportError"
  artifacts:
    - path: src/mcp_canada/shared/arcgis_hub.py
      provides: "list_arcgis_server_services + get_arcgis_server_layers — bare ArcGIS Server REST directory enumeration (no Hub Search API in front)"
      contains: "def list_arcgis_server_services"
    - path: src/mcp_canada/shared/__tests__/test_arcgis_hub.py
      provides: "TestListArcgisServerServices / TestGetArcgisServerLayers with outgoing-param assertions"
      contains: "class TestListArcgisServerServices"
    - path: src/mcp_canada/modules/new_brunswick/__init__.py
      provides: "MODULE_NAME='new_brunswick' + MODULE_DESCRIPTION (en+fr) for FileSystemProvider auto-discovery"
      contains: "MODULE_NAME = \"new_brunswick\""
    - path: src/mcp_canada/modules/new_brunswick/constants.py
      provides: "GeoNB base + per-service URLs + re-verified layer ids, federal CKAN base, 511 env var, TTLs, rate groups, ALL_NB_TOOL_NAMES"
      contains: "CROWN_LAND_LAYER"
    - path: src/mcp_canada/modules/new_brunswick/client.py
      provides: "three module-level limiters, _api_get, _geonb_query, _511_get, Five11NotConfigured, and every client stub with a locked signature"
      contains: "class Five11NotConfigured"
    - path: src/mcp_canada/modules/new_brunswick/tools.py
      provides: "nb_get_crown_land (the tracer) plus the standalone-decorator imports Plans 02-06 build on"
      contains: "async def nb_get_crown_land"
    - path: src/mcp_canada/modules/new_brunswick/__tests__/conftest.py
      provides: "CKAN package_search, GeoNB service-directory, MapServer layer and GeoJSON feature fixtures + autouse cache/limiter patch"
      contains: "GeoNB_DNR_Crown_Land"
    - path: .planning/phases/21-new-brunswick-government-open-data/21-SPIKE.md
      provides: "Live 62-service directory dump + per-service layer-id re-verification verdicts"
      contains: "GeoNB_DNR_Crown_Land"
  key_links:
    - from: "src/mcp_canada/modules/new_brunswick/client.py"
      to: "src/mcp_canada/shared/arcgis_hub.py"
      via: "import — GeoNB fetchers call query_feature_service / list_arcgis_server_services / get_arcgis_server_layers"
      pattern: "from mcp_canada\\.shared import arcgis_hub"
    - from: "src/mcp_canada/shared/arcgis_hub.py"
      to: "src/mcp_canada/shared/http.py"
      via: "decode_json on every new response body (ERR-05)"
      pattern: "decode_json\\(response, url\\)"
    - from: "src/mcp_canada/modules/new_brunswick/__init__.py"
      to: "FileSystemProvider"
      via: "MODULE_NAME + MODULE_DESCRIPTION exports — auto-discovery, no server.py edit"
      pattern: "MODULE_NAME\\s*=\\s*\"new_brunswick\""
---

<objective>
Wave 0 for the `new_brunswick` module — the 8th province. Four jobs, in this order:

1. **TRACER (leads the phase).** Wire ONE capability — "an agent retrieves New Brunswick Crown
   Land parcels" — end to end through every layer this phase will touch: `constants.py` →
   `client.py` (limiter + cache + flatten) → `tools.py` (`@tool` + `@upstream_guard` + envelope +
   `lang`) → FileSystemProvider auto-registration → a live GeoNB call. This proves D-05 (the
   load-bearing bet that `shared/arcgis_hub.py:query_feature_service` works **unchanged** against a
   bare ArcGIS Server MapServer) on the executor's best early-context tokens. All 11 curated tools
   rest on that bet; if it is wrong, it is wrong after one commit rather than after ten.

2. **Blocking decision.** Plan-time live probing found `gnb.socrata.com` — a real, keyless,
   312-dataset NB provincial Socrata portal. 21-CONTEXT.md states the opposite ("NB has no
   provincial catalogue", "there is no NB Socrata instance"). The user decides the discovery
   surface before the tool manifest is locked in Task 4.

3. **Extend `shared/arcgis_hub.py`** with the phase's only genuinely new code — a service-directory
   enumerator standing in for the unavailable Hub Search API (D-06) — and use it live to produce
   `21-SPIKE.md`, re-verifying every curated layer id.

4. **Scaffold the module** (7 files + 4 test files) with every constant, schema and locked client
   signature, so Plans 02-06 fill bodies and never edit `constants.py` or collide with each other.

GeoNB is ArcGIS **Server** (62 `MapServer` services, zero `FeatureServer`), NOT ArcGIS Hub — the Hub
at `geonb-snb.opendata.arcgis.com` returns HTTP 401. Discovery for tabular data is the **federal**
CKAN filtered `organization:nb` (221 datasets), NOT a provincial CKAN — `data.gnb.ca`,
`opendata.gnb.ca` and `nbopendata.ca` do not resolve.

Purpose: prove the architecture on one real slice, settle the contradicted premise, and lock every
contract before business logic.
Output: a live-verified `nb_get_crown_land`, two new shared functions with contract tests, a
pytest-collectable `new_brunswick` module of stubs, and `21-SPIKE.md`.

**Validation note (recorded per planning contract):** the spec-less probe fallback was **skipped**
for this phase — no SPEC.md exists, so there is no `## Edge Coverage` / `## Prohibitions` section to
lift and no probe predicates were generated this run. `must_haves.truths` are derived from the
21-CONTEXT.md `<domain>` boundary, locked decisions D-01..D-12 and 21-RESEARCH.md findings instead.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-new-brunswick-government-open-data/21-CONTEXT.md
@.planning/phases/21-new-brunswick-government-open-data/21-RESEARCH.md
@.planning/phases/21-new-brunswick-government-open-data/21-PATTERNS.md
@.planning/phases/21-new-brunswick-government-open-data/21-VALIDATION.md
@.planning/phases/21-new-brunswick-government-open-data/COVERAGE.md
@CLAUDE.md
@.claude/rules/modules.md
@.claude/rules/tests.md
@.claude/rules/engineering-standards.md

# The file being extended, and its existing test file
@src/mcp_canada/shared/arcgis_hub.py
@src/mcp_canada/shared/__tests__/test_arcgis_hub.py

# Structural templates for the 7-file module + test scaffold
@src/mcp_canada/modules/saskatchewan/constants.py
@src/mcp_canada/modules/saskatchewan/client.py
@src/mcp_canada/modules/saskatchewan/__tests__/conftest.py
@src/mcp_canada/modules/nova_scotia/__init__.py

# Error-classification contract every tool in this phase must satisfy
@src/mcp_canada/shared/errors.py
@src/mcp_canada/shared/envelope.py
@src/mcp_canada/shared/http.py

<interfaces>
<!-- Shared utilities the new_brunswick module consumes. Do NOT redefine; import from shared. -->

From src/mcp_canada/shared/arcgis_hub.py (EXISTING — use unchanged):
  async def query_feature_service(service_url, layer_id, where="1=1", out_fields="*",
                                  include_geometry=False, max_records=MAX_RECORDS,
                                  *, httpx_client=None) -> tuple[list[dict], bool]
      Builds {service_url}/{layer_id}/query with f=geojson, auto-paginates on
      exceededTransferLimit, coalesces `where or "1=1"` (httpx drops None params).
  async def get_layer_metadata(service_url, layer_id, *, httpx_client=None) -> dict
      -> {max_record_count, fields: [{name, type}], geometry_type, name}
  async def get_count(service_url, layer_id, where="1=1", *, httpx_client=None) -> int
  MAX_RECORDS = 5000 ; DEFAULT_PAGE_SIZE = 1000 ; DEFAULT_TIMEOUT = 30.0

From src/mcp_canada/shared/http.py:
  async def api_get(url, params=None, headers=None) -> Any
      Returns already-parsed JSON (dict OR list). Phase 15-05 contract: do NOT call
      .raise_for_status() or .json() on the return value.
  def decode_json(response, url="") -> Any        # raises httpx.DecodingError, not ValueError
  def decode_json_bytes(content, url="") -> Any

From src/mcp_canada/shared/cache.py:
  async def cached_fetch(key, ttl, fetcher) -> tuple[Any, bool]   # (data, was_cached)

From src/mcp_canada/shared/rate_limiter.py:
  def get_limiter(source, rate) -> TokenBucket    # .acquire() is awaitable

From src/mcp_canada/shared/envelope.py:
  def make_response(data, *, api_name, api_url, cached, lang) -> dict
  def make_error(code, message, *, lang, **extras) -> dict
  def upstream_guard(api_name) -> Callable        # applied UNDER @tool

From src/mcp_canada/shared/errors.py:
  InvalidInput -> INVALID_INPUT ; NotFound -> NOT_FOUND ; UpstreamData -> UPSTREAM_ERROR
  A bare `raise ValueError` is banned in source.

From src/mcp_canada/shared/parsers.py:
  async def fetch_and_parse(url, ...) -> list[dict]   # CKAN CSV/XLSX/GeoJSON resources

<new_shared_surface>
<!-- The ONLY new shared code in this phase (D-06, RESEARCH Pattern 3). -->
async def list_arcgis_server_services(base_url, *, httpx_client=None) -> list[dict]
    GET {base_url}?f=json with params {"f": "json"} -> return data.get("services", [])
    Each entry: {"name": ..., "type": "MapServer"}. Folders are NOT returned.

async def get_arcgis_server_layers(service_url, *, httpx_client=None) -> dict
    GET {service_url}/MapServer?f=json with params {"f": "json"}
    -> {"layers": [{"id", "name"}], "tables": [{"id", "name"}]}
Both mirror search_hub_datasets' dual-path httpx_client injection and decode via decode_json.
</new_shared_surface>
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — end-to-end "retrieve NB Crown Land parcels", one path only</name>
  <files>
    src/mcp_canada/modules/new_brunswick/__init__.py,
    src/mcp_canada/modules/new_brunswick/constants.py,
    src/mcp_canada/modules/new_brunswick/client.py,
    src/mcp_canada/modules/new_brunswick/tools.py,
    src/mcp_canada/modules/new_brunswick/__tests__/__init__.py,
    src/mcp_canada/modules/new_brunswick/__tests__/conftest.py,
    src/mcp_canada/modules/new_brunswick/__tests__/test_tools.py
  </files>
  <read_first>
    src/mcp_canada/shared/arcgis_hub.py,
    src/mcp_canada/shared/envelope.py,
    src/mcp_canada/shared/errors.py,
    src/mcp_canada/shared/cache.py,
    src/mcp_canada/shared/rate_limiter.py,
    src/mcp_canada/modules/saskatchewan/client.py,
    src/mcp_canada/modules/saskatchewan/tools.py,
    src/mcp_canada/modules/nova_scotia/__init__.py,
    .planning/phases/21-new-brunswick-government-open-data/21-PATTERNS.md,
    .claude/rules/modules.md
  </read_first>
  <behavior>
    - nb_get_crown_land(lang="en") with no filter returns a _meta envelope whose _meta.source.api is
      "new-brunswick-geonb" and whose data carries a non-empty features list plus count and truncated keys.
    - Each feature dict carries the keys OBJECTID and HOLDER (live-verified field names on layer 3).
    - nb_get_crown_land(holder=2) sends a WHERE clause restricting HOLDER to 2 and returns only matching rows.
    - nb_get_crown_land(lang="fr") returns _meta.lang set to fr.
    - When the client raises httpx.HTTPStatusError, the tool returns an error envelope whose code is
      UPSTREAM_ERROR — it never raises.
    - The layer id passed to query_feature_service is 3.
  </behavior>
  <action>
    Build the thinnest complete slice through every layer this phase touches. Do NOT create other
    tools, other services, or other layers — one path only, production quality.

    `__init__.py`: `MODULE_NAME = "new_brunswick"` plus `MODULE_DESCRIPTION` and
    `MODULE_DESCRIPTION_FR`. Model on `nova_scotia/__init__.py`. The description names the three
    upstream surfaces — federal CKAN filtered to organization nb, GeoNB ArcGIS Server MapServer, and
    key-gated NB 511 — and states that GeoNB is ArcGIS Server, not Hub. Do NOT touch `server.py`;
    FileSystemProvider discovers the module from these two exports.

    `constants.py` (tracer subset only — Task 4 expands it):
      GEONB_BASE_URL = "https://geonb.snb.ca/arcgis/rest/services"
      CROWN_LAND_SERVICE = f"{GEONB_BASE_URL}/GeoNB_DNR_Crown_Land/MapServer"
      CROWN_LAND_LAYER: Final[int] = 3
      CROWN_LAND_FIELDS = "OBJECTID,HOLDER,Shape_Length,Shape_Area"
      RATE_GROUP_GEONB = "new_brunswick_geonb" ; RATE_LIMIT_GEONB = 5.0
      CACHE_KEY_PREFIX = "new_brunswick:" ; CACHE_TTL_META = 86400 ; MAX_RECORDS = 5000
    Add a module docstring stating that GeoNB layer ids are non-guessable and are resolved from
    `{service}/MapServer?f=json`, citing Crown Land as the worked case.

    `client.py` (tracer subset): import `arcgis_hub` from `mcp_canada.shared`, `cached_fetch`,
    `get_limiter`, and the constants. Declare `_geonb_limiter = get_limiter(RATE_GROUP_GEONB,
    RATE_LIMIT_GEONB)` at module level. Implement `fetch_crown_land(holder: int | None = None,
    limit: int = MAX_RECORDS) -> tuple[dict[str, Any], bool]`: build the WHERE clause from the typed
    `holder` argument server-side (never from a caller-supplied clause string), acquire the limiter,
    route through `cached_fetch` keyed with `CACHE_KEY_PREFIX`, and delegate to
    `arcgis_hub.query_feature_service` with `CROWN_LAND_SERVICE`, `layer_id=CROWN_LAND_LAYER`,
    `out_fields=CROWN_LAND_FIELDS`, `include_geometry=False`, `max_records=limit`. Return
    `({"features": [...], "count": len(features), "truncated": bool}, was_cached)`.

    `tools.py`: module docstring stating that every tool uses the standalone `@tool` from
    `fastmcp.tools`, accepts `lang: Literal["en","fr"] = "en"`, returns `make_response` or
    `make_error`, and uses the `nb_` prefix. Define `_API_NAME_GEONB = "new-brunswick-geonb"`.
    Implement `nb_get_crown_land(holder: int | None = None, limit: int = MAX_RECORDS,
    lang: Literal["en","fr"] = "en") -> dict` decorated with `@tool` on top and
    `@upstream_guard(_API_NAME_GEONB)` beneath it (that ordering is what
    `tests/test_tool_error_handling.py` requires). Docstring carries a first line, a `Use for:` line
    and a single-line `Keywords:` line with at least 8 terms, and documents that `holder` is a raw
    integer code with no server-exposed domain (RESEARCH Pitfall 4) so agents do not read it as a
    name. Return `make_response(...)` with `api_name=_API_NAME_GEONB` and
    `api_url=CROWN_LAND_SERVICE`.

    `__tests__/conftest.py`: an autouse fixture that patches `cached_fetch` to a pass-through and the
    limiter to a no-op (copy the saskatchewan conftest shape), plus a `crown_land_geojson` fixture —
    a GeoJSON FeatureCollection whose features carry the four live field names above.

    `__tests__/test_tools.py`: `TestNbGetCrownLandTools` with the six behaviors above, patching
    `mcp_canada.modules.new_brunswick.tools._client.fetch_crown_land` with `AsyncMock`. Assert the
    layer id by patching `arcgis_hub.query_feature_service` and reading `call_args`.

    Do not add a dependency. Do not raise a bare `ValueError` anywhere.
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/new_brunswick/__tests__/test_tools.py -x -q 2>&1 | tail -15</automated>
    <automated>uv run python -c "
import asyncio, json
from mcp_canada.modules.new_brunswick.tools import nb_get_crown_land
r = asyncio.run(nb_get_crown_land(lang='en', limit=25))
assert 'error' not in r, r
assert r['_meta']['source']['api'] == 'new-brunswick-geonb', r['_meta']
f = r['data']['features']
assert len(f) &gt; 0, 'LIVE GeoNB returned zero Crown Land rows'
assert 'HOLDER' in f[0] and 'OBJECTID' in f[0], f[0]
print('TRACER LIVE OK', len(f), 'rows')
"</automated>
  </verify>
  <acceptance_criteria>
    - `src/mcp_canada/modules/new_brunswick/__init__.py` contains `MODULE_NAME = "new_brunswick"`
    - `constants.py` contains `CROWN_LAND_LAYER: Final[int] = 3`
    - `git diff --stat src/mcp_canada/server.py` is empty (module auto-registers; server.py untouched)
    - `tools.py` contains `@upstream_guard(_API_NAME_GEONB)` and `async def nb_get_crown_land`
    - `uv run pytest src/mcp_canada/modules/new_brunswick/__tests__/test_tools.py -x` exits 0
    - The live tracer command in `<verify>` prints `TRACER LIVE OK` with a row count above 0
    - `uv run pytest tests/test_tool_error_handling.py tests/test_error_classification_defaults.py tests/test_upstream_error_classification.py -q` exits 0 with the new tool present
    - `grep -rn "raise ValueError" src/mcp_canada/modules/new_brunswick/ | grep -v '^Binary'` returns no lines
  </acceptance_criteria>
  <reversibility rating="costly">
    Extending `shared/arcgis_hub.py` rather than adding `shared/arcgis_server.py` (D-04) — a later
    extraction would touch every NB call site plus the shared tests. Flagged, not gated.
  </reversibility>
  <done>nb_get_crown_land returns live GeoNB Crown Land rows in a _meta envelope, driven through constants -> client -> @tool -> FileSystemProvider, using layer 3. Unit tests green, project-wide structural guards green, server.py untouched, no new dependency.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Does the live `gnb.socrata.com` NB provincial Socrata portal join this phase's discovery surface, or is it documented and deferred?</decision>
  <context>
21-CONTEXT.md's single most load-bearing claim is that New Brunswick has no provincial catalogue,
and it instructs this phase to correct CLAUDE.md by removing NB from the Socrata note. Live probing
at plan time (2026-07-30) contradicts both:

  GET https://gnb.socrata.com/api/catalog/v1?domains=gnb.socrata.com&only=datasets
    -> HTTP 200, resultSetSize 312  (674 assets of all types)
  GET https://gnb.socrata.com/resource/4zbh-z2ij.json?$limit=2
    -> HTTP 200, real bilingual childcare-facility rows
  GET https://gnb.socrata.com/api/views/4zbh-z2ij.json
    -> HTTP 200, dataset metadata with attribution

Populated `domain_category` values: GeoNB 26, Public Accounts 24, Senior Executive Expenses 14,
Population and Demographics 7, Health and Wellness 5, Business and Economy 4, and 6 more.

It is keyless, and `shared/socrata.py` (shipped in Phase 20) already speaks this API — the
integration cost is zero new client code. The federal-CKAN NB resource download URLs point *at* it
(`gnb.socrata.com/api/views/{id}/rows.csv`), which is how it surfaced.

What CONTEXT.md verified correctly and this does NOT overturn: `data.gnb.ca`, `opendata.gnb.ca` and
`nbopendata.ca` really do not resolve, and the GeoNB ArcGIS Hub really does return HTTP 401.

Why this is a gate rather than a silent planner choice: MCP tool names are a published API surface,
so adding or repointing discovery tools after they ship is effectively one-way for downstream
agents. D-01 is a locked decision and stays locked unless you say otherwise here. Whichever option
you pick, Plan 07 writes the *verified* facts into CLAUDE.md — it will not record the claim that NB
has no Socrata instance, because that claim is false.
  </context>
  <options>
    <option id="option-a">
      <name>Add two gnb.socrata.com tools alongside the locked federal-CKAN discovery (recommended)</name>
      <pros>NB's real provincial portal becomes reachable; 312 datasets with usable categories; zero new client code (reuses shared/socrata.py); D-01 stays intact — the federal CKAN 5 are untouched; keeps the phase inside D-08's 18-22 band.</pros>
      <cons>Swaps out two curated GeoNB tools to stay at 22: nb_get_provincial_parks and nb_get_mineral_occurrences drop to the long tail (both remain reachable via nb_query_geonb_layer). Adds a fourth upstream surface to test and document.</cons>
    </option>
    <option id="option-b">
      <name>Ship exactly as locked in CONTEXT.md; document gnb.socrata.com and defer it</name>
      <pros>Zero deviation from D-01/D-02/D-07/D-08; the 22-tool manifest below is already built for this; the finding is still captured in docs://nb/portal-guide, CLAUDE.md and COVERAGE.md so no knowledge is lost.</pros>
      <cons>NB's actual provincial portal stays unreachable through this module for now; a follow-up phase pays the integration cost that is near zero today.</cons>
    </option>
    <option id="option-c">
      <name>Repoint discovery to gnb.socrata.com as primary and demote the federal CKAN</name>
      <pros>Single most complete NB discovery surface (312 provincial datasets vs 221 federal mirrors).</pros>
      <cons>Directly contradicts locked D-01; largest rework; the federal CKAN carries bilingual title_translated/notes_translated that D-12 depends on. Not recommended.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b, or option-c</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend shared/arcgis_hub.py with bare-ArcGIS-Server directory enumeration (RED to GREEN) + live 21-SPIKE.md</name>
  <files>
    src/mcp_canada/shared/arcgis_hub.py,
    src/mcp_canada/shared/__tests__/test_arcgis_hub.py,
    .planning/phases/21-new-brunswick-government-open-data/21-SPIKE.md
  </files>
  <read_first>
    src/mcp_canada/shared/arcgis_hub.py,
    src/mcp_canada/shared/__tests__/test_arcgis_hub.py,
    src/mcp_canada/shared/http.py,
    .planning/phases/21-new-brunswick-government-open-data/21-RESEARCH.md,
    .planning/phases/21-new-brunswick-government-open-data/21-PATTERNS.md
  </read_first>
  <behavior>
    - list_arcgis_server_services("https://geonb.snb.ca/arcgis/rest/services") issues one GET to that
      exact URL with params {"f": "json"} and returns the value of the response's services key.
    - A trailing slash on base_url is stripped before the request.
    - When the response body has no services key, an empty list is returned rather than a KeyError.
    - The folders key is never included in the return value.
    - get_arcgis_server_layers("https://geonb.snb.ca/arcgis/rest/services/GeoNB_DNR_Crown_Land")
      issues one GET to that URL with "/MapServer" appended and params {"f": "json"}, and returns a
      dict with exactly the keys layers and tables, each a list of {id, name} dicts.
    - Missing layers or tables arrays yield empty lists, not KeyError.
    - Both functions use an injected httpx_client when one is passed and otherwise open their own
      AsyncClient, matching search_hub_datasets.
    - Both call response.raise_for_status() before decoding.
    - A body that is not valid JSON raises httpx.DecodingError, never a ValueError subclass (ERR-05).
  </behavior>
  <action>
    Append the two functions described in `<new_shared_surface>` to `shared/arcgis_hub.py`. Copy the
    dual-path structure of `search_hub_datasets` verbatim in shape: build `url`, build `params`,
    branch on `httpx_client is not None`, `await client.get(url, params=params)`,
    `response.raise_for_status()`, then `decode_json(response, url)`. Add both names to the module
    docstring's public-function list. Do NOT modify `search_hub_datasets`, `query_feature_service`,
    `get_layer_metadata`, `get_count` or `shape_hub_dataset` — the extension is additive on the
    discovery side only (D-04, D-05, D-06).

    Write the two new test classes into the existing `shared/__tests__/test_arcgis_hub.py`, following
    the `TestSearchHubDatasets` structure already in that file. Both classes MUST assert the outgoing
    params dict read off `call_args`, not merely the URL — that is the regression the
    Manitoba/Saskatchewan phases learned to pin. Use `AsyncMock(return_value=...)`, never a
    `MagicMock` whose `json` is a lambda. Write the assertions first, confirm they fail against the
    unextended module, then implement.

    Then run the new enumerator against the live portal and write
    `.planning/phases/21-new-brunswick-government-open-data/21-SPIKE.md` with these sections:
      "## 1. GeoNB service directory (live)" — the count returned and the full name/type list.
      "## 2. Curated layer-id re-verification" — a table with columns Service, Layer id used, Layer
      name, Record count, Key fields, Verdict, produced by calling `get_arcgis_server_layers` then
      `get_layer_metadata` and `get_count` for each of: GeoNB_ENV_FloodHazardIndex,
      GeoNB_ENV_Historical_Floods, GeoNB_ENV_Wetlands, GeoNB_ELG_Contaminated_Sites,
      GeoNB_DNR_Crown_Land, GeoNB_DNR_MineralOccurrences, GeoNB_DNR_ProvincialParks,
      GeoNB_SNB_Parcels, GeoNB_DPS_Civic_Address, GeoNB_Health_Facilities,
      GeoNB_EECD_PublicSchools. Verdict is CONFIRMED when the id in 21-RESEARCH.md's Code Examples
      table matches live, or REVISED with the corrected id. Any REVISED id MUST also be corrected in
      `constants.py` during Task 4.
      "## 3. Retired-service check" — confirm GeoNB_DNR_WildlifeRefuges layer 0 is the retired
      placeholder and stays excluded.
      "## 4. Field-name capture" — the exact `fields` array names for each curated layer, since GeoNB
      publishes truncated shapefile-derived names (RESEARCH Pitfall 2).
    For anything unreachable from the sandbox, record the best-effort finding plus the exact
    re-runnable command and mark it UNRESOLVED so Task 4 does not hardcode a guess.
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/shared/__tests__/test_arcgis_hub.py -x -q 2>&1 | tail -20</automated>
    <automated>uv run pytest src/mcp_canada/shared/__tests__/ tests/test_upstream_error_classification.py -q 2>&1 | tail -10</automated>
    <automated>test -f .planning/phases/21-new-brunswick-government-open-data/21-SPIKE.md &amp;&amp; grep -q "GeoNB_DNR_Crown_Land" .planning/phases/21-new-brunswick-government-open-data/21-SPIKE.md &amp;&amp; grep -q "GeoNB_SNB_Parcels" .planning/phases/21-new-brunswick-government-open-data/21-SPIKE.md &amp;&amp; grep -qE "CONFIRMED|REVISED" .planning/phases/21-new-brunswick-government-open-data/21-SPIKE.md &amp;&amp; echo SPIKE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `shared/arcgis_hub.py` contains `def list_arcgis_server_services` and `def get_arcgis_server_layers`
    - `shared/__tests__/test_arcgis_hub.py` contains `class TestListArcgisServerServices` and `class TestGetArcgisServerLayers`
    - Both new test classes assert on `call_args` params (outgoing dict), verifiable by `grep -c "call_args" src/mcp_canada/shared/__tests__/test_arcgis_hub.py` increasing over its pre-task value
    - `grep -c "decode_json" src/mcp_canada/shared/arcgis_hub.py` increases by at least 2 over its pre-task value
    - `git diff src/mcp_canada/shared/arcgis_hub.py` shows no modification inside the bodies of `query_feature_service`, `get_layer_metadata`, `get_count` or `search_hub_datasets`
    - `uv run pytest src/mcp_canada/shared/__tests__/test_arcgis_hub.py -x` exits 0
    - `uv run pytest tests/test_upstream_error_classification.py -q` exits 0
    - `21-SPIKE.md` exists and its section 2 table has a Verdict cell for all 11 curated services
  </acceptance_criteria>
  <done>Two additive functions in shared/arcgis_hub.py, both decoding via decode_json and both pinned by outgoing-param contract tests. Existing functions untouched. 21-SPIKE.md records the live 62-service directory, a CONFIRMED/REVISED verdict per curated layer id, the retired-service exclusion, and the exact field-name arrays.</done>
</task>

<task type="auto">
  <name>Task 4: Expand the scaffold — full constants, schemas, locked client stubs, prompt/resource skeletons, test scaffolds</name>
  <files>
    src/mcp_canada/modules/new_brunswick/constants.py,
    src/mcp_canada/modules/new_brunswick/schemas.py,
    src/mcp_canada/modules/new_brunswick/client.py,
    src/mcp_canada/modules/new_brunswick/prompts.py,
    src/mcp_canada/modules/new_brunswick/resources.py,
    src/mcp_canada/modules/new_brunswick/__tests__/conftest.py,
    src/mcp_canada/modules/new_brunswick/__tests__/test_client.py,
    src/mcp_canada/modules/new_brunswick/__tests__/test_tools.py,
    src/mcp_canada/modules/new_brunswick/__tests__/test_prompts_resources.py
  </files>
  <read_first>
    src/mcp_canada/modules/new_brunswick/constants.py,
    src/mcp_canada/modules/new_brunswick/client.py,
    src/mcp_canada/modules/saskatchewan/constants.py,
    src/mcp_canada/modules/saskatchewan/client.py,
    src/mcp_canada/modules/saskatchewan/__tests__/conftest.py,
    src/mcp_canada/modules/manitoba/client.py,
    src/mcp_canada/modules/manitoba/constants.py,
    src/mcp_canada/modules/ckan/client.py,
    src/mcp_canada/modules/alberta/client.py,
    .planning/phases/21-new-brunswick-government-open-data/21-SPIKE.md,
    .planning/phases/21-new-brunswick-government-open-data/21-RESEARCH.md,
    .planning/phases/21-new-brunswick-government-open-data/COVERAGE.md
  </read_first>
  <action>
    Expand the tracer subset into the full contract surface. Every layer id below comes from
    21-SPIKE.md section 2 — if a verdict there is REVISED, use the revised id, not the value written
    here.

    `constants.py` — add to the tracer subset:
      Federal CKAN: CKAN_BASE_URL = "https://open.canada.ca/data/api/3" ;
        NB_ORG_FQ = "organization:nb" (non-overridable — never expose an organization parameter) ;
        RATE_GROUP_CKAN = "new_brunswick_ckan" ; RATE_LIMIT_CKAN = 5.0
      GeoNB per-service URLs built from GEONB_BASE_URL and these service names, each with its
      re-verified layer id constant:
        GeoNB_ENV_FloodHazardIndex layer 0 ; GeoNB_ENV_Historical_Floods layer 0 (and 8 for the 1973
        event) ; GeoNB_ENV_Wetlands layer 2 ; GeoNB_ELG_Contaminated_Sites layer 0 ;
        GeoNB_DNR_Crown_Land layer 3 ; GeoNB_DNR_MineralOccurrences layer 0 ;
        GeoNB_DNR_ProvincialParks layer 0 ; GeoNB_SNB_Parcels layer 0 ;
        GeoNB_DPS_Civic_Address layer 0 ; GeoNB_Health_Facilities dispatch dict
        HEALTH_FACILITY_LAYERS mapping the six facility types to layers 0-5 ;
        GeoNB_EECD_PublicSchools dispatch dict SCHOOL_SECTOR_LAYERS mapping anglophone to 0 and
        francophone to 1.
      GEONB_EXCLUDED_SERVICES — the 5 basemaps plus GeoNB_DNR_WildlifeRefuges, so
        nb_list_geonb_services can filter them out and explain why.
      FILTER_REQUIRED_TOOLS — the three large-layer tools that must reject an unfiltered call
        (Parcels 604,520 rows; Civic_Address 373,172; Wetlands 163,206) per threat T-21-03.
      511: FIVE11_BASE_URL = "https://511.gnb.ca/api/v2/get" ;
        FIVE11_KEY_ENV = "NEW_BRUNSWICK_511_KEY" ; RATE_GROUP_511 = "new_brunswick_511" ;
        RATE_LIMIT_511 = 2.0 ; USER_AGENT matching the manitoba constant.
      Cache TTLs: CACHE_TTL_LIVE = 300 (511) ; CACHE_TTL_SEARCH = 3600 (CKAN search) ;
        CACHE_TTL_META = 86400 (curated GeoNB layers, service directory) ;
        CACHE_TTL_ANNUAL = 604800 (parks, counties, districts).
      ALL_NB_TOOL_NAMES — the locked 22-name manifest, in the exact order of the
        "Artifacts this phase produces" section below, adjusted for the Task 2 checkpoint outcome.
      DEFAULT_PAGE_SIZE = 1000.
    Document at the top of the file: GeoNB is ArcGIS Server, not Hub (the Hub returns 401); layer ids
    are non-guessable and were resolved live; `data.gnb.ca`, `opendata.gnb.ca` and `nbopendata.ca` do
    not resolve; discovery is the federal CKAN filtered to the nb organization; and the Task 2
    checkpoint outcome for gnb.socrata.com.

    `schemas.py` — flat Pydantic v2 models, `from __future__ import annotations`, snake_case,
    `Field(default=None)` for optionals, nothing mirroring upstream nesting:
      NBDatasetSummary (id, name, title, description, notes, organization, subject: list[str],
        topic_category: list[str], keywords: list[str], num_resources, formats: list[str],
        metadata_modified, url)
      NBDatasetDetails (adds resources: list[dict], license_title, license_url, date_published,
        maintainer, frequency, spatial)
      NBOrganization (name, name_fr, dataset_count)
      NBCategory (name, name_fr, count)
      NBGeoNBService (name, type, department, curated_tool, excluded_reason)
      NBGeoNBLayer (id, name, geometry_type, record_count, fields: list[dict])
      NBFloodHazardArea, NBHistoricalFlood, NBWetland, NBContaminatedSite, NBCrownLandParcel,
      NBMineralOccurrence, NBProvincialPark, NBParcel, NBCivicAddress, NBHealthFacility,
      NBPublicSchool, NB511Event, NB511WinterRoad, NB511Camera
    Use the exact truncated GeoNB field names captured in 21-SPIKE.md section 4 as the source of the
    field list for each geospatial model — do not infer names from the layer display name
    (RESEARCH Pitfall 2).

    `client.py` — add to the tracer subset:
      1. `_ckan_limiter` and `_511_limiter` alongside the existing `_geonb_limiter` (three rate
         groups: federal CKAN, GeoNB, 511 — one per upstream surface, not one per GeoNB service).
      2. `_api_get(path, params)` for the federal CKAN, modeled on `alberta/client.py:_api_get`:
         calls `api_get` (which returns already-parsed JSON — do NOT call `.raise_for_status()` or
         `.json()` on its return), unwraps the CKAN envelope, and raises `UpstreamData` when the
         envelope reports failure or is not a dict.
      3. `_build_fq(extra_fq)` returning the NB organization filter alone when `extra_fq` is falsy,
         and the NB organization filter AND-ed with `extra_fq` otherwise. The NB clause is always
         first and is never caller-overridable (threat T-21-04).
      4. `_shape_dataset(raw, lang)` — copy the fallback chain from `ckan/client.py:_shape_dataset`
         verbatim: prefer the requested language from `title_translated`, then its English key, then
         the plain title; same three-step chain for `notes_translated` and the plain notes. Do NOT
         deduplicate NB's separately-published FR/EN record pairs (RESEARCH Pitfall 5).
      5. `class Five11NotConfigured(Exception)` and `_511_get(endpoint, params)` copied from
         `manitoba/client.py`, renamed for NB. The raised message states only that the env var named
         by FIVE11_KEY_ENV is unset and points to https://511.gnb.ca — it MUST NOT interpolate the
         value read from the environment (threat T-21-02).
      6. `_geonb_query(service_url, layer_id, where, out_fields, include_geometry, limit, ttl,
         cache_key)` — the shared curated helper: acquire `_geonb_limiter`, route through
         `cached_fetch`, delegate to `arcgis_hub.query_feature_service`, return
         `({"features", "count", "truncated"}, was_cached)`.
      7. Every remaining client function as a `raise NotImplementedError("Plan NN implements")` stub
         carrying its FULL LOCKED signature and returning `tuple[..., bool]`. SIGNATURES ARE LOCKED
         HERE — Plans 02-06 fill bodies only. Group and annotate by owning plan:
         - Plan 02 (federal CKAN): fetch_search_datasets(query="", extra_fq=None, limit=10,
           offset=0, lang="en"), fetch_dataset_details(dataset_id, lang="en"),
           fetch_query_dataset(dataset_id, resource_index=0, limit=DEFAULT_PAGE_SIZE, lang="en"),
           fetch_organizations(lang="en"), fetch_categories(lang="en")
         - Plan 04 (GeoNB discovery + flood/water): fetch_geonb_services(query="",
           include_excluded=False), fetch_geonb_service_layers(service_name),
           fetch_geonb_layer_features(service_name, layer_id, where=None, out_fields="*",
           limit=MAX_RECORDS, include_geometry=False), fetch_flood_hazard_areas(sheet=None,
           limit=MAX_RECORDS), fetch_historical_floods(event=None, limit=MAX_RECORDS),
           fetch_wetlands(wetland_class=None, status=None, limit=MAX_RECORDS),
           fetch_contaminated_sites(status=None, limit=MAX_RECORDS)
         - Plan 05 (Crown land/forestry + parcels/civic): fetch_mineral_occurrences(commodity=None,
           name=None, limit=MAX_RECORDS), fetch_provincial_parks(name=None, limit=MAX_RECORDS),
           fetch_parcels(pid=None, county=None, limit=MAX_RECORDS),
           fetch_civic_addresses(community=None, street=None, civic_number=None, limit=MAX_RECORDS)
         - Plan 06 (health/education + 511): fetch_health_facilities(facility_type, name=None,
           limit=MAX_RECORDS), fetch_public_schools(sector="anglophone", district=None,
           limit=MAX_RECORDS), fetch_road_events(), fetch_winter_road_conditions(),
           fetch_traffic_cameras()
         If the Task 2 checkpoint selected option-a, add fetch_gnb_socrata_search(query="", limit=10,
         offset=0) and fetch_gnb_socrata_query(dataset_id, where=None, select=None, limit=1000) as
         Plan 02 stubs and drop fetch_provincial_parks and fetch_mineral_occurrences.

    `prompts.py` — imports only, zero definitions: `from fastmcp.prompts import prompt` and
    `from fastmcp.prompts.prompt import Message`, plus a comment naming Plan 03 as the owner.
    `resources.py` — imports only, zero definitions: `import json` and
    `from fastmcp.resources import resource`, plus a comment naming Plan 03 as the owner.

    `__tests__/conftest.py` — extend the tracer fixture set with: a federal CKAN `package_search`
    envelope fixture carrying two NB packages including `title_translated` and `notes_translated`
    dicts and a resource whose url points at gnb.socrata.com; a bilingual-pair fixture reproducing
    the separately-published FR and EN childcare records (Pitfall 5); a GeoNB service-directory
    fixture with a services array and a folders array to prove folders are dropped; a MapServer
    layer fixture with layers and tables arrays; per-curated-layer GeoJSON feature fixtures using
    the exact field names from 21-SPIKE.md section 4; an empty FeatureCollection fixture (a valid
    success, never an error); and a fixture representing a 511 response used only when a key is set.

    `__tests__/test_client.py` — module docstring plus placeholder classes with empty bodies, one per
    client function above, plus `TestSharedApiGetContract` (Plan 02 fills it — patches the
    module-local `api_get` and asserts the OUTGOING params dict including the non-overridable NB
    organization clause) and `TestShapeDatasetBilingual` (Plan 02 fills it).
    `__tests__/test_tools.py` — keep `TestNbGetCrownLandTools` from Task 1; add placeholder classes
    for every remaining tool plus `TestNbEnvelopes` and `TestNbLangParam` (Plan 07 parametrizes both).
    `__tests__/test_prompts_resources.py` — `TestNbPrompts` and `TestNbResources` placeholders
    (Plan 03 fills them).

    Every file must import cleanly. Do not add a dependency. Do not edit `server.py`.
  </action>
  <verify>
    <automated>uv run python -c "
from mcp_canada.modules.new_brunswick import MODULE_NAME, MODULE_DESCRIPTION
from mcp_canada.modules.new_brunswick import constants as c, schemas, client, tools, prompts, resources
assert MODULE_NAME == 'new_brunswick'
assert c.CROWN_LAND_LAYER == 3
assert c.NB_ORG_FQ == 'organization:nb'
assert c.FIVE11_KEY_ENV == 'NEW_BRUNSWICK_511_KEY'
assert len(c.ALL_NB_TOOL_NAMES) == 22, c.ALL_NB_TOOL_NAMES
assert isinstance(c.HEALTH_FACILITY_LAYERS, dict) and isinstance(c.SCHOOL_SECTOR_LAYERS, dict)
assert c.GEONB_EXCLUDED_SERVICES and c.FILTER_REQUIRED_TOOLS
for fn in ('_api_get','_build_fq','_shape_dataset','_geonb_query','_511_get','fetch_crown_land','fetch_search_datasets','fetch_geonb_services','fetch_parcels','fetch_health_facilities','fetch_road_events'):
    assert hasattr(client, fn), fn
assert hasattr(client, 'Five11NotConfigured')
for m in ('NBDatasetSummary','NBCrownLandParcel','NBCivicAddress','NBHealthFacility','NBGeoNBService'):
    assert hasattr(schemas, m), m
print('SCAFFOLD OK')
"</automated>
    <automated>uv run pytest src/mcp_canada/modules/new_brunswick/__tests__/ --collect-only -q 2>&1 | tail -12</automated>
    <automated>uv run pyright src/mcp_canada/modules/new_brunswick/ src/mcp_canada/shared/arcgis_hub.py 2>&1 | tail -8</automated>
    <automated>uv run ruff check src/mcp_canada/modules/new_brunswick/ src/mcp_canada/shared/arcgis_hub.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "..."` scaffold check above prints `SCAFFOLD OK`
    - `constants.py` contains `FIVE11_KEY_ENV` and `NB_ORG_FQ`, and `ALL_NB_TOOL_NAMES` has exactly 22 entries
    - `client.py` contains `class Five11NotConfigured` and `def _build_fq`
    - `grep -c "NotImplementedError" src/mcp_canada/modules/new_brunswick/client.py` is at least 16
    - `grep -rn "raise ValueError" src/mcp_canada/modules/new_brunswick/` returns no lines
    - `grep -rn "@mcp\." src/mcp_canada/modules/new_brunswick/` returns no lines
    - `grep -rn "response.json()\|json.loads" src/mcp_canada/modules/new_brunswick/ src/mcp_canada/shared/arcgis_hub.py` returns no lines outside `shared/http.py`
    - `uv run pytest src/mcp_canada/modules/new_brunswick/__tests__/ --collect-only` exits 0
    - `uv run pyright src/mcp_canada/modules/new_brunswick/` reports 0 errors
    - `uv run ruff check src/mcp_canada/modules/new_brunswick/` exits 0
    - `git diff --stat src/mcp_canada/server.py` is empty
  </acceptance_criteria>
  <done>Full contract surface locked: every constant (including re-verified layer ids and the 22-name manifest), every flat schema model, three limiters, five fully-implemented private helpers, and every downstream client function declared as a stub with a locked signature. prompts.py and resources.py import cleanly with zero definitions. Test scaffolds collect. pyright and ruff clean. server.py untouched.</done>
</task>

</tasks>

## Artifacts this phase produces

New symbols and files created by Phase 21 (the plan-review source-grounding pass should treat these
as newly-created, not as drift against existing source):

**New shared functions** (`src/mcp_canada/shared/arcgis_hub.py`):
`list_arcgis_server_services`, `get_arcgis_server_layers`

**New module directory:** `src/mcp_canada/modules/new_brunswick/` with `__init__.py`,
`constants.py`, `schemas.py`, `client.py`, `tools.py`, `prompts.py`, `resources.py`, and
`__tests__/{__init__,conftest,test_client,test_tools,test_prompts_resources}.py`

**New module-level exports:** `MODULE_NAME`, `MODULE_DESCRIPTION`, `MODULE_DESCRIPTION_FR`

**New constants** (`constants.py`): `GEONB_BASE_URL`, `CKAN_BASE_URL`, `NB_ORG_FQ`,
`CROWN_LAND_SERVICE`, `CROWN_LAND_LAYER`, `CROWN_LAND_FIELDS`, `FLOOD_HAZARD_SERVICE`,
`FLOOD_HAZARD_LAYER`, `HISTORICAL_FLOODS_SERVICE`, `HISTORICAL_FLOODS_LAYER`,
`HISTORICAL_FLOODS_1973_LAYER`, `WETLANDS_SERVICE`, `WETLANDS_LAYER`,
`CONTAMINATED_SITES_SERVICE`, `CONTAMINATED_SITES_LAYER`, `MINERAL_OCCURRENCES_SERVICE`,
`MINERAL_OCCURRENCES_LAYER`, `PROVINCIAL_PARKS_SERVICE`, `PROVINCIAL_PARKS_LAYER`,
`PARCELS_SERVICE`, `PARCELS_LAYER`, `CIVIC_ADDRESS_SERVICE`, `CIVIC_ADDRESS_LAYER`,
`HEALTH_FACILITIES_SERVICE`, `HEALTH_FACILITY_LAYERS`, `PUBLIC_SCHOOLS_SERVICE`,
`SCHOOL_SECTOR_LAYERS`, `GEONB_EXCLUDED_SERVICES`, `FILTER_REQUIRED_TOOLS`, `FIVE11_BASE_URL`,
`FIVE11_KEY_ENV`, `USER_AGENT`, `RATE_GROUP_CKAN`, `RATE_LIMIT_CKAN`, `RATE_GROUP_GEONB`,
`RATE_LIMIT_GEONB`, `RATE_GROUP_511`, `RATE_LIMIT_511`, `CACHE_KEY_PREFIX`, `CACHE_TTL_LIVE`,
`CACHE_TTL_SEARCH`, `CACHE_TTL_META`, `CACHE_TTL_ANNUAL`, `DEFAULT_PAGE_SIZE`, `MAX_RECORDS`,
`ALL_NB_TOOL_NAMES`

**New schema classes** (`schemas.py`): `NBDatasetSummary`, `NBDatasetDetails`, `NBOrganization`,
`NBCategory`, `NBGeoNBService`, `NBGeoNBLayer`, `NBFloodHazardArea`, `NBHistoricalFlood`,
`NBWetland`, `NBContaminatedSite`, `NBCrownLandParcel`, `NBMineralOccurrence`, `NBProvincialPark`,
`NBParcel`, `NBCivicAddress`, `NBHealthFacility`, `NBPublicSchool`, `NB511Event`,
`NB511WinterRoad`, `NB511Camera`

**New client symbols** (`client.py`): `Five11NotConfigured`, `_api_get`, `_build_fq`,
`_shape_dataset`, `_geonb_query`, `_511_get`, `_ckan_limiter`, `_geonb_limiter`, `_511_limiter`,
and `fetch_search_datasets`, `fetch_dataset_details`, `fetch_query_dataset`, `fetch_organizations`,
`fetch_categories`, `fetch_geonb_services`, `fetch_geonb_service_layers`,
`fetch_geonb_layer_features`, `fetch_flood_hazard_areas`, `fetch_historical_floods`,
`fetch_wetlands`, `fetch_contaminated_sites`, `fetch_crown_land`, `fetch_mineral_occurrences`,
`fetch_provincial_parks`, `fetch_parcels`, `fetch_civic_addresses`, `fetch_health_facilities`,
`fetch_public_schools`, `fetch_road_events`, `fetch_winter_road_conditions`,
`fetch_traffic_cameras` (plus `fetch_gnb_socrata_search` / `fetch_gnb_socrata_query` only if the
Task 2 checkpoint selects option-a)

**New tools** (`tools.py`, 22): `nb_search_datasets`, `nb_get_dataset_details`, `nb_query_dataset`,
`nb_list_organizations`, `nb_list_categories`, `nb_list_geonb_services`,
`nb_get_geonb_service_layers`, `nb_query_geonb_layer`, `nb_get_flood_hazard_areas`,
`nb_get_historical_floods`, `nb_get_wetlands`, `nb_get_contaminated_sites`, `nb_get_crown_land`,
`nb_get_mineral_occurrences`, `nb_get_provincial_parks`, `nb_get_parcels`,
`nb_get_civic_addresses`, `nb_get_health_facilities`, `nb_get_public_schools`,
`nb_get_road_events`, `nb_get_winter_road_conditions`, `nb_get_traffic_cameras`; plus
`_API_NAME_CKAN`, `_API_NAME_GEONB`, `_API_NAME_511`, `_NOT_CONFIGURED_MSG_EN`,
`_NOT_CONFIGURED_MSG_FR`, `ALL_NB_TOOLS`

**New prompts** (`prompts.py`, 6): `nb_flood_risk_assessment`, `nb_crown_land_report`,
`nb_property_lookup`, `nb_quick_dataset_search`, `nb_health_facility_finder`,
`nb_bilingual_dataset_lookup`

**New resources** (`resources.py`, 7): `data://nb/geonb-services`, `data://nb/counties`,
`data://nb/health-regions`, `data://nb/school-districts`, `docs://nb/portal-guide`,
`docs://nb/geonb-query-guide`, `template://nb/flood-risk-report`

**New test classes** (`shared/__tests__/test_arcgis_hub.py`): `TestListArcgisServerServices`,
`TestGetArcgisServerLayers`

**New integration test class** (`tests/integration/test_tool_scenarios.py`):
`TestNewBrunswickToolScenarios`

**New planning artifacts:** `21-SPIKE.md`, `COVERAGE.md`

**New requirement IDs** (backfilled into `.planning/REQUIREMENTS.md` by Plan 07): `NB-01`..`NB-25`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → MCP tool | Untrusted tool arguments (`where`, `q`, `holder`, `county`, `dataset_id`) cross here |
| MCP tool → GeoNB ArcGIS Server | A SQL-92 `where` clause crosses to `geonb.snb.ca` |
| MCP tool → federal CKAN Solr | `q` and `fq` fragments cross to `open.canada.ca` |
| MCP tool → NB 511 | An API credential crosses outbound to `511.gnb.ca` |
| process environment → module | `NEW_BRUNSWICK_511_KEY` is read at call time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Tampering | `where=` clauses sent to GeoNB `/query` by curated tools | high | mitigate | Curated tools NEVER accept a raw WHERE string. `client.py` builds every `where` server-side from typed, validated parameters, single-quote-escaping any string value before interpolation. The one raw-query escape hatch (`nb_query_geonb_layer`) documents ArcGIS's own SQL-92 parser as the trust boundary, identical to every prior ArcGIS-backed province. |
| T-21-02 | Information Disclosure | `Five11NotConfigured` message and 511 error envelopes | medium | mitigate | The exception message states only that the env var named by `FIVE11_KEY_ENV` is unset and links `https://511.gnb.ca`; it never interpolates the value read from the environment. A unit test asserts a sentinel key value set in the environment does not appear in the returned envelope. |
| T-21-03 | Denial of Service | Unbounded GeoNB queries over Parcels (604,520), Civic_Address (373,172), Wetlands (163,206) | high | mitigate | `FILTER_REQUIRED_TOOLS` — `nb_get_parcels`, `nb_get_civic_addresses` and `nb_get_wetlands` raise `InvalidInput` (→ `INVALID_INPUT`) **before any network call** when no filter argument is supplied, following BC's `bc_get_water_wells` 130K-record guard. `query_feature_service`'s existing `MAX_RECORDS=5000` cap plus the `truncated` flag bound every other path. |
| T-21-04 | Tampering | `fq` composition on federal CKAN discovery tools | high | mitigate | `_build_fq` always emits the NB organization clause first and AND-s any additional fragment onto it; no `organization` parameter is exposed to callers, so a discovery tool cannot be steered off NB (RESEARCH Anti-Pattern). `TestSharedApiGetContract` pins the outgoing `fq` value. |
| T-21-05 | Information Disclosure | Cache keys shared across modules in the process-wide aiocache | low | accept | All NB data is public open data and every key is prefixed `new_brunswick:`; a collision would be a correctness bug, not a disclosure. Prefix asserted in unit tests. |
| T-21-SC | Tampering | npm/pip/cargo installs | high | accept | This phase introduces **zero** new external packages — 21-RESEARCH.md's Package Legitimacy Audit records "Not applicable", and `.claude/rules/engineering-standards.md` forbids new dependencies. No `[ASSUMED]`/`[SUS]`/`[SLOP]` package exists to gate. Acceptance is void the moment a plan adds a dependency; `git diff pyproject.toml uv.lock` must stay empty for this phase (asserted in Plan 07). |
</threat_model>

<verification>
1. `uv run pytest src/mcp_canada/shared/__tests__/test_arcgis_hub.py -x` — two new classes green, existing classes untouched and green
2. `uv run pytest src/mcp_canada/modules/new_brunswick/__tests__/ -x` — tracer tests green, scaffolds collect
3. The live tracer command in Task 1 prints `TRACER LIVE OK` with a non-zero row count
4. `uv run pytest tests/test_tool_error_handling.py tests/test_error_classification_defaults.py tests/test_upstream_error_classification.py -q` — project-wide structural guards green with the new tool present
5. `uv run pyright src/mcp_canada/modules/new_brunswick/ src/mcp_canada/shared/arcgis_hub.py` — 0 errors
6. `uv run ruff check src/ tests/` — 0 errors
7. `git diff --stat src/mcp_canada/server.py pyproject.toml uv.lock` — empty
8. `21-SPIKE.md` carries the live 62-service directory and a CONFIRMED/REVISED verdict for all 11 curated layer ids
</verification>

<success_criteria>
- TRACER: `nb_get_crown_land` returns live GeoNB rows in a `_meta` envelope, using layer 3, through constants → client → `@tool` → FileSystemProvider
- The Task 2 checkpoint is resolved and the chosen option is recorded in `constants.py`'s docstring and in the plan summary
- `shared/arcgis_hub.py` has exactly two additive functions, both decoding via `decode_json`, both pinned by outgoing-param contract tests; the five existing functions are byte-identical apart from the docstring's public-function list
- `21-SPIKE.md` re-verifies every curated layer id live; any REVISED id is reflected in `constants.py`
- The full scaffold exists: 22-name locked manifest, ~20 flat schema models, three limiters, five implemented private helpers, ≥16 locked client stubs
- `pytest --collect-only` succeeds on both test directories; pyright and ruff clean
- No new dependency; `server.py` untouched; no bare `raise ValueError`; no `@mcp.` decorator; no raw JSON decode
</success_criteria>

<output>
Create `.planning/phases/21-new-brunswick-government-open-data/21-01-SUMMARY.md` documenting: the
tracer's live result (row count, field names, layer id), the Task 2 checkpoint decision and its
consequences for the manifest, the two new shared function signatures and their pinned outgoing
params, every CONFIRMED/REVISED layer-id verdict from 21-SPIKE.md, the final `ALL_NB_TOOL_NAMES`
list, the locked client signatures Plans 02-06 must fill, and any deviation from the
saskatchewan/manitoba scaffold patterns.
</output>
