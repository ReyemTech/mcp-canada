---
phase: 09-sdmx-composite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_canada/modules/statcan/constants.py
  - src/mcp_canada/modules/statcan/schemas.py
  - src/mcp_canada/modules/statcan/client.py
  - src/mcp_canada/modules/statcan/__tests__/conftest.py
  - src/mcp_canada/modules/statcan/__tests__/test_client.py
autonomous: true
requirements: [SC-10, SC-11, SC-12]

must_haves:
  truths:
    - "get_sdmx_structure returns parsed dimension codelists for a productId"
    - "get_sdmx_data returns flattened observation rows with resolved dimension names"
    - "get_sdmx_data raises ValueError when both lastN and date range are provided"
    - "get_sdmx_vector_data returns flattened observations for a single vector by date range"
    - "_build_sdmx_key translates a named dimension dict into dot-separated SDMX key syntax"
  artifacts:
    - path: "src/mcp_canada/modules/statcan/constants.py"
      provides: "SDMX_BASE_URL, SDMX_XML_NAMESPACES, _SDMX_API_NAME constants"
      contains: "SDMX_BASE_URL"
    - path: "src/mcp_canada/modules/statcan/schemas.py"
      provides: "SDMXDimension, SDMXCodeValue, SDMXStructure, SDMXObservationRow models"
      contains: "class SDMXStructure"
    - path: "src/mcp_canada/modules/statcan/client.py"
      provides: "get_sdmx_structure, get_sdmx_data, get_sdmx_vector_data, _parse_structure_xml, _flatten_sdmx_json, _build_sdmx_key, _make_suggested_key"
      contains: "async def get_sdmx_structure"
    - path: "src/mcp_canada/modules/statcan/__tests__/conftest.py"
      provides: "SDMX XML fixture, SDMX-JSON data fixture, SDMX vector JSON fixture"
      contains: "SDMX_STRUCTURE_XML"
    - path: "src/mcp_canada/modules/statcan/__tests__/test_client.py"
      provides: "Unit tests for all SDMX client functions"
      contains: "test_sdmx"
  key_links:
    - from: "src/mcp_canada/modules/statcan/client.py"
      to: "src/mcp_canada/modules/statcan/constants.py"
      via: "imports SDMX_BASE_URL, SDMX_XML_NAMESPACES"
      pattern: "from.*constants import.*SDMX_BASE_URL"
    - from: "src/mcp_canada/modules/statcan/client.py"
      to: "src/mcp_canada/modules/statcan/schemas.py"
      via: "imports SDMXStructure, SDMXObservationRow"
      pattern: "from.*schemas import.*SDMXStructure"
---

<objective>
SDMX client layer: constants, schemas, and three async client functions for StatCan SDMX REST API — structure (XML), data (JSON), and vector (JSON) queries — plus comprehensive unit tests.

Purpose: SC-10, SC-11, SC-12 client implementations that Plan 02 tools will wrap.
Output: Three new client functions, SDMX schemas, constants, and unit tests covering all paths.
</objective>

<execution_context>
@/Users/mariomeyer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomeyer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-sdmx-composite/09-RESEARCH.md

<interfaces>
<!-- Existing interfaces the executor needs. -->

From src/mcp_canada/modules/statcan/constants.py:
```python
STATCAN_VERIFY: bool = True
BASE_URL = "https://www150.statcan.gc.ca/t1/wds/rest/"
RATE_GROUP = "statcan"
RATE_LIMIT = 20.0
TIMEOUT_LARGE = 90.0
CACHE_TTL_CUBES = 3600
CACHE_TTL_META = 86400
CACHE_TTL_CODESETS = 604800
CACHE_TTL_OBS = 3600
_API_NAME = "statcan-wds"
```

From src/mcp_canada/modules/statcan/client.py:
```python
def _make_statcan_client(timeout: float = 30.0) -> httpx.AsyncClient
async def _statcan_fetch(method: str, url: str, *, json: Any = None, timeout: float = 30.0) -> Any
# _statcan_retry decorator (tenacity) retries on TimeoutException, ConnectError, ValueError
# _limiter_acquire() is module-level for patchability
```

From src/mcp_canada/modules/statcan/schemas.py:
```python
class ObservationRow(BaseModel):
    ref_per: str; ref_per_raw: str; value: float | None; decimals: int
    scalar_factor_code: int; scalar_factor: str; frequency_code: int; frequency: str
    status_code: int; symbol_code: int; release_time: str
```

From src/mcp_canada/shared/cache.py:
```python
async def cached_fetch(key: str, ttl: int, fetcher: Callable) -> tuple[Any, bool]
```

From src/mcp_canada/shared/rate_limiter.py:
```python
def get_limiter(source: str, rate: float) -> TokenBucket
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SDMX constants, schemas, and test fixtures</name>
  <files>
    src/mcp_canada/modules/statcan/constants.py,
    src/mcp_canada/modules/statcan/schemas.py,
    src/mcp_canada/modules/statcan/__tests__/conftest.py
  </files>
  <behavior>
    - SDMXCodeValue has fields: id (str), name_en (str), name_fr (str)
    - SDMXDimension has fields: position (int), id (str), codelist_id (str), codes (list[SDMXCodeValue])
    - SDMXStructure has fields: product_id (int), dimensions (list[SDMXDimension]), suggested_key (str)
    - SDMXObservationRow has fields: period (str), value (float | None), dimensions (dict[str, str])
    - Constants: SDMX_BASE_URL ends with "/", _SDMX_API_NAME is "statcan-sdmx", SDMX_XML_NAMESPACES has "mes", "str", "com" keys
  </behavior>
  <action>
    1. Add to constants.py (append, do not modify existing):
       - `SDMX_BASE_URL = "https://www150.statcan.gc.ca/t1/wds/sdmx/statcan/rest/"`
       - `_SDMX_API_NAME = "statcan-sdmx"`
       - `SDMX_XML_NAMESPACES` dict with mes, str, com namespace URIs (see RESEARCH.md Pattern 1)

    2. Add to schemas.py (append after CodeSets):
       - `SDMXCodeValue(BaseModel)` — id: str, name_en: str, name_fr: str
       - `SDMXDimension(BaseModel)` — position: int, id: str, codelist_id: str, codes: list[SDMXCodeValue]
       - `SDMXStructure(BaseModel)` — product_id: int, dimensions: list[SDMXDimension], suggested_key: str = ""
       - `SDMXObservationRow(BaseModel)` — period: str, value: float | None, dimensions: dict[str, str]

    3. Add SDMX fixtures to conftest.py (append, do not modify existing fixtures):
       - `SDMX_STRUCTURE_XML`: Valid SDMX 2.1 XML string with 2 dimensions, 2+ codes each, proper namespace declarations. Use CPI table 18100004 as the model (Geography + Products dimensions). Include both en and fr Name elements.
       - `SDMX_DATA_JSON`: Valid SDMX-JSON compact format dict with 1 dataset, 2 series, 3 observations each. Structure includes series dimensions with values and observation TIME_PERIOD dimension. Series keys use ":" delimiter per SDMX-JSON spec (try ":" first, fall back to ".").
       - `SDMX_VECTOR_JSON`: Same SDMX-JSON shape as data but for a single vector endpoint response.

    Write unit tests for schemas (construction, field types) in test_client.py FIRST (RED), then create the schemas (GREEN).
  </action>
  <verify>
    <automated>cd /Users/mariomeyer/code/ReyemTech/mcp-canada && uv run pytest src/mcp_canada/modules/statcan/__tests__/test_client.py -x -v -k "sdmx_schema or sdmx_constant"</automated>
  </verify>
  <done>SDMX constants in constants.py, 4 SDMX schema models in schemas.py, 3 SDMX fixtures in conftest.py, schema unit tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SDMX client functions with unit tests</name>
  <files>
    src/mcp_canada/modules/statcan/client.py,
    src/mcp_canada/modules/statcan/__tests__/test_client.py
  </files>
  <behavior>
    - _parse_structure_xml(xml_text, product_id) returns SDMXStructure with sorted dimensions and populated codes
    - _make_suggested_key(structure) returns dot-joined first code of each dimension
    - _build_sdmx_key(dim_dict, structure) translates named dict to SDMX key; "all" maps to wildcard; list values joined with "+"
    - _build_sdmx_key with unknown dimension name silently wildcards that position
    - _flatten_sdmx_json(payload) returns list of SDMXObservationRow with resolved dimension names and period values
    - _flatten_sdmx_json handles both ":" and "." delimited series keys
    - get_sdmx_structure(product_id) fetches XML, parses, caches with "statcan_sdmx:structure:{pid}" key and CACHE_TTL_META
    - get_sdmx_data(product_id, key, start_period, end_period, last_n) raises ValueError when last_n and date range both provided
    - get_sdmx_data returns flattened rows from SDMX-JSON
    - get_sdmx_vector_data(vector_id, start_period, end_period) fetches /vector/v{id} with date range params
    - All three functions use _limiter_acquire() before HTTP calls
    - All three use _make_statcan_client() for HTTP
  </behavior>
  <action>
    Add to client.py (append after existing functions, do not modify existing code):

    1. **Private helpers:**
       - `_parse_structure_xml(xml_text: str, product_id: int) -> SDMXStructure` — parse SDMX 2.1 XML using `xml.etree.ElementTree`. Extract codelists, map to dimensions, sort by position. Call `_make_suggested_key` to populate `suggested_key`. Pass `SDMX_XML_NAMESPACES` to every `findall`/`find` call (Pitfall 3). Import ET at module level: `import xml.etree.ElementTree as ET`.
       - `_make_suggested_key(structure: SDMXStructure) -> str` — dot-join first code.id of each dimension (sorted by position).
       - `_build_sdmx_key(dim_dict: dict[str, str | list[str]], structure: SDMXStructure) -> str` — case-insensitive dim name matching against dimension.id. "all", empty string, empty list = wildcard (empty position). List values joined with "+". See RESEARCH.md Pattern 4.
       - `_flatten_sdmx_json(payload: dict) -> list[SDMXObservationRow]` — navigate SDMX-JSON compact format. Series keys: try splitting on ":" first; if that yields only 1 part and "." is present, split on ".". Resolve dimension indices against structure values arrays. See RESEARCH.md Pattern 3.

    2. **Public functions (all return (data, was_cached) tuples):**
       - `get_sdmx_structure(product_id: int) -> tuple[SDMXStructure, bool]` — use `cached_fetch` with key `f"statcan_sdmx:structure:{product_id}"` and TTL `CACHE_TTL_META`. Fetcher: `_limiter_acquire()`, then GET `SDMX_BASE_URL + f"structure/Data_Structure_{product_id}"` using `_make_statcan_client()`. No special Accept header (XML default). Parse with `_parse_structure_xml`.
       - `get_sdmx_data(product_id: int, key: str, *, start_period: str | None = None, end_period: str | None = None, last_n: int | None = None) -> tuple[list[SDMXObservationRow], bool]` — enforce mutual exclusion: raise `ValueError("Cannot use both lastN and date range (startPeriod/endPeriod) simultaneously")` if `last_n is not None and (start_period or end_period)`. Build params dict. GET `SDMX_BASE_URL + f"data/DF_{product_id}/{key}"` with `Accept: application/json` header. Use `_limiter_acquire()`. NOT cached (observation data). Flatten with `_flatten_sdmx_json`.
       - `get_sdmx_vector_data(vector_id: int, *, start_period: str | None = None, end_period: str | None = None) -> tuple[list[SDMXObservationRow], bool]` — GET `SDMX_BASE_URL + f"vector/v{vector_id}"` with params and `Accept: application/json`. Use `_limiter_acquire()`. NOT cached. Flatten with `_flatten_sdmx_json`.

    3. Import new constants and schemas at the top of client.py (add to existing import blocks).

    Write ALL unit tests FIRST (RED phase) in test_client.py using fixtures from conftest.py, mocking `_make_statcan_client` and `_limiter_acquire`. Then implement (GREEN). Tests must cover:
    - `_parse_structure_xml` happy path (returns correct dimensions, codes, suggested_key)
    - `_build_sdmx_key` with full dict, partial dict, "all" values, list values, unknown dim name
    - `_flatten_sdmx_json` happy path (correct row count, resolved names, periods)
    - `_flatten_sdmx_json` with ":" vs "." delimiters
    - `get_sdmx_structure` caching behavior
    - `get_sdmx_data` mutual exclusion (ValueError)
    - `get_sdmx_data` happy path with lastN
    - `get_sdmx_data` happy path with date range
    - `get_sdmx_vector_data` happy path
  </action>
  <verify>
    <automated>cd /Users/mariomeyer/code/ReyemTech/mcp-canada && uv run pytest src/mcp_canada/modules/statcan/__tests__/test_client.py -x -v -k "sdmx"</automated>
  </verify>
  <done>Three SDMX client functions implemented and tested. All sdmx tests in test_client.py pass. Mutual exclusion enforced. Structure cached with statcan_sdmx: prefix. XML parsed with correct namespaces. SDMX-JSON flattened with resolved dimension names.</done>
</task>

</tasks>

<verification>
- `uv run pytest src/mcp_canada/modules/statcan/__tests__/test_client.py -x -v -k "sdmx"` — all SDMX client tests pass
- `uv run pytest src/mcp_canada/modules/statcan/__tests__/ -x` — existing WDS tests still pass (no regressions)
- `uv run ruff check src/mcp_canada/modules/statcan/` — no lint errors
- `uv run pyright src/mcp_canada/modules/statcan/` — no type errors
</verification>

<success_criteria>
- get_sdmx_structure parses XML and returns SDMXStructure with correct dimensions and suggested_key
- get_sdmx_data enforces mutual exclusion and returns flattened SDMXObservationRow list
- get_sdmx_vector_data fetches by vectorId and returns flattened rows
- _build_sdmx_key translates named dimension dicts correctly
- All SDMX cache keys prefixed with "statcan_sdmx:" (no collision with WDS)
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-sdmx-composite/09-01-SUMMARY.md`
</output>
