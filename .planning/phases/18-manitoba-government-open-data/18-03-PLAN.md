---
phase: 18-manitoba-government-open-data
plan: 03
type: execute
wave: 2
depends_on: [18-02]
files_modified:
  - src/mcp_canada/modules/manitoba/client.py
  - src/mcp_canada/modules/manitoba/tools.py
  - src/mcp_canada/modules/manitoba/__tests__/test_client.py
  - src/mcp_canada/modules/manitoba/__tests__/test_tools.py
autonomous: true
requirements:
  - MB-07
  - MB-08
  - MB-09
must_haves:
  truths:
    - "manitoba_get_flood_alerts returns overland flood watch/warning polygons (bilingual Type_EN/Type_FR) from Overland_Flood_Alerts FeatureServer — and returns an empty features list (NOT an error) when no alerts are active"
    - "manitoba_get_river_stations returns hydrometric station points with flood watch/warning status (station discovery, NOT real-time level readings — Pitfall 5)"
    - "manitoba_get_provincial_waterways returns typed water-control features (dike/floodway/dam/diversion/reservoir) from Provincial_Waterways FeatureServer, dispatched by optional f_type filter"
    - "All three use arcgis_hub.query_feature_service wrapped in cached_fetch, returning {features, count, truncated} payloads"
  artifacts:
    - path: src/mcp_canada/modules/manitoba/tools.py
      provides: "manitoba_get_flood_alerts, manitoba_get_river_stations, manitoba_get_provincial_waterways"
      contains: "def manitoba_get_flood_alerts"
    - path: src/mcp_canada/modules/manitoba/client.py
      provides: "fetch_flood_alerts, fetch_river_stations, fetch_provincial_waterways bodies"
  key_links:
    - from: "src/mcp_canada/modules/manitoba/client.py"
      to: "Overland_Flood_Alerts / Provincial_Waterways FeatureServers"
      via: "arcgis_hub.query_feature_service(FLOOD_ALERTS_FS_URL / PROVINCIAL_WATERWAYS_FS_URL, 0, ...)"
      pattern: "query_feature_service\\("
---

<objective>
Implement Manitoba's flood/hydrology domain (MB-07, MB-08, MB-09) — the province's defining hazard. Flood alerts are live bilingual polygons; river stations are discovery points with flood status; waterways are the static water-control infrastructure layer.

Purpose: Curate the seasonal/high-value flood queries Manitoba is known for.
Output: 3 flood/hydrology tools + client bodies + unit tests (incl. the empty-flood-alert edge case).
</objective>

<execution_context>
@/Users/mariomeyer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomeyer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-manitoba-government-open-data/18-RESEARCH.md
@.planning/phases/18-manitoba-government-open-data/18-SPIKE.md
@.planning/phases/18-manitoba-government-open-data/18-01-SUMMARY.md
@.planning/phases/18-manitoba-government-open-data/18-02-SUMMARY.md
@CLAUDE.md
@.claude/rules/modules.md

# Reference: FeatureServer client/tool pattern (truncated payload shape)
@src/mcp_canada/modules/alberta/client.py
@src/mcp_canada/modules/alberta/tools.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flood/hydrology client bodies + tests</name>
  <files>
    src/mcp_canada/modules/manitoba/client.py,
    src/mcp_canada/modules/manitoba/__tests__/test_client.py
  </files>
  <behavior>
    - fetch_flood_alerts(max_records, include_geometry=True): query FLOOD_ALERTS_FS_URL layer 0, out_fields Type_EN,Type_FR,Start_Date,End_Date; include_geometry default True (polygons need geometry for map use). Returns ({"features":[...], "count":N, "truncated":bool}, was_cached) at CACHE_TTL_LIVE. CRITICAL: empty features list ([], False) is a valid result, NOT an error.
    - fetch_river_stations(max_records, include_geometry=False): query RIVER_CONDITIONS_FS_URL (from spike); returns station points with flood-status field. If URL UNRESOLVED in spike, degrade gracefully: return empty payload with a documented note, OR route via discovery — do not crash.
    - fetch_provincial_waterways(f_type=None, max_records, include_geometry=False): query PROVINCIAL_WATERWAYS_FS_URL; where=f"F_TYPE='{mapped}'" when f_type given (validate against WATERWAY_TYPES). Returns {features,count,truncated}.
    - Test the empty-flood-alert path explicitly: patch query_feature_service → ([], False); assert payload features == [] and was_cached returned, no exception.
  </behavior>
  <action>
    Fill fetch_flood_alerts, fetch_river_stations, fetch_provincial_waterways. Use get_limiter(RATE_GROUP_HUB, RATE_LIMIT_HUB).acquire() before each query (or a module-level limiter as alberta does). Mirror alberta.client.fetch_active_fires payload shape exactly. For waterways f_type validation, raise ValueError on invalid type (tool layer maps to INVALID_INPUT with valid= list). Fill TestManitobaGetFloodAlerts (incl. empty-response test), TestManitobaGetRiverStations, TestManitobaGetWaterways. Patch arcgis_hub.query_feature_service with AsyncMock returning ([...], False).
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/manitoba/__tests__/test_client.py -x -k "FloodAlerts or RiverStations or Waterways" -v</automated>
  </verify>
  <done>3 flood/hydrology client functions implemented. Empty-flood-alert returns valid empty payload (not error). Invalid f_type raises ValueError. River-stations degrades gracefully if FS URL unresolved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 3 flood/hydrology @tool functions + tool tests</name>
  <files>
    src/mcp_canada/modules/manitoba/tools.py,
    src/mcp_canada/modules/manitoba/__tests__/test_tools.py
  </files>
  <behavior>
    - manitoba_get_flood_alerts(include_geometry=True, lang="en"): make_response payload {features,count,truncated}; empty list when no active alerts.
    - manitoba_get_river_stations(lang="en"): station points + flood status.
    - manitoba_get_provincial_waterways(f_type=None, max_records=5000, include_geometry=False, lang="en"): invalid f_type → make_error("INVALID_INPUT", valid=list(WATERWAY_TYPES)).
  </behavior>
  <action>
    Add the 3 @tool functions following conventions (standalone @tool, lang, make_response/make_error, manitoba_ prefix, single-line Use for: + 8+ Keywords:). Flood-alerts docstring must state: bilingual Type_EN/Type_FR; returns empty when no alerts active (normal, not an error); flood bulletins (HFC PDFs) are NOT available here. River-stations docstring: station LOCATIONS + status only, not real-time level readings (HYDAT is at wateroffice.ec.gc.ca). Fill TestManitobaGetFloodAlertsTool (incl. empty edge case via MCP-layer-style mock), waterways INVALID_INPUT test, river-stations test.
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/manitoba/__tests__/test_tools.py -x -k "FloodAlerts or RiverStations or Waterways" -v</automated>
  </verify>
  <done>3 flood/hydrology tools defined and tested incl. empty-alert + INVALID_INPUT paths. Docstrings document bilingual fields and the no-real-time-levels caveat.</done>
</task>

</tasks>

<verification>
1. `uv run pytest src/mcp_canada/modules/manitoba/__tests__/ -x`
2. `uv run python -c "from mcp_canada.modules.manitoba import tools; assert hasattr(tools,'manitoba_get_flood_alerts'); print('OK')"`
</verification>

<success_criteria>
- MB-07, MB-08, MB-09 implemented
- Empty flood-alert response handled as valid (not error)
- Waterways f_type dispatch validated
- River-stations documented as discovery (not real-time levels)
</success_criteria>

<output>
Create `.planning/phases/18-manitoba-government-open-data/18-03-SUMMARY.md`: 3 tools, confirm empty-alert handling, note river-stations FS URL resolution status from spike, any F_TYPE casing surprises.
</output>
