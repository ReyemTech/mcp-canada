---
phase: 18-manitoba-government-open-data
plan: 07
type: execute
wave: 6
depends_on: [18-06]
files_modified:
  - src/mcp_canada/modules/manitoba/prompts.py
  - src/mcp_canada/modules/manitoba/resources.py
  - src/mcp_canada/modules/manitoba/__tests__/test_prompts_resources.py
autonomous: true
requirements:
  - MB-18
must_haves:
  truths:
    - "6 bilingual @prompt functions exist (3 guided list[Message] + 3 quick-lookup str) with manitoba_ prefix and lang param"
    - "7 zero-parameter @resource functions exist using data://, docs://, template:// URI schemes with bilingual content inline"
    - "All prompts/resources use standalone fastmcp decorators (NEVER @mcp.*) and are auto-discovered by FileSystemProvider"
    - "Resources have ZERO parameters (no lang param — would promote to ResourceTemplate and drop from resources/list)"
  artifacts:
    - path: src/mcp_canada/modules/manitoba/prompts.py
      provides: "6 @prompt functions (3 guided + 3 quick lookups)"
      contains: "def manitoba_explore_flood_or_water"
    - path: src/mcp_canada/modules/manitoba/resources.py
      provides: "7 @resource functions (data://, docs://, template://)"
      contains: "data://manitoba/health-regions"
  key_links:
    - from: "src/mcp_canada/modules/manitoba/prompts.py"
      to: "FileSystemProvider prompts/list"
      via: "standalone @prompt from fastmcp.prompts"
      pattern: "@prompt"
    - from: "src/mcp_canada/modules/manitoba/resources.py"
      to: "FileSystemProvider resources/list"
      via: "standalone @resource, zero-parameter functions"
      pattern: "@resource"
---

<objective>
Add Manitoba's prompts and resources (Phase 40 pattern) — 6 bilingual prompts and 7 zero-parameter resources. Guided prompts chain the new tools for flood/water, transport, and agriculture/health workflows; quick lookups guide single-tool calls. Resources embed Manitoba reference data (departments, 5 RHAs, major rivers) and guidance docs/templates.

Purpose: Native MCP discoverability — prompts as slash-commands, resources as reference data agents read repeatedly.
Output: prompts.py (6) + resources.py (7) + tests.
</objective>

<execution_context>
@/Users/mariomeyer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomeyer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-manitoba-government-open-data/18-RESEARCH.md
@.planning/phases/18-manitoba-government-open-data/18-CONTEXT.md
@CLAUDE.md

# Reference: prompts/resources pattern
@src/mcp_canada/modules/alberta/prompts.py
@src/mcp_canada/modules/alberta/resources.py
@src/mcp_canada/modules/york_region/resources.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 6 bilingual prompts</name>
  <files>
    src/mcp_canada/modules/manitoba/prompts.py,
    src/mcp_canada/modules/manitoba/__tests__/test_prompts_resources.py
  </files>
  <action>
    Add 6 @prompt functions (standalone @prompt from fastmcp.prompts — NEVER @mcp.prompt). Each takes lang: Annotated[Literal["en","fr"], "Language: 'en' or 'fr'"] = "en" and has a docstring describing when to use it. Use manitoba_ prefix.

    Guided workflows (return list[Message], user + assistant roles, ≥2 messages, name specific tools):
    - manitoba_explore_flood_or_water — chain manitoba_get_flood_alerts → manitoba_get_river_stations → manitoba_get_provincial_waterways
    - manitoba_explore_transport — chain manitoba_get_road_events → manitoba_get_winter_road_conditions → manitoba_get_traffic_cameras (note key requirement)
    - manitoba_explore_agriculture_or_health — chain manitoba_get_drought_status / manitoba_get_livestock_prices / manitoba_get_surgical_wait_times / manitoba_get_health_facilities

    Quick lookups (return str with tool name + param instructions):
    - manitoba_quick_dataset_search — guide manitoba_search_datasets
    - manitoba_check_road_conditions — guide manitoba_get_winter_road_conditions (mention NOT_CONFIGURED if no key)
    - manitoba_flood_outlook_now — guide manitoba_get_flood_alerts

    Fill TestManitobaPrompts: assert 6 prompts discoverable, bilingual (en/fr produce different content), guided ones return list[Message], quick ones return str.
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/manitoba/__tests__/test_prompts_resources.py::TestManitobaPrompts -x -v</automated>
  </verify>
  <done>6 prompts defined (3 guided + 3 quick), bilingual, standalone @prompt, manitoba_ prefix. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: 7 zero-parameter resources</name>
  <files>
    src/mcp_canada/modules/manitoba/resources.py,
    src/mcp_canada/modules/manitoba/__tests__/test_prompts_resources.py
  </files>
  <action>
    Add 7 @resource functions (standalone @resource from fastmcp.resources — NEVER @mcp.resource). ZERO parameters each (no lang — would promote to ResourceTemplate). Type-prefixed + module-prefixed URIs. Use the reference data tables from 18-RESEARCH.md "Manitoba-Specific Reference Data":
    - data://manitoba/departments — provincial ministries with bilingual labels (json.dumps, en/fr inline)
    - data://manitoba/health-regions — the 5 RHAs (WRHA, PMH, IERHA, SHSS, NHR) with coverage + major hospitals, bilingual
    - data://manitoba/major-rivers — Red, Assiniboine, Winnipeg, Souris + floodway, with flood-risk + key cities
    - docs://manitoba/flood-data-guide — markdown: flood-outlook vs river-level vs forecast distinctions; ArcGIS Hub layers vs HFC PDFs; HYDAT note
    - docs://manitoba/portal-guide — markdown: geoportal.gov.mb.ca ArcGIS Hub structure, which tool to use, data.manitoba.ca/MLI deprecation note, OpenMB licence
    - template://manitoba/dataset-report — markdown template with {placeholder} fields
    - template://manitoba/flood-report — markdown template with {placeholder} fields

    data:// resources must return valid JSON (json.dumps), bilingual content inline. Fill TestManitobaResources: assert 7 resources discoverable via resources/list, data:// return valid JSON, str(r.uri) containment checks (AnyUrl→str), zero-parameter (in resources/list not ResourceTemplate).
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/manitoba/__tests__/test_prompts_resources.py::TestManitobaResources -x -v</automated>
  </verify>
  <done>7 resources defined (3 data + 2 docs + 2 template), zero-parameter, valid JSON for data://, bilingual inline. Tests pass.</done>
</task>

</tasks>

<verification>
1. `uv run pytest src/mcp_canada/modules/manitoba/__tests__/test_prompts_resources.py -x -v`
2. `uv run python -c "from mcp_canada.modules.manitoba import prompts, resources; print('OK')"`
</verification>

<success_criteria>
- 6 prompts (3 guided + 3 quick) bilingual, standalone @prompt, manitoba_ prefix
- 7 resources zero-parameter, type-prefixed URIs, bilingual data:// JSON
- All auto-discoverable by FileSystemProvider (no server.py changes)
</success_criteria>

<output>
Create `.planning/phases/18-manitoba-government-open-data/18-07-SUMMARY.md`: list the 6 prompts + 7 resources with URIs, confirm zero-parameter resource compliance.
</output>
