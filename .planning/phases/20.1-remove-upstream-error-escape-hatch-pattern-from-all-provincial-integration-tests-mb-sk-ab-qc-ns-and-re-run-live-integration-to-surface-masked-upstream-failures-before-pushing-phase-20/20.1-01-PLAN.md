---
phase: 20.1
plan: "01"
title: Masking guard + PRESERVE marker
status: complete
---

# Plan 20.1-01: Masking guard + PRESERVE marker

<objective>
Build the structural guard that detects masking idioms in tests/integration/, and register the tolerates_upstream_error pytest marker. Written FIRST so its output is the worklist for plans 02-05, committed LAST so main is never red. Guard must run in the default unit suite (D-13) and the marker must carry a mandatory reason (D-14).
</objective>
