---
phase: 16-quebec-government-open-data
plan: 04
type: execute
wave: 4
depends_on: ["16-01", "16-02", "16-03"]
files_modified:
  - src/mcp_canada/modules/quebec/client.py
  - src/mcp_canada/modules/quebec/tools.py
  - src/mcp_canada/modules/quebec/prompts.py
  - src/mcp_canada/modules/quebec/resources.py
  - src/mcp_canada/modules/quebec/__tests__/test_client.py
  - src/mcp_canada/modules/quebec/__tests__/test_tools.py
  - src/mcp_canada/modules/quebec/__tests__/test_prompts_resources.py
  - tests/integration/test_tool_scenarios.py
  - tests/integration/test_prompts_resources_scenarios.py
  - README.md
  - CLAUDE.md
autonomous: true
requirements: []
# Closes out Phase 16 — completes the 18-tool Quebec surface and wires it through docs,
# prompts, resources, and live integration tests.

must_haves:
  truths:
    - "quebec_get_air_quality_stations queries RSQAQ stations datastore (245 rows) and filters active stations (DATE_FERMETURE is null)"
    - "quebec_get_air_quality_index queries ArcGIS REST FeatureServer (IQA_resultat_REST/FeatureServer/0/query) via api_get with ?f=json"
    - "quebec_get_water_quality_monitoring is a metadata/discovery tool that returns the package_show details for the water quality package (SHP/GPKG format, not directly parseable)"
    - "quebec_get_electricity_data fetches the Hydro-Québec historique-production-consommation CSV from the dataset's first CSV resource URL"
    - "quebec_get_forest_fires_history is a metadata/discovery tool returning package_show details for feux-de-foret (SHP/GPKG archive)"
    - "quebec_get_protected_areas is a metadata/discovery tool returning package_show details for aires-protegees-au-quebec (SHP/GPKG archive)"
    - "6 bilingual @prompt functions exist (3 guided list[Message] workflows + 3 quick-lookup str prompts)"
    - "7 zero-parameter @resource functions exist (3 data://, 2 docs://, 2 template://)"
    - "Every @resource returns valid JSON (for data://) or markdown string (for docs:// and template://)"
    - "Integration tests exist for quebec_search_datasets, quebec_list_organizations, quebec_list_categories (groups not tags), quebec_get_er_wait_times, quebec_get_health_installations, quebec_get_road_works live through MCP Client layer"
    - "Prompts and resources are discoverable via MCP Client (list_prompts, read_resource)"
    - "README.md has a new Quebec section in the Tool Catalog and tool count is updated (previous + 18)"
    - "CLAUDE.md Portal Technologies table shows Quebec as another CKAN portal entry (no new tech row)"
  artifacts:
    - path: "src/mcp_canada/modules/quebec/client.py"
      provides: "Final 6 client functions: fetch_air_quality_stations, fetch_air_quality_index, fetch_water_quality_monitoring, fetch_electricity_data, fetch_forest_fires_history, fetch_protected_areas"
      contains: "RSQAQ_STATIONS_RESOURCE_ID"
    - path: "src/mcp_canada/modules/quebec/tools.py"
      provides: "Final 6 curated @tool functions — module now exposes 18 total tools"
      contains: "quebec_get_air_quality_index"
    - path: "src/mcp_canada/modules/quebec/prompts.py"
      provides: "6 bilingual @prompt functions"
      contains: "@prompt"
      min_lines: 120
    - path: "src/mcp_canada/modules/quebec/resources.py"
      provides: "7 zero-parameter @resource functions (3 data, 2 docs, 2 template)"
      contains: "@resource"
      min_lines: 100
    - path: "README.md"
      provides: "Quebec section in tool catalog with all 18 tools and updated total count"
      contains: "quebec_search_datasets"
    - path: "CLAUDE.md"
      provides: "CKAN portal list mentions Quebec alongside Ontario, Toronto, Federal, BC CKAN"
      contains: "Quebec"
  key_links:
    - from: "src/mcp_canada/modules/quebec/tools.py:quebec_get_air_quality_index"
      to: "ArcGIS REST FeatureServer (IQA_resultat_REST)"
      via: "api_get with ?f=json&where=1=1&outFields=*"
      pattern: "f=json"
    - from: "src/mcp_canada/modules/quebec/resources.py:quebec_ministries"
      to: "data://quebec/ministries URI"
      via: "@resource decorator with URI + json.dumps body"
      pattern: "data://quebec/ministries"
    - from: "tests/integration/test_tool_scenarios.py:TestQuebecToolScenarios"
      to: "Live Données Québec API"
      via: "MCP Client.call_tool through discover_tools/call_tool"
      pattern: "call_tool.*quebec_"
---

<objective>
Complete Phase 16 by implementing the final 6 curated tools (environment + demographics + energy),
adding 6 bilingual prompts and 7 zero-parameter resources, wiring live integration tests,
and updating README.md + CLAUDE.md.

Purpose: This is the "close out" plan. After Plan 04 completes, the Quebec module has
18 working tools, 6 prompts, 7 resources, live integration coverage, and documentation
reflects the new tool catalog.

Output: 18-tool Quebec module, discoverable via discover_tools and callable via call_tool,
with prompts/resources visible natively in MCP clients.
</objective>

<execution_context>
@/Users/mariomeyer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomeyer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-quebec-government-open-data/16-RESEARCH.md
@.planning/phases/16-quebec-government-open-data/16-CONTEXT.md
@.planning/phases/16-quebec-government-open-data/16-01-SUMMARY.md
@.planning/phases/16-quebec-government-open-data/16-02-SUMMARY.md
@.planning/phases/16-quebec-government-open-data/16-03-SUMMARY.md

# Reference BC Phase 15 Plan 04 for prompts+resources+integration patterns
@.planning/phases/15-british-columbia-government-open-data/15-04-PLAN.md
@src/mcp_canada/modules/british_columbia/prompts.py
@src/mcp_canada/modules/british_columbia/resources.py

# Reference York Region for prompt/resource patterns (guided + quick + data/docs/template)
@src/mcp_canada/modules/york_region/prompts.py
@src/mcp_canada/modules/york_region/resources.py

# Integration test reference
@tests/integration/test_tool_scenarios.py
@tests/integration/test_prompts_resources_scenarios.py

# Current Quebec state
@src/mcp_canada/modules/quebec/client.py
@src/mcp_canada/modules/quebec/tools.py
@src/mcp_canada/modules/quebec/constants.py
@src/mcp_canada/modules/quebec/prompts.py
@src/mcp_canada/modules/quebec/resources.py

# README to update
@README.md
@CLAUDE.md

<interfaces>
From constants.py (Plan 01):
```python
RSQAQ_STATIONS_RESOURCE_ID = "cebea532-a9e0-4a39-8c2d-54f33d937c73"
AQ_INDEX_URL = "https://services3.arcgis.com/0lL78GhXbg1Po7WO/arcgis/rest/services/IQA_resultat_REST/FeatureServer/0/query"
PKG_FOREST_FIRES = "feux-de-foret"
PKG_PROTECTED_AREAS = "aires-protegees-au-quebec"
PKG_WATER_QUALITY = "suivi-physicochimique-des-rivieres-et-du-fleuve"
PKG_ELECTRICITY = "historique-production-consommation"
```

From Plan 02 (client.py helpers):
```python
async def _api_get(path, params=None) -> dict  # parsed-dict contract
async def fetch_dataset_details(package_id) -> tuple[QuebecDatasetDetails, bool]
```

FastMCP prompt/resource decorators:
```python
from fastmcp.prompts import prompt
from fastmcp.prompts.prompt import Message
from fastmcp.resources import resource
```

Integration test wiring (see tests/integration/test_tool_scenarios.py existing classes
for the fixture/provider setup boilerplate).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Final 6 curated client + tool functions (env/demographics/energy) with unit tests</name>
  <files>
    src/mcp_canada/modules/quebec/client.py,
    src/mcp_canada/modules/quebec/tools.py,
    src/mcp_canada/modules/quebec/__tests__/test_client.py,
    src/mcp_canada/modules/quebec/__tests__/test_tools.py
  </files>
  <behavior>
    - `fetch_air_quality_stations(active_only=True)` returns only stations with `DATE_FERMETURE is None`
    - `fetch_air_quality_index()` calls the ArcGIS REST query endpoint and returns parsed feature list
    - `fetch_water_quality_monitoring()` returns dataset metadata (title, notes, resources list) — metadata-only
    - `fetch_electricity_data()` picks the first CSV resource from the Hydro-Québec package and parses it
    - `fetch_forest_fires_history()` returns dataset metadata only (SHP archive not directly parseable)
    - `fetch_protected_areas()` returns dataset metadata only (SHP/GPKG archive not directly parseable)
    - All 6 tools respond with `make_response` envelope; metadata tools note in docstring that full data requires downloading SHP archives externally
  </behavior>
  <action>
    **Step 1: Client bodies** (replace remaining 6 NotImplementedError stubs):

    ```python
    async def fetch_air_quality_stations(
        active_only: bool = True,
        limit: int = 500,
    ) -> tuple[list[QuebecAirQualityStation], bool]:
        cache_key = f"quebec:rsqaq:stations:{active_only}:{limit}"

        async def _fetch() -> list[QuebecAirQualityStation]:
            params = {"limit": limit}
            result = await _datastore_get(RSQAQ_STATIONS_RESOURCE_ID, params)
            out: list[QuebecAirQualityStation] = []
            for r in (result.get("records") or []):
                if active_only and r.get("DATE_FERMETURE"):
                    continue
                out.append(QuebecAirQualityStation(
                    station_id=r.get("ID_STATION"),
                    station_name=r.get("NOM_STATION"),
                    admin_region=r.get("RA"),
                    address=r.get("ADRESSE"),
                    municipality=r.get("MUNICIPALITE"),
                    milieu_type=r.get("TYPE_MILIEU"),
                    date_opened=r.get("DATE_OUVERTURE"),
                    date_closed=r.get("DATE_FERMETURE"),
                    latitude=_safe_float(r.get("LATITUDE")),
                    longitude=_safe_float(r.get("LONGITUDE")),
                ))
            return out

        return await cached_fetch(cache_key, CACHE_TTL_META, _fetch)


    async def fetch_air_quality_index(limit: int = 200) -> tuple[list[dict[str, Any]], bool]:
        """ArcGIS REST FeatureServer query — not CKAN."""
        cache_key = f"quebec:rsqaq:index:{limit}"

        async def _fetch() -> list[dict[str, Any]]:
            params = {
                "f": "json",
                "where": "1=1",
                "outFields": "*",
                "resultRecordCount": limit,
            }
            await _limiter.acquire()
            envelope = await api_get(AQ_INDEX_URL, params, headers=DEFAULT_HEADERS)
            if not isinstance(envelope, dict):
                return []
            features = envelope.get("features") or []
            out = []
            for f in features:
                attrs = f.get("attributes") or {}
                geom = f.get("geometry") or {}
                out.append({**attrs, "longitude": geom.get("x"), "latitude": geom.get("y")})
            return out

        return await cached_fetch(cache_key, CACHE_TTL_ACTIVE, _fetch)


    async def fetch_water_quality_monitoring() -> tuple[dict[str, Any], bool]:
        """Metadata-only (package_show) — underlying resources are SHP/GPKG archives."""
        details, cached = await fetch_dataset_details("suivi-physicochimique-des-rivieres-et-du-fleuve")
        return (details.model_dump(), cached)


    async def fetch_forest_fires_history() -> tuple[dict[str, Any], bool]:
        details, cached = await fetch_dataset_details("feux-de-foret")
        return (details.model_dump(), cached)


    async def fetch_protected_areas() -> tuple[dict[str, Any], bool]:
        details, cached = await fetch_dataset_details("aires-protegees-au-quebec")
        return (details.model_dump(), cached)


    async def fetch_electricity_data(limit: int = 500) -> tuple[list[dict[str, Any]], bool]:
        """Fetch Hydro-Québec historique-production-consommation CSV from first CSV resource."""
        cache_key = f"quebec:hydro:electricity:{limit}"

        async def _fetch() -> list[dict[str, Any]]:
            details, _ = await fetch_dataset_details("historique-production-consommation")
            csv_url: str | None = None
            for r in details.resources:
                if (r.format or "").upper() == "CSV" and r.url:
                    csv_url = r.url
                    break
            if csv_url is None:
                return []
            rows, _ = await fetch_and_parse(csv_url, ttl=CACHE_TTL_META)
            return rows[:limit]

        return await cached_fetch(cache_key, CACHE_TTL_META, _fetch)
    ```

    **Step 2: Tool bodies** (replace the 6 remaining Plan 04 placeholders):

    ```python
    @tool
    async def quebec_get_air_quality_stations(
        active_only: bool = True,
        limit: int = 500,
        lang: Literal["en", "fr"] = "en",
    ) -> dict[str, Any]:
        """Get the RSQAQ air quality monitoring station network across Quebec (MELCCFP).

        Use for: Locating Quebec air quality stations by municipality, region, or environment type (urban/rural/industrial); 245 total stations including historical.
        Keywords: quebec, air quality, rsqaq, stations, monitoring, melccfp, pollution, stations qualite air, environnement, surveillance, network, reseau
        """
        try:
            rows, cached = await _client.fetch_air_quality_stations(active_only=active_only, limit=min(max(limit, 1), 1000))
        except httpx.HTTPStatusError as exc:
            msg = f"Upstream error: {exc}" if lang == "en" else f"Erreur: {exc}"
            return make_error("UPSTREAM_ERROR", msg, lang=lang)
        return make_response(
            data=[r.model_dump() for r in rows],
            api_name=API_NAME,
            api_url=BASE_URL + "datastore_search",
            cached=cached,
            lang=lang,
        )


    @tool
    async def quebec_get_air_quality_index(
        limit: int = 200,
        lang: Literal["en", "fr"] = "en",
    ) -> dict[str, Any]:
        """Get current Quebec air quality index (IQA) readings from MELCCFP ArcGIS REST FeatureServer.

        Use for: Real-time air quality index values across Quebec monitoring stations (hourly refresh); includes pollutant concentrations and index categories.
        Keywords: quebec, air quality index, iqa, real-time, melccfp, pollution, stations, arcgis, indice qualite air, pollutants, hourly, temps reel
        """
        try:
            rows, cached = await _client.fetch_air_quality_index(limit=min(max(limit, 1), 1000))
        except Exception as exc:
            msg = f"ArcGIS REST error: {exc}" if lang == "en" else f"Erreur ArcGIS: {exc}"
            return make_error("UPSTREAM_ERROR", msg, lang=lang)
        return make_response(
            data=rows,
            api_name=API_NAME,
            api_url="https://services3.arcgis.com/0lL78GhXbg1Po7WO/arcgis/rest/services/IQA_resultat_REST/FeatureServer",
            cached=cached,
            lang=lang,
        )


    @tool
    async def quebec_get_water_quality_monitoring(
        lang: Literal["en", "fr"] = "en",
    ) -> dict[str, Any]:
        """Get metadata for Quebec surface water quality monitoring (IQBP) dataset from MELCCFP.

        Use for: Discovering the physicochemical water quality monitoring network for Quebec rivers and the St. Lawrence; data archived as downloadable GeoJSON/SHP.
        Keywords: quebec, water quality, iqbp, rivers, surveillance, melccfp, physicochimique, monitoring, environnement, suivi eau, streams, qualite

        Note: Full data is distributed as GeoJSON ZIP or Shapefile archives which require external download. This tool returns the dataset metadata and resource URLs.
        """
        try:
            data, cached = await _client.fetch_water_quality_monitoring()
        except httpx.HTTPStatusError as exc:
            msg = f"Upstream error: {exc}" if lang == "en" else f"Erreur: {exc}"
            return make_error("UPSTREAM_ERROR", msg, lang=lang)
        return make_response(
            data=data,
            api_name=API_NAME,
            api_url=BASE_URL + "package_show",
            cached=cached,
            lang=lang,
        )


    @tool
    async def quebec_get_electricity_data(
        limit: int = 500,
        lang: Literal["en", "fr"] = "en",
    ) -> dict[str, Any]:
        """Get Hydro-Québec historical electricity production and consumption data.

        Use for: Historical Quebec electricity production and consumption statistics from Hydro-Québec open data; annual/monthly CSV.
        Keywords: quebec, electricity, hydro-quebec, production, consumption, energy, hydro, energie, historique, electricite, utilities, power

        Note: Replaces the planned outage tool — Hydro-Québec outage data is NOT published on Données Québec. For real-time outages, see hydroquebec.com/pannes.
        """
        try:
            rows, cached = await _client.fetch_electricity_data(limit=min(max(limit, 1), 2000))
        except Exception as exc:
            msg = f"Upstream error: {exc}" if lang == "en" else f"Erreur: {exc}"
            return make_error("UPSTREAM_ERROR", msg, lang=lang)
        return make_response(
            data=rows,
            api_name=API_NAME,
            api_url=BASE_URL + "package_show",
            cached=cached,
            lang=lang,
        )


    @tool
    async def quebec_get_forest_fires_history(
        lang: Literal["en", "fr"] = "en",
    ) -> dict[str, Any]:
        """Get metadata for Quebec historical forest fire perimeters archive (MFFP).

        Use for: Discovering the MFFP feux-de-foret archive (historical fire perimeters); full data is distributed as SHP/GPKG/FGDB downloads.
        Keywords: quebec, forest fires, wildfires, mffp, mrn, feux foret, historical, perimeters, archive, forestry, incendies, forest

        Note: SOPFEU active fire data is NOT on Données Québec — this tool covers the historical archive only. For current wildfires, see sopfeu.qc.ca directly.
        """
        try:
            data, cached = await _client.fetch_forest_fires_history()
        except httpx.HTTPStatusError as exc:
            msg = f"Upstream error: {exc}" if lang == "en" else f"Erreur: {exc}"
            return make_error("UPSTREAM_ERROR", msg, lang=lang)
        return make_response(
            data=data,
            api_name=API_NAME,
            api_url=BASE_URL + "package_show",
            cached=cached,
            lang=lang,
        )


    @tool
    async def quebec_get_protected_areas(
        lang: Literal["en", "fr"] = "en",
    ) -> dict[str, Any]:
        """Get metadata for Quebec registered protected areas and OECM registry (MELCCFP).

        Use for: Discovering the MELCCFP protected areas registry — 10,000+ registered protected areas across Quebec (parks, ecological reserves, wildlife sanctuaries).
        Keywords: quebec, protected areas, parks, sepaq, melccfp, conservation, aires protegees, environnement, parcs, reserves, wilderness, sanctuaries

        Note: Full geometric data is distributed as SHP/GPKG archives. This tool returns dataset metadata and resource URLs.
        """
        try:
            data, cached = await _client.fetch_protected_areas()
        except httpx.HTTPStatusError as exc:
            msg = f"Upstream error: {exc}" if lang == "en" else f"Erreur: {exc}"
            return make_error("UPSTREAM_ERROR", msg, lang=lang)
        return make_response(
            data=data,
            api_name=API_NAME,
            api_url=BASE_URL + "package_show",
            cached=cached,
            lang=lang,
        )
    ```

    **Step 3: Fill the 6 Plan 04 test_client.py test classes** (TestFetchAirQualityStations,
    TestFetchAirQualityIndex, TestFetchWaterQualityMonitoring, TestFetchElectricityData,
    TestFetchForestFiresHistory, TestFetchProtectedAreas) with AsyncMock-based unit tests.

    **Step 4: Fill the 6 Plan 04 test_tools.py test classes** (TestQuebecGetForestFiresHistory,
    TestQuebecGetAirQualityStations, TestQuebecGetAirQualityIndex, TestQuebecGetWaterQualityMonitoring,
    TestQuebecGetElectricityData, TestQuebecGetProtectedAreas) with happy path + error path tests.

    Run the full module test suite + test_quality.py:
    ```
    uv run pytest src/mcp_canada/modules/quebec/__tests__/ tests/test_quality.py -x
    uv run pytest --cov=src/mcp_canada/modules/quebec --cov-fail-under=95
    ```
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/quebec/__tests__/ tests/test_quality.py -x && uv run pytest --cov=src/mcp_canada/modules/quebec --cov-report=term-missing --cov-fail-under=95</automated>
  </verify>
  <done>
    - 18 Quebec tools registered (5 discovery + 13 curated, matching CONTEXT.md target)
    - All 18 tools pass test_quality.py (Use for + 8+ Keywords)
    - Module-level coverage ≥95%
    - No remaining NotImplementedError stubs in client.py
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 6 bilingual prompts + 7 zero-parameter resources + unit tests</name>
  <files>
    src/mcp_canada/modules/quebec/prompts.py,
    src/mcp_canada/modules/quebec/resources.py,
    src/mcp_canada/modules/quebec/__tests__/test_prompts_resources.py
  </files>
  <behavior>
    - 6 @prompt functions registered: quebec_explore_health, quebec_explore_transport_conditions, quebec_explore_environment (guided list[Message] workflows); quebec_quick_dataset_search, quebec_check_road_conditions, quebec_active_fires_now (quick lookups returning str)
    - Each prompt accepts `lang: Annotated[Literal["en", "fr"], "Language: 'en' or 'fr'"] = "en"` parameter
    - 7 @resource functions registered with zero parameters:
      - `data://quebec/ministries` — JSON of DQ organizations with bilingual labels
      - `data://quebec/regions` — 17 Quebec admin regions (French + English names + code)
      - `data://quebec/mrcs` — List of MRCs with region mapping
      - `docs://quebec/catalog-federation-quirks` — markdown explaining federated catalog
      - `docs://quebec/bilingual-metadata-guide` — markdown explaining French-primary metadata
      - `template://quebec/dataset-report` — markdown template with {placeholders}
      - `template://quebec/road-conditions-report` — markdown template with bilingual placeholders
    - `data://` resources return valid JSON (parseable)
    - `docs://` and `template://` resources return markdown strings (non-empty)
  </behavior>
  <action>
    **Step 1: Implement `prompts.py`** — 6 bilingual prompts. Reference
    `src/mcp_canada/modules/british_columbia/prompts.py` for structure. Each prompt uses
    module prefix `quebec_`.

    Example guided workflow (list[Message]):
    ```python
    from typing import Annotated, Literal

    from fastmcp.prompts import prompt
    from fastmcp.prompts.prompt import Message


    @prompt
    def quebec_explore_health(
        lang: Annotated[Literal["en", "fr"], "Language: 'en' or 'fr'"] = "en",
    ) -> list[Message]:
        """Guided workflow for exploring Quebec health data (ER wait times, MSSS installations)."""
        if lang == "fr":
            return [
                Message(role="user", content="Je veux explorer les données de santé du Québec — temps d'attente aux urgences et installations MSSS."),
                Message(
                    role="assistant",
                    content=(
                        "Je peux vous aider à explorer les données de santé publique du Québec. Voici le workflow recommandé:\n\n"
                        "1. **Temps d'attente actuels:** Appelez `quebec_get_er_wait_times(lang=\"fr\")` pour obtenir la situation horaire aux 116 urgences du Québec.\n"
                        "2. **Localiser les installations:** Appelez `quebec_get_health_installations(instal_type=\"CHSGS\", lang=\"fr\")` pour tous les hôpitaux, ou `instal_type=\"CLSC\"` pour les CLSC.\n"
                        "3. **Filtrer par région RSS:** Ajoutez `rss_name=\"Montréal\"` pour restreindre à une région sociosanitaire.\n"
                        "4. **Stocker pour comparaison:** Utilisez `ds_create_table` + `ds_insert` pour persister les données et comparer avec SQL.\n\n"
                        "Quelle région sociosanitaire ou installation voulez-vous examiner?"
                    ),
                ),
            ]
        return [
            Message(role="user", content="I want to explore Quebec health data — ER wait times and MSSS installations."),
            Message(
                role="assistant",
                content=(
                    "I can help you explore Quebec public health data. Recommended workflow:\n\n"
                    "1. **Current wait times:** Call `quebec_get_er_wait_times()` for hourly situation at Quebec's 116 EDs.\n"
                    "2. **Locate installations:** Call `quebec_get_health_installations(instal_type=\"CHSGS\")` for all hospitals, or `instal_type=\"CLSC\"` for community clinics.\n"
                    "3. **Filter by RSS region:** Add `rss_name=\"Montréal\"` to narrow by health region.\n"
                    "4. **Store for comparison:** Use `ds_create_table` + `ds_insert` to persist and cross-compare with SQL.\n\n"
                    "Note: Quebec health data uses French-primary field names. Which region or facility do you want to examine?"
                ),
            ),
        ]
    ```

    Implement similarly:
    - `quebec_explore_transport_conditions` — chains `quebec_get_road_conditions` + `quebec_get_road_works` + `quebec_get_road_events`
    - `quebec_explore_environment` — chains `quebec_get_air_quality_index` + `quebec_get_air_quality_stations` + `quebec_get_water_quality_monitoring`

    Quick lookups (return `str`):
    ```python
    @prompt
    def quebec_quick_dataset_search(
        query: str,
        lang: Annotated[Literal["en", "fr"], "Language: 'en' or 'fr'"] = "en",
    ) -> str:
        """Quick lookup — searches Données Québec via quebec_search_datasets."""
        if lang == "fr":
            return f"Appelez `quebec_search_datasets(q=\"{query}\", lang=\"fr\")` pour rechercher dans le catalogue Données Québec (1 593 jeux de données, 139 organisations)."
        return f"Call `quebec_search_datasets(q=\"{query}\")` to search the Données Québec catalog (1,593 datasets, 139 orgs)."


    @prompt
    def quebec_check_road_conditions(
        lang: Annotated[Literal["en", "fr"], "Language: 'en' or 'fr'"] = "en",
    ) -> str:
        """Quick lookup — current Quebec winter road conditions from MTQ."""
        if lang == "fr":
            return "Appelez `quebec_get_road_conditions(lang=\"fr\")` pour obtenir l'état actuel du réseau routier (données hivernales MTQ)."
        return "Call `quebec_get_road_conditions()` for current MTQ winter road network status."


    @prompt
    def quebec_active_fires_now(
        lang: Annotated[Literal["en", "fr"], "Language: 'en' or 'fr'"] = "en",
    ) -> str:
        """Quick lookup — active wildfire information (SOPFEU redirect)."""
        if lang == "fr":
            return "Note: les données SOPFEU d'incendies actifs ne sont PAS publiées sur Données Québec. Pour les incendies actifs, consultez sopfeu.qc.ca. Pour l'historique des feux, appelez `quebec_get_forest_fires_history()`."
        return "Note: SOPFEU active wildfire data is NOT on Données Québec. For active fires, see sopfeu.qc.ca. For historical fire archive metadata, call `quebec_get_forest_fires_history()`."
    ```

    **Step 2: Implement `resources.py`** — 7 zero-parameter resources. Reference
    `src/mcp_canada/modules/british_columbia/resources.py` for structure. Remember: NO
    function parameters — any param promotes to ResourceTemplate.

    ```python
    import json

    from fastmcp.resources import resource


    @resource("data://quebec/ministries")
    def quebec_ministries() -> str:
        """Catalog of Données Québec provincial ministries and key organizations with bilingual labels."""
        data = {
            "description_fr": "Principales organisations publiant sur Données Québec",
            "description_en": "Main organizations publishing on Données Québec",
            "organizations": [
                {"slug": "msss", "name_fr": "Ministère de la Santé et des Services sociaux", "name_en": "Ministry of Health and Social Services"},
                {"slug": "mtq", "name_fr": "Ministère des Transports et de la Mobilité durable", "name_en": "Ministry of Transportation"},
                {"slug": "mrn", "name_fr": "Ministère des Ressources naturelles et des Forêts", "name_en": "Ministry of Natural Resources and Forests"},
                {"slug": "developpement-durable-environnement-et-lutte-contre-les-changements-climatiques", "name_fr": "Ministère de l'Environnement (MELCCFP)", "name_en": "Ministry of Environment"},
                {"slug": "affaires-municipales-et-occupation-du-territoire", "name_fr": "Ministère des Affaires municipales (MAMH)", "name_en": "Ministry of Municipal Affairs"},
                {"slug": "hydro-quebec", "name_fr": "Hydro-Québec", "name_en": "Hydro-Québec"},
                {"slug": "isq", "name_fr": "Institut de la statistique du Québec", "name_en": "Quebec Statistics Institute"},
                {"slug": "sepaq", "name_fr": "Société des établissements de plein air du Québec", "name_en": "SEPAQ (Quebec Parks)"},
                {"slug": "msp", "name_fr": "Ministère de la Sécurité publique", "name_en": "Ministry of Public Safety"},
            ],
            "note_fr": "Utilisez `quebec_list_organizations` pour la liste complète des 139 organisations.",
            "note_en": "Use `quebec_list_organizations` for the full list of 139 orgs.",
        }
        return json.dumps(data, ensure_ascii=False, indent=2)


    @resource("data://quebec/regions")
    def quebec_regions() -> str:
        """Quebec's 17 administrative regions with codes and bilingual names."""
        data = {
            "regions": [
                {"code": "01", "name_fr": "Bas-Saint-Laurent", "name_en": "Lower St. Lawrence"},
                {"code": "02", "name_fr": "Saguenay–Lac-Saint-Jean", "name_en": "Saguenay–Lac-Saint-Jean"},
                {"code": "03", "name_fr": "Capitale-Nationale", "name_en": "National Capital"},
                {"code": "04", "name_fr": "Mauricie", "name_en": "Mauricie"},
                {"code": "05", "name_fr": "Estrie", "name_en": "Estrie"},
                {"code": "06", "name_fr": "Montréal", "name_en": "Montreal"},
                {"code": "07", "name_fr": "Outaouais", "name_en": "Outaouais"},
                {"code": "08", "name_fr": "Abitibi-Témiscamingue", "name_en": "Abitibi-Témiscamingue"},
                {"code": "09", "name_fr": "Côte-Nord", "name_en": "North Shore"},
                {"code": "10", "name_fr": "Nord-du-Québec", "name_en": "Northern Quebec"},
                {"code": "11", "name_fr": "Gaspésie–Îles-de-la-Madeleine", "name_en": "Gaspésie–Îles-de-la-Madeleine"},
                {"code": "12", "name_fr": "Chaudière-Appalaches", "name_en": "Chaudière-Appalaches"},
                {"code": "13", "name_fr": "Laval", "name_en": "Laval"},
                {"code": "14", "name_fr": "Lanaudière", "name_en": "Lanaudière"},
                {"code": "15", "name_fr": "Laurentides", "name_en": "Laurentians"},
                {"code": "16", "name_fr": "Montérégie", "name_en": "Montérégie"},
                {"code": "17", "name_fr": "Centre-du-Québec", "name_en": "Central Quebec"},
            ],
        }
        return json.dumps(data, ensure_ascii=False, indent=2)


    @resource("data://quebec/mrcs")
    def quebec_mrcs() -> str:
        """Reference list of key Quebec MRCs (Regional County Municipalities) by region.

        Note: Not exhaustive — Quebec has ~100 MRCs. For the complete list use `quebec_get_population_by_municipality` which returns MRC data per municipality.
        """
        data = {
            "description_fr": "Liste de référence des principales MRC par région administrative",
            "description_en": "Reference list of key MRCs by administrative region",
            "note_fr": "Pour la liste complète, utilisez quebec_get_population_by_municipality.",
            "note_en": "For the full list, use quebec_get_population_by_municipality.",
            "sample_mrcs": [
                {"region_code": "03", "mrc_name_fr": "La Côte-de-Beaupré"},
                {"region_code": "06", "mrc_name_fr": "Montréal"},
                {"region_code": "16", "mrc_name_fr": "La Vallée-du-Richelieu"},
                {"region_code": "17", "mrc_name_fr": "Drummond"},
            ],
        }
        return json.dumps(data, ensure_ascii=False, indent=2)


    @resource("docs://quebec/catalog-federation-quirks")
    def quebec_federation_quirks() -> str:
        """Documentation explaining the federated nature of Données Québec and how to narrow searches."""
        return """# Données Québec — Catalog Federation Quirks / Particularités de la fédération

## English

Données Québec is a **federated** open data catalog — it includes datasets from:
- Provincial ministries (MSSS, MTQ, MELCCFP, MAMH, MRN)
- Parastatal entities (Hydro-Québec, SEPAQ, ISQ)
- Municipalities (Ville de Montréal, BIXI, ARTM, and dozens more)
- NGOs and civic data publishers

When you call `quebec_search_datasets(q="...")`, results span all 139 organizations.
To narrow by ministry, use the `organization` parameter:
- `quebec_search_datasets(q="hopital", organization="msss")`
- `quebec_search_datasets(q="route", organization="mtq")`

**Montreal overlap:** Montreal city data appears here today but will be comprehensively
covered in a future Montreal-specific module (Phase 27). If your task is Montreal-only,
wait for that module or use the `organization` filter to target Montreal orgs.

**Tags vs groups:** Quebec uses `group_list` (10 thematic groups) for categorization,
not `tag_list`. Use `quebec_list_categories` for high-level browsing. Tags (4,200+)
are too noisy for direct enumeration.

## Français

Données Québec est un catalogue **fédéré** regroupant:
- Ministères provinciaux (MSSS, MTQ, MELCCFP, MAMH, MRN)
- Organismes parapublics (Hydro-Québec, SEPAQ, ISQ)
- Municipalités (Ville de Montréal, BIXI, ARTM, etc.)
- ONG et organismes citoyens

Le paramètre `organization` permet de filtrer par organisation.
Le paramètre `group` permet de filtrer par thématique (10 groupes).
"""


    @resource("docs://quebec/bilingual-metadata-guide")
    def quebec_bilingual_metadata_guide() -> str:
        """Documentation explaining French-primary metadata and bilingual column selection."""
        return """# Données Québec — Bilingual Metadata / Métadonnées bilingues

## English

**Key fact:** Données Québec metadata (titles, descriptions, field names) is **French-only**.
Unlike the BC Data Catalogue which uses `title_translated` dicts, DQ uses plain `title`/`notes`
fields in French. The `lang` parameter on Quebec tools affects:
- Error messages (bilingual via inline ternary)
- Column selection for MTQ WFS CSV tools which DO have bilingual columns
- It does NOT translate dataset titles or descriptions (no English available upstream)

**MTQ WFS CSV bilingual columns:**
- `chantiers_mtmdet` (road works): `descriptionFrancais` / `descriptionAnglais`
- `conditions_routieres` (winter conditions): `DescriptionEtatChausseeFR` / `DescriptionEtatChausseeEN`
- `evenements` (road events): French-only (no EN columns)

**MSSS datastore field names:** UPPER_SNAKE_CASE (INSTAL_COD, INSTAL_NOM, CLSC, CHSGS, CHSLD, CHPSY).
Boolean flags use `"Oui"` / `"Non"` (French) — client helpers convert to Python bool.

## Français

Les métadonnées (titres, descriptions) sur Données Québec sont en **français uniquement**.
Le paramètre `lang` n'affecte que les messages d'erreur et la sélection de colonnes bilingues
dans les CSV WFS du MTQ.
"""


    @resource("template://quebec/dataset-report")
    def quebec_dataset_report_template() -> str:
        """Template for summarizing a Quebec dataset."""
        return """# {title}

**Organisation / Organization:** {organization_title} (`{organization_slug}`)
**Licence / License:** {license_id}
**Fréquence / Update frequency:** {update_frequency}
**Nombre de ressources / Resources:** {num_resources}

## Description

{notes}

## Ressources / Resources

{resources_list}

---
Source: Données Québec — https://www.donneesquebec.ca/recherche/dataset/{name}
"""


    @resource("template://quebec/road-conditions-report")
    def quebec_road_conditions_report_template() -> str:
        """Template for road conditions summary."""
        return """# État du réseau routier — {region} / Road Conditions — {region}

**Route:** {route}
**État de la chaussée / Pavement status:**
- 🇫🇷 {condition_fr}
- 🇬🇧 {condition_en}

**Visibilité / Visibility:**
- 🇫🇷 {visibility_fr}
- 🇬🇧 {visibility_en}

**Dernière mise à jour / Last updated:** {last_updated}

Source: MTQ WFS — https://ws.mapserver.transports.gouv.qc.ca/swtq
"""
    ```

    Register all 7 resources in `__all__` and ensure 6 prompts in `prompts.py` `__all__`.

    **Step 3: Fill test_prompts_resources.py bodies**

    ```python
    import json
    import pytest
    from mcp_canada.modules.quebec import prompts as q_prompts
    from mcp_canada.modules.quebec import resources as q_resources

    class TestQuebecPrompts:
        async def test_explore_health_workflow(self):
            messages = q_prompts.quebec_explore_health(lang="en")
            assert len(messages) >= 2
            assert messages[0].role == "user"
            assert messages[1].role == "assistant"
            assert "quebec_get_er_wait_times" in messages[1].content

        async def test_explore_health_workflow_fr(self):
            messages = q_prompts.quebec_explore_health(lang="fr")
            assert "urgences" in messages[0].content or "urgence" in messages[1].content.lower()

        async def test_quick_dataset_search_returns_str(self):
            result = q_prompts.quebec_quick_dataset_search(query="sante")
            assert isinstance(result, str)
            assert "quebec_search_datasets" in result
            assert "sante" in result

        async def test_active_fires_now_redirects(self):
            result = q_prompts.quebec_active_fires_now()
            assert "sopfeu.qc.ca" in result.lower()

    class TestQuebecResources:
        def test_ministries_json_valid(self):
            content = q_resources.quebec_ministries()
            data = json.loads(content)
            assert "organizations" in data
            assert any(o["slug"] == "msss" for o in data["organizations"])

        def test_regions_17_entries(self):
            content = q_resources.quebec_regions()
            data = json.loads(content)
            assert len(data["regions"]) == 17

        def test_mrcs_catalog_valid_json(self):
            content = q_resources.quebec_mrcs()
            data = json.loads(content)
            assert "sample_mrcs" in data

        def test_federation_quirks_markdown(self):
            content = q_resources.quebec_federation_quirks()
            assert content.startswith("#")
            assert "federated" in content.lower()

        def test_bilingual_guide_mentions_french(self):
            content = q_resources.quebec_bilingual_metadata_guide()
            assert "French" in content or "français" in content

        def test_dataset_report_template_placeholders(self):
            content = q_resources.quebec_dataset_report_template()
            assert "{title}" in content
            assert "{organization_title}" in content

        def test_road_conditions_template_bilingual_placeholders(self):
            content = q_resources.quebec_road_conditions_report_template()
            assert "{condition_fr}" in content
            assert "{condition_en}" in content
    ```
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/quebec/__tests__/test_prompts_resources.py -x -v</automated>
  </verify>
  <done>
    - 6 prompts + 7 resources registered
    - All 3 data:// resources return valid JSON (json.loads succeeds)
    - All prompts accept lang parameter
    - Quick lookups return str, guided workflows return list[Message] with ≥2 messages
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration tests (MCP Client layer) + README.md + CLAUDE.md updates</name>
  <files>
    tests/integration/test_tool_scenarios.py,
    tests/integration/test_prompts_resources_scenarios.py,
    README.md,
    CLAUDE.md
  </files>
  <behavior>
    - `TestQuebecToolScenarios` has at least 8 real integration test methods covering: search, list_orgs, list_categories (groups not tags), ER wait times, health installations, road works, bridge structures (with filter), discover_tools finding quebec_*
    - `TestQuebecPromptsResources` verifies prompts discoverable via `client.list_prompts()` and resources readable via `client.read_resource("data://quebec/ministries")`
    - Integration tests call tools through MCP Client layer using the project's standard boilerplate (`mcp` from server, `_build_providers`, `BM25SearchTransform`)
    - Tests mark with `@pytest.mark.integration` (skipped by default)
    - README.md has a new Quebec section in the Tool Catalog listing all 18 quebec_ tools with one-line descriptions
    - README.md tool count in header increments by 18
    - CLAUDE.md "Portal Technologies" table continues to show CKAN as the technology used for Quebec (no new row; Quebec reuses CKAN pattern from Ontario/Toronto/Federal/BC CKAN)
  </behavior>
  <action>
    **Step 1: Implement integration tests** — replace xfail stubs from Plan 01 with real
    test bodies. Reference `src/mcp_canada/modules/british_columbia/__tests__/` fixtures
    and existing `TestBcToolScenarios` class structure in `tests/integration/test_tool_scenarios.py`.

    Required test methods in `TestQuebecToolScenarios`:

    ```python
    class TestQuebecToolScenarios:
        """Live Données Québec + MTQ WFS CSV integration tests."""

        @pytest.mark.integration
        async def test_search_datasets_finds_sante_packages(self, mcp_client):
            result = await mcp_client.call_tool('call_tool', {
                'name': 'quebec_search_datasets',
                'arguments': {'q': 'sante', 'rows': 5},
            })
            data = json.loads(result.content[0].text)
            assert "_meta" in data
            assert data["_meta"]["source"]["api"] == "donnees-quebec"
            assert len(data["data"]) > 0

        @pytest.mark.integration
        async def test_list_organizations_returns_msss(self, mcp_client):
            result = await mcp_client.call_tool('call_tool', {
                'name': 'quebec_list_organizations',
                'arguments': {},
            })
            data = json.loads(result.content[0].text)
            slugs = [o["name"] for o in data["data"]]
            assert "msss" in slugs
            assert "mtq" in slugs

        @pytest.mark.integration
        async def test_list_categories_uses_groups_not_tags(self, mcp_client):
            """Verifies DQ returns 10 thematic groups (Phase 16 lesson — not tag_list)."""
            result = await mcp_client.call_tool('call_tool', {
                'name': 'quebec_list_categories',
                'arguments': {},
            })
            data = json.loads(result.content[0].text)
            # DQ has 10 thematic groups, not 4200 tags
            assert 5 <= len(data["data"]) <= 20

        @pytest.mark.integration
        async def test_get_er_wait_times_returns_116_rows(self, mcp_client):
            result = await mcp_client.call_tool('call_tool', {
                'name': 'quebec_get_er_wait_times',
                'arguments': {},
            })
            data = json.loads(result.content[0].text)
            assert "_meta" in data
            # 116 hospital EDs approximately
            assert 100 <= len(data["data"]) <= 150

        @pytest.mark.integration
        async def test_get_health_installations_clsc_filter(self, mcp_client):
            result = await mcp_client.call_tool('call_tool', {
                'name': 'quebec_get_health_installations',
                'arguments': {'instal_type': 'CLSC', 'limit': 20},
            })
            data = json.loads(result.content[0].text)
            assert "_meta" in data
            assert all(r["is_clsc"] for r in data["data"])

        @pytest.mark.integration
        async def test_get_road_works_live_wfs_csv(self, mcp_client):
            result = await mcp_client.call_tool('call_tool', {
                'name': 'quebec_get_road_works',
                'arguments': {'lang': 'en'},
            })
            data = json.loads(result.content[0].text)
            assert "_meta" in data
            assert isinstance(data["data"], list)  # may be empty off-season

        @pytest.mark.integration
        async def test_get_bridge_structures_requires_filter(self, mcp_client):
            """No filter — must return INVALID_INPUT error."""
            result = await mcp_client.call_tool('call_tool', {
                'name': 'quebec_get_bridge_structures',
                'arguments': {},
            })
            data = json.loads(result.content[0].text)
            assert "error" in data
            assert data["error"]["code"] == "INVALID_INPUT"

        @pytest.mark.integration
        async def test_discover_tools_finds_quebec_tools(self, mcp_client):
            result = await mcp_client.call_tool('discover_tools', {
                'query': 'Quebec emergency room wait times',
                'max_results': 10,
            })
            data = json.loads(result.content[0].text)
            # Should find quebec_get_er_wait_times
            tool_names = [t.get("name", "") for t in data.get("tools", [])]
            assert any("quebec" in n.lower() for n in tool_names)
    ```

    Use the existing `mcp_client` fixture if present in the integration test file, or
    wire up inline following the pattern in `.claude/rules/tests.md`.

    **Step 2: `TestQuebecPromptsResources` integration**:

    ```python
    class TestQuebecPromptsResources:
        @pytest.mark.integration
        async def test_prompts_discoverable(self, mcp_client):
            prompts = await mcp_client.list_prompts()
            names = [p.name for p in prompts]
            assert "quebec_explore_health" in names
            assert "quebec_quick_dataset_search" in names

        @pytest.mark.integration
        async def test_ministries_resource_valid_json(self, mcp_client):
            result = await mcp_client.read_resource("data://quebec/ministries")
            content = result[0].text
            data = json.loads(content)
            assert "organizations" in data

        @pytest.mark.integration
        async def test_resources_discoverable(self, mcp_client):
            resources = await mcp_client.list_resources()
            uris = [str(r.uri) for r in resources]
            assert any("data://quebec/ministries" in u for u in uris)
            assert any("docs://quebec/catalog-federation-quirks" in u for u in uris)
    ```

    **Step 3: Update `README.md`**

    Read the current README. Find the "Tool Catalog" section. Add a new "Quebec Government
    Open Data" subsection listing all 18 quebec_ tools with the first line of each docstring.
    Update the tool count in the README header (current count + 18 new tools).

    Also update any "Modules" listing in the table of contents to include Quebec.

    Example Quebec section:
    ```markdown
    ### Quebec Government Open Data

    Données Québec CKAN (1,593 datasets, 139 orgs) + MTQ WFS CSV. French-primary metadata.

    | Tool | Description |
    |------|-------------|
    | `quebec_search_datasets` | Search the Données Québec open data catalogue. |
    | `quebec_get_dataset_details` | Get full details for a Données Québec dataset. |
    | `quebec_query_dataset` | Query records from a Données Québec dataset. |
    | `quebec_list_organizations` | List all 139 DQ organizations. |
    | `quebec_list_categories` | List the 10 thematic groups. |
    | `quebec_get_health_installations` | MSSS health installations (hospitals, CLSCs). |
    | `quebec_get_er_wait_times` | Current Quebec ER wait times (hourly). |
    | `quebec_get_population_by_municipality` | MAMH municipal registry. |
    | `quebec_get_road_conditions` | MTQ winter road conditions. |
    | `quebec_get_road_works` | MTQ active construction zones. |
    | `quebec_get_road_events` | MTQ current road events/warnings. |
    | `quebec_get_bridge_structures` | MTQ bridge/culvert/tunnel inventory. |
    | `quebec_get_air_quality_stations` | RSQAQ air quality monitoring stations. |
    | `quebec_get_air_quality_index` | Current IQA readings (ArcGIS REST). |
    | `quebec_get_water_quality_monitoring` | IQBP water quality metadata. |
    | `quebec_get_electricity_data` | Hydro-Québec historical production/consumption. |
    | `quebec_get_forest_fires_history` | MFFP historical fire archive metadata. |
    | `quebec_get_protected_areas` | MELCCFP protected areas registry metadata. |
    ```

    **Step 4: Update `CLAUDE.md`**

    Find the Portal Technologies table. Add "Quebec (Phase 16)" to the First used column
    for the CKAN row. Add a Quebec CKAN bullet mention under "Federal CKAN, Ontario, Toronto,
    BC CKAN" so the example list is accurate. Do NOT add a new row — Quebec reuses the
    existing CKAN technology.

    Example edit:
    ```diff
    -| **CKAN** | `shared/http.py` + per-module `_api_get` | Federal CKAN, Ontario, Toronto, BC CKAN | `BASE_URL + /api/3/action/` |
    +| **CKAN** | `shared/http.py` + per-module `_api_get` | Federal CKAN, Ontario, Toronto, BC CKAN, Quebec (Phase 16) | `BASE_URL + /api/3/action/` |
    ```

    **Step 5: Run full phase check:**

    ```bash
    uv run pytest src/mcp_canada/modules/quebec/__tests__/ tests/test_quality.py -x -v
    uv run pytest --cov=src/mcp_canada/modules/quebec --cov-fail-under=95
    uv run pytest tests/integration/ -v -m integration --timeout=120 -k Quebec
    uv run pyright src/mcp_canada/modules/quebec/
    ```

    Fix any failures. Integration tests require live network; expect possible flakes on
    MTQ WFS endpoints.
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/quebec/__tests__/ tests/test_quality.py -x && uv run pytest --cov=src/mcp_canada/modules/quebec --cov-fail-under=95 --cov-report=term-missing && grep -q "quebec_search_datasets" README.md && grep -q "Quebec" CLAUDE.md</automated>
  </verify>
  <done>
    - TestQuebecToolScenarios has 8 integration tests (unit tests skipped by default, marker verified)
    - TestQuebecPromptsResources integration tests present
    - README.md Quebec section added with 18 tool entries, tool count updated
    - CLAUDE.md mentions Quebec in CKAN portal row
    - Full module coverage ≥95%
    - pyright green
    - No test_quality.py failures
  </done>
</task>

</tasks>

<verification>
- 18 Quebec tools total (verified by counting `@tool` in tools.py)
- 6 prompts, 7 resources registered
- `grep -c "quebec_" README.md` returns ≥18
- CLAUDE.md mentions Quebec in CKAN portal entry
- `uv run pytest src/mcp_canada/modules/quebec/ tests/test_quality.py` passes
- `uv run pytest --cov=src/mcp_canada/modules/quebec --cov-fail-under=95` passes
- Live integration tests (when run with `-m integration`) show at least 6/8 Quebec scenarios passing (MTQ WFS road_conditions may flake)
- `grep -c "raise_for_status\|tag_list" src/mcp_canada/modules/quebec/` returns 0
</verification>

<success_criteria>
- Complete 18-tool Quebec module shippable
- Prompts + resources discoverable via MCP Client natively
- Documentation reflects Quebec addition (README.md + CLAUDE.md)
- Live integration tests green (or gracefully degrade on flaky MTQ endpoints)
- Phase 15 contract lessons (parsed-dict `_api_get`, TestSharedApiGetContract, inline bilingual ternary, User-Agent) verifiable via grep
</success_criteria>

<output>
After completion, create `.planning/phases/16-quebec-government-open-data/16-04-SUMMARY.md`
</output>
