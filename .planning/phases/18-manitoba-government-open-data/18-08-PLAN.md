---
phase: 18-manitoba-government-open-data
plan: 08
type: execute
wave: 7
depends_on: [18-07]
files_modified:
  - src/mcp_canada/modules/manitoba/__tests__/test_tools.py
  - tests/integration/test_tool_scenarios.py
  - tests/integration/test_prompts_resources_scenarios.py
  - README.md
  - docs/MODULES.md
  - CLAUDE.md
  - EXAMPLES.md
autonomous: true
requirements:
  - MB-18
must_haves:
  truths:
    - "Parametrized TestManitobaEnvelopes asserts every manitoba_ tool returns a valid _meta envelope; TestManitobaLangParam asserts lang passes through for every tool"
    - "TestManitobaToolScenarios (integration, via MCP Client) covers happy-path + discovery + error-handling for the curated tools, incl. the empty-flood-alert and 511 NOT_CONFIGURED scenarios"
    - "TestManitobaPromptsResources integration verifies prompts via list_prompts() and resources via read_resource()"
    - "README.md has a Manitoba section in the Tool Catalog, tool count updated; docs/MODULES.md has a Manitoba entry; CLAUDE.md lists Manitoba under the ArcGIS Hub portal technology; EXAMPLES.md has a Manitoba cross-module example"
    - "Coverage ≥95% across src/mcp_canada; full unit suite green; Manitoba integration tests green (or skipped where 511 key unavailable)"
  artifacts:
    - path: tests/integration/test_tool_scenarios.py
      provides: "TestManitobaToolScenarios class"
      contains: "class TestManitobaToolScenarios"
    - path: README.md
      provides: "Manitoba tool catalog section + updated tool count"
      contains: "Manitoba"
  key_links:
    - from: "tests/integration/test_tool_scenarios.py"
      to: "MCP Client (discover_tools / call_tool)"
      via: "client.call_tool('call_tool', {...}) for manitoba_ tools"
      pattern: "manitoba_"
---

<objective>
Close Phase 18 (MB-18): parametrized envelope/lang tests over every Manitoba tool, integration tests through the MCP Client layer, documentation sync (README/MODULES/CLAUDE/EXAMPLES), and the 95% coverage gate.

Purpose: Prove all Manitoba tools follow conventions, are discoverable through MCP, and the catalog docs stay in sync.
Output: Green parametrized + integration suites, updated docs, coverage ≥95%.
</objective>

<execution_context>
@/Users/mariomeyer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomeyer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-manitoba-government-open-data/18-RESEARCH.md
@.planning/phases/18-manitoba-government-open-data/18-VALIDATION.md
@.planning/phases/18-manitoba-government-open-data/18-01-SUMMARY.md
@.planning/phases/18-manitoba-government-open-data/18-02-SUMMARY.md
@.planning/phases/18-manitoba-government-open-data/18-06-SUMMARY.md
@.planning/phases/18-manitoba-government-open-data/18-07-SUMMARY.md
@CLAUDE.md
@.claude/rules/tests.md
@.claude/rules/modules.md

# Reference: Alberta closing plan (parametrized + integration + docs)
@src/mcp_canada/modules/alberta/__tests__/test_tools.py
@tests/integration/test_tool_scenarios.py
@tests/integration/test_prompts_resources_scenarios.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parametrized envelope/lang tests + integration scenarios</name>
  <files>
    src/mcp_canada/modules/manitoba/__tests__/test_tools.py,
    tests/integration/test_tool_scenarios.py,
    tests/integration/test_prompts_resources_scenarios.py
  </files>
  <behavior>
    - TestManitobaEnvelopes: parametrize over the full list of manitoba_ tools; each (with mocked client) returns make_response with _meta.source.api set. Use AsyncMock for client functions.
    - TestManitobaLangParam: parametrize over all tools; lang="fr" passes through to envelope _meta.lang.
    - TestManitobaToolScenarios (integration, @pytest.mark.integration): wire providers + BM25 transform per .claude/rules/tests.md; call tools through client.call_tool('call_tool', {...}). Scenarios as natural-language prompts: "current Manitoba flood alerts" (empty list OK), "Manitoba provincial parks", "Manitoba surgical wait times for cardiac surgery", "Manitoba drought status", discover_tools finds manitoba_get_flood_alerts. Error: invalid f_type → structured error. 511: assert NOT_CONFIGURED when no key (or live if key present).
    - TestManitobaPromptsResources (integration): list_prompts() includes the 6 manitoba_ prompts; read_resource() returns valid content for the 7 resources.
  </behavior>
  <action>
    Fill the parametrized TestManitobaEnvelopes + TestManitobaLangParam in test_tools.py (mirror alberta's Plan 09). Append TestManitobaToolScenarios to tests/integration/test_tool_scenarios.py and TestManitobaPromptsResources to tests/integration/test_prompts_resources_scenarios.py — call through the MCP Client (NOT client functions directly). Assert on response shape, not specific values (data changes daily). Use long timeouts (30s). Handle flaky APIs (check r.content before .json()). Mark integration tests @pytest.mark.integration (skipped by default). For 511, skip live scenario if MANITOBA_511_KEY absent; always test NOT_CONFIGURED via the MCP layer.
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/manitoba/__tests__/test_tools.py::TestManitobaEnvelopes src/mcp_canada/modules/manitoba/__tests__/test_tools.py::TestManitobaLangParam -x -v</automated>
  </verify>
  <done>Parametrized envelope/lang tests green over every manitoba_ tool. Integration scenario classes appended and collectable (run with -m integration). Empty-flood-alert + 511 NOT_CONFIGURED scenarios present.</done>
</task>

<task type="auto">
  <name>Task 2: Docs sync (README/MODULES/CLAUDE/EXAMPLES) + coverage gate</name>
  <files>
    README.md,
    docs/MODULES.md,
    CLAUDE.md,
    EXAMPLES.md
  </files>
  <action>
    - README.md: add a Manitoba section to the Tool Catalog listing every manitoba_ tool with the first line of its docstring; update the header tool count (add the new Manitoba tools to the current total) and the API/module count. Add the OpenMB licence attribution note (similar to other provincial sections).
    - docs/MODULES.md: add a Manitoba entry (portal = geoportal.gov.mb.ca ArcGIS Hub org mMUesHYPkXjaFGfS, tool list, prompts/resources).
    - CLAUDE.md: add Manitoba to the ArcGIS Hub row of the Portal Technologies table ("First used Phase 14 York Region; Phase 17 Alberta; Phase 18 Manitoba"). Note the 511 v3 key-required quirk and the data.manitoba.ca/MLI deprecation in the appropriate place.
    - EXAMPLES.md: add one Manitoba cross-module SQL example (e.g., fetch surgical wait times or livestock prices, store to datastore, JOIN with StatCan).
    Keep counts exact — recount manitoba_ tools from tools.py before editing the README header.
    Then run the full coverage gate.
  </action>
  <verify>
    <automated>uv run pytest --cov=src/mcp_canada --cov-fail-under=95 -q</automated>
  </verify>
  <done>README/MODULES/CLAUDE/EXAMPLES updated and in sync with the implemented tool set. Tool count accurate. Coverage ≥95%. Full unit suite green.</done>
</task>

</tasks>

<verification>
1. `uv run pytest --cov=src/mcp_canada --cov-fail-under=95 -q` — coverage gate passes
2. `uv run pytest tests/integration/ -v -m integration --timeout=120 -k "Manitoba"` — integration green (511 live skipped if no key)
3. `grep -c "manitoba_" README.md` — Manitoba tools listed in catalog
4. `uv run ruff check src/mcp_canada/modules/manitoba/` and `uv run pyright src/mcp_canada/modules/manitoba/` — clean
</verification>

<success_criteria>
- MB-18 complete: every manitoba_ tool envelope/lang verified (parametrized) + discoverable via discover_tools through MCP Client
- Integration scenarios pass (incl. empty-flood-alert + 511 NOT_CONFIGURED)
- 6 prompts + 7 resources verified via MCP integration
- README/MODULES/CLAUDE/EXAMPLES in sync, tool count accurate
- Coverage ≥95%, full suite green
</success_criteria>

<output>
Create `.planning/phases/18-manitoba-government-open-data/18-08-SUMMARY.md`: final tool count, coverage %, integration scenario list, docs updated, MB-01…MB-18 matrix status, any deferred items (e.g., 511 live integration).
</output>
