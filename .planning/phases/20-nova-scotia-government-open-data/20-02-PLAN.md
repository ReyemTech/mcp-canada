---
phase: 20-nova-scotia-government-open-data
plan: 02
type: execute
wave: 1
depends_on: [20-01]
files_modified:
  - src/mcp_canada/modules/nova_scotia/client.py
  - src/mcp_canada/modules/nova_scotia/tools.py
  - src/mcp_canada/modules/nova_scotia/__tests__/test_client.py
  - src/mcp_canada/modules/nova_scotia/__tests__/test_tools.py
autonomous: true
requirements:
  - NS-02
  - NS-03
  - NS-04
  - NS-05
  - NS-06
must_haves:
  truths:
    - "ns_search_datasets queries the Socrata catalog (/api/catalog/v1?domains=data.novascotia.ca) by keyword with pagination, sending {domains, q, limit, only} (offset only when >0), returning flat dataset summaries + resultSetSize total"
    - "ns_get_dataset_details returns a dataset's schema columns, attribution, and license from /api/views/{id}.json"
    - "ns_query_dataset runs a SoQL query against /resource/{id}.json with $where/$select/$order/$limit/$offset/$q/$group; geometry inclusion controlled by an include_geometry param that injects/omits the_geom in $select"
    - "ns_list_organizations returns Nova Scotia publishers/attributions derived from catalog owner/attribution/domain_metadata"
    - "ns_list_categories uses q= + client-side classification.domain_category aggregation (NOT the broken categories= param) and returns 20+ categories incl. 'Fishing and Aquaculture'"
    - "TestSharedApiGetContract is filled: patches mcp_canada.modules.nova_scotia.client.socrata and asserts the module sends the correct catalog/SoQL params via socrata.* (the Manitoba lesson at the module layer); offset/$offset omitted at 0"
    - "Every discovery tool: standalone @tool, lang Literal en/fr, make_response/make_error, ns_ prefix, single-line Use for: + 8+ Keywords:; list_categories docstring notes the categories= param is broken and this tool uses q=+client-side filtering"
  artifacts:
    - path: src/mcp_canada/modules/nova_scotia/tools.py
      provides: "5 discovery @tool functions (ns_search_datasets, ns_get_dataset_details, ns_query_dataset, ns_list_organizations, ns_list_categories)"
      contains: "def ns_search_datasets"
    - path: src/mcp_canada/modules/nova_scotia/client.py
      provides: "5 discovery client bodies (fetch_search_datasets, fetch_dataset_details, fetch_query_dataset, fetch_organizations, fetch_categories)"
      contains: "domain_category"
  key_links:
    - from: "src/mcp_canada/modules/nova_scotia/client.py"
      to: "mcp_canada.shared.socrata.search_catalog / query_dataset / get_dataset_metadata"
      via: "discovery client bodies call the shared SODA client with app_token=APP_TOKEN"
      pattern: "socrata\\.(search_catalog|query_dataset|get_dataset_metadata)"
    - from: "src/mcp_canada/modules/nova_scotia/client.py"
      to: "classification.domain_category (client-side category filter)"
      via: "fetch_categories aggregates domain_category from results (categories= param is broken)"
      pattern: "domain_category"
---

<objective>
Implement the 5 Socrata catalog discovery tools (NS-02…NS-06) — the entry point for everything not curated: agents search data.novascotia.ca, inspect a dataset's schema, and run arbitrary SoQL against any dataset via ns_query_dataset.

CRITICAL workarounds baked in from the first commit:
- The catalog `categories=` param is BROKEN (returns 0 live). `ns_list_categories` MUST use `q=` (or a no-q full fetch) + client-side aggregation of `classification.domain_category`. Pin this with a unit test (no `categories=` in outgoing params) and a live test in Plan 07.
- Discovery uses `/api/catalog/v1` (NOT `api/views.json`).
- `offset`/`$offset` are omitted when 0 (handled in shared/socrata.py; assert it stays that way at the module layer).

Purpose: Establish the Socrata discovery surface and fill TestSharedApiGetContract so the param contract is enforced from the first wave.
Output: 5 working discovery tools + client bodies + unit tests.
</objective>

<execution_context>
@/Users/mariomeyer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomeyer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-nova-scotia-government-open-data/20-RESEARCH.md
@.planning/phases/20-nova-scotia-government-open-data/20-01-SUMMARY.md
@CLAUDE.md
@.claude/rules/modules.md
@.claude/rules/tests.md

# Reference: discovery-tool shape (search/details/query/orgs/categories) + module-local contract test
@src/mcp_canada/modules/saskatchewan/client.py
@src/mcp_canada/modules/saskatchewan/tools.py
@src/mcp_canada/modules/saskatchewan/__tests__/test_client.py
@src/mcp_canada/shared/socrata.py

<interfaces>
<!-- shared/socrata.py public surface (built in Plan 01). Module-local alias: `from mcp_canada.shared import socrata`.
       socrata.search_catalog(domain, q="", limit=10, offset=0, only="datasets", *, app_token=None, httpx_client=None) -> dict {results, resultSetSize}
       socrata.get_dataset_metadata(domain, dataset_id, *, app_token=None, httpx_client=None) -> dict (flat: columns, attribution, license_name...)
       socrata.query_dataset(domain, dataset_id, where=None, select=None, order=None, limit=1000, offset=0, q=None, group=None, *, app_token=None, httpx_client=None) -> list[dict]
       socrata.shape_catalog_result(result) -> dict (id, name, description, category, tags, department, permalink, ...)
     Client stubs (signatures LOCKED in Plan 01): fetch_search_datasets(query="", limit=10, offset=0),
       fetch_dataset_details(dataset_id), fetch_query_dataset(dataset_id, where=None, select=None, order=None, limit, offset=0, q=None, group=None, include_geometry=False),
       fetch_organizations(), fetch_categories().
     APP_TOKEN = os.environ.get("NS_APP_TOKEN") at module scope — pass app_token=APP_TOKEN to every socrata.* call.
     Patch point for the module-local contract test: mcp_canada.modules.nova_scotia.client.socrata (AsyncMock its methods). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Discovery client bodies + TestSharedApiGetContract + param-regression tests</name>
  <files>
    src/mcp_canada/modules/nova_scotia/client.py,
    src/mcp_canada/modules/nova_scotia/__tests__/test_client.py
  </files>
  <behavior>
    - fetch_search_datasets(query="", limit=10, offset=0): calls socrata.search_catalog(BASE_DOMAIN, q=query, limit=clamp(limit,1,1000), offset=offset, only="datasets", app_token=APP_TOKEN); returns ({"results":[shape_catalog_result(r) for r in results], "total": resultSetSize}, was_cached) at CACHE_TTL_SEARCH.
    - fetch_dataset_details(dataset_id): calls socrata.get_dataset_metadata(...); returns ({"details": {...flat columns/attribution/license_name}}, was_cached) at CACHE_TTL_META.
    - fetch_query_dataset(dataset_id, where, select, order, limit, offset, q, group, include_geometry=False): when include_geometry is False AND select is None, leave select None (Socrata returns all incl. the_geom — document that generic query returns geometry unless agent uses $select); when include_geometry is False AND select provided, pass select through unchanged. Calls socrata.query_dataset(...); returns ({"rows": rows, "count": len(rows), "truncated": len(rows) >= limit}, was_cached) at CACHE_TTL_SEARCH.
    - fetch_organizations(): fetches a wide catalog page (limit high, q omitted/blank), aggregates unique owner.display_name / attribution into a sorted list with dataset counts; returns ({"organizations":[{"name":..,"dataset_count":..}]}, was_cached) at CACHE_TTL_META.
    - fetch_categories(): fetches catalog results (q blank/wide), aggregates classification.domain_category into sorted unique list with counts; NEVER sends categories= param; returns ({"categories":[{"name":..,"count":..}]}, was_cached) at CACHE_TTL_META.
    - TestSharedApiGetContract: patch nova_scotia.client.socrata; assert fetch_search_datasets forwards q/limit/offset/only and app_token to socrata.search_catalog; assert fetch_categories never passes a categories= argument and reads domain_category from results.
    - Param/behavior regression (the Manitoba lesson): on fetch_search_datasets("aquaculture", offset=0) the socrata.search_catalog call has offset=0 (shared client omits it from the wire); on offset=10 it forwards offset=10. fetch_categories aggregates from classification.domain_category, returns >=1 category from the fixture.
  </behavior>
  <action>
    Fill the 5 discovery client stubs from Plan 01. Use socrata.* (the shared SODA client) for all catalog/resource
    calls, always passing app_token=APP_TOKEN. Use CACHE_KEY_PREFIX for cache keys and await _limiter.acquire()
    before each network call (wrap socrata calls in the fetcher closure passed to cached_fetch). Flatten catalog
    entries with socrata.shape_catalog_result. For fetch_categories/fetch_organizations, aggregate client-side from
    the catalog results (the categories= param is broken — never send it). Clamp limit to [1, 1000].

    Fill TestSharedApiGetContract + the 5 discovery client test classes. Patch socrata at
    mcp_canada.modules.nova_scotia.client.socrata (module-local — mirror the saskatchewan/BC/Alberta module-local
    contract pattern). Use AsyncMock(return_value=...) for socrata.search_catalog/query_dataset/get_dataset_metadata.
    Cover happy path + the param-forwarding assertions + the "no categories= ever" assertion + the domain_category
    aggregation. Use the conftest fixtures (sample_catalog_response, sample_views_metadata) from Plan 01.
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/nova_scotia/__tests__/test_client.py::TestSharedApiGetContract src/mcp_canada/modules/nova_scotia/__tests__/test_client.py::TestNsSearchDatasets src/mcp_canada/modules/nova_scotia/__tests__/test_client.py::TestNsGetDatasetDetails src/mcp_canada/modules/nova_scotia/__tests__/test_client.py::TestNsQueryDataset src/mcp_canada/modules/nova_scotia/__tests__/test_client.py::TestNsListOrganizations src/mcp_canada/modules/nova_scotia/__tests__/test_client.py::TestNsListCategories -x -v</automated>
  </verify>
  <done>5 discovery client functions implemented against shared/socrata.py with app_token passthrough. TestSharedApiGetContract + 5 discovery client test classes pass, including the param-forwarding assertions and the categories=-is-never-sent assertion. fetch_categories aggregates classification.domain_category client-side.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 5 discovery @tool functions + tool tests</name>
  <files>
    src/mcp_canada/modules/nova_scotia/tools.py,
    src/mcp_canada/modules/nova_scotia/__tests__/test_tools.py
  </files>
  <behavior>
    - ns_search_datasets(query="", limit=10, offset=0, lang="en"): make_response with _meta source api="nova-scotia-socrata", api_url=CATALOG_URL; data has results+total+offset+limit.
    - ns_get_dataset_details(dataset_id, lang="en"): details incl. columns/attribution/license_name.
    - ns_query_dataset(dataset_id, where=None, select=None, order=None, limit=1000, offset=0, q=None, group=None, include_geometry=False, lang="en"): routed SoQL payload (rows/count/truncated).
    - ns_list_organizations(lang="en") / ns_list_categories(lang="en"): list payloads.
    - Each maps client exceptions to make_error (UPSTREAM_ERROR; NOT_FOUND on 404-shaped errors).
  </behavior>
  <action>
    Add the 5 @tool functions. Standalone `@tool` from fastmcp.tools (NEVER @mcp.tool). Every tool:
    lang: Literal["en","fr"] = "en", returns make_response(...)/make_error(...), ns_ prefix, single-line docstring
    with "Use for:" + "Keywords:" (8+ terms). In ns_search_datasets docstring note this is the Government of Nova
    Scotia Socrata portal (data.novascotia.ca). In ns_list_categories docstring note the catalog categories= param
    is broken and this tool aggregates classification.domain_category from results. In ns_query_dataset docstring
    note that geometry (the_geom) is returned unless the agent narrows $select, and give a SoQL example
    ($where=county='Halifax', $select=..., $limit=). Patch the client at the tools namespace
    (mcp_canada.modules.nova_scotia.tools._client.*) in tests. Fill the 5 discovery tool test classes
    (happy path + error path + lang passthrough to envelope).
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/nova_scotia/__tests__/test_tools.py -x -k "SearchDatasets or GetDatasetDetails or QueryDataset or ListOrganizations or ListCategories" -v</automated>
    <automated>uv run pytest src/mcp_canada/modules/nova_scotia/__tests__/ -q 2>&1 | tail -10</automated>
  </verify>
  <done>5 discovery tools defined and tested. Envelope has _meta source api="nova-scotia-socrata"; lang passes through; errors return make_error not exceptions. test_quality docstring checks pass (Use for: + 8+ Keywords:).</done>
</task>

</tasks>

<verification>
1. `uv run pytest src/mcp_canada/modules/nova_scotia/__tests__/ -x` — green
2. `uv run python -c "from mcp_canada.modules.nova_scotia import tools; assert hasattr(tools,'ns_search_datasets') and hasattr(tools,'ns_query_dataset') and hasattr(tools,'ns_list_categories'); print('OK')"`
3. `uv run pyright src/mcp_canada/modules/nova_scotia/` — clean
</verification>

<success_criteria>
- NS-02…NS-06 implemented: 5 discovery tools callable
- TestSharedApiGetContract green (module forwards correct Socrata params; categories= never sent)
- ns_list_categories aggregates classification.domain_category client-side (the broken-param workaround)
- ns_query_dataset exposes full SoQL surface incl. include_geometry control
- All discovery tools follow conventions (standalone @tool, lang, envelope, ns_ prefix, Use-for + 8+ Keywords)
</success_criteria>

<output>
After completion, create `.planning/phases/20-nova-scotia-government-open-data/20-02-SUMMARY.md`: list the 5 tools, confirm the socrata param contract + categories= workaround, the api_name used (nova-scotia-socrata), and the ns_query_dataset geometry-control behavior.
</output>
