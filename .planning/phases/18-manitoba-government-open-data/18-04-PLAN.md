---
phase: 18-manitoba-government-open-data
plan: 04
type: execute
wave: 3
depends_on: [18-03]
files_modified:
  - src/mcp_canada/modules/manitoba/client.py
  - src/mcp_canada/modules/manitoba/tools.py
  - src/mcp_canada/modules/manitoba/__tests__/test_client.py
  - src/mcp_canada/modules/manitoba/__tests__/test_tools.py
autonomous: true
requirements:
  - MB-10
  - MB-11
  - MB-12
  - MB-13
must_haves:
  truths:
    - "manitoba_get_drought_status returns D0-D4 drought intensity polygons filtered to Manitoba by default bbox (Pitfall 8 — not all of North America)"
    - "manitoba_get_ag_weather_stations returns 100+ agricultural weather station points with AgRegion and per-station URL to live hourly readings, with optional AgRegion filter"
    - "manitoba_get_livestock_prices returns weekly cattle OR hog prices dispatched by livestock param, with optional historical flag"
    - "manitoba_get_crop_regions returns bilingual crop reporting region polygons (REGION/RÉGION)"
    - "All four use arcgis_hub.query_feature_service wrapped in cached_fetch with appropriate TTLs (drought STATIC weekly, others META)"
  artifacts:
    - path: src/mcp_canada/modules/manitoba/tools.py
      provides: "manitoba_get_drought_status, manitoba_get_ag_weather_stations, manitoba_get_livestock_prices, manitoba_get_crop_regions"
      contains: "def manitoba_get_livestock_prices"
    - path: src/mcp_canada/modules/manitoba/client.py
      provides: "fetch_drought_status, fetch_ag_weather_stations, fetch_livestock_prices, fetch_crop_regions bodies"
  key_links:
    - from: "src/mcp_canada/modules/manitoba/client.py"
      to: "Canada_USA_Drought_Monitor FeatureServer"
      via: "query_feature_service with MANITOBA_BBOX geometry/envelope filter"
      pattern: "DROUGHT_MONITOR_FS_URL|MANITOBA_BBOX"
    - from: "src/mcp_canada/modules/manitoba/client.py"
      to: "MB_Cattle_Prices_Current_year + hog prices FeatureServers"
      via: "fetch_livestock_prices dispatch by livestock param"
      pattern: "CATTLE_PRICES_FS_URL|HOG_PRICES_FS_URL"
---

<objective>
Implement Manitoba's agriculture & drought domain (MB-10…MB-13) — the substitute for the dropped Manitoba Hydro/energy domain. Prairie-distinguishing data: drought monitor, ag weather stations, livestock (cattle/hog) prices, crop reporting regions.

Purpose: Curate the Manitoba-distinguishing prairie ag/markets data.
Output: 4 agriculture/drought tools + client bodies + unit tests.
</objective>

<execution_context>
@/Users/mariomeyer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomeyer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-manitoba-government-open-data/18-RESEARCH.md
@.planning/phases/18-manitoba-government-open-data/18-SPIKE.md
@.planning/phases/18-manitoba-government-open-data/18-01-SUMMARY.md
@CLAUDE.md
@.claude/rules/modules.md

@src/mcp_canada/modules/alberta/client.py
@src/mcp_canada/modules/alberta/tools.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Agriculture/drought client bodies + tests</name>
  <files>
    src/mcp_canada/modules/manitoba/client.py,
    src/mcp_canada/modules/manitoba/__tests__/test_client.py
  </files>
  <behavior>
    - fetch_drought_status(filter_province=True, max_records): query DROUGHT_MONITOR_FS_URL layer 0, out_fields DM,OBS_DATE,SOURCE; when filter_province → apply Manitoba bbox via geometry/geometryType=esriGeometryEnvelope + spatialRel=esriSpatialRelIntersects (MANITOBA_BBOX). Returns {features,count,truncated} at CACHE_TTL_STATIC (weekly source update).
    - fetch_ag_weather_stations(ag_region=None, max_records): query AG_WEATHER_STATIONS_FS_URL; where=f"AgRegion='{ag_region}'" if given; out_fields StnName,LatDD,LongDD,Elevation,AgRegion,URL. CACHE_TTL_META.
    - fetch_livestock_prices(livestock="cattle", historical=False, max_records): dispatch URL by livestock (cattle→CATTLE_PRICES_FS_URL; hog→HOG_PRICES_FS_URL from spike); validate livestock in {"cattle","hog"} else ValueError. out_fields week,Auction,Parameter,Measure,Value. CACHE_TTL_META. If hog FS URL UNRESOLVED in spike, degrade gracefully (UPSTREAM_ERROR/empty) — documented.
    - fetch_crop_regions(max_records, include_geometry=False): query CROP_REGIONS_FS_URL; bilingual REGION/RÉGION fields. CACHE_TTL_STATIC.
  </behavior>
  <action>
    Fill the 4 agriculture/drought client stubs. For the drought bbox filter, the query_feature_service signature does not take a geometry param — pass the spatial filter by extending the where clause is NOT possible for bbox, so either (a) add a thin direct api_get query against the FeatureServer /query endpoint with geometry params for drought only, OR (b) fetch all + client-side filter by Manitoba bbox if the layer is small. Choose based on the live layer size noted in research (national coverage → prefer geometry-param query via a small local helper). Document the choice. Validate livestock param (ValueError on invalid). Fill TestManitobaGetDroughtStatus, TestManitobaGetAgWeatherStations, TestManitobaGetLivestockPrices (both cattle+hog + invalid), TestManitobaGetCropRegions. Use AsyncMock; patch query_feature_service / api_get as appropriate.
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/manitoba/__tests__/test_client.py -x -k "Drought or AgWeather or Livestock or CropRegions" -v</automated>
  </verify>
  <done>4 client functions implemented. Drought filtered to Manitoba by default. Livestock dispatches cattle/hog + validates. Crop regions bilingual.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 4 agriculture/drought @tool functions + tool tests</name>
  <files>
    src/mcp_canada/modules/manitoba/tools.py,
    src/mcp_canada/modules/manitoba/__tests__/test_tools.py
  </files>
  <behavior>
    - manitoba_get_drought_status(filter_province=True, lang="en"): D0-D4 polygons; docstring notes default Manitoba bbox filter.
    - manitoba_get_ag_weather_stations(ag_region=None, lang="en"): stations + URL to live readings.
    - manitoba_get_livestock_prices(livestock="cattle", historical=False, lang="en"): invalid livestock → make_error("INVALID_INPUT", valid=["cattle","hog"]).
    - manitoba_get_crop_regions(lang="en"): bilingual region polygons.
  </behavior>
  <action>
    Add the 4 @tool functions following conventions (standalone @tool, lang, make_response/make_error, manitoba_ prefix, single-line Use for: + 8+ Keywords:). ag-weather docstring: URL field links to live hourly data per station (agrimaps.gov.mb.ca). drought docstring: Pitfall 8 — Manitoba-filtered by default. Fill the 4 tool test classes (happy + INVALID_INPUT for livestock + lang passthrough).
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/manitoba/__tests__/test_tools.py -x -k "Drought or AgWeather or Livestock or CropRegions" -v</automated>
  </verify>
  <done>4 tools defined + tested. Livestock INVALID_INPUT path covered. Drought Manitoba-filter documented.</done>
</task>

</tasks>

<verification>
1. `uv run pytest src/mcp_canada/modules/manitoba/__tests__/ -x`
2. `uv run python -c "from mcp_canada.modules.manitoba import tools; assert hasattr(tools,'manitoba_get_livestock_prices') and hasattr(tools,'manitoba_get_drought_status'); print('OK')"`
</verification>

<success_criteria>
- MB-10…MB-13 implemented
- Drought filtered to Manitoba by default (not continental)
- Livestock cattle/hog dispatch + validation
- Crop regions bilingual
</success_criteria>

<output>
Create `.planning/phases/18-manitoba-government-open-data/18-04-SUMMARY.md`: 4 tools, drought bbox-filter approach chosen, hog FS URL resolution status, any AgRegion value list discovered.
</output>
