---
phase: 20.1
plan: "03"
subsystem: tests/integration (BC)
tags: [de-masking, bc, pinned-fixtures]
key_files:
  modified: [tests/integration/test_tool_scenarios.py]
decisions:
  - "Pinned two canonical BCDC package ids rather than discovering by search (D-11)"
  - "A vanished canonical dataset should fail loudly, not skip"
commits: [78c4e16]
metrics:
  duration: "25min"
  completed_date: "2026-07-25"
---

# Plan 20.1-03: De-mask BC

BC was never in quick task 1's scope. Its 5 `pytest.skip` calls were the worse
half — both WFS-routing tests skipped when a BCDC search came back empty, so the
routing path under test was frequently never exercised and nothing distinguished
that from a pass.

Replaced with pinned ids: `22c7cb44` (BC Wildfire Fire Perimeters - Historical,
WFS-queryable) and `7ec1a555` (BC Greenhouse Gas Emissions, file resources).
Added a test asserting the two-step CKAN→WFS contract directly — details must
carry `queryable_via_wfs` and `object_name`.

Tightened empties that were silently acceptable: 2023 fire perimeters (a record
season), PROVINCIAL PARK designation, and mineral tenure must all return rows.
