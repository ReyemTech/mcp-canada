---
phase: 19-saskatchewan-government-open-data
plan: 06
type: execute
wave: 5
depends_on: [19-05]
files_modified:
  - src/mcp_canada/modules/saskatchewan/prompts.py
  - src/mcp_canada/modules/saskatchewan/resources.py
  - src/mcp_canada/modules/saskatchewan/__tests__/test_prompts_resources.py
autonomous: true
requirements:
  - SK-15
must_haves:
  truths:
    - "6 bilingual @prompt functions exist (3 guided list[Message] + 3 quick-lookup str) with saskatchewan_ prefix and lang param, chaining the 14 implemented tools"
    - "7 zero-parameter @resource functions exist using data://, docs://, template:// URI schemes with bilingual content inline"
    - "All prompts/resources use standalone fastmcp decorators (NEVER @mcp.*) and are auto-discovered by FileSystemProvider"
    - "Resources have ZERO parameters (no lang param — would promote to ResourceTemplate and drop from resources/list)"
    - "docs://saskatchewan/portal-guide documents the multi-org architecture (geohub primary org zcv98lgAl8xQ04cW + WSA org 7MBdlVpjqbfBhQer + SPSA egis), the deferred transport (511 key-gated) and health domains, the Petroleum-FeatureServer-400 discovery-only routing, and the GOS Standard Unrestricted Use Data Licence v2.0"
  artifacts:
    - path: src/mcp_canada/modules/saskatchewan/prompts.py
      provides: "6 @prompt functions (3 guided + 3 quick lookups)"
      contains: "def saskatchewan_explore_agriculture"
    - path: src/mcp_canada/modules/saskatchewan/resources.py
      provides: "7 @resource functions (data://, docs://, template://)"
      contains: "data://saskatchewan/crop-regions"
  key_links:
    - from: "src/mcp_canada/modules/saskatchewan/prompts.py"
      to: "FileSystemProvider prompts/list"
      via: "standalone @prompt from fastmcp.prompts"
      pattern: "@prompt"
    - from: "src/mcp_canada/modules/saskatchewan/resources.py"
      to: "FileSystemProvider resources/list"
      via: "standalone @resource, zero-parameter functions"
      pattern: "@resource"
---

<objective>
Add Saskatchewan's prompts and resources (Phase 40 pattern) — 6 bilingual prompts and 7 zero-parameter resources, scaled to the shipped 14-tool surface. Guided prompts chain the new tools for agriculture/mining, environment/wildfire, and water workflows; quick lookups guide single-tool calls. Resources embed Saskatchewan reference data (crop regions, major river basins, single SHA health authority) and guidance docs/templates — including a portal guide that explains the fragmented multi-org architecture and the deferred domains.

Purpose: Native MCP discoverability — prompts as slash-commands, resources as reference data agents read repeatedly.
Output: prompts.py (6) + resources.py (7) + tests.
</objective>

<execution_context>
@/Users/mariomeyer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomeyer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-saskatchewan-government-open-data/19-RESEARCH.md
@.planning/phases/19-saskatchewan-government-open-data/19-CONTEXT.md
@.planning/phases/19-saskatchewan-government-open-data/19-SPIKE.md
@CLAUDE.md

# Reference: prompts/resources pattern
@src/mcp_canada/modules/manitoba/prompts.py
@src/mcp_canada/modules/manitoba/resources.py
@src/mcp_canada/modules/alberta/resources.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 6 bilingual prompts</name>
  <files>
    src/mcp_canada/modules/saskatchewan/prompts.py,
    src/mcp_canada/modules/saskatchewan/__tests__/test_prompts_resources.py
  </files>
  <action>
    Add 6 @prompt functions (standalone @prompt from fastmcp.prompts — NEVER @mcp.prompt). Each
    takes lang: Annotated[Literal["en","fr"], "Language: 'en' or 'fr'"] = "en" and has a docstring
    describing when to use it. Use saskatchewan_ prefix.

    Guided workflows (return list[Message], user + assistant roles, >=2 messages, name specific tools):
    - saskatchewan_explore_agriculture — chain saskatchewan_get_crop_yields → saskatchewan_get_grain_elevators → saskatchewan_get_mineral_mines (the ag + resource economy)
    - saskatchewan_explore_environment — chain saskatchewan_get_fire_bans → saskatchewan_get_historic_wildfires → saskatchewan_get_air_quality
    - saskatchewan_explore_water — chain saskatchewan_get_wsa_stations → saskatchewan_get_wsa_reservoirs (note HyperLink_Graph for live readings)

    Quick lookups (return str with tool name + param instructions):
    - saskatchewan_quick_dataset_search — guide saskatchewan_search_datasets
    - saskatchewan_fire_ban_status_now — guide saskatchewan_get_fire_bans (mention ban_scope dispatch + empty=no-bans)
    - saskatchewan_crop_yield_lookup — guide saskatchewan_get_crop_yields (mention provincial vs 5 regions)

    Fill TestSaskPrompts: assert 6 prompts discoverable, bilingual (en/fr produce different content),
    guided ones return list[Message], quick ones return str.
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/saskatchewan/__tests__/test_prompts_resources.py::TestSaskPrompts -x -v</automated>
  </verify>
  <done>6 prompts defined (3 guided + 3 quick), bilingual, standalone @prompt, saskatchewan_ prefix. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: 7 zero-parameter resources</name>
  <files>
    src/mcp_canada/modules/saskatchewan/resources.py,
    src/mcp_canada/modules/saskatchewan/__tests__/test_prompts_resources.py
  </files>
  <action>
    Add 7 @resource functions (standalone @resource from fastmcp.resources — NEVER @mcp.resource).
    ZERO parameters each (no lang — would promote to ResourceTemplate). Type-prefixed + module-
    prefixed URIs. Use the reference data tables from 19-RESEARCH.md "Saskatchewan-Specific Reference
    Data":
    - data://saskatchewan/crop-regions — the 5 crop reporting regions (Southeast/Southwest/Central/Northeast/Northwest) with location + signature crops, bilingual labels (json.dumps, en/fr inline)
    - data://saskatchewan/major-basins — the 6 major river basins (Qu'Appelle, North/South Saskatchewan, Assiniboine, Churchill, Athabasca) with major river + WSA-monitored flag, bilingual
    - data://saskatchewan/health-regions — the single Saskatchewan Health Authority (SHA, province-wide since 2017 merger) with the note that no public SHA facility FeatureServer exists (health deferred), bilingual
    - docs://saskatchewan/portal-guide — markdown: geohub.saskatchewan.ca ArcGIS Hub multi-org architecture (primary org zcv98lgAl8xQ04cW + WSA org 7MBdlVpjqbfBhQer + SPSA gis.saskatchewan.ca/egis), which tool to use for what, the data.saskatchewan.ca-does-not-exist note, the deferred transport (511 key-gated, no stubs) + health domains, the Petroleum-FeatureServer-400 discovery-only routing, the WSA_Reservoirs layer-26 quirk, and the GOS Standard Unrestricted Use Data Licence v2.0 (egis@gov.sk.ca)
    - docs://saskatchewan/agriculture-data-guide — markdown: crop-yield estimates (bu/acre) vs the PDF-only weekly crop reports; the Crop_Production_2025 boundary-only caveat; grain-elevator PR='SK' filtering; mineral mine dispatch
    - template://saskatchewan/dataset-report — markdown template with {placeholder} fields
    - template://saskatchewan/wildfire-report — markdown template with {placeholder} fields (fire ban + air quality + historic wildfire summary)

    data:// resources must return valid JSON (json.dumps), bilingual content inline. Fill
    TestSaskResources: assert 7 resources discoverable via resources/list, data:// return valid JSON,
    str(r.uri) containment checks (AnyUrl→str), zero-parameter (in resources/list not
    ResourceTemplate).
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/saskatchewan/__tests__/test_prompts_resources.py::TestSaskResources -x -v</automated>
  </verify>
  <done>7 resources defined (3 data + 2 docs + 2 template), zero-parameter, valid JSON for data://, bilingual inline. portal-guide documents the multi-org architecture + deferred domains + licence. Tests pass.</done>
</task>

</tasks>

<verification>
1. `uv run pytest src/mcp_canada/modules/saskatchewan/__tests__/test_prompts_resources.py -x -v`
2. `uv run python -c "from mcp_canada.modules.saskatchewan import prompts, resources; print('OK')"`
</verification>

<success_criteria>
- 6 prompts (3 guided + 3 quick) bilingual, standalone @prompt, saskatchewan_ prefix
- 7 resources zero-parameter, type-prefixed URIs, bilingual data:// JSON
- portal-guide documents multi-org architecture, deferred transport+health, Petroleum 400 routing, layer-26 quirk, GOS licence
- All auto-discoverable by FileSystemProvider (no server.py changes)
</success_criteria>

<output>
Create `.planning/phases/19-saskatchewan-government-open-data/19-06-SUMMARY.md`: list the 6 prompts + 7 resources with URIs, confirm zero-parameter resource compliance, note the deferred-domain documentation placement.
</output>
