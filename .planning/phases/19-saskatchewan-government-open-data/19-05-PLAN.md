---
phase: 19-saskatchewan-government-open-data
plan: 05
type: execute
wave: 4
depends_on: [19-04]
files_modified:
  - src/mcp_canada/modules/saskatchewan/client.py
  - src/mcp_canada/modules/saskatchewan/tools.py
  - src/mcp_canada/modules/saskatchewan/__tests__/test_client.py
  - src/mcp_canada/modules/saskatchewan/__tests__/test_tools.py
autonomous: true
requirements:
  - SK-13
  - SK-14
must_haves:
  truths:
    - "saskatchewan_get_wsa_stations queries the WSA org (services1 / 7MBdlVpjqbfBhQer) Hydrometric_Gauging_Stations_V2 layer 0 with where=Province='SK'; optional basin filter; HyperLink_Graph (live-reading URL) present and non-null in results"
    - "saskatchewan_get_wsa_reservoirs queries the WSA org WSA_Reservoirs FeatureServer at LAYER 26 (NOT 0 — layer 0 is empty); Reservoir_Name and Dam_Name present"
    - "Both tools use RATE_GROUP_WSA + CACHE_TTL_META, the WSA org base (NOT the primary Hub org), standalone @tool, lang en/fr, make_response/make_error, saskatchewan_ prefix, 8+ Keywords"
  artifacts:
    - path: src/mcp_canada/modules/saskatchewan/tools.py
      provides: "2 WSA water @tool functions (stations, reservoirs)"
      contains: "def saskatchewan_get_wsa_stations"
    - path: src/mcp_canada/modules/saskatchewan/client.py
      provides: "fetch_wsa_stations, fetch_wsa_reservoirs bodies"
      contains: "WSA_RESERVOIRS_LAYER"
  key_links:
    - from: "src/mcp_canada/modules/saskatchewan/client.py"
      to: "Hydrometric_Gauging_Stations_V2 FeatureServer (WSA org 7MBdlVpjqbfBhQer)"
      via: "query_feature_service WSA_STATIONS_FS_URL layer 0, where=Province='SK'"
      pattern: "WSA_STATIONS_FS_URL"
    - from: "src/mcp_canada/modules/saskatchewan/client.py"
      to: "WSA_Reservoirs FeatureServer layer 26 (WSA org)"
      via: "query_feature_service WSA_RESERVOIRS_FS_URL, layer_id=WSA_RESERVOIRS_LAYER (26)"
      pattern: "WSA_RESERVOIRS_LAYER"
---

<objective>
Implement Saskatchewan's water domain — 2 curated tools (SK-13, SK-14) against the SEPARATE WSA ArcGIS org (services1 / 7MBdlVpjqbfBhQer): hydrometric gauging stations (with HyperLink_Graph to live readings) and reservoirs. The reservoirs tool carries the single most mock-invisible bug in the phase: WSA_Reservoirs data is at LAYER 26, not layer 0 (layer 0 returns empty). The constant WSA_RESERVOIRS_LAYER=26 (set in Plan 01) must be used; the live integration test (Plan 07) is what truly proves it.

Purpose: Round out the curated surface with water infrastructure; complete the 9-curated-tool target.
Output: 2 curated tools + client bodies + unit tests.
</objective>

<execution_context>
@/Users/mariomeyer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomeyer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-saskatchewan-government-open-data/19-RESEARCH.md
@.planning/phases/19-saskatchewan-government-open-data/19-01-SUMMARY.md
@.planning/phases/19-saskatchewan-government-open-data/19-04-SUMMARY.md
@CLAUDE.md
@.claude/rules/modules.md
@.claude/rules/tests.md

# Reference: curated FeatureServer client + tool pattern
@src/mcp_canada/modules/manitoba/client.py
@src/mcp_canada/modules/alberta/client.py

<interfaces>
<!-- Live-verified (2026-06-15), WSA org services1/7MBdlVpjqbfBhQer:
     Hydrometric stations: .../Hydrometric_Gauging_Stations_V2/FeatureServer/0
       out_fields: Station_Number,Station_Name,Province,Latitude,Longitude,Major_Basin,Station_Type,Station_Class,Operated_By,HyperLink_Graph
       where="Province='SK'" default ; optional basin -> AND Major_Basin LIKE '%<basin>%'
       HyperLink_Graph e.g. https://www.wsask.ca/hydrographs/05MB006-hrly.html
     Reservoirs: .../WSA_Reservoirs/FeatureServer/26   <-- LAYER 26, NOT 0 (layer 0 empty)
       out_fields: Reservoir_Name,Dam_Name,Imagery_Date,Water_Level_MASL ; where="1=1" -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WSA client bodies + client tests</name>
  <files>
    src/mcp_canada/modules/saskatchewan/client.py,
    src/mcp_canada/modules/saskatchewan/__tests__/test_client.py
  </files>
  <behavior>
    - fetch_wsa_stations(basin=None, max_records=MAX_RECORDS): query_feature_service(WSA_STATIONS_FS_URL, WSA_STATIONS_LAYER(0), where="Province='SK'" (+ " AND Major_Basin LIKE '%<basin>%'" when set), explicit out_fields incl. HyperLink_Graph). get_limiter(RATE_GROUP_WSA, RATE_LIMIT_WSA). Returns ({"features":[...], "count":N, "truncated":bool}, was_cached) at CACHE_TTL_META.
    - fetch_wsa_reservoirs(max_records=MAX_RECORDS): query_feature_service(WSA_RESERVOIRS_FS_URL, WSA_RESERVOIRS_LAYER(26), where="1=1", explicit out_fields). get_limiter(RATE_GROUP_WSA). CACHE_TTL_META.
    Both wrap query_feature_service in cached_fetch; mirror manitoba.client curated fetchers.
  </behavior>
  <action>
    Fill the 2 client stubs. Use the WSA org constants (WSA_STATIONS_FS_URL / WSA_RESERVOIRS_FS_URL /
    WSA_RESERVOIRS_LAYER=26) — NEVER the primary Hub org base. Patch arcgis_hub.query_feature_service
    in tests. Fill TestSaskGetWSAStations (assert where="Province='SK'" + basin filter composition;
    assert HyperLink_Graph present in flattened row) and TestSaskGetWSAReservoirs (CRITICAL: assert
    query_feature_service was called with layer_id == 26 via call_args — this is the layer-ID bug
    mocks can encode at the call-arg level even though only the live test fully proves it). Assert on
    the service URL + layer + where passed to query_feature_service.
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/saskatchewan/__tests__/test_client.py::TestSaskGetWSAStations src/mcp_canada/modules/saskatchewan/__tests__/test_client.py::TestSaskGetWSAReservoirs -x -v</automated>
  </verify>
  <done>2 client functions implemented against the WSA org. Stations default to Province='SK' with optional basin filter; reservoirs query layer 26 (asserted via call_args). HyperLink_Graph present in station results. Client tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 2 WSA @tool functions + tool tests</name>
  <files>
    src/mcp_canada/modules/saskatchewan/tools.py,
    src/mcp_canada/modules/saskatchewan/__tests__/test_tools.py
  </files>
  <behavior>
    - saskatchewan_get_wsa_stations(basin=None, lang="en"): make_response; docstring notes HyperLink_Graph links to live hourly hydrographs at wsask.ca.
    - saskatchewan_get_wsa_reservoirs(lang="en"): make_response; docstring notes reservoir + dam names.
  </behavior>
  <action>
    Add the 2 @tool functions. Standalone @tool; lang Literal en/fr; make_response/make_error;
    saskatchewan_ prefix; single-line "Use for:" + 8+ "Keywords:". Use api source
    "saskatchewan-wsa" in the envelope to distinguish the WSA org. Patch client at tools namespace in
    tests. Fill the 2 tool test classes (happy + lang passthrough; assert envelope api="saskatchewan-wsa").
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/saskatchewan/__tests__/test_tools.py -x -k "WSAStations or WSAReservoirs" -v</automated>
  </verify>
  <done>2 WSA tools defined and tested. lang passes through; envelope api="saskatchewan-wsa". test_quality docstring checks pass.</done>
</task>

</tasks>

<verification>
1. `uv run pytest src/mcp_canada/modules/saskatchewan/__tests__/ -x` — green
2. `uv run python -c "from mcp_canada.modules.saskatchewan import tools; assert hasattr(tools,'saskatchewan_get_wsa_stations') and hasattr(tools,'saskatchewan_get_wsa_reservoirs'); print('OK')"`
</verification>

<success_criteria>
- SK-13, SK-14 implemented: 2 curated WSA tools callable — completes the 9 curated + 5 discovery = 14 tools
- Reservoirs query layer 26 (asserted via call_args); stations default Province='SK' with HyperLink_Graph
- Both use the WSA org base (NOT the primary Hub org)
- All tools follow conventions
</success_criteria>

<output>
After completion, create `.planning/phases/19-saskatchewan-government-open-data/19-05-SUMMARY.md`: list the 2 tools, confirm WSA org usage + layer 26 for reservoirs, the source api name (saskatchewan-wsa), and that all 14 tools (5 discovery + 9 curated) now exist.
</output>
