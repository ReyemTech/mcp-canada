---
phase: 19-saskatchewan-government-open-data
plan: 07
type: execute
wave: 6
depends_on: [19-06]
files_modified:
  - src/mcp_canada/modules/saskatchewan/__tests__/test_tools.py
  - tests/integration/test_tool_scenarios.py
  - tests/integration/test_prompts_resources_scenarios.py
  - README.md
  - docs/MODULES.md
  - CLAUDE.md
  - EXAMPLES.md
autonomous: true
requirements:
  - SK-15
must_haves:
  truths:
    - "Parametrized TestSaskEnvelopes asserts every saskatchewan_ tool returns a valid _meta envelope; TestSaskLangParam asserts lang='fr' passes through for every tool"
    - "TestSaskatchewanToolScenarios (integration, via MCP Client) hits the REAL endpoints and asserts FIELD PRESENCE + non-null values (the Manitoba lesson): 'Canola' in crop yields, HyperLink_Graph in WSA stations, AQHI in air quality, Reservoir_Name from WSA reservoirs layer 26, discover_tools finds saskatchewan_ tools, fire bans empty-is-valid, search returns numberMatched>0 via startindex pagination"
    - "TestSaskatchewanPromptsResources integration verifies the 6 prompts via list_prompts() and the 7 resources via read_resource()"
    - "README.md has a Saskatchewan section in the Tool Catalog with all 14 saskatchewan_ tools, the header tool/module/API count updated, and the GOS licence attribution; docs/MODULES.md has a Saskatchewan entry; CLAUDE.md lists Saskatchewan under the ArcGIS Hub portal technology and notes transport+health deferred; EXAMPLES.md has a Saskatchewan cross-module SQL example"
    - "Coverage >=95% across src/mcp_canada; full unit suite green; Saskatchewan live integration tests green"
  artifacts:
    - path: tests/integration/test_tool_scenarios.py
      provides: "TestSaskatchewanToolScenarios class with live field-presence assertions"
      contains: "class TestSaskatchewanToolScenarios"
    - path: README.md
      provides: "Saskatchewan tool catalog section + updated tool count"
      contains: "Saskatchewan"
  key_links:
    - from: "tests/integration/test_tool_scenarios.py"
      to: "MCP Client (discover_tools / call_tool)"
      via: "client.call_tool('call_tool', {...}) for saskatchewan_ tools, asserting field presence + non-null"
      pattern: "saskatchewan_"
---

<objective>
Close Phase 19 (SK-15): parametrized envelope/lang tests over every Saskatchewan tool, LIVE integration tests through the MCP Client layer that assert FIELD PRESENCE + non-null values (not just response shape — the Manitoba lesson, where mocks masked a live 400), documentation sync (README/MODULES/CLAUDE/EXAMPLES), and the 95% coverage gate.

Purpose: Prove all 14 Saskatchewan tools follow conventions, are discoverable through MCP, behave correctly against the REAL endpoints (including the layer-26 reservoirs and startindex pagination that only live runs can confirm), and the catalog docs stay in sync.
Output: Green parametrized + live-integration suites, updated docs, coverage >=95%.
</objective>

<execution_context>
@/Users/mariomeyer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomeyer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-saskatchewan-government-open-data/19-RESEARCH.md
@.planning/phases/19-saskatchewan-government-open-data/19-VALIDATION.md
@.planning/phases/19-saskatchewan-government-open-data/19-01-SUMMARY.md
@.planning/phases/19-saskatchewan-government-open-data/19-02-SUMMARY.md
@.planning/phases/19-saskatchewan-government-open-data/19-03-SUMMARY.md
@.planning/phases/19-saskatchewan-government-open-data/19-04-SUMMARY.md
@.planning/phases/19-saskatchewan-government-open-data/19-05-SUMMARY.md
@.planning/phases/19-saskatchewan-government-open-data/19-06-SUMMARY.md
@CLAUDE.md
@.claude/rules/tests.md
@.claude/rules/modules.md

# Reference: Manitoba/Alberta closing plan (parametrized + LIVE field-presence integration + docs)
@src/mcp_canada/modules/alberta/__tests__/test_tools.py
@tests/integration/test_tool_scenarios.py
@tests/integration/test_prompts_resources_scenarios.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parametrized envelope/lang tests + LIVE field-presence integration scenarios</name>
  <files>
    src/mcp_canada/modules/saskatchewan/__tests__/test_tools.py,
    tests/integration/test_tool_scenarios.py,
    tests/integration/test_prompts_resources_scenarios.py
  </files>
  <behavior>
    - TestSaskEnvelopes: parametrize over the full list of 14 saskatchewan_ tools; each (with mocked client) returns make_response with _meta.source.api set. Use AsyncMock for client functions.
    - TestSaskLangParam: parametrize over all 14 tools; lang="fr" passes through to envelope _meta.lang.
    - TestSaskatchewanToolScenarios (integration, @pytest.mark.integration, @pytest.mark.timeout(30)): wire providers + BM25 transform per .claude/rules/tests.md; call tools through client.call_tool('call_tool', {...}). MANDATORY field-presence + non-null assertions (the Manitoba lesson):
        * "Saskatchewan crop yields" → saskatchewan_get_crop_yields(region="provincial"): assert a "Canola" key exists with a non-null numeric value in a result row (NOT just response shape).
        * "Saskatchewan grain elevators" → assert at least one row has a non-null capacity_tonne / station and PR=='SK'.
        * "Saskatchewan potash mines" → saskatchewan_get_mineral_mines(mineral="potash"): assert Name + Company present, non-null.
        * "Saskatchewan air quality in Regina" → saskatchewan_get_air_quality: assert AQHI present (string/URL) and at least one pollutant reading present.
        * "Saskatchewan fire bans" → saskatchewan_get_fire_bans(ban_scope="urban"): assert no error envelope; result is a list (empty in off-season is VALID — assert "error" not in data, count >= 0).
        * "Saskatchewan WSA hydrometric stations" → saskatchewan_get_wsa_stations(): assert Station_Number AND HyperLink_Graph present in >=1 result (catches the layer-ID / org bug).
        * "Saskatchewan reservoirs" → saskatchewan_get_wsa_reservoirs(): assert Reservoir_Name present in >=1 result (PROVES layer 26, not 0 — layer 0 returns empty).
        * discover_tools with "Saskatchewan crops" finds saskatchewan_search_datasets; calling saskatchewan_search_datasets(query="crops") returns total/numberMatched >= 1 (PROVES startindex pagination end-to-end).
        * Error: saskatchewan_get_mineral_mines(mineral="gold") → structured INVALID_INPUT, not an exception.
    - TestSaskatchewanPromptsResources (integration): list_prompts() includes the 6 saskatchewan_ prompts; read_resource() returns valid content for the 7 resources.
  </behavior>
  <action>
    Fill the parametrized TestSaskEnvelopes + TestSaskLangParam in test_tools.py (mirror alberta's
    Plan 09 / manitoba's Plan 08). Append TestSaskatchewanToolScenarios to
    tests/integration/test_tool_scenarios.py and TestSaskatchewanPromptsResources to
    tests/integration/test_prompts_resources_scenarios.py — call through the MCP Client (NOT client
    functions directly). For the curated live tests, assert FIELD PRESENCE + non-null per the
    behavior list — do NOT settle for asserting only "_meta" exists (that is exactly what masked the
    Manitoba live 400). Assert on field presence, not specific daily values (data changes). Use long
    timeouts (30s). Handle flaky APIs (check r.content before .json(); re-run once on transient
    failure). Mark integration tests @pytest.mark.integration (skipped by default). For fire bans,
    accept an empty list as a valid success.

    Then RUN the live integration suite (-m integration -k Saskatchewan) to confirm the field-presence
    assertions pass against the real endpoints — this is the phase's acceptance evidence, especially
    for WSA reservoirs (layer 26) and startindex pagination.
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/saskatchewan/__tests__/test_tools.py::TestSaskEnvelopes src/mcp_canada/modules/saskatchewan/__tests__/test_tools.py::TestSaskLangParam -x -v</automated>
    <automated>uv run pytest tests/integration/test_tool_scenarios.py tests/integration/test_prompts_resources_scenarios.py -v -m integration -k "Saskatchewan" --timeout=120 2>&1 | tail -30</automated>
  </verify>
  <done>Parametrized envelope/lang tests green over all 14 saskatchewan_ tools. Live integration scenarios pass against real endpoints asserting field presence + non-null (Canola, HyperLink_Graph, AQHI, Reservoir_Name from layer 26, numberMatched>0 via startindex). Empty-fire-ban + invalid-mineral scenarios present. Prompts/resources discoverable via MCP.</done>
</task>

<task type="auto">
  <name>Task 2: Docs sync (README/MODULES/CLAUDE/EXAMPLES) + coverage gate</name>
  <files>
    README.md,
    docs/MODULES.md,
    CLAUDE.md,
    EXAMPLES.md
  </files>
  <action>
    - README.md: add a Saskatchewan section to the Tool Catalog listing all 14 saskatchewan_ tools
      with the first line of each docstring; update the header tool count (add 14), the API/module
      count, and add the GOS Standard Unrestricted Use Data Licence v2.0 attribution note (similar to
      other provincial sections). Recount saskatchewan_ tools from tools.py before editing the header.
    - docs/MODULES.md: add a Saskatchewan entry (portal = geohub.saskatchewan.ca ArcGIS Hub org
      zcv98lgAl8xQ04cW + WSA org 7MBdlVpjqbfBhQer + SPSA gis.saskatchewan.ca/egis; tool list;
      6 prompts + 7 resources; note transport + health deferred).
    - CLAUDE.md: add Saskatchewan to the ArcGIS Hub row of the Portal Technologies table
      ("First used Phase 14 York Region; Phase 17 Alberta; Phase 18 Manitoba; Phase 19 Saskatchewan").
      Note the multi-org fragmentation (primary + WSA + SPSA), that transport (Highway Hotline 511) and
      health (no public SHA FeatureServer) are deferred for Saskatchewan, and that the
      shared/arcgis_hub.py startindex pagination fix landed in Phase 19 (benefits York/Alberta/Manitoba too).
    - EXAMPLES.md: add one Saskatchewan cross-module SQL example (e.g., fetch crop yields or grain
      elevators, store to datastore, JOIN with StatCan agriculture data).
    Keep counts exact. Then run the full coverage gate.
  </action>
  <verify>
    <automated>uv run pytest --cov=src/mcp_canada --cov-fail-under=95 -q 2>&1 | tail -15</automated>
    <automated>grep -c "saskatchewan_" README.md</automated>
  </verify>
  <done>README/MODULES/CLAUDE/EXAMPLES updated and in sync with the implemented 14-tool set. Tool count accurate. CLAUDE.md notes the shared startindex fix + deferred transport/health. Coverage >=95%. Full unit suite green.</done>
</task>

</tasks>

<verification>
1. `uv run pytest --cov=src/mcp_canada --cov-fail-under=95 -q` — coverage gate passes
2. `uv run pytest tests/integration/ -v -m integration --timeout=120 -k "Saskatchewan"` — live integration green (field-presence assertions)
3. `grep -c "saskatchewan_" README.md` — Saskatchewan tools listed in catalog
4. `uv run ruff check src/mcp_canada/modules/saskatchewan/` and `uv run pyright src/mcp_canada/modules/saskatchewan/` — clean
</verification>

<success_criteria>
- SK-15 complete: every saskatchewan_ tool envelope/lang verified (parametrized) + discoverable via discover_tools through MCP Client
- LIVE integration scenarios pass asserting field presence + non-null (Canola, HyperLink_Graph, AQHI, Reservoir_Name layer 26, numberMatched>0) — the Manitoba lesson honored
- Empty-fire-ban + invalid-mineral scenarios pass
- 6 prompts + 7 resources verified via MCP integration
- README/MODULES/CLAUDE/EXAMPLES in sync, tool count accurate
- Coverage >=95%, full suite green
</success_criteria>

<output>
Create `.planning/phases/19-saskatchewan-government-open-data/19-07-SUMMARY.md`: final tool count (14), coverage %, the live integration scenario list with the field-presence assertions, docs updated, SK-01…SK-15 matrix status, and any deferred items (transport, health, Petroleum oil/gas, WSA water quality).
</output>
