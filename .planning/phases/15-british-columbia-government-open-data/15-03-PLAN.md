---
phase: 15-british-columbia-government-open-data
plan: 03
type: execute
wave: 3
depends_on: ["15-01", "15-02"]
files_modified:
  - src/mcp_canada/modules/british_columbia/client.py
  - src/mcp_canada/modules/british_columbia/tools.py
  - src/mcp_canada/modules/british_columbia/__tests__/test_client.py
  - src/mcp_canada/modules/british_columbia/__tests__/test_tools.py
autonomous: true
requirements: []
# Phase 15 has no explicit REQ IDs in REQUIREMENTS.md yet; this plan delivers the
# curated WFS tool surface (15 tools) that makes the phase useful in practice.

must_haves:
  truths:
    - "_wfs_fetch implementation in client.py wraps shared/ogc.wfs_page_all with cached_fetch (bc:wfs:{layer}:{cql}:{max_records}:{include_geometry}) and rate limiting (bc_wfs, 5.0 req/s)"
    - "_wfs_fetch uses CACHE_TTL_ACTIVE (5min) for active fires layer and CACHE_TTL_STATIC (24h) for all other layers"
    - "All 15 curated tools are standalone @tool functions with lang: Literal[en, fr] = en, make_response/make_error envelope, Use-for + Keywords (8+) docstrings, bc_ prefix"
    - "bc_get_water_wells requires at least one of CITY, WELL_CLASS, or AQUIFER_ID — returns make_error(INVALID_INPUT) otherwise (130K-record guard per RESEARCH Pitfall 5)"
    - "bc_get_active_fires uses CACHE_TTL_ACTIVE and supports optional status (FIRE_STATUS) and centre (FIRE_CENTRE) CQL filters"
    - "bc_get_fire_perimeters requires year (FIRE_YEAR) filter to bound the 676+ historical polygons by year"
    - "bc_get_cut_blocks uses the FTEN_CUT_BLOCK_POLY_SVW layer (not the superseded FTEN_CUT_BLOCK_POLYGONS — Pitfall 9)"
    - "bc_get_protected_areas uses WHSE_TANTALIS.TA_PARK_ECORES_PA_SVW (not WHSE_PARKS_ECOLOGY which returns 400 — Pitfall 8)"
    - "bc_get_climate_stations and bc_get_wildfire_weather_stations both query WHSE_LAND_AND_NATURAL_RESOURCE.PROT_WEATHER_STATIONS_SP and document the shared layer in their docstrings"
    - "Every curated tool catches WfsError and returns make_error(UPSTREAM_ERROR, exception_code=e.code)"
    - "All 15 tools expose a truncated boolean in the response data when the 5000-record cap is hit"
    - "test_quality.py keyword count passes for all 15 curated tools"
  artifacts:
    - path: "src/mcp_canada/modules/british_columbia/client.py"
      provides: "_wfs_fetch body (replaces Plan 01 stub)"
      contains: "wfs_page_all"
    - path: "src/mcp_canada/modules/british_columbia/tools.py"
      provides: "15 additional standalone @tool functions covering wildfire (2), forestry (3), environment (3), natural resources (2), health (2), transportation (2), climate (1)"
      contains: "bc_get_active_fires"
      min_lines: 500
    - path: "src/mcp_canada/modules/british_columbia/__tests__/test_client.py"
      provides: "TestWfsFetchShared class populated — cache key, TTL selection, rate limit, WfsError propagation"
      contains: "TestWfsFetchShared"
    - path: "src/mcp_canada/modules/british_columbia/__tests__/test_tools.py"
      provides: "15 filled curated tool test classes (TestBcGetActiveFires through TestBcGetClimateStations)"
      contains: "TestBcGetWaterWells"
  key_links:
    - from: "src/mcp_canada/modules/british_columbia/client.py"
      to: "src/mcp_canada/shared/ogc.py"
      via: "wfs_page_all call from _wfs_fetch"
      pattern: "wfs_page_all\\("
    - from: "src/mcp_canada/modules/british_columbia/client.py"
      to: "src/mcp_canada/shared/cache.py"
      via: "cached_fetch wraps wfs_page_all with layer-specific TTL"
      pattern: "cached_fetch"
    - from: "src/mcp_canada/modules/british_columbia/client.py"
      to: "src/mcp_canada/shared/rate_limiter.py"
      via: "get_limiter(RATE_GROUP_WFS, RATE_LIMIT_WFS)"
      pattern: "get_limiter.*bc_wfs"
    - from: "src/mcp_canada/modules/british_columbia/tools.py"
      to: "src/mcp_canada/modules/british_columbia/constants.py"
      via: "Imports of all 15 *_LAYER constants"
      pattern: "from .constants import"
---

<objective>
Implement the body of `_wfs_fetch` in client.py and add the 15 curated bc_ @tool functions in tools.py. This plan completes the WFS half of the module: caching strategy per layer, simplified filter parameters that translate to CQL, and the full curated data surface covering wildfire, forestry, environment, natural resources, health, transportation, and climate.

Purpose: Agents ask "what is burning right now in BC?" and "what are the active provincial parks?" Curated tools make those queries one-shot instead of requiring the bc_search_datasets -> bc_get_dataset_details -> bc_query_features discovery chain. The 15 object_names were live-verified in RESEARCH.md — they are the authoritative layer contracts.

Output: Fully populated _wfs_fetch + 15 curated @tool functions + test classes for each.
</objective>

<execution_context>
@/Users/mariomeyer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomeyer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-british-columbia-government-open-data/15-CONTEXT.md
@.planning/phases/15-british-columbia-government-open-data/15-RESEARCH.md
@.planning/phases/15-british-columbia-government-open-data/15-VALIDATION.md
@.planning/phases/15-british-columbia-government-open-data/15-01-SUMMARY.md
@.planning/phases/15-british-columbia-government-open-data/15-02-SUMMARY.md

@src/mcp_canada/modules/british_columbia/constants.py
@src/mcp_canada/modules/british_columbia/schemas.py
@src/mcp_canada/modules/british_columbia/client.py
@src/mcp_canada/modules/british_columbia/tools.py
@src/mcp_canada/modules/british_columbia/__tests__/conftest.py

@src/mcp_canada/shared/ogc.py
@src/mcp_canada/shared/cache.py
@src/mcp_canada/shared/rate_limiter.py
@src/mcp_canada/shared/envelope.py

# Closest reference — york_region curated tools with query_feature_service routing:
@src/mcp_canada/modules/york_region/tools.py

<interfaces>
From shared/ogc.py (Plan 01):
- wfs_page_all(base_url, type_name, cql_filter, bbox, max_records, page_size, srs, include_geometry, httpx_client=None) -> (features, truncated)
- WfsError(code, message)

From shared/cache.py:
- cached_fetch(key, ttl, fetcher) -> (data, was_cached)

From shared/rate_limiter.py:
- get_limiter(source, rate)

From modules/british_columbia/constants.py (Plan 01):
- WFS_BASE_URL, RATE_GROUP_WFS, RATE_LIMIT_WFS, CACHE_TTL_ACTIVE, CACHE_TTL_STATIC, MAX_RECORDS, WFS_PAGE_SIZE
- ACTIVE_FIRES_LAYER, FIRE_PERIMETERS_LAYER, FOREST_TENURE_LAYER, CUT_BLOCKS_LAYER, PROTECTED_AREAS_LAYER, WATER_WELLS_LAYER, WEATHER_STATIONS_LAYER, LOCAL_PARKS_LAYER, MINING_TENURE_LAYER, FISH_HABITAT_LAYER, EMERGENCY_ROOMS_LAYER, WALK_IN_CLINICS_LAYER, HIGHWAY_PROFILES_LAYER, ROAD_STRUCTURES_LAYER, CLIMATE_STATIONS_LAYER

From Plan 02 tools.py:
- _build_cql(filters) helper (if placed at module level — if private to bc_query_features, duplicate or refactor to module level for curated tool reuse)

Curated tool catalog (RESEARCH.md §Curated Dataset Catalog — 15 tools total):

Wildfire (2):
- bc_get_active_fires(status: str | None, centre: str | None, min_size_hectares: float | None, max_records: int, include_geometry: bool, lang)
    layer = ACTIVE_FIRES_LAYER, ttl = CACHE_TTL_ACTIVE
    CQL fields: FIRE_STATUS, FIRE_CENTRE, CURRENT_SIZE (>=)
- bc_get_fire_perimeters(year: int, cause: str | None, min_size_hectares: float | None, max_records: int, include_geometry: bool, lang)
    layer = FIRE_PERIMETERS_LAYER, ttl = CACHE_TTL_STATIC
    year is REQUIRED (to bound 676+ fires/year historical dataset)
    CQL fields: FIRE_YEAR, FIRE_CAUSE, FIRE_SIZE_HECTARES (>=)

Forestry (3):
- bc_get_forest_tenure(status: str | None = 'ACTIVE', tenure_type: str | None, client_name: str | None, district: str | None, max_records, include_geometry, lang)
    layer = FOREST_TENURE_LAYER, ttl = CACHE_TTL_STATIC
    CQL fields: LIFE_CYCLE_STATUS_CODE, ML_TYPE_CODE, CLIENT_NAME (LIKE with %), ADMIN_DISTRICT_NAME
- bc_get_cut_blocks(status: str | None = 'ACTIVE', district: str | None, client_name: str | None, max_records, include_geometry, lang)
    layer = CUT_BLOCKS_LAYER (FTEN_CUT_BLOCK_POLY_SVW — Pitfall 9), ttl = CACHE_TTL_STATIC
    CQL fields: LIFE_CYCLE_STATUS_CODE, ADMIN_DISTRICT_NAME, CLIENT_NAME
- bc_get_protected_areas(designation: str | None, min_area_ha: float | None, name: str | None, max_records, include_geometry, lang)
    layer = PROTECTED_AREAS_LAYER (WHSE_TANTALIS — Pitfall 8), ttl = CACHE_TTL_STATIC
    CQL fields: PROTECTED_LANDS_DESIGNATION, OFFICIAL_AREA_HA (>=), PROTECTED_LANDS_NAME (LIKE)

Environment (3):
- bc_get_water_wells(city: str | None, well_class: str | None, aquifer_id: int | None, intended_use: str | None, max_records, include_geometry, lang)
    layer = WATER_WELLS_LAYER, ttl = CACHE_TTL_STATIC
    REQUIRES at least one of city/well_class/aquifer_id — return INVALID_INPUT otherwise (Pitfall 5, 130K records)
    CQL fields: CITY, WELL_CLASS, AQUIFER_ID, INTENDED_WATER_USE
- bc_get_wildfire_weather_stations(name: str | None, min_elevation: int | None, max_records, include_geometry, lang)
    layer = WEATHER_STATIONS_LAYER, ttl = CACHE_TTL_STATIC
    CQL fields: STATION_NAME (LIKE), ELEVATION (>=)
- bc_get_local_parks(municipality: str | None, regional_district: str | None, park_type: str | None, max_records, include_geometry, lang)
    layer = LOCAL_PARKS_LAYER, ttl = CACHE_TTL_STATIC
    CQL fields: MUNICIPALITY, REGIONAL_DISTRICT, PARK_TYPE

Natural Resources (2):
- bc_get_mining_tenure(tenure_type: Literal["mineral","placer"] | None, owner_name: str | None, min_area_ha: float | None, max_records, include_geometry, lang)
    layer = MINING_TENURE_LAYER, ttl = CACHE_TTL_STATIC
    Translate tenure_type mineral -> TENURE_TYPE_CODE='M', placer -> TENURE_TYPE_CODE='P'
    CQL fields: TENURE_TYPE_CODE, OWNER_NAME (LIKE), AREA_IN_HECTARES (>=)
- bc_get_fish_habitat(feature_code: str | None, max_records, include_geometry, lang)
    layer = FISH_HABITAT_LAYER, ttl = CACHE_TTL_STATIC
    CQL fields: FEATURE_CODE

Health (2):
- bc_get_emergency_rooms(locality: str | None, wheelchair_accessible: bool | None, max_records, include_geometry, lang)
    layer = EMERGENCY_ROOMS_LAYER, ttl = CACHE_TTL_STATIC
    CQL fields: LOCALITY, WHEELCHAIR_ACCESSIBLE_IND ('Y'/'N')
- bc_get_walk_in_clinics(locality: str | None, max_records, include_geometry, lang)
    layer = WALK_IN_CLINICS_LAYER, ttl = CACHE_TTL_STATIC
    CQL fields: LOCALITY

Transportation (2):
- bc_get_highway_profiles(highway_number: str | None, admin_unit: str | None, min_lanes: int | None, max_records, include_geometry, lang)
    layer = HIGHWAY_PROFILES_LAYER, ttl = CACHE_TTL_STATIC
    CQL fields: HIGHWAY_NUMBER, ADMIN_UNIT_NAME, NUMBER_OF_LANES (>=)
- bc_get_road_structures(structure_type: str | None, max_records, include_geometry, lang)
    layer = ROAD_STRUCTURES_LAYER, ttl = CACHE_TTL_STATIC
    CQL fields: (structure type field — TBD during live query; pass through as-is)

Climate (1):
- bc_get_climate_stations(name: str | None, min_elevation: int | None, max_records, include_geometry, lang)
    layer = CLIMATE_STATIONS_LAYER (aliases WEATHER_STATIONS_LAYER — same BCGW layer), ttl = CACHE_TTL_STATIC
    Docstring explicitly states: "Same BCGW layer as bc_get_wildfire_weather_stations; exposes the 260 stations from a climate-analysis perspective. For long-term climate normals use ECCC data."
    CQL fields: STATION_NAME, ELEVATION
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement _wfs_fetch body in client.py + 5 curated tools (wildfire + forestry)</name>
  <files>
    src/mcp_canada/modules/british_columbia/client.py,
    src/mcp_canada/modules/british_columbia/tools.py,
    src/mcp_canada/modules/british_columbia/__tests__/test_client.py,
    src/mcp_canada/modules/british_columbia/__tests__/test_tools.py
  </files>
  <behavior>
    TestWfsFetchShared (test_client.py):
    - test_wfs_fetch_uses_wfs_base_url — patched wfs_page_all called with WFS_BASE_URL
    - test_wfs_fetch_passes_layer_to_type_name — layer string forwarded verbatim
    - test_wfs_fetch_forwards_cql_filter
    - test_wfs_fetch_respects_max_records
    - test_wfs_fetch_forwards_include_geometry
    - test_wfs_fetch_cache_key_incorporates_layer_cql_max_records_include_geometry
    - test_wfs_fetch_uses_active_ttl_for_active_fires_layer — ACTIVE_FIRES_LAYER maps to CACHE_TTL_ACTIVE
    - test_wfs_fetch_uses_static_ttl_for_other_layers
    - test_wfs_fetch_rate_limited_bc_wfs_5hz — get_limiter called with bc_wfs, 5.0
    - test_wfs_fetch_wraps_wfs_error — WfsError from wfs_page_all propagates up (test asserts raise) so tool layer can catch it
    - test_wfs_fetch_returns_features_and_truncated — returns tuple((features, truncated), was_cached)

    TestBcGetActiveFires (test_tools.py):
    - test_returns_meta_envelope_with_bc_wfs_api
    - test_builds_cql_with_status_and_centre_filters — status=Out, centre=Cariboo Fire Centre -> CQL FIRE_STATUS='Out' AND FIRE_CENTRE='Cariboo Fire Centre'
    - test_no_cql_when_all_filters_none
    - test_min_size_uses_gte_operator — min_size_hectares=1000 -> CURRENT_SIZE >= 1000
    - test_wraps_wfs_error_as_upstream_error
    - test_propagates_lang_fr
    - test_returns_truncated_flag
    - test_docstring_has_8_plus_keywords

    TestBcGetFirePerimeters:
    - test_requires_year_param — calling with year=None returns make_error(INVALID_INPUT)
    - test_builds_cql_with_year — year=2023 -> FIRE_YEAR=2023
    - test_optional_cause_filter — cause='Human' -> AND FIRE_CAUSE='Human'
    - test_returns_meta_envelope

    TestBcGetForestTenure:
    - test_defaults_to_active_status — default status='ACTIVE' -> LIFE_CYCLE_STATUS_CODE='ACTIVE'
    - test_client_name_uses_like_operator — client_name='Weyerhaeuser' -> CLIENT_NAME LIKE 'Weyerhaeuser%'
    - test_meta_envelope

    TestBcGetCutBlocks:
    - test_uses_ften_cut_block_poly_svw_layer — assert layer string is WHSE_FOREST_TENURE.FTEN_CUT_BLOCK_POLY_SVW (Pitfall 9)
    - test_defaults_to_active_status
    - test_meta_envelope

    TestBcGetProtectedAreas:
    - test_uses_whse_tantalis_layer — assert layer is WHSE_TANTALIS.TA_PARK_ECORES_PA_SVW (Pitfall 8)
    - test_designation_filter — 'PROVINCIAL PARK' -> PROTECTED_LANDS_DESIGNATION='PROVINCIAL PARK'
    - test_min_area_filter — min_area_ha=100 -> OFFICIAL_AREA_HA >= 100
    - test_meta_envelope
  </behavior>
  <action>
    Read `src/mcp_canada/modules/york_region/tools.py` FIRST to see the curated tool pattern with shared client helper usage.

    Step A — implement `_wfs_fetch` in client.py (replace Plan 01 stub):

    ```
    _ACTIVE_LAYERS = {ACTIVE_FIRES_LAYER}  # set for O(1) lookup

    async def _wfs_fetch(
        layer: str,
        cql: str | None = None,
        max_records: int = MAX_RECORDS,
        include_geometry: bool = False,
    ) -> tuple[tuple[list[dict], bool], bool]:
        """Cached + rate-limited WFS fetch. Returns ((features, truncated), was_cached)."""
        ttl = CACHE_TTL_ACTIVE if layer in _ACTIVE_LAYERS else CACHE_TTL_STATIC
        cache_key = f"bc:wfs:{layer}:{cql or ''}:{max_records}:{include_geometry}"
        limiter = get_limiter(RATE_GROUP_WFS, RATE_LIMIT_WFS)

        async def fetcher():
            await limiter.acquire()
            features, truncated = await wfs_page_all(
                WFS_BASE_URL,
                type_name=layer,
                cql_filter=cql,
                max_records=max_records,
                page_size=WFS_PAGE_SIZE,
                include_geometry=include_geometry,
            )
            return (features, truncated)

        result, was_cached = await cached_fetch(cache_key, ttl, fetcher)
        return result, was_cached
    ```

    NOTE: WfsError should NOT be caught here — let it bubble so each curated tool can translate it to make_error with the exception code. (Test test_wfs_fetch_wraps_wfs_error asserts the raise.)

    Step B — add the 5 @tool functions to tools.py: `bc_get_active_fires`, `bc_get_fire_perimeters`, `bc_get_forest_tenure`, `bc_get_cut_blocks`, `bc_get_protected_areas`.

    For each, follow this template:

    ```
    @tool
    async def bc_get_active_fires(
        status: str | None = None,
        centre: str | None = None,
        min_size_hectares: float | None = None,
        max_records: int = MAX_RECORDS,
        include_geometry: bool = False,
        lang: Literal["en", "fr"] = "en",
    ) -> dict:
        """Query currently active wildfires in British Columbia.

        Use for: Checking real-time BC wildfire incidents, their status, cause, and size.
        Keywords: british columbia bc wildfire active fire incident status cause centre emergency province
        """
        filters: dict[str, Any] = {}
        if status:
            filters["FIRE_STATUS"] = status
        if centre:
            filters["FIRE_CENTRE"] = centre
        cql = _build_cql(filters)
        if min_size_hectares is not None:
            extra = f"CURRENT_SIZE >= {float(min_size_hectares)}"
            cql = f"{cql} AND {extra}" if cql else extra
        try:
            (features, truncated), was_cached = await _wfs_fetch(
                ACTIVE_FIRES_LAYER, cql=cql,
                max_records=max_records, include_geometry=include_geometry,
            )
        except WfsError as e:
            return make_error("UPSTREAM_ERROR", f"WFS error: {e.message}", lang=lang, exception_code=e.code)
        return make_response(
            data={"features": features, "truncated": truncated},
            api_name="bc-wfs",
            api_url=WFS_BASE_URL,
            cached=was_cached,
            lang=lang,
        )
    ```

    Apply the same template for the other 4 tools in this task:
    - bc_get_fire_perimeters: year REQUIRED — validate `if year is None: return make_error("INVALID_INPUT", "year is required for fire perimeters", lang=lang)`; CQL with FIRE_YEAR={int(year)} base
    - bc_get_forest_tenure: default status="ACTIVE" -> LIFE_CYCLE_STATUS_CODE; CLIENT_NAME uses LIKE '{value}%' — ensure _build_cql handles a LIKE variant OR build the CLIENT_NAME clause manually with `LIKE '...'` and join
    - bc_get_cut_blocks: CUT_BLOCKS_LAYER (FTEN_CUT_BLOCK_POLY_SVW — NOT the deprecated POLYGONS layer)
    - bc_get_protected_areas: PROTECTED_AREAS_LAYER (WHSE_TANTALIS — NOT WHSE_PARKS_ECOLOGY); min_area_ha manual clause for OFFICIAL_AREA_HA >= float

    If _build_cql (from Plan 02) only handles equality, extend it to optionally accept a tuple ("LIKE", value) or add a private _build_cql_advanced helper. Document choice in module docstring.

    Fill in the 5 listed test classes in test_tools.py. Use the same patching pattern as Plan 02:
    `@patch("mcp_canada.modules.british_columbia.tools._wfs_fetch")` — mock to return ((sample_features, False), False). Use conftest fixtures (sample_wfs_active_fires_geojson features extracted, sample_wfs_fire_perimeters_geojson, sample_wfs_protected_areas_geojson).

    Also fill TestWfsFetchShared in test_client.py. Patch `mcp_canada.modules.british_columbia.client.wfs_page_all` to assert call args; patch `mcp_canada.modules.british_columbia.client.cached_fetch` to intercept cache-key formation.
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/british_columbia/__tests__/test_client.py::TestWfsFetchShared src/mcp_canada/modules/british_columbia/__tests__/test_tools.py -x -q -k "TestBcGetActiveFires or TestBcGetFirePerimeters or TestBcGetForestTenure or TestBcGetCutBlocks or TestBcGetProtectedAreas"</automated>
  </verify>
  <done>
    - _wfs_fetch body implemented with active-vs-static TTL selection
    - 5 curated @tool functions exist with correct layer constants, CQL construction, and error handling
    - TestWfsFetchShared passes green
    - TestBcGetActiveFires, TestBcGetFirePerimeters, TestBcGetForestTenure, TestBcGetCutBlocks, TestBcGetProtectedAreas all pass green
    - test_quality.py keyword checks pass for all 5 tools
    - uv run ruff check src/mcp_canada/modules/british_columbia/ is clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement remaining 10 curated tools (environment, natural resources, health, transportation, climate)</name>
  <files>
    src/mcp_canada/modules/british_columbia/tools.py,
    src/mcp_canada/modules/british_columbia/__tests__/test_tools.py
  </files>
  <behavior>
    10 tool classes to populate in test_tools.py. Each must include at least: test_returns_meta_envelope, test_builds_expected_cql, test_propagates_lang, test_wraps_wfs_error. Special tests noted below.

    TestBcGetWaterWells:
    - test_requires_at_least_one_filter — no filters -> make_error(INVALID_INPUT) — CRITICAL Pitfall 5 guard
    - test_city_filter — city=Kamloops -> CITY='Kamloops'
    - test_well_class_filter — WELL_CLASS
    - test_aquifer_id_numeric_no_quotes — AQUIFER_ID=123 (int)
    - test_multiple_filters_joined_with_AND

    TestBcGetWildfireWeatherStations:
    - test_name_like_filter — name='Kamloops' -> STATION_NAME LIKE 'Kamloops%'
    - test_min_elevation_gte — min_elevation=1000 -> ELEVATION >= 1000
    - test_meta_envelope

    TestBcGetLocalParks:
    - test_municipality_filter — MUNICIPALITY
    - test_regional_district_filter
    - test_meta_envelope

    TestBcGetMiningTenure:
    - test_mineral_maps_to_tenure_type_code_M — tenure_type='mineral' -> TENURE_TYPE_CODE='M'
    - test_placer_maps_to_tenure_type_code_P — tenure_type='placer' -> TENURE_TYPE_CODE='P'
    - test_invalid_tenure_type_returns_invalid_input
    - test_owner_name_like_filter
    - test_min_area_ha_gte

    TestBcGetFishHabitat:
    - test_feature_code_filter
    - test_meta_envelope

    TestBcGetEmergencyRooms:
    - test_locality_filter — LOCALITY='Vancouver'
    - test_wheelchair_accessible_true_maps_to_Y — WHEELCHAIR_ACCESSIBLE_IND='Y'
    - test_wheelchair_accessible_false_maps_to_N
    - test_meta_envelope

    TestBcGetWalkInClinics:
    - test_locality_filter
    - test_meta_envelope

    TestBcGetHighwayProfiles:
    - test_highway_number_filter
    - test_admin_unit_filter
    - test_min_lanes_gte

    TestBcGetRoadStructures:
    - test_structure_type_filter
    - test_meta_envelope

    TestBcGetClimateStations:
    - test_uses_weather_stations_layer_alias — assert layer is PROT_WEATHER_STATIONS_SP (same as wildfire weather)
    - test_docstring_mentions_shared_layer_and_eccc — docstring contains the word "ECCC" or "Environment Canada" and mentions the shared layer
    - test_name_filter
    - test_min_elevation_gte

    Implementation (GREEN):
    10 additional @tool functions following the same template as Task 1.
  </behavior>
  <action>
    Add 10 more @tool functions to tools.py. Follow the same template as Task 1 — each tool:
    1. Builds a `filters: dict[str, Any]` of the equality clauses
    2. Calls `_build_cql(filters)` for the base CQL
    3. Manually appends LIKE or >= clauses if needed (concatenate with ` AND `)
    4. Calls `_wfs_fetch(layer=CORRESPONDING_LAYER, cql=cql, max_records=max_records, include_geometry=include_geometry)`
    5. Catches WfsError -> make_error UPSTREAM_ERROR with exception_code extra
    6. Returns make_response with api_name=bc-wfs, api_url=WFS_BASE_URL

    Layer-specific notes (from RESEARCH.md):

    1. `bc_get_water_wells`: REQUIRED validation at top — `if city is None and well_class is None and aquifer_id is None: return make_error("INVALID_INPUT", "bc_get_water_wells requires at least one of city, well_class, or aquifer_id (130K records)", lang=lang)`. Use WATER_WELLS_LAYER.
       Keywords: british columbia bc water wells groundwater aquifer well class city drinking water

    2. `bc_get_wildfire_weather_stations`: WEATHER_STATIONS_LAYER. STATION_NAME uses LIKE. ELEVATION uses >=.
       Keywords: british columbia bc wildfire weather stations monitoring temperature humidity rainfall rh ffmc

    3. `bc_get_local_parks`: LOCAL_PARKS_LAYER. Equality on MUNICIPALITY, REGIONAL_DISTRICT, PARK_TYPE.
       Keywords: british columbia bc local parks municipal regional greenspace recreation municipality district

    4. `bc_get_mining_tenure`: MINING_TENURE_LAYER. tenure_type Literal['mineral','placer']:
       ```
       TENURE_TYPE_MAP = {"mineral": "M", "placer": "P"}
       if tenure_type and tenure_type not in TENURE_TYPE_MAP: return make_error("INVALID_INPUT", ...)
       if tenure_type: filters["TENURE_TYPE_CODE"] = TENURE_TYPE_MAP[tenure_type]
       ```
       OWNER_NAME LIKE. AREA_IN_HECTARES >=.
       Keywords: british columbia bc mining tenure mineral placer claim owner area hectares prospecting

    5. `bc_get_fish_habitat`: FISH_HABITAT_LAYER. Equality on FEATURE_CODE.
       Keywords: british columbia bc fish habitat salmon herring coastal holding area crims marine wildlife

    6. `bc_get_emergency_rooms`: EMERGENCY_ROOMS_LAYER. Equality on LOCALITY. wheelchair_accessible bool -> WHEELCHAIR_ACCESSIBLE_IND 'Y' or 'N'.
       Keywords: british columbia bc emergency rooms hospital 24 hour health care er accessibility facility

    7. `bc_get_walk_in_clinics`: WALK_IN_CLINICS_LAYER. Equality on LOCALITY.
       Keywords: british columbia bc walk in clinic medical primary care health facility locality provider

    8. `bc_get_highway_profiles`: HIGHWAY_PROFILES_LAYER. Equality on HIGHWAY_NUMBER, ADMIN_UNIT_NAME. NUMBER_OF_LANES >=.
       Keywords: british columbia bc highway profile road number lanes transportation ministry mot segment admin

    9. `bc_get_road_structures`: ROAD_STRUCTURES_LAYER. Pass structure_type through as equality (field name can be inspected during integration testing — for now accept the parameter and put it in a STRUCTURE_TYPE_CODE-like field; document in docstring that the exact field may need adjustment after live query).
       Keywords: british columbia bc road structures bridges culverts tunnels infrastructure transportation ministry mot

    10. `bc_get_climate_stations`: CLIMATE_STATIONS_LAYER (aliases WEATHER_STATIONS_LAYER — same BCGW layer). Docstring MUST include: "This exposes the same BCGW layer as bc_get_wildfire_weather_stations (WHSE_LAND_AND_NATURAL_RESOURCE.PROT_WEATHER_STATIONS_SP). For long-term climate normals, use Environment Canada (ECCC) data via the weather module." STATION_NAME LIKE. ELEVATION >=.
       Keywords: british columbia bc climate weather stations temperature precipitation eccc historical normal observation

    Populate the 10 corresponding test classes using the same patching pattern as Task 1. Use conftest fixtures for sample WFS GeoJSON responses.

    Final check after all 10 are written:
    - `grep -c "^async def bc_" src/mcp_canada/modules/british_columbia/tools.py` should report 20 total (5 discovery + 15 curated)
    - `test_quality.py` passes for all 20 tools (every docstring has Use for + Keywords with 8+ keywords)
    - Full bc module test suite passes
  </action>
  <verify>
    <automated>uv run pytest src/mcp_canada/modules/british_columbia/__tests__/ -x -q</automated>
  </verify>
  <done>
    - 10 additional curated @tool functions exist (total 20 bc_ tools: 5 discovery + 15 curated)
    - TestBcGetWaterWells::test_requires_at_least_one_filter passes (130K-record guard enforced)
    - All 10 curated test classes pass green
    - test_quality.py keyword checks pass for all 20 tools
    - bc_get_cut_blocks uses FTEN_CUT_BLOCK_POLY_SVW (not FTEN_CUT_BLOCK_POLYGONS)
    - bc_get_protected_areas uses WHSE_TANTALIS.TA_PARK_ECORES_PA_SVW
    - bc_get_climate_stations docstring explicitly documents the layer alias with bc_get_wildfire_weather_stations and points to ECCC for normals
    - uv run ruff check src/mcp_canada/modules/british_columbia/ is clean
    - uv run pytest src/mcp_canada/modules/british_columbia/__tests__/ -x -q passes end to end
  </done>
</task>

</tasks>

<verification>
- uv run pytest src/mcp_canada/modules/british_columbia/__tests__/ -x -q passes green
- uv run ruff check src/mcp_canada/modules/british_columbia/ is clean
- grep count: `grep -c "^async def bc_" src/mcp_canada/modules/british_columbia/tools.py` returns 20
- All 20 tools have Use for + Keywords (8+) docstrings verified by test_quality.py
- _wfs_fetch uses ACTIVE TTL only for the active fires layer
- Critical layer corrections (FTEN_CUT_BLOCK_POLY_SVW, WHSE_TANTALIS.TA_PARK_ECORES_PA_SVW) are enforced by tests
</verification>

<success_criteria>
- 15 curated BC WFS tools exposed through tools.py, all callable through MCP
- All layer object_names match RESEARCH.md verified list exactly
- Water wells 130K-record guard prevents unbounded calls
- Shared WFS fetch pattern (_wfs_fetch) enables future curated BC tools to reuse caching + rate limiting
- Full module unit test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/15-british-columbia-government-open-data/15-03-SUMMARY.md`
</output>
</content>
</invoke>