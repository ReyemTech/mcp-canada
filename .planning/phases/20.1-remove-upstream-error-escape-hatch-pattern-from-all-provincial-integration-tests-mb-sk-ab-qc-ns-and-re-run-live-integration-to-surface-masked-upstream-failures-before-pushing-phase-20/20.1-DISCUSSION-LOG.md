# Phase 20.1: Finish integration de-masking and fix the failures it exposed - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-25
**Phase:** 20.1-remove-upstream-error-escape-hatch-pattern-from-all-provincial-integration-tests
**Areas discussed:** Scope, Failure policy, Transient handling, Shape drift, BOC, Live outages, Weather, BC skips, Granularity, Guard mechanism, PRESERVE declaration, Extras

**Framing note:** scouting established before discussion that the ROADMAP title
describes work quick task 1 already completed (idiom A fully eliminated). The
discussion therefore addressed what the phase should become, not how to redo it.

---

## Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Fix the 17 live failures | BOC (5), YR (3), StatCan (3), Toronto (3), SDMX (1), IRCC (2) — mostly shape drift | ✓ |
| Finish de-masking (weather + BC + YR + AB) | The 27 guards quick-1's five-province scope never reached | ✓ |
| Add a guard against reintroduction | Lint/test failing when a masking idiom appears | ✓ |
| Audit the PRESERVE list | Re-verify the ~10 deliberately-tolerant tests still deserve it | ✓ |

**User's choice:** all four.
**Notes:** Multi-select — the phase absorbs everything quick-1 left open rather than
splitting across follow-ups.

---

## Failure policy

| Option | Description | Selected |
|--------|-------------|----------|
| Fix code if broken, fix test if drifted | Triage each failure individually | ✓ |
| Fix all in this phase, no deferrals | Every surfaced failure resolved before close | |
| Fix in-scope, file the rest | What quick-1 did — why the 17 are still open | |
| Separate the two halves | De-mask now, fix failures in a follow-up phase | |

**User's choice:** Fix code if broken, fix test if drifted.
**Notes:** Combined with the "definition of done" extra (below), this means triage
per failure *and* no silent deferral at the end.

---

## Transient handling

| Option | Description | Selected |
|--------|-------------|----------|
| Alberta hardened pattern | Allow error branch, assert code in {UPSTREAM_ERROR, RATE_LIMITED} | ✓ |
| Strict — any error fails | `assert "_meta" in data`, no exceptions | |
| Retry once, then fail | Retry before failing; adds runtime | |
| Strict by default, documented exceptions | Strict with an inline-commented PRESERVE list | |

**User's choice:** Alberta hardened pattern.
**Notes:** Already implemented in-tree by quick-1 on `test_alberta_road_events`, so
this standardises an existing pattern rather than inventing one.

---

## Shape drift

| Option | Description | Selected |
|--------|-------------|----------|
| Flat-list wins — fix the tools | CLAUDE.md mandates aggressive flattening; dicts are a violation | |
| Keyed dicts are legitimate — fix the tests | Multi-series responses are genuinely keyed | |
| Case by case | Decide per tool during planning, with justification | ✓ |

**User's choice:** Case by case.
**Notes:** Recorded in CONTEXT as D-05 with a `costly` reversibility rating — changing
a tool's `data` shape breaks callers, README examples, and the documented envelope.

---

## BOC

| Option | Description | Selected |
|--------|-------------|----------|
| Investigate before deciding | Check git history for when the dict shape appeared | ✓ |
| Treat as regression — restore list | v1.0 module with documented list-shaped examples | |
| Treat as intentional — update tests and docs | Assume the new shape is wanted | |

**User's choice:** Investigate before deciding.
**Notes:** Assigned to the research step rather than resolved by assumption.

---

## Live outages

| Option | Description | Selected |
|--------|-------------|----------|
| Hardened pattern — assert the error code | Same policy as transients | ✓ |
| Verify reachability first | A permanently-dead endpoint means the tool needs fixing | |
| Raise the timeout for CKAN | Treat the ReadTimeout as cumulative slowness | |

**User's choice:** Hardened pattern.
**Notes:** CONTEXT keeps the reachability check as D-09 anyway — tolerating a
permanently-dead endpoint would mask a dead tool, which is the failure class this
phase exists to end. The two are complementary, not alternatives.

---

## Weather

| Option | Description | Selected |
|--------|-------------|----------|
| In, with hardened pattern | De-mask all 18, each keeping a coded error branch | ✓ |
| In, fully strict | Treat ECCC flakiness as signal worth seeing | |
| Out — its own phase | 18 guards across 4 files is nearly a phase by itself | |

**User's choice:** In, with hardened pattern.
**Notes:** Weather is the largest module (34 tools) and the least loudly tested.

---

## BC skips

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — pin to a known dataset | Replace dynamic search with a stable package id | ✓ |
| Yes — but assert search first | Keep dynamic discovery, fail instead of skip | |
| No — legitimate conditional | Skipping is honest about unrunnable tests | |

**User's choice:** Pin to a known dataset.
**Notes:** A skip is a mask with better manners — "BCDC search returned no results"
silently means the WFS-routing path was never exercised.

---

## Granularity

| Option | Description | Selected |
|--------|-------------|----------|
| One plan per module | Each de-masked and live-run before the next | ✓ |
| One sweep, then one live run | Faster; mixes causes if the suite goes red | |
| By idiom, not by module | How quick-1 organised its two tasks | |

**User's choice:** One plan per module.
**Notes:** CONTEXT flags a tension — this multiplies live runs, and the suite has
slowed from 219s to 20+ minutes. Mitigation suggested: run only the affected module's
tests between plans, full suite once at the end.

---

## Guard mechanism

| Option | Description | Selected |
|--------|-------------|----------|
| AST check in tests/test_quality.py | Structural; catches idioms nobody has written yet | |
| Regex check on the known idioms | Simpler and more legible; misses novel idioms | |
| Ruff custom rule | Lint-time; stretches the fixed dependency policy | |
| You decide | Requirement is only that reintroduction fails automatically | ✓ |

**User's choice:** You decide.
**Notes:** Recorded as Claude's Discretion. The firm constraint kept in CONTEXT: the
guard runs in the **unit** suite (like `test_quality.py`), not only the live run, so
it is enforced on every commit.

---

## PRESERVE declaration

| Option | Description | Selected |
|--------|-------------|----------|
| Marker comment the guard reads | `# PRESERVE: reason` — visible and machine-checkable | |
| pytest marker | `@pytest.mark.tolerates_upstream_error(reason="...")` | ✓ |
| Central registry | One list in conftest.py | |

**User's choice:** pytest marker.
**Notes:** Needs registering in pytest config or it emits warnings — noted in CONTEXT
integration points.

---

## Extras

| Option | Description | Selected |
|--------|-------------|----------|
| Note the unit-test root cause as deferred | Same defect class one layer down | ✓ |
| Rename the phase | Title describes finished work; goal is a placeholder | ✓ |
| Record quick-1 as prior art in CONTEXT | Point agents at the idiom taxonomy and 17-failure table | ✓ |
| Set a definition of done | Green suite, or every failure has a written accepted reason | ✓ |

**User's choice:** all four.

---

## Claude's Discretion

- **Guard implementation mechanism** (D-13) — AST vs. regex vs. other. Constraint:
  must run in the unit suite.
- Plan count and task breakdown within each module's plan.
- Whether the PRESERVE audit is its own plan or folded into each module's plan.

## Deferred Ideas

- **The same defect class in unit tests** — tautological assertions built from the
  constant under test, fixtures fabricated to match wrong code, hand-transcribed
  reference catalogs never checked upstream. Found and fixed in StatCan on
  2026-07-25; other modules likely carry it. Its own phase.
- **Research cross-Canada ER wait times datasets** — reviewed via todo matching, not
  folded. No overlap with this phase's scope.
